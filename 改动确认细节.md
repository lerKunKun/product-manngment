## E1
E1-Q1 A:新增 `sys_org_dingtalk_config` 表，per-COMPANY 一对一（org_id PK），存 corpId / appKey / appSecret(AES) / agentId / eventToken / eventAesKey / status。后端读配置改成按 tenantId 查这张表，env 仅做 fallback。

E1-Q2 A:①「加组织 → 立即拉一次部门 + 用户」②「组织详情页有「立即同步」按钮」③「定时同步留 Wave 3」（cron 每天凌晨）

E1-Q3 A:unionId 已存在则**跳过 + 写 sync_log**，避免覆盖手工调整。`sync_log` 表记录每次同步的 added/skipped/failed 数。

E1-Q4 A: 钉钉验证码是做敏感操作的验证码


## E2

E2-Q1 A:建议先 table 版，G 的 canvas 单独做

E2-Q2 A:需要批量操作 批量改部门 / 批量改角色 / 批量冻结

E2-Q3 A:暴露，高敏感操作，要审计 + 强二次确认



## F

F-Q1 A:F1 详情布局重构纯仿照Shopify后台产品编辑页结构  F2 文件子系统（拖拽上传、R2 同步、媒体预览、标签必填）F3 CSV 导出选中/全量

F-Q2 A:先存本地再后台 worker 上传 确认上传R2完毕后删除本地

F-Q3 A:图片改动要立即体现到下次导出 CSV 里 包括顺序和cdn链接

F-Q4 A:视频预览要做缩略图，office 下载要做病毒扫描占位


## G

G-Q1 A:经典的三栏联动权限管理页面 左树、中列表、右表格的联动布局

G-Q2 A:前端 i18n 字典（运行时改不需要 DB migration

G-Q3 A:是组织-角色-用户canvas