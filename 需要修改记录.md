# 需要修改记录

> 状态标记：✅ 已完成 / 🔵 Wave 3 backlog
> Wave 1 完成日期：2026-05-03
> Wave 2-A 完成日期：2026-05-03（E1/E2/F1/G1 四个 track 并行交付）
> Wave 2-B 完成日期：2026-05-03（F2/F3/G2 三个 track 并行交付）

---

## 一、原始需求清单（按提交顺序）

### A. 前端通用
1. ✅ 前端状态弹框需要添加 code+中文描述
   - 实现：`frontend-admin/lib/i18n/error-codes.ts` 22 条字典 + `formatErrorToast()` 接到 `lib/api/client.ts`；toast 显示 `[10001] 未登录或登录已过期`

### B. 店铺管理
2. ✅ 接入店铺授权失败
   - 实现：`ShopifyOAuthController` HMAC 校验改用全部 query 参数（除 hmac/signature 外）；`scopes` 列写入前 `encodeScopesJson()` 转 JSON 数组（fix DataIntegrityViolation）

### C. 个人中心
7. ✅ 加头像（钉钉同步，空回退默认）
   - 实现：`SysUser.avatarUrl`（V26）；`/user/me` 返回 avatarUrl；默认图 `frontend-admin/public/assets/defaults/avatar.png`
8. ✅ 基本信息新增 归属公司、职位
   - 实现：`SysUser.position`（V26）+ `companyName` 通过 `default_tenant_id` 反查 `sys_org.name`
9. ✅ 钉钉绑定按钮（未绑定时显示）
   - 实现：profile 页 dingtalk 行附「绑定钉钉」按钮，跳 `/auth/dingtalk/qrcode`

### D. 钉钉子系统（Wave 2-A E1）
3. ✅ 钉钉验证码现在无法获取
   - 根因：`SensitiveOpService.requestCode` 单线 env 配置（`DingTalkProperties.mainAppKey/mainAppSecret`），dev env 没设 → `getAccessToken()` throw `DEPENDENCY_DOWN` 被 try/catch 吃掉
   - 修复：改成先调 `DingTalkConfigService.resolveByTenantId(user.defaultTenantId)` 走多 corp 路径；命中不到再 fallback env
4. ✅ 钉钉同步组织和员工
   - 实现：`DingTalkSyncService.syncDepartments/syncUsers/syncAll`，BFS 钉钉部门 API；按 unionId 唯一键 upsert sys_user，**已存在跳过不覆盖**；全程写 `dingtalk_sync_log`（V29 新表）
5. ✅ 添加组织时填写钉钉应用配置（不再走 env，多组织多套配置）
   - 实现：V29 新表 `sys_org_dingtalk_config`（per-COMPANY 一对一，appSecret AES-GCM 加密入库）；`OrgController` 加 GET/PUT `/org/{id}/dingtalk-config` + `POST /org/{id}/sync` 立即同步；建组织时检测有配置则异步触发首次同步
   - 前端：orgs 页选中 COMPANY 节点显示「钉钉应用配置」表单 + 「立即同步」按钮，结果 toast「新增 X / 跳过 Y / 失败 Z」

### E. 用户（员工）管理（Wave 2-A E2）
6. ✅ 用户（员工）管理 缺失模块
   - 实现：新建 `admin/user/` 后端模块，15 个 `/admin/users/*` 端点（list/get/create/update/delete/freeze/unfreeze/reset-password/assign-roles + 3 个 batch + impersonate + 2 个 _meta）
   - 前端 `app/(authed)/admin/users/page.tsx` 列表 + 批量条 + 编辑 modal
   - 批量操作：批量改部门 / 批量改角色 / 批量冻结
   - **impersonate**：写 audit_log + 强二次确认（钉钉验证码）；颁短期 access token + SessionService 建会话；不发 refresh cookie；前端 AppShell 顶部 banner「以 X 身份登录中 · 返回原身份」

### F. 产品库
10. ✅ 列表列改造（主图 + 名称 + 价格 + 上架状态）（Wave 1）
    - 实现：后端 `ProductService.listWithCardInfo()` 4 SQL N+1 batch；新增索引 `idx_storeprod_product_status`（V27）；前端列改造 + Badge

11. ✅ 产品详情 Shopify 化（F1 子轨道）（Wave 2-A）
    - 实现：`products/[id]/page.tsx` 完全重写双栏布局（左 7 主区 + 右 3 栏），无 tab 切换
    - 4 个新 `_components/`：ExternalLinksBar（顶部链接 chips） / MediaSection（图片网格 + HTML5 拖拽） / VariantsTable（按 `product_option.position` 升序动态列头）/ RightPanel（Status / Organization / Metafields）
    - 后端补 4 个端点：GET `/product/{id}/option`、CRUD `/product/{id}/metafield`、DELETE `/product-image/{id}`、POST `/product/{id}/image/reorder`

### G. 角色管理（Wave 2-A G1）
12. ✅ 权限中英文描述（G-Q2 选项 B）
    - 实现：新建 `frontend-admin/lib/i18n/permissions.ts`，`PERMISSION_META: Record<string, {zh, en, module}>` 覆盖 V2 种子全部 23 条权限码；`PERMISSION_MODULES` 9 个模块；fallback 未登记 code 显示原 code
13. ✅ 三栏联动权限管理页（G-Q1）
    - 实现：`admin/role/page.tsx` 重写为左（角色分类树 PLATFORM/TENANT/DEPT/自定义）+ 中（角色列表 + 新建按钮）+ 右（权限矩阵按模块分组 + 模块全选三态 + 折叠展开 + diff 保存）
    - 钉钉 step-up（敏感验证码）保留

### H. 数据库治理（Wave 1）
14. ✅ 全表 + 字段中文 comment
    - 实现：V28（849 行），覆盖 47 张表 / 544 列，原 ENUM/NOT NULL/DEFAULT/ON UPDATE/AUTO_INCREMENT 全部保留

### I. 登录态 / XSS 加固（衍生需求，Wave 1）
- ✅ 登录态长寿（refresh token 7d）
- ✅ XSS 加固（refresh 改 httpOnly cookie、access 仅内存、CSP、DOMPurify 净化富文本）
- ✅ 在线设备 + 一键踢出其他设备（20s 心跳）

---

## 二、Wave 2-B 完成项（2026-05-03 三个 track 并行交付）

### F2. ✅ 文件子系统
- V30 migration：product_image 加 7 列（tags/remark/local_path/r2_status/r2_error/thumbnail_url/media_type）+ idx_r2_status；product_doc 加 6 列（同上 + scan_status）
- 后端→R2 路径选 **方案 B 变体**（backend 进程内 `@Scheduled` 5s 轮询 PENDING 行，直接 putObject 不走 worker；asset-worker 留占位 `upload_local_to_r2.py` 注释何时切给它）
- 上传：`POST /product/{id}/image/upload` multipart + tags(JSON) + remark；立即返 `{id, r2_status:PENDING}`，前端轮询直到 DONE
- 视频缩略图：thumbnail_url 写 NULL 走前端 `<video poster={undefined}>` 浏览器自动取首帧；ffmpeg 抽帧留 TODO 给 asset-worker
- 病毒扫描占位：`GET /file/{type}/{id}/scan-status` 返 `{scanned, status, message}`；image/video 直返 CLEAN；office 读 product_doc.scan_status；显式 TODO 注释指 ClamAV/VirusTotal
- 前端：FileUploadDropzone 拖拽组件 + TagInput combobox（已有 + 新加）+ MediaPreviewLightbox（image/PDF/video 内联）；MediaSection 完全重写

### F3. ✅ 产品 CSV 导出（选中 / 全量）
- 后端 `/product/export` 加 `mode=selected|all` + `ids` + `keyword` + `status`
- 新增第 43 列 `Image URLs`：按 `product_image.position ASC` 取所有 src 用 `;` 拼接（图片改动立即体现）
- N+1 → 2 SQL/页（每页 200 product batch 拉 variants + images）
- ids 上限 1000；超限走标准 4xx JSON
- 前端 fetch+blob 下载（token 走 Authorization header），列表顶栏「导出全部」+ 批量条「导出选中 (N)」

### G2. ✅ 组织-角色-用户 canvas
- 新页 `app/(authed)/admin/canvas/page.tsx` 600 行；reactflow 11.11.4
- 三层固定坐标 layout：左组织（按 depth 缩进）/ 中角色 / 右用户（带头像）；边 org→role 紫色 + role→user 灰色
- 拖拽 user 节点到 role 节点：confirm Dialog → SensitiveActionDialog（钉钉 step-up）→ `userAdminApi.assignRoles(uid, [...existing, newRoleId])`（合并）
- 已有的角色拖拽被 toast 拦截；撤销分配留 TODO
- AppShell「系统」分组加「角色画布」入口

---

## 三、🔵 Wave 3 backlog

- 钉钉用户/部门**定时全量同步**（cron 凌晨执行；E1 已做被动触发）
- 角色权限**审计**：谁在什么时间改了哪个角色的权限矩阵
- 产品详情**版本对比**（基于 product_snapshot）
- 媒体文件**真病毒扫描**（接 ClamAV / VirusTotal）
- impersonate access token TTL 严格 10 分钟（当前受 JwtProperties.accessTtl 30min 控制；改 JwtUtil 加重载方法）
- 多组织钉钉应用配置的 secret 字段做轮换机制

---

## 四、Migrations 索引

| 编号 | 内容 | 来源 |
|---|---|---|
| V26 | sys_user 加 avatar_url + position | Wave 1 个人中心 |
| V27 | store_product 加 idx_storeprod_product_status | Wave 1 产品列表 |
| V28 | 全库 47 张表 / 544 列加中文 COMMENT | Wave 1 DB 治理 |
| V29 | sys_org_dingtalk_config + dingtalk_sync_log（双表）| Wave 2-A E1 钉钉子系统 |
| V30 | product_image / product_doc 加文件生命周期列（tags/remark/local_path/r2_status/r2_error/thumbnail_url/media_type/scan_status）| Wave 2-B F2 文件子系统 |
