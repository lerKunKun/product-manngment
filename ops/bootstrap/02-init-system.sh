#!/usr/bin/env bash
# 系统初始化：swap、内核参数、ufw、SSH 改端口、fail2ban
# 用法：sudo bash 02-init-system.sh [--dry-run]
set -euo pipefail
echo ">>> [02] 系统初始化（占位，Wave 1 完整实现）..."
echo "TODO: swap, sysctl, ufw default deny, ssh port change, fail2ban jail.local"
