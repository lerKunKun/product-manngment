# 技术债登记

> 用途：追踪未解的技术债（带"何时再看"的触发条件），与《进度记录.md》解耦。
> 每完成一项搬到 §3 已闭环；新增一项填 §1 未解。
> 编排日期：2026-05-02（Wave 2 完工时刷新）

---

## 1. 未解技术债（带触发条件）

### #5 Worker progress emit 同步 httpx，每 file 一次 POST

| | |
|---|---|
| **现象** | `asset-worker/app/services/progress.py` 的 `ProgressEmitter.emit()` 每次 emit 是 blocking httpx POST。一个产品 50 张图 → 50 次同步 POST，加在 pull 总耗时上 |
| **不阻塞原因** | 当前每个 pull 的 emit 次数 ≤ 文件数 + 3 lifecycle 事件，单次 POST < 50ms，dev 量级 N=10 文件总开销 < 500ms 可接受 |
| **触发再看** | (a) 单 pull 文件数 ≥ 100（大产品）； (b) backend 接收 progress 端点延时 ≥ 200ms； (c) 用户报"pull 太慢"。三选一时优化 |
| **预估修复工作量** | 1 PD：改成异步 queue（`asyncio.Queue` + 后台 task）或批量缓冲（每 10 个 emit 或 5s flush 一次） |
| **风险** | 低 — emit 是 best-effort，丢失也无业务影响 |

### #7 Webhook 没 currency → product_price_history 硬编 USD

| | |
|---|---|
| **现象** | Shopify webhook 的 product 对象不带 store currency，PriceInventoryHistoryService 给历史表写 `currency='USD'` |
| **不阻塞原因** | 当前所有店铺都是 USD（dev 假设）；多币种是 W3-PUR 才需要的能力 |
| **触发再看** | W3-PUR 上线，需要"多店多币种采购成本对比"时 |
| **预估修复工作量** | 1.5 PD：(a) Store entity 加 currency 列（V13）+ default 取自 Shopify shop API； (b) PriceInventoryHistoryService 注入 StoreMapper 取 currency； (c) 数据迁移：旧行 currency=USD 不变 |
| **风险** | 低 — 改 enum 兼容 |

### #10 推送批量同步阻塞 HTTP；真异步队列待加

| | |
|---|---|
| **现象** | W2-PUSH-05 选了同步循环 `pushBatch`：100 个产品推同一店 → HTTP 阻塞 5-10 分钟，客户端 timeout |
| **不阻塞原因** | 当前手工触发推送，单批 ≤ 5 个，sync 没事；批量自动化（W3-NEW 一键开店）才需要真异步 |
| **触发再看** | (a) W3-NEW saga 启动时；OR (b) 单批超 20 个产品时 |
| **解法选择** | <br>**Option A**：`@Async` + 独立 `PushAsyncWorker` bean（避开 self-injection 循环）；<br>**Option B**：RabbitMQ `push.queue` + 消费者池；<br>**Option C**：Redisson DelayedQueue 复用现有 Redis 基础设施 |
| **推荐** | Option A 最简单（Spring 已 `@EnableAsync`）；Option B 在生产横向扩展时升级 |
| **预估修复工作量** | A=1 PD / B=2 PD（含 RabbitMQ 拓扑 + 消费者）/ C=1.5 PD |
| **风险** | 中 — 引入新错误模式（任务在线程池/队列里失败，需要观测/告警） |

### #11 StatusSyncService 当前是骨架，无真 Shopify diff

| | |
|---|---|
| **现象** | W2-SYNC-02 的 04:00 全量兜底扫只统计本地 `store_product` 行数，不调 Shopify Admin API 比对真实状态。漂移只能等用户手动操作时被动发现 |
| **不阻塞原因** | webhook 链路（W2-SYNC-01）已实时同步状态变化；只有少数边缘情况（Shopify 端被人工修改 + webhook 丢包）需要兜底扫修正 |
| **触发再看** | W3-NEW 一键开店上线，引入合作者店铺池后，店铺数量增多 + 部分店开关 webhook，漂移概率上升 |
| **预估修复工作量** | 3 PD：(a) Worker 加 `/sync/store-products` 端点（Shopify GraphQL bulk operations 拉取该店所有产品 status + ids）； (b) Backend StatusSyncService 拉清单 + diff `store_product` + 修正状态； (c) 失败/超时处理 + 钉钉告警 |
| **风险** | 中 — Shopify GraphQL bulk operations 是异步的，需要轮询 + 长时窗口 |

---

## 2. 已延后但永久延后（不再追）

> 当前没有这一类。

---

## 3. 已闭环（按修复时间倒序）

| # | 描述 | 修复批次 |
|---|---|---|
| #1 | Worker `/pull/*` dry-run 不真传 R2 → backend SNAP-04 链路 dry-run 走不到 SUCCESS | Day 5 → Day 6 间清债 |
| #2 | `bin/dev-backend.sh` 的 `set -a; source .env; set +a` 覆盖命令行 export 的 env | Day 5 → Day 6 间清债 |
| #3 | `DingTalkSyncConsumer` 全局 manual ack 但没显式 ack → 消息累积 unacked | Day 5 → Day 6 间清债 |
| #4 | `ProductSnapshot` 缺 `started_at` 字段，RUNNING 阶段不可观测 | Day 6 → Day 7 间清债（V11） |
| #6 | `ProductImage` 没 `r2_key` → push 走 src URL，私有 R2 时无图 | Day 6 → Day 7 间清债（V12） |
| #8 | "立即快照"按钮用 `prompt()` 选 storeId，UX 差 | Day 6 → Day 7 间清债（Dialog 组件） |
| #9 | PushService.payloadJson 缺顶层 productId，TaskController 反查麻烦 | Wave 2 收尾时清（在线 Edit） |
| #12 | `SagaService.start(initialPayload)` 不自动 merge `templateId / replaceRules / productIds` 进 SagaContext，PRODUCTS 步要靠 e2e SQL `JSON_SET` workaround 注入 | 2026-05-02 W3-PERF-04 期间清（SagaService.java + e2e-saga.sh） |

---

## 4. 登记规则

- 新发现技术债：必填 现象 / 不阻塞原因 / 触发再看 / 工作量估算 / 风险
- 修复后：移到 §3，时间戳保留
- 永久放弃（设计变更后不再相关）：移到 §2，写明放弃理由
- "触发再看"应是**可观测条件**（"用户报慢"、"批量超 20"、"W3-NEW 上线"），不要写"以后"
