# 并行改进编排（Sprint 1）

> 编排：2026-05-03
> 用途：把《项目检查报告》P0/P1 + 《前端改进文档》Sprint 1 的事项切成 5 个并行 track。
> 文件冲突边界：每 track 拥有独立目录；共享文件（Application.java / AppShell.tsx）由 Track 3 统一占有。
> 原则：每 track 工时 ≤ 8 分钟实际执行（避免 stream timeout）。

---

## 0. Track 划分

| Track | 内容 | 工时 | 主要文件 |
|---|---|---|---|
| **T1** | 后端关键修复（SecurityConfig + ApprovalEngine 任一角色 + 6 个关键 metric） | ~3 PD | `core/SecurityConfig` `approval/ApprovalEngine` 新建 `metrics/` 包 |
| **T2** | 后端补 3 个 admin controller（NotificationLog + AuditLog + SysRole） | ~2 PD | 各包新建 controller + service |
| **T3** | 前端 Shell 重构 + 设计系统 + dashboard + 全局未读红点 | ~3 PD | `AppShell.tsx`（owner）+ `components/ui/`（新增 6 组件）+ `dashboard/page.tsx` + header bell |
| **T4** | 前端采购 tab + 审批 payload 表单化 | ~2 PD | `products/[id]/`（加 tab）+ `lib/api/purchase.ts` + `approvals/[id]/page.tsx` |
| **T5** | 前端 admin 2 页（通知日志 + 审计日志） | ~2 PD | `app/(authed)/admin/notification-log/` + `audit-log/` + 2 个 API client |

**总工时**：~12 PD（5 人并行约 1.5 周；本次单人 + agent 并行约 1.5 小时）

---

## 1. 共享文件冲突避免

| 文件 | Owner | 其他 track 怎么办 |
|---|---|---|
| `frontend-admin/components/layout/AppShell.tsx` | T3 | T5 想加菜单项 → T3 prompt 已涵盖 admin 菜单组（"系统"二级），T5 不动此文件 |
| `backend-api/src/main/java/com/biou/shopifyhub/Application.java` | — | 当前 @MapperScan 已含 `core.mapper`（覆盖 SysRole）+ `audit.mapper`（覆盖 SysAuditLog）+ `notification.subscription.mapper`（覆盖 NotificationLog），**无需任何 track 修改** |
| `backend-api/.../core/SecurityConfig.java` | T1 | 其他 track 不动 |
| `backend-api/.../notification/NotificationEventCode.java` | — | 已 ready，无 track 需要新增 |
| `backend-api/.../notification/NotificationDispatcher.java` | — | T1 ApprovalEngine 用 `notifier.notifyUser`，已 wire；不动 dispatcher |

---

## 2. T1 详细任务（后端关键修复）

### 2.1 SecurityConfig 切 .authenticated()

**文件**：`backend-api/src/main/java/com/biou/shopifyhub/core/SecurityConfig.java`

**操作**：
- `.anyRequest().permitAll()` → `.anyRequest().authenticated()`
- 审计当前 permitAll 列表是否完整覆盖：login / logout / health / dingtalk callback / invitation accept / preview / oauth / webhook / internal/asset / ops/backup / password-reset / swagger
- 新增 `requestMatchers("/internal/**").permitAll()`（loopback 内部 API 通用前缀）
- 测试：`mvn -DskipTests compile` 通过；启动后已有 e2e (smoke-test.sh) 应仍通过（带 JWT）

### 2.2 ApprovalEngine 任一角色签批通知修复

**文件**：`backend-api/src/main/java/com/biou/shopifyhub/approval/ApprovalEngine.java`

**当前**：`notifyApprovers` 在 `currentApproverRole != null` 分支只 `log.info`，不发通知。

**修复**：
- 注入 `SysUserRoleMapper` + `SysRoleMapper`
- 角色模式时 `roleMapper.selectByCode(role) → SysRole` → `userRoleMapper.selectList(eq role_id)` → 拿全员 userId list
- 调 `notifier.notifyUsers(userIds, APPROVAL_PENDING, ...)`

### 2.3 6 个关键 metric 暴露

**新建包**：`backend-api/src/main/java/com/biou/shopifyhub/core/metrics/`

**6 个 metric**（用 Micrometer，已在 pom）：
- `MetricsExporter.java`：注入 `MeterRegistry`，注册：
  - Counter: `shopifyhub_r2_upload_total{result}` — 钩到 FileService.uploadBytes（用 AOP 或直接在方法内 increment）
  - Counter: `shopifyhub_notification_send_total{result}` — 在 NotificationSendService 加
  - Counter: `shopifyhub_auth_login_total{result}` — 在 AuthService.login 加
  - Gauge: `shopifyhub_backup_last_success_seconds` — `BackupNotifyController.notifySuccess` 时 set
  - Gauge: `shopifyhub_audit_archive_last_success_seconds` — `AuditArchiveScheduler` 成功时 set
  - Gauge: `shopifyhub_approval_pending_max_age_seconds` — 新建 `@Scheduled` 每分钟扫 PENDING 最久秒

注意：直接在 service 内 inject MeterRegistry + 用 `Counter.builder().register()`，不用全局静态 holder。

---

## 3. T2 详细任务（后端补 admin controller）

### 3.1 NotificationLogController

**文件**：`backend-api/src/main/java/com/biou/shopifyhub/notification/subscription/NotificationLogController.java`

**端点**：
- `GET /admin/notification-log` query: `eventCode` `channel` `status` `userId` `from` `to` `page=1` `size=50`
- `GET /admin/notification-log/{id}`
- `POST /admin/notification-log/{id}/retry`（强制重试一次，调 NotificationSendService.retry）
- `GET /admin/notification-log/stats?from&to` 返回 today PENDING/SENT/FAILED 计数

### 3.2 SysAuditLogController

**文件**：`backend-api/src/main/java/com/biou/shopifyhub/audit/SysAuditLogController.java`

**端点**：
- `GET /admin/audit-log` query: `userId` `module` `action` `sensitive` `from` `to` `page` `size`
- 返回 `Page<SysAuditLog>`

### 3.3 SysRoleController

**文件**：`backend-api/src/main/java/com/biou/shopifyhub/rbac/SysRoleController.java`

**端点**：
- `GET /admin/role`
- `GET /admin/role/{id}` 含 permission codes + user count
- `POST /admin/role` 新建（仅自定义角色，内置不可改）
- `PUT /admin/role/{id}/permissions` body: `{permissionCodes: [...]}`
- `GET /admin/role/{id}/users`

每个端点都用 `Result<T>` 包装，参考 `ApprovalController` 风格。

---

## 4. T3 详细任务（前端 Shell + 设计系统 + dashboard）

### 4.1 AppShell.tsx NAV 二级分组

**新结构**：
```ts
const NAV_GROUPS = [
  { section: null, items: [{ href: "/dashboard", label: "首页" }] },
  { section: "业务", items: [
    { href: "/products", label: "产品库" },
    { href: "/stores", label: "店铺管理" },
    { href: "/partner-stores", label: "合作者店铺池" },
    { href: "/newstore", label: "一键开店" },
  ]},
  { section: "资产", items: [
    { href: "/assets", label: "资产快照" },
    { href: "/snapshots", label: "产品快照" },
    { href: "/templates", label: "模板库" },
  ]},
  { section: "流程", items: [
    { href: "/approvals", label: "审批中心" },
    { href: "/invitations", label: "临时员工邀请" },
    { href: "/cross-auth", label: "跨公司授权" },
  ]},
  { section: "我的", items: [
    { href: "/inbox", label: "通知中心" },
    { href: "/profile", label: "个人中心" },
  ]},
  { section: "工具", items: [
    { href: "/tasks", label: "任务监控" },
    { href: "/recyclebin", label: "回收站" },
    { href: "/guides", label: "指导文档" },
  ]},
  { section: "系统", items: [
    { href: "/admin/notification-log", label: "通知日志" },
    { href: "/admin/audit-log", label: "审计日志" },
  ]},
];
```

每 section header 灰色小字 + section 内项目缩进 0。section=null 不渲染 header。

### 4.2 Header 加全局未读红点

`AppShell.tsx` 顶部 header 加一个 🔔 icon button：
- 30s 轮询 `/notification/inapp/unread-count`（用 SWR / 或 setInterval）
- 红点 + 数字 badge（>99 显示 "99+"）
- 点击跳 `/inbox`

### 4.3 设计系统补 6 组件

新建 `components/ui/`：
- `Skeleton.tsx`（参考 shadcn 风格）
- `Tabs.tsx`（基于 @radix-ui/react-tabs；安装 `pnpm add @radix-ui/react-tabs`）
- `Pagination.tsx`（自实现：上一页 / 下一页 / 页码 / 总数）
- `Breadcrumb.tsx`（按 pathname 自动生成 + 可手动覆盖）
- `DropdownMenu.tsx`（基于 @radix-ui/react-dropdown-menu，已可能传递依赖）
- `Badge.tsx`（统一 status badge：variants = success / warning / error / info / neutral）

每个组件 < 50 行；不需要写 demo 页，直接放 `components/ui/`。

### 4.4 dashboard 4 卡片

**文件**：`frontend-admin/app/(authed)/dashboard/page.tsx`

4 个卡片：
- 「产品总数」— GET `/products?page=1&size=1` 取 total
- 「在线店铺数」— GET `/store?status=ACTIVE`
- 「今日推送任务」— GET `/tasks?type=PUSH&from=todayStart`
- 「待办审批」— GET `/approval/pending/me`

每卡：大数字 + 小标签 + trend 指示（如有）。
布局：CSS grid 2x2（移动端 1 列）。

底部「最近 5 个失败任务」列表（GET `/tasks?status=FAILED&size=5`）。

---

## 5. T4 详细任务（前端采购 + 审批表单化）

### 5.1 采购 tab 在 `/products/[id]`

**新建**：`frontend-admin/lib/api/purchase.ts`
```ts
export type PurchaseInfo = {
  variantId: number;
  sku: string;
  cost?: number;
  weightG?: number;
  logisticsTag?: string;
  purchaseUrl?: string;
};

export type SkuChangeLog = {
  id: number;
  variantId: number;
  oldSku: string;
  newSku: string;
  actorId: number;
  syncedStoreCount?: number;
  createdAt: string;
};

export const purchaseApi = {
  getByProduct: (productId) => api.get<PurchaseInfo[]>(`/product/${productId}/purchase`),
  updateVariant: (variantId, body) => api.put<void>(`/variant/${variantId}/purchase`, body),
  changeSku: (variantId, newSku, sensitiveToken) => 
    api.post<void>(`/variant/${variantId}/sku`, { newSku }, { headers: { "X-Sensitive-Token": sensitiveToken }}),
  skuLog: (variantId) => api.get<SkuChangeLog[]>(`/variant/${variantId}/sku-log`),
};
```

**修改**：`frontend-admin/app/(authed)/products/[id]/page.tsx`
- 顶部加 Tabs：基本信息 / 媒体 / **采购** / 店铺映射
- 采购 tab：variant 列表表格 → 行可编辑（成本 / 克重 / 物流 tag / 链接）
- SKU 列：点击 → dialog 含「新 SKU + 钉钉 6 位码」→ 提交后右侧 toast 显示同步进度
- 底部「SKU 变更历史」时间线

### 5.2 审批 payload 按 type 渲染表单

**修改**：`frontend-admin/app/(authed)/approvals/[id]/page.tsx`

把现有的「textarea + JSON.parse」改造为：

```tsx
function PayloadForm({ type, payload, onChange }) {
  if (type === "PRODUCT_ACCESS") {
    return <>
      <input ... value={payload.productId} onChange={...} placeholder="产品 ID" />
      <textarea ... value={payload.reason} onChange={...} placeholder="申请理由" />
    </>;
  }
  if (type === "CROSS_COMPANY_AUTH") {
    return <>
      <input ... value={payload.userId} placeholder="受让人 userId" />
      <select ... value={payload.scopeType}><option>COMPANY</option><option>STORE</option></select>
      <input ... value={payload.scopeId} placeholder="scope ID" />
      <input type="datetime-local" ... value={payload.expiresAt} />
    </>;
  }
  // 兜底：fallback to JSON textarea
  return <textarea value={JSON.stringify(payload, null, 2)} ... />;
}
```

仅在 `canResubmit` 区域用 `PayloadForm`，详情查看仍 JSON pretty。

---

## 6. T5 详细任务（前端 admin 2 页）

### 6.1 通知日志 admin 页

**新建**：
- `frontend-admin/lib/api/notificationLog.ts`
- `frontend-admin/app/(authed)/admin/notification-log/page.tsx`

UI：
- 顶部统计卡：今日 SENT / FAILED / PENDING（调 `/admin/notification-log/stats`）
- 过滤栏：event_code / channel / status / userId / 时间范围
- 表格：时间 / 用户 / event_code / channel / status badge / attempt_count / next_retry_at / 操作（详情 / 重试）
- 行点击 → 展开 body_text + error_msg
- 重试按钮（仅 status=FAILED 时启用）→ POST `/{id}/retry`

### 6.2 审计日志 admin 页

**新建**：
- `frontend-admin/lib/api/auditLog.ts`
- `frontend-admin/app/(authed)/admin/audit-log/page.tsx`

UI：
- 过滤栏：用户 / 模块 / action / 时间 / sensitive=true checkbox
- 表格：时间 / 用户 / 模块.action / URI / IP / 状态码 / sensitive badge
- 行展开：request_payload (pretty JSON) + user_agent
- 顶部「导出 CSV」按钮（按当前过滤条件）

均需要分页（用 Track 3 提供的 Pagination 组件；如未就绪先自建简化版）。

---

## 7. 启动检查表

每 track 完工时输出：
1. 创建/修改文件清单
2. 编译 / typecheck 结果（mvn compile / pnpm tsc --noEmit）
3. 已知卡点 / TODO

合并后我（编排者）做：
- Application.java @MapperScan 不用改（已覆盖）
- 跑 mvn compile + pnpm tsc 全量验证
- git commit + push

---

_最后更新：2026-05-03_
