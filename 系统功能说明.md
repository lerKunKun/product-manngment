# 系统功能说明

> 编写：2026-05-03（Wave 4 收尾）
> 用途：面向产品 / 业务 / QA / 新工程师的「这个系统能做什么」清单。
> 与设计文档区别：本文档只关心 **功能 + 入口 + 用法**，不展开内部架构（那是《系统设计文档.md》职责）。

---

## 0. 系统定位

**Biou × Shopify Hub** 是 Shopify 多店铺管理 + 产品库 + 一键开店 SaaS 控制台，由 Biou 自营的多家境外店铺统一管理。

主线场景：
1. **产品 Master Data 管理**：跨店铺统一产品库（标题 / 描述 / 价格 / 库存 / 媒体）
2. **多店推送**：把产品从产品库推送到任意（一家或多家）Shopify 店
3. **一键开店**：基于模板 + 已有产品，10 分钟开一家全新店（OAuth → 推主题 → 推产品）
4. **快照与回滚**：店铺主题 / 产品的版本快照，必要时回滚到任意历史点
5. **审计 / 审批 / 通知 / 监控**：合规留存 + 审批工作流 + 多通道通知 + 全栈监控告警

---

## 1. 角色与登录

### 1.1 用户类型

| 类型 | 来源 | 登录方式 | 示例 |
|---|---|---|---|
| `STAFF` 正式员工 | 钉钉企业部门同步 | 钉钉扫码（主用）+ 用户名密码（兜底） | 公司内 APP 开发 / 运营 |
| `TEMP` 临时账号 | 邀请发起人创建（最长 90 天） | 邮件激活后用工号 + 密码登录 | 第三方代运营 / 短期合作 |

### 1.2 登录入口

| 路径 | 用途 |
|---|---|
| `/login` | 用户名密码登录 + 钉钉扫码 |
| `/login/dingtalk-success` | 钉钉回调中转 |
| `/forgot-password` | 邮件触发密码重置 |
| `/reset-password?token=xxx` | 邮件链接落地，输入新密码 |
| `/invitation/accept?token=xxx` | 临时员工邮件激活 |

### 1.3 安全机制

- 5 次密码错误锁 15 分钟（Redis 滑窗）
- JWT RSA 签名（access 30min / refresh 7d）+ 黑名单（登出立即失效）
- 危险操作（`@RequireSensitiveOp`）需钉钉 6 位验证码二次确认
- 跨公司 / 撤销 / 删除等操作走二次确认链路

---

## 2. 组织 + RBAC + 数据范围

### 2.1 组织树

- 钉钉部门同步：每天 02:00 兜底全量 + 实时 `org_dept_*` 事件
- 员工同步：实时 `user_*` 事件 → sys_user 增 / 改 / 软删
- 离职 30 天软删后物理删（W1-AUD-02）

### 2.2 RBAC

10 个内置角色 + 23 个权限点，关键角色：
- `SUPER_ADMIN`：跨租户 / 跨公司可见
- `COMPANY_ADMIN`：本公司全部
- `OPS_LEAD`：运营负责人，可发起跨公司授权
- `STORE_MANAGER`：负责若干店铺
- `EMPLOYEE`：默认普通员工
- `TEMP_USER`：邀请创建的临时账号

### 2.3 数据范围（DataScope）

`@DataScope` 注解 + MyBatis 拦截器自动注入 SQL `WHERE` 条件，控制：
- 租户级（platform / 公司 / 部门）
- 店铺级（仅看负责的店）
- 跨公司临时授权（W3-DS）

---

## 3. 临时员工邀请

**入口**：`/invitations`

**操作**：
- 发起：邮箱 + 角色（多选） + 数据范围（公司 / 店铺） + 过期时间（≤ 90 天）
- 撤销：未激活前可撤销
- 列表：进行中 / 已激活 / 已过期 / 已撤销

**自动化**：
- 邀请发起后邮件含**临时密码 + 激活链接**
- 24h / 7d / 1d 到期前钉钉 + 邮件双通道提醒
- 到期自动状态置 EXPIRED + 软删用户
- 90 天后物理删除（含审计行）

---

## 4. 店铺管理

**入口**：`/stores`

**两种接入方式**：

| 方式 | 步骤 | 适用场景 |
|---|---|---|
| Custom App Token | 商家在 admin → Apps → Develop apps → 拷贝 token → 系统填入 | dev / staging / 自营店 |
| Shopify OAuth | 系统跳 install URL → 商家授权 → 回调拿 token → 入库（AES-GCM 加密） | 生产 Public App 分发 |

**功能**：
- 列表 + 详情（含 token 状态 / 过期时间）
- 30 天到期前 `TOKEN_EXPIRING` 钉钉提醒
- 卸载（`app/uninstalled` webhook）自动 inactive

**合作者店铺池**（Wave 3）：
- 入口 `/partner-stores`
- 标记 `is_dev_store` / `is_partner_collab`
- 一键开店 saga 用它分配 dev store 资源

---

## 5. 产品库

**入口**：`/products`

**核心能力**：
- 完整产品数据：标题 / handle / vendor / type / tags / SEO / variants（含 SKU / 价格 / 库存 / barcode）/ 媒体（图片 / 视频）/ 富文本 body_html
- CSV 42 列 import / export（与 Shopify 原生兼容）
- R2 图片上传（< 20MB / 白名单 MIME）
- 富文本 TipTap 编辑器 + 行内图片上传（W1-PROD）

**操作**：
- 列表（搜索 + 分页 + 状态过滤）
- 详情（编辑 / 删除 / 媒体管理 / 富文本）
- 导入页：上传 CSV → 预览 → diff → 确认导入
- 导出页：当前过滤条件下全表导出

**关联视图**：
- 该产品已被推送到哪些店（store_product 表）
- 该产品的快照历史（snapshot 列表）

---

## 6. 产品推送

**入口**：从 `/products/[id]` 点「推送到店」 OR `/tasks` 看推送任务

**支持**：
- 单品推送（一对一 / 一对多店铺）
- 批量推送（产品库当前过滤集合 → 多店）
- 媒体先上 Shopify Files → 替换 src URL → 再创建 product
- 冲突处理：店内已存在同 handle 时弹 `PENDING` 等用户决策（覆盖 / 跳过 / 创建变体）

**任务跟踪**：
- 同步 / 异步混合：< 5 个产品同步；批量走 RabbitMQ
- `/tasks` 看 task_log 进度 + SSE 实时进度条
- 失败 → `PRODUCT_PUSH_FAIL` 钉钉通知

---

## 7. 主题资产 + 快照

### 7.1 资产快照（asset_snapshot）

**入口**：`/assets`

**用途**：拉一个店铺的完整主题（liquid / settings / sections / templates）→ 落 R2 → 可重新部署到其它店

**操作**：
- 立即拉取（选店铺 → worker 跑 `/pull/theme` → 落 R2）
- SSE 进度条
- 列表 + 详情（按 store / 时间过滤）

### 7.2 产品快照（product_snapshot）

**入口**：`/snapshots`

**用途**：单产品在某店的完整状态 dump（CSV + JSON）→ R2 → 出错时回滚

**自动**：
- `products/update` webhook → 5min 去抖 → 异步生成快照
- 价格 / 库存差异另写 `product_price_history` / `product_inventory_history`

**手动**：
- 选店 + 产品 → 立即快照

---

## 8. 任务 / Saga / 进度

**入口**：`/tasks`

**展示**：
- 所有异步任务（推送 / 拉取 / saga 步骤）
- 状态：PENDING / RUNNING / SUCCESS / FAILED
- 父子任务（saga 主任务 → 多个子步骤）
- 失败重试（3 次后 dead letter）

---

## 9. 一键开店 Saga（Wave 3 主线）

**入口**：`/newstore`

**12 步流程**：
1. 输入新店域名 + 选模板 + 选产品集合
2. Shopify OAuth 装 app（用户授权）
3. 创建 `store` 行 + 加密 token 入库
4. 拉模板 zip 从 R2 → 推主题
5. 推 collections / menu / policies
6. 批量推产品（含媒体）
7. 推 base_template 默认 settings
8. 跑 `_dry_run` 验证
9. 走完通知 `NEW_STORE_SUCCESS`

**容错**：
- 任何步骤失败可手动重试
- saga_state_machine 持久化每步状态
- 成功后 `/newstore/[id]` 看完整 trace

---

## 10. 跨公司授权

**入口**：`/cross-auth`

**用途**：A 公司给 B 公司用户临时读 A 的产品（合作场景）

**约束**：
- 必带 `expires_at`（最长 180 天，最短 5 分钟）
- 24h 内到期前钉钉 `CROSS_AUTH_EXPIRING` 提醒（去重一次）
- 到期自动改 EXPIRED；可手动撤销（`@RequireSensitiveOp` 二次确认）

---

## 11. 模板库 + 指导文档（Wave 3）

### 11.1 模板库 `/templates`

平台维护的 base_template（主题 zip + default_replace_rules + default settings），一键开店时选用。

### 11.2 指导文档 `/guides`

平台运营写的步骤指引（Markdown），新人 onboarding。

---

## 12. 审批中心（Wave 4 新增）

**入口**：`/approvals`

**支持的审批类型**：
- `PRODUCT_ACCESS`：申请产品库使用权（按产品 / 集合）
- `CROSS_COMPANY_AUTH`：跨公司授权（默认 7 天，可改）

**审批模式**：
- 单签：指定具体审批人 ID
- 任一角色签：指定角色 code（任一持有该角色的人都可以批）

**操作**：
- 提交：含 type + payload + 审批人 / 角色
- 通过：通过 + 留 comment + 触发 type 对应 hook（PRODUCT_ACCESS → 写 sys_data_scope；CROSS_COMPANY_AUTH → 写 cross_company scope）
- 驳回：必填 comment；申请人可改 payload 重提
- 撤回：申请人可在 PENDING 时撤回
- 重提：仅 REJECTED 可重提（编辑 payload + 重置审批人）

**通知**：
- 提交后 → `APPROVAL_PENDING` 给审批人
- 决策后 → `APPROVAL_DECIDED` 给申请人
- 重提 → `APPROVAL_PENDING` 再次触发

**视图 tab**：
- 我审批的（待办）
- 我提交的
- 全部（按类型 / 状态过滤）

---

## 13. 通知订阅 + 多通道（Wave 4 新增）

### 13.1 用户视角 — 订阅设置

**入口**：`/profile` → 「通知订阅」section

**矩阵**：
- 行：每个通知事件（共 16 个）
- 列：DINGTALK / EMAIL / INAPP 三通道复选
- 每行总开关：「启用 / 关闭」

**事件分类**（6 个 category）：
| Category | 示例事件 |
|---|---|
| INVITATION | INVITATION_SENT / TEMP_USER_EXPIRING |
| STORE | NEW_STORE_SUCCESS / TOKEN_EXPIRING |
| PUSH | PRODUCT_PUSH_FAIL / THEME_PUSH_FAIL |
| APPROVAL | APPROVAL_PENDING / CROSS_AUTH_EXPIRING |
| OPS | BACKUP_FAIL / HIGH_RISK_OP |
| SYSTEM | STAFF_FROZEN |

### 13.2 系统视角 — 调度链路

```
事件触发点 (例: ApprovalEngine.approve)
     │
     ▼
NotificationDispatcher.notifyUser    ←  保留 Wave 1 签名
     │
     ▼
NotificationSendService.dispatch
     │
     ├─ 查 SubscriptionService.isSubscribed (按用户订阅过滤)
     ├─ 落 PENDING notification_log（按通道）
     ├─ 调通道：
     │    DINGTALK → MultiCorpDingTalkResolver → 钉钉 asyncsend_v2
     │    EMAIL    → EmailService.sendHtml
     │    INAPP    → InappBridge.send → InappService → inapp_message
     ├─ 成功 → SENT；失败 → FAILED + attempt_count + next_retry_at
     │
     ▼
NotificationRetryScheduler (cron 每分钟)
     │
     ├─ 扫 FAILED + attempt < 4 + next_retry_at <= NOW()
     ├─ 退避 60s / 300s / 900s
     └─ 4 次仍失败 → HIGH_RISK_OP 兜底告警 (Redis 1h dedupe)
```

### 13.3 多 corpId 钉钉

`sys_user.dingtalk_corp_id` → 查 `dingtalk_corp` 表拿 access_token → 发该企业。当前实现：所有 corp fallback 主企业；perCorp token cache 留生产分阶段。

---

## 14. 站内信（Wave 4 新增）

**入口**：`/inbox`

**功能**：
- Tab：全部 / 未读
- 单条点击 → 展开 body_html + 自动 markRead + 跳 link_url（如有）
- 顶部 unread count + 「全部标已读」
- 分页 20 条

**触发**：所有订阅了 INAPP 通道的事件都会写 inapp_message。

---

## 15. 个人中心

**入口**：`/profile`

**功能**：
- 基本信息：工号 / 用户名 / 邮箱 / 用户类型 / 状态 / 钉钉绑定
- 修改密码（≥ 8 位 + BCrypt）
- 通知订阅矩阵（见 §13）
- 临时密码强制修改（`passwordMustChange=true` 时阻断其他操作）

---

## 16. 回收站

**入口**：`/recyclebin`

**功能**：
- 三 tab：用户 / 组织 / 店铺
- 软删 90 天倒计时
- 恢复（钉钉二次确认）
- 90 天后自动物理删除（含 R2 资产）

---

## 17. 审计归档 + 备份（Wave 4 新增）

### 17.1 sys_audit_log 月归档

**自动**：每月 1 号 02:30 cron `AuditArchiveScheduler`：
- 归档「上上月」数据（确保跨月延迟下仍可读）
- JSONL → gzip → AES-256-GCM → R2（`audit-archive/YYYY/YYYY-MM.jsonl.gz.enc`）
- SHA-256 校验上传成功 → 物理删源
- 元数据写 `audit_archive_log`

**手动**：`POST /api/ops/backup/audit-archive/run` body `{month: "2026-04"}`（loopback only）

**查询历史**：见《restore-sop.md》§8（解密 + jq 查询）。

### 17.2 RDS 每日加密备份

**节点 A cron**：每天 03:00 `ops/backup/rds-backup.sh`：
- mysqldump --single-transaction
- gzip + AES-GCM (python cryptography) 或降级 AES-CBC（仅当 python 不可用）
- 上传 R2 桶 `shopifyhub-backup`
- 仅保留最新 1 份（lifecycle + 脚本 prune 双保险）
- 失败钉钉 `BACKUP_FAIL`

### 17.3 软删 90 天物理删（R2 端）

**节点 A cron**：每天 04:30 `ops/backup/audit-purge.sh`：
- 列 product_image / product_doc / template / snapshot 中所有 r2_key
- diff R2 桶 listing → 删除孤儿 object
- 24h grace（避免与上传期竞态）

### 17.4 恢复 SOP

`ops/backup/restore-sop.md` 含完整 PITR + 解密 + 重建 + Flyway 修复 + e2e 冒烟流程。

---

## 18. 监控 + 告警（Wave 4 完整化）

### 18.1 栈

- **Prometheus** 9090（scrape backend / node-exporter / cadvisor / RabbitMQ / blackbox）
- **Grafana** 3001（业务面板 + JVM + 主机）
- **Loki + Promtail** 3100（日志聚合）
- **Alertmanager** 9093（路由分流 ops / backend 钉钉群）

### 18.2 告警规则（5 个 group）

| group | rules |
|---|---|
| api-health | BackendDown / HighErrorRate5xx / LoginFailureSpike |
| jvm-resource | JvmHeapHigh / NodeDiskLow / NodeCpuHigh |
| business | StoreTokensExpiringSoon / ProductPushFailureRate / MqQueueBacklog |
| ops-alerts (W4-MON-01) | NodeDiskCritical / R2UploadFailureRate / R2UploadFailureCritical / SslCertExpiring30d / SslCertExpiring7d / DailyBackupOverdue / AuditArchiveOverdue / NotificationSendFailureRate / ApprovalPendingTooLong / CrossAuthExpiringSoon |
| infra-placeholder | RDSCPUHigh（待 cloudwatch_exporter） |

### 18.3 路由

- `severity=critical` 或 `alert_class∈{backup, archive}` → ops 群
- `team=ops + warning` → ops 群
- `team=backend + warning` → backend 群
- 其余 → default no-op

详见 `ops/monitoring/README.md`。

---

## 19. 灾备演练（Wave 4 新增）

**SOP**：`ops/disaster-recovery/dr-drill-sop.md`

**频次**：
- 季度（每 3 月）：L1 完整路径 dump 解密 + 重建 + e2e 冒烟
- 半年：L3 整库恢复
- 全年：L4 整机房切换

**RTO / RPO**：
- L2 单节点损坏：≤ 2h / ≤ 5min
- L3 RDS 数据损坏：≤ 4h / ≤ 24h
- L4 整机房失联：≤ 8h / ≤ 24h

**演练验收**：实际 RTO ≤ 表 0 预期 1.5 倍 + 18/18 e2e 全绿 + 钉钉 access_token 全程不 401。

---

## 20. 端到端冒烟脚本

| 脚本 | 覆盖 | 期望 |
|---|---|---|
| `bin/smoke-test.sh` | 登录 → 邀请 → 列表 → 接受 → DB 校验 | 6/6 全绿 |
| `bin/e2e-saga.sh` | 一键开店 saga 12 步 | 18/18 全绿 |
| `ops/release/wave4-regression-e2e.md` | Wave 4 G1-G5 全功能 | 手测 + ✓ |

---

## 21. 路由总览（前端 16+5 = 21 路由）

| 路由 | 模块 |
|---|---|
| `/dashboard` | 首页 |
| `/login` `/login/dingtalk-success` | 登录（公开）|
| `/forgot-password` `/reset-password` | 密码重置（公开，W4 新）|
| `/invitation/accept` | 邀请激活（公开）|
| `/invitations` | 临时员工邀请管理 |
| `/stores` | 店铺管理 |
| `/partner-stores` | 合作者店铺池 |
| `/products` `/products/[id]` `/products/import` | 产品库 |
| `/assets` | 资产快照 |
| `/snapshots` | 产品快照 |
| `/tasks` | 任务监控 |
| `/recyclebin` | 回收站 |
| `/profile` | 个人中心 + 通知订阅 |
| `/cross-auth` | 跨公司授权 |
| `/templates` | 模板库 |
| `/guides` | 指导文档 |
| `/newstore` | 一键开店 saga |
| `/approvals` `/approvals/[id]` | 审批中心（W4 新）|
| `/inbox` | 通知中心（W4 新）|

---

## 22. 数据字典快览

24 张主要业务表：

| 表 | 用途 |
|---|---|
| sys_user / sys_user_role / sys_role / sys_permission / sys_data_scope | 用户 + RBAC + 数据范围 |
| sys_org / sys_org_dingtalk_sync_log | 组织树 + 钉钉同步 |
| sys_audit_log | 审计 |
| user_invitation | 临时员工邀请 |
| dingtalk_corp | 多企业钉钉配置 |
| store / store_product / store_product_variant | 店铺 + 产品店内映射 |
| product / product_variant / product_image / product_doc / product_doc_block | 产品库 |
| asset_snapshot / asset_file | 主题资产快照 |
| product_snapshot / product_price_history / product_inventory_history | 产品快照 + 价格 / 库存历史 |
| task / push_conflict | 异步任务 + 推送冲突 |
| base_template / base_template_version | 平台模板库 |
| guide_doc | 指导文档 |
| preview_theme | 预览主题分配池 |
| approval_flow / approval_log | 审批中心（W4 新）|
| notification_event_def / notification_subscription / notification_log | 通知订阅 + 发送轨迹（W4 新）|
| inapp_message | 站内信（W4 新）|
| password_reset_token | 密码重置（W4 新）|
| audit_archive_log | 审计月归档元数据（W4 新）|

完整 schema 见 `backend-api/src/main/resources/db/migration/V*.sql`（V1-V24）。

---

_最后更新：2026-05-03（Wave 4 收尾）_
