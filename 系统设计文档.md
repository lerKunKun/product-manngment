# Shopify 多店铺资产管理与一键开店平台 — 系统设计文档

> 文档版本：**v1.3**
> 起草日期：2026-04-28
> 最近更新：2026-05-01（v1.3）
> 状态：决策完成，可进入 MVP 脚手架阶段

## 变更日志

| 版本 | 日期 | 摘要 |
|---|---|---|
| v1.0 | 2026-04-28 | 初稿。基于 shopify-theme-pull skill 与 FAQ/FAQ2/FAQ3 输出整体设计。 |
| v1.1 | 2026-04-28 | 整合 FAQ5/FAQ A6/FAQ A7：① 改为公网分布式部署（4 台服务器 + RDS 托管）；② 删除内网 cloudflared tunnel 方案，OAuth 回调走公网；③ 新增钉钉组织架构自动同步、自助注册、离职冷冻；④ 新增 EMPLOYEE 默认角色与多 corpId 白名单；⑤ 域名按"分公司简称.主域"规则；⑥ 价格用产品库价格、库存默认 1000；⑦ 主题安装走 `themes.json src=签名URL`；⑧ 富文本图片走"批签 + 短期 token"折中方案；⑨ 加合作者店铺池接入流程；⑩ 加 SMTP 邮件通道；⑪ 加公网安全加固栈与外部探活；⑫ 第 15 节重整为新的开放问题清单。 |
| v1.2 | 2026-04-28 | 整合 FAQ A8 终稿决策：① 生产数据库 = AWS RDS（MySQL 8）+ ElastiCache（Redis 8），区域 us-west-2，与节点 A 同区域；② 测试环境 = 开发机 / 测试服自建 MySQL 8 + Redis 8；③ 主域名锁定 `biounetwork.com`，企业邮箱 `mail.biounetwork.com`；④ Shopify Partner App（测试）已就绪；⑤ CI/CD 主线切换为 **GitHub Actions**（self-hosted runner 部署到节点 C），不再启用自建 GitLab；⑥ 第 13 节工时调整为"先 Wave 0 脚手架"；⑦ R-1~R-4 在第 15 节标记已确认。 |
| v1.3 | 2026-05-01 | 服务器全部重新采购，部署架构调整：① 节点重排为 A(US 4c6g 主应用) / B(HK 2c2g 钉钉网关 + 跳板) / C(SG 4c8g 监控 + CI + backend 备用副本) / D(SG 域名邮箱专属服务器，独立采购，不混跑业务)；② 删除原节点 D 国内边缘（SMTP gateway / 国内跳板 / Webhook 反代），跳板能力上移到节点 B（HK），SMTP 由 backend 直连 `mail.biounetwork.com`，不再走转发；③ 节点 B 不再承担 backend-api 备用副本，专职钉钉网关 + jump-host；备用副本挪到节点 C；④ 新增 **临时员工邮件邀请** 功能：与钉钉自助注册并行的第二入口，邀请时强制指定角色 / 数据范围 / 过期时间（最长 90 天，无默认值），邀请链接 48h 有效，邮件下发临时密码（首次登录强制改密），账号到期自动 EXPIRED + 收回所有数据范围；⑤ 新增 `user_invitation` 表、`sys_user` 加 `user_type` / `expires_at` / `EXPIRED` 状态、新增 `TEMP_STAFF` 内置角色、新增 `TEMP_USER_INVITE` / `TEMP_USER_REVOKE` 权限点；⑥ R-3 改为"邮箱专属服务器，业务直连 SMTP"。 |

---

## 0. 文档说明

- `/Users/zhangxueqian/development/product-manngment/products_export.csv`（Shopify 标准产品 CSV 模板，42 列）

文档面向：架构师、后端、前端、运维、产品。开发可直接据此拆任务。

> 文档末尾保留 **第 15 节 待确认遗留项**，开发过程中如发现新疑点请回写到那一节。

---

## 1. 项目目标

构建一个**多分公司、多部门、多店铺**的 Shopify 资产中台，支持：

1. **资产拉取与快照存储**：把每个店铺的 active theme（含 liquid/assets/locales）、产品、policy、menu、collection 完整拉下来，永久存档。
2. **统一产品库**：跨分公司可见的全平台产品中心，含产品信息、采购信息、媒体、需求文档；支持申请-审批后获得使用权。
3. **一键推送**：产品/主题/媒体一键推送到指定店铺；详情页可在 Partner dev store 上预览。
4. **一键开店**：新店授权 → 媒体 → collection → 产品 → 主题，全自动复刻并替换域名/品牌。
5. **权限与隔离**：分公司级数据隔离 + 跨公司审批授权（7 天时效）+ 钉钉单点登录 + 钉钉消息通知（事件可订阅）。
6. **灾备能力**：店铺意外被封/数据损毁时，可基于全量快照在新店快速重建。
7. **公网部署**：分布式多区域服务器，安全合规上线。

---

## 2. 核心功能清单

### 2.1 店铺与资产
- 店铺列表（按分公司维度）、Token 状态总览、Token 自动刷新
- Active Theme 整包拉取（liquid / assets / locales / templates / sections / config / 媒体）
- Policy / Menu / Collection 手动备份
- 媒体文件（视频、图片）下载并落 R2
- 产品按 status 变更或内容字段变更自动建快照（5 分钟去抖）
- 价格/库存改动走单独历史表，不建快照

### 2.2 产品管理
- **产品信息**：完全对齐 Shopify 官方 CSV 42 字段；CRUD；CSV 导入/导出；多店铺批量推送；广告贴/对标页/产品素材链接（仅存链接不抓取）
- **采购管理**：SKU、采购成本、克重、物流标注 tag、采购链接；改 SKU 二次确认（钉钉验证码）+ 自动同步到所有已映射店铺 + 强制审计
- **产品媒体**：主图、详情页媒体、需求文档（TipTap 富文本，单文件 ≤ 20MB，最多 20 个，支持 md/docx/xls/csv/pdf）
- **多店铺上架状态**：webhook 实时 + 定时全量兜底；状态在产品信息页可双向同步，其他页面只读

### 2.3 推送与开店
- 单个/批量产品推送指定店铺
  - 匹配键：`Handle`
  - 价格：使用产品库 `product_variant.price` 实际价格
  - 库存：默认 1000
  - `Continue selling when out of stock`：默认开
  - 冲突处理：先跳过整批后续，最后批量决策（覆盖/跳过）
- **详情页预览**：选用分公司合作者店铺池中的 dev store，动态创建 preview theme，返回预览链接（每用户每店保留 3 个 + 24h 未访问清理）
- **一键开店**：新店 OAuth 授权 + App 安装 → 媒体上传 → collections → 产品 → menu/policy/markets 提示文档 → 模板 zip 通过 `themes.json src=签名URL` 安装 → 发布

### 2.4 组织、权限、流程
- 多分公司、多部门组织树
- 钉钉企业内部应用扫码登录（多 corpId 白名单）+ 用户名密码登录二选一
- 钉钉组织架构 + 员工自动同步（实时事件 + 每天 02:00 定时兜底）
- 自助注册：扫码即开账号 + 工号生成（jobNumber 优先，空则 `EMP000001` 自增）+ 备用密码（用户首次登录修改）+ 默认 EMPLOYEE 角色
- 离职：钉钉 `user_leave_org` 事件触发**冷冻 30 天**（禁用登录），到期软删除（90 天后物理删）
- RBAC + 数据范围（分公司隔离）+ 显式跨公司授权（默认 7 天时效，到期前 24h 钉钉提醒续期，24h 不续自动收回）
- **统一审批中心**：跨公司授权审批 + 产品库使用权审批均走同一引擎；单签；驳回可改重提
- **钉钉消息**：用户可订阅（按事件类型勾选）；钉钉 access_token 自动维护

### 2.5 平台与运维
- 审计日志（1 年 MySQL + 永久 R2 加密归档）
- MySQL 每日 03:00 加密备份至 R2（AES-256-GCM）
- 软删除（90 天可恢复）
- GitHub Actions CI/CD（公网 runner + 节点 C self-hosted runner 用于生产部署）
- 一键开荒服脚本
- Prometheus + Grafana + Loki 自监控 + UptimeRobot 外部探活
- Cloudflare WAF + Access + Rate Limit 公网防护

---

## 3. 技术栈

| 层 | 选型 | 版本 |
|---|---|---|
| 前端 | Next.js + React + shadcn/ui + TailwindCSS + TanStack Query + Zustand + TipTap | Next.js 15 / React 19 |
| 后端 | Spring Boot + Spring Security + MyBatis-Plus + Redisson + Flyway | Spring Boot 3.3.x / Java 21 |
| 关系库 | MySQL 8.x（云厂商托管 RDS：platform + tenant_01~05） | 8.x |
| 缓存 | Redis（云厂商托管） | 8.x |
| 消息队列 | RabbitMQ（自建） | 4.x |
| 对象存储 | Cloudflare R2（单桶 + 前缀 + lifecycle） | - |
| 资产拉取 worker | FastAPI（封装现有 Python skill） | Python 3.12 |
| 容器化 | Docker + docker-compose（多机部署，预留 swarm 演进） | - |
| CI/CD | **GitHub Actions**（公网 runner + self-hosted runner 部署到节点 C） | latest |
| 代码托管 | GitHub | latest |
| 监控 | Prometheus + Grafana + Loki + Alertmanager；UptimeRobot 外部探活 | latest |
| 鉴权 | JWT (access 30min) + Redis 黑名单 refresh (7d) + 钉钉扫码 OAuth | - |
| 邮件 | SMTP（接公司域名邮箱） | - |
| CDN/WAF | Cloudflare Free（DNS + CDN + WAF + Rate Limit + Access） | - |

**强制要求**：所有功能模块通过 RabbitMQ 事件解耦，禁止直接跨模块调用 Service。

---

## 4. 整体架构

### 4.1 逻辑分层

```
┌────────────────────────────────────────────────────────────────────────┐
│  Cloudflare（DNS + CDN + WAF + Access + Rate Limit）                    │
│  - 主域 biounetwork.com                                                 │
│  - admin.biounetwork.com / api.biounetwork.com / oauth.biounetwork.com  │
│  - {companyCode}.biounetwork.com（分公司前端）                          │
└────────────────────────────────────────────────────────────────────────┘
                                  │
┌────────────────────────────────────────────────────────────────────────┐
│  Nginx（运行在主应用节点，TLS 终结 + 反代）                             │
└────────────────────────────────────────────────────────────────────────┘
                  │                          │
       ┌──────────▼──────────┐    ┌──────────▼──────────┐
       │  frontend-admin     │    │  backend-api        │
       │  Next.js 15         │    │  Spring Boot 3.3    │
       │  shadcn/ui          │    │  Java 21 + VT       │
       └─────────────────────┘    └──────────┬──────────┘
                                             │
        ┌────────────────────────────────────┼─────────────────────────┐
        │                                    │                         │
   ┌────▼──────┐  ┌──────────┐  ┌──────────┐ ┌───▼────┐  ┌────────────┐
   │ MySQL 8   │  │ Redis 8  │  │RabbitMQ 4│ │ R2     │  │asset-worker│
   │ 托管 RDS  │  │ 托管     │  │ 自建      │ │(R2 SDK)│  │ FastAPI    │
   │ platform  │  │          │  │          │ │        │  │ +Shopify   │
   │ tenant01  │  │          │  │          │ │        │  │  CLI       │
   │   ...     │  │          │  │          │ │        │  └────────────┘
   │ tenant05  │  │          │  │          │ │        │
   └───────────┘  └──────────┘  └──────────┘ └────────┘
                                             │
                            ┌────────────────▼─────────────────┐
                            │   外部对接                         │
                            │   - Shopify Admin API (REST/GQL)  │
                            │   - Shopify Webhooks              │
                            │   - 钉钉开放平台（扫码 + 消息 +    │
                            │     通讯录订阅 + 多 corpId）       │
                            │   - SMTP（公司域名邮箱）           │
                            └───────────────────────────────────┘
```

### 4.2 模块划分（后端 Spring Boot）

```
backend-api/
├── core            # 公共：异常、响应包装、租户上下文、AOP、JWT
├── auth            # 用户名密码 + 钉钉扫码 + JWT + 二次验证
├── invitation      # 临时员工邮件邀请：发起、接受、首次设密、到期收回
├── rbac            # 角色、权限、菜单、按钮、数据范围
├── org             # 分公司 / 部门 / 用户 / 钉钉同步
├── tenant          # 多数据源动态路由
├── store           # 店铺、Shopify Token、scopes、合作者店铺池
├── asset           # 主题、policy、menu、collection、媒体快照
├── product         # 产品信息、CSV 导入导出、快照
├── purchase        # 采购信息
├── product_media   # 媒体、需求文档
├── push            # 产品推送、主题推送、媒体推送
├── preview         # 详情页预览
├── newstore        # 一键开店编排
├── approval        # 统一审批中心
├── notification    # 钉钉通知 + 邮件 + 用户订阅
├── audit           # 审计日志
├── ops             # 备份、归档、定时任务（含临时账号到期巡检）
└── oauth           # Shopify OAuth 回调（合并到 backend，不再独立）
```

每个模块对外只暴露 REST + 事件订阅，内部禁止直调彼此 Service。

---

## 5. 部署架构（公网分布式）

### 5.1 服务器资源与角色分配

| 节点 | 规格 | 区域 | 角色 |
|---|---|---|---|
| **节点 A**（主应用） | 4c6g | 美国 | nginx / backend-api（主） / frontend-admin / asset-worker / RabbitMQ / Redis 本地缓存 |
| **节点 B**（钉钉网关 + 跳板） | 2c2g | 香港 | dingtalk-gateway 容器（专门承接钉钉 API 出/入站，国际线路稳定）+ jump-host（运维 SSH 入口，国内→HK 30~50ms） |
| **节点 C**（监控 + CI + 备用） | 4c8g | 新加坡 | Prometheus / Grafana / Loki / Promtail / Alertmanager / **GitHub Actions self-hosted runner**（生产部署 SSH 编排）+ backend-api 备用副本（健康检查为主，故障人工切流） |
| **节点 D**（域名邮箱） | 由邮箱方案决定 | 新加坡 | `mail.biounetwork.com` 邮箱服务器（专职、独立采购/部署，**不混跑任何业务容器**，避免影响邮件 deliverability/uptime） |

> 节点 A 是主战场；节点 B/C 各司一职。MySQL/Redis 不部署在任何节点，由云厂商托管（独立计费）。**节点 D 仅作为业务系统的对外发件链路接入点（backend 直连 SMTP），不参与系统运行时编排。**

### 5.2 节点详细职责

**节点 A — 美国主应用**
```yaml
services:
  nginx:           # 80/443，TLS 终结，反代
  frontend-admin:  # next start
  backend-api:     # java -jar，主实例
  asset-worker:    # uvicorn app:app
  rabbitmq:        # 自建 MQ，docker volume 持久化
  redis-local:     # 仅作热点缓存（与托管 Redis 区分）
  promtail:        # 日志采集 → 发往节点 C 的 Loki
  node-exporter:   # 暴露给节点 C 的 Prometheus
  sshd:            # 仅监听 WireGuard 网卡，不对公网开放
```
为什么放美国：
- 紧邻 Shopify（API 延迟 < 80ms）
- Shopify Webhook 公网回调延迟低
- 主带宽吃产品/主题推送，海外服务器带宽便宜
- 与 RDS（us-west-2）同区域，私网延迟 < 2ms

**节点 B — 香港钉钉网关 + 跳板**
```yaml
services:
  dingtalk-gateway:  # 自写小服务，接收钉钉事件订阅 webhook + 转发钉钉消息
  jump-host:         # 公网 sshd（改非默认端口 + key only + fail2ban）
                     # 运维 SSH 入口，下游节点用 ProxyJump 经此跳到 A/C 的 WireGuard 网卡
  promtail:
  node-exporter:
```
为什么放香港：
- 钉钉 API 在国内，HK 路由延迟 30~50ms（美国到钉钉 200ms+，且偶发被风控）
- 钉钉事件订阅 webhook 要求公网可达，HK 服务器有公网 IPv4
- 国内运维 SSH→HK 30~50ms，体感最好；下游节点 sshd 收回 WireGuard 私网，攻击面收敛到 HK 单点
- HK 节点资源富余（dingtalk-gateway < 1G RAM），承担跳板零边际成本

**节点 C — 新加坡监控 + CI + 备用副本**
```yaml
services:
  prometheus:        # 抓所有节点 metrics
  grafana:           # 可视化
  loki:              # 日志聚合（保留 90 天）
  alertmanager:      # 告警分发（钉钉 + 邮件）
  gh-runner:         # GitHub Actions self-hosted runner（docker），用于生产部署
                     # 通过 ProxyJump 经节点 B 跳板登录节点 A 执行 docker-compose pull && up -d
  backend-api-bk:    # 备用应用副本（健康检查为主，故障时人工切流）
  sshd:              # 仅监听 WireGuard 网卡
```
为什么放新加坡：
- 节点 A/B 都能低延迟接入
- 与主应用解耦，主应用挂了监控还能告警
- 部署 runner 与生产应用物理隔离，泄露面小
- 备用副本与主应用跨区域，单区域故障可切流（DNS 切换为主，蓝绿留 v2）

**节点 D — 新加坡域名邮箱（独立）**

`mail.biounetwork.com` 已由邮箱方案商单独部署，本系统**不在该机器上跑任何业务容器**：
- 业务发件链路：`backend-api → mail.biounetwork.com:465（SMTPS，公网或同区域私网）`
- 邮箱机仅暴露标准邮件端口（25 / 465 / 587 / 993 / 995），按邮箱产品自身安全策略加固
- 业务系统不持有该机器的 SSH 凭证，运维由邮箱方案侧负责
- 不需要纳入 WireGuard mesh，也不需要 GitHub Actions runner 推送

为什么放新加坡：
- 邮箱本身在 SG，与节点 C / 业务发件链路同区域，延迟最低
- 不再需要"国内 IP 信誉好"作为发件机选址理由，原 v1.2 节点 D smtp-gateway 转发链路撤销

### 5.3 数据库托管选型（已确认）

**生产环境**：
- **MySQL 8.0**：AWS RDS，区域 `us-west-2`（与节点 A 同区域，私网延迟 < 2ms）
  - 实例规格起步：`db.t4g.medium`（2c4g，可弹性扩到 m6g 系列）
  - 启用 Multi-AZ（双可用区主备）
  - 自动备份保留 7 天 + PITR
  - 强制 SSL 连接（`require_secure_transport=ON`）
- **Redis 8**：AWS ElastiCache for Redis，区域 `us-west-2`
  - 起步 `cache.t4g.small`，单节点；后续按需启用副本
  - 启用 in-transit / at-rest 加密
- **RabbitMQ**：自建在节点 A，docker volume 持久化

**测试 / 开发环境**：
- 自建 MySQL 8 + Redis 8（本地 docker-compose 或测试服）
- RabbitMQ 同样自建
- 与生产隔离的独立 R2 桶或 prefix
- 用 Shopify Partner App 测试 store 跑通流程

**测试 → 生产差异**：

| 项          | 测试                | 生产                        |
| ---------- | ----------------- | ------------------------- |
| MySQL      | 自建 docker         | AWS RDS Multi-AZ          |
| Redis      | 自建 docker         | AWS ElastiCache           |
| MQ         | 自建 docker         | 自建 docker（同）              |
| R2         | 测试桶 / prefix      | 生产桶                       |
| 钉钉         | 测试企业 corpId       | 生产 corpId                 |
| Shopify    | Partner dev store | 真实店铺 + Custom App / OAuth |
| Cloudflare | 不开 WAF / Access   | 全开                        |
| 备份         | 不备份               | 每日加密 dump                 |

### 5.4 网络拓扑与安全

```
                      Internet
                          │
              Cloudflare（DNS / CDN / WAF / Access）
                          │
            ┌─────────────┼──────────────┐
            ▼             ▼              ▼
       节点 A (US)   节点 B (HK)    节点 C (SG)
       公网 IP       公网 IP        公网 IP（仅 GitHub Actions self-hosted runner 出站）
       sshd→WG       sshd 公网      sshd→WG
                     (jump-host)
            │             │              │
            └──── WireGuard 私网 Mesh ────┘
                          │
                          ▼
                     RDS MySQL / ElastiCache Redis
                     （仅节点 A/C 私网可达）
                          │
                  R2（S3 兼容 SDK 通过公网访问）

       节点 D (SG, mail.biounetwork.com)
       独立邮箱服务器，不入 WireGuard mesh
       业务侧仅作为 SMTP 客户端连接，不反向访问
```

**安全约束**：
- MySQL/Redis 仅 RDS 安全组放行节点 A/C 私网 IP，**绝不暴露公网**
- 节点 A/B/C 用 WireGuard 建私网 mesh，运维 SSH 入口仅在节点 B（HK）暴露公网
- 节点 A/C 的 sshd 改 listen WireGuard 网卡 IP，不对公网开放；运维通过 `ssh -J jump@hk` 跳板进入
- SSH 仅 key 登录、改非默认端口、fail2ban；HK 跳板额外开 Cloudflare Access（钉钉 SSO，可选）
- 所有节点 ufw 默认 deny，仅放行所需端口
- Cloudflare 之外无任何 80/443 暴露源站（Cloudflare 通过 Origin Cert 透传）
- HK 跳板挂掉的破窗手段：云厂商控制台 console / VNC（每家自带，免费），提前留 root 密码于密码管理器
- 节点 D（邮箱）不在 mesh 内，业务系统仅以 SMTP 客户端身份单向连接，不持有其 SSH 凭证

### 5.5 域名与子域规划

| 域名 | 用途 | 解析到 |
|---|---|---|
| `admin.biounetwork.com` | 主公司管理员入口（看所有租户） | Cloudflare → 节点 A nginx |
| `{companyCode}.biounetwork.com`（如 `hy.biounetwork.com`） | 分公司管理员入口（仅本公司） | 同上，按 Host header 区分 |
| `api.biounetwork.com` | 后端 API | 同上，路径 `/api/*` |
| `oauth.biounetwork.com` | Shopify OAuth 回调（也可并入 api） | 同上，路径 `/oauth/callback` |
| `webhook.biounetwork.com` | Shopify Webhook 接收 | 同上，路径 `/webhook/*` |
| `dingtalk.biounetwork.com` | 钉钉事件订阅回调 + 出站代理 | Cloudflare → 节点 B |
| `monitor.biounetwork.com` | Grafana 内部访问（启用 Cloudflare Access） | Cloudflare → 节点 C |
| `mail.biounetwork.com` | 域名邮箱服务器（节点 D，独立采购） | MX + A 直接指向邮箱机，不经 Cloudflare 代理 |
| `invitation.biounetwork.com` 或 `admin.biounetwork.com/invitation` | 临时员工邀请落地页 | Cloudflare → 节点 A nginx；可作为 admin 子路径，无需独立子域 |
| ~~`gitlab.biounetwork.com`~~ | 已废弃，CI/CD 改用 GitHub Actions | — |

**`companyCode` 规则**：
- 来源：`sys_org.code`（分公司组织节点的 code 字段）
- 命名：地区拼音简写（如 `hy` 汉阳、`bj` 北京、`sh` 上海）
- 全局唯一，禁止与系统保留前缀冲突（`admin / api / oauth / webhook / dingtalk / monitor / www / mail / cdn`）

**前端按子域分流逻辑**：
- 用户访问 `hy.biounetwork.com` → 前端 SSR 时识别 host → 携带 `X-Tenant-Code: hy` 调 API
- 后端鉴权拦截器：解析 host → 校验"用户所属公司或被授权可访问该公司" → 不匹配返回 403
- `admin.biounetwork.com` 仅平台超管可登录，普通员工被引导到自己公司的子域

### 5.6 开荒脚本

`ops/bootstrap/` 目录提供：
- `01-install-docker.sh`：装 docker、compose、必要工具
- `02-init-system.sh`：开 swap、调内核参数（vm.max_map_count、somaxconn）、ufw 规则、改 SSH 端口、装 fail2ban
- `03-setup-wireguard.sh`：节点 A/B/C 间私网 mesh 建立（节点 D 邮箱独立，不入 mesh）
- `04-deploy-stack.sh`：拉镜像、生成 .env、起 compose（按节点 A/B/C 不同 compose 文件；节点 D 由邮箱方案商部署，不在本脚本范围）
- `05-init-db.sh`：连 RDS、跑 Flyway 迁移、预创建 5 个租户库
- `06-create-r2-lifecycle.sh`：调 Cloudflare API 创建 lifecycle 规则、签发 R2 凭证
- `07-setup-cloudflare-access.sh`：管理员域名启用 Access policy
- `08-register-gh-runner.sh`：节点 C 注册 GitHub Actions self-hosted runner，绑定 repo + label
- `09-setup-jump.sh`：节点 B 配置 jump 用户 + ProxyJump 转发规则；节点 A/C 的 sshd 改 listen WireGuard 网卡
- `10-setup-smtp.sh`：在节点 A backend 端验证与 `mail.biounetwork.com:465` 的 SMTPS 连通性 + 发件域名 SPF/DKIM/DMARC 自检
- `99-smoke-test.sh`：自检（健康检查、登录、写入 R2、发钉钉测试消息、发邮件测试、发临时员工邀请测试邮件）

每个脚本支持 `--dry-run` 与幂等执行。

---

## 6. 数据库设计

### 6.1 库分布

| 库名 | 用途 | 物理位置 |
|---|---|---|
| `platform` | 平台主库 | 托管 RDS |
| `tenant_01` ~ `tenant_05` | 分公司业务库 | 托管 RDS（同实例不同 schema 起步，未来可横向拆实例） |

> **决策**：产品主表落 `platform`（**方案 A**）。租户库只存"该店铺推送了哪些产品 + 推送状态"。
> **扩容**：分公司 ≥ 6 时加新库 `tenant_06`，通过 `sys_tenant_datasource` 表热加载；**禁止共享库**。

### 6.2 平台主库核心表

```sql
-- ===== 用户与组织 =====
sys_user(
  id, employee_no,                   -- 工号：钉钉 jobNumber 优先；为空则 EMP000001 自增；TEMP 用户工号 TMP000001 自增
  username, password_hash,           -- 备用密码（首次扫码注册后要求修改；TEMP 用户邮件下发临时密码后强制改）
  password_must_change BOOLEAN,
  email, phone,
  dingtalk_unionid, dingtalk_userid, dingtalk_corp_id,
  default_tenant_id,
  user_type ENUM('STAFF','TEMP') DEFAULT 'STAFF',  -- STAFF=正式员工（钉钉来源）；TEMP=邮件邀请的临时员工
  expires_at TIMESTAMP NULL,                         -- 仅 TEMP 必填：账号到期时间，到期 ops 任务切 EXPIRED
  status ENUM('ACTIVE','FROZEN','DELETED','EXPIRED'), -- FROZEN=离职冷冻30天；EXPIRED=临时账号到期失效
  frozen_at,                                  -- 冷冻起始时间
  expired_at,                                 -- 实际到期切换时间（与 expires_at 不同：前者是计划，后者是执行）
  invited_by BIGINT NULL,                     -- 仅 TEMP：邀请人 user_id
  invitation_id BIGINT NULL,                  -- 仅 TEMP：关联 user_invitation.id
  last_login_at, last_login_ip,
  created_at, updated_at, deleted_at
)
sys_org(
  id, parent_id, type ENUM('COMPANY','DEPT'), name, code,
  tenant_id,                          -- COMPANY 节点对应 tenant_xx；DEPT 继承父
  path,                               -- 物化路径，如 /1/3/12/，用于子树查询
  dingtalk_dept_id,                   -- 与钉钉部门映射
  dingtalk_corp_id,                   -- COMPANY 节点对应钉钉企业 ID
  sort, status, created_at, updated_at, deleted_at
)
sys_org_dingtalk_sync_log(            -- 同步日志
  id, sync_type ENUM('FULL','EVENT'), trigger_event,
  affected_users INT, affected_depts INT,
  status, error_msg, started_at, finished_at
)

-- ===== RBAC =====
sys_role(id, code, name, scope ENUM('PLATFORM','TENANT','DEPT'), description, builtin BOOLEAN)
-- 内置角色：PLATFORM_SUPER, COMPANY_ADMIN, DEPT_LEAD, STORE_ADMIN, OPERATION, PURCHASING, DESIGNER, READONLY, EMPLOYEE
sys_permission(id, code, resource, action, name, parent_id, type ENUM('MENU','BUTTON','API'))
sys_role_permission(role_id, permission_id)
sys_user_role(user_id, role_id, org_id)         -- 角色绑定到具体组织节点
sys_data_scope(                                 -- 显式数据授权（跨公司 + 临时员工初始权限）
  id, user_id, scope_type ENUM('STORE','COMPANY','PRODUCT'),
  scope_id, access ENUM('READ','WRITE'),
  granted_by, granted_at, expires_at,           -- 时效字段（必填）
  approval_id,                                  -- 关联审批单（TEMP 用户来源时为 NULL，由邀请直接生成无需审批）
  source ENUM('CROSS_AUTH','INVITATION') DEFAULT 'CROSS_AUTH', -- INVITATION = 邀请时直接生成
  status ENUM('ACTIVE','EXPIRING','EXPIRED','REVOKED'), -- EXPIRING = 24h 内到期已发提醒
  created_at
)

-- ===== 临时员工邮件邀请 =====
user_invitation(
  id, email,                                    -- 受邀人邮箱（必填，发送邀请目标）
  target_company_id,                            -- 邀请绑定的分公司（必填，单一，不可跨）
  target_dept_id,                               -- 可选：绑定到具体部门
  role_codes JSON,                              -- 邀请时分配的角色 code 列表（必填，至少 1 个，通常含 TEMP_STAFF）
  data_scopes JSON,                             -- 邀请时分配的数据范围（必填，结构同 sys_data_scope）
  account_expires_at TIMESTAMP NOT NULL,        -- 临时账号到期时间（必填，最长发起后 90 天，前后端强校验）
  invitation_token VARCHAR(64) UNIQUE,          -- 邀请链接一次性 token（HMAC 签名）
  link_expires_at TIMESTAMP NOT NULL,           -- 邀请链接 48h 有效期（不同于账号到期）
  initial_password_hash,                        -- BCrypt 后的临时密码；明文仅在邀请邮件中出现一次
  status ENUM('PENDING','ACCEPTED','LINK_EXPIRED','REVOKED'),
  invited_by, invited_at,
  accepted_at,                                  -- 受邀人点链接 + 输入临时密码后落账时间
  created_user_id BIGINT NULL,                  -- 接受后创建的 sys_user.id
  revoked_by, revoked_at, revoke_reason,
  INDEX(email, status), INDEX(invitation_token), INDEX(account_expires_at)
)

-- ===== 租户路由 =====
sys_tenant(id, code, name, datasource_key, status)
sys_tenant_datasource(
  id, tenant_id, jdbc_url, username, encrypted_password,
  pool_min, pool_max, status
)

-- ===== 钉钉企业配置（多 corpId） =====
dingtalk_corp(
  id, corp_id UNIQUE, corp_name,
  is_main BOOLEAN,                              -- 主公司 = 1，其他分公司 = 0
  agent_id, app_key, encrypted_app_secret,
  access_token, access_token_expires_at,        -- 7000s 自动刷新
  bound_company_org_id,                         -- 关联 sys_org（COMPANY 节点）
  whitelist_corp_ids JSON,                      -- 主公司：所有分公司 corpId；分公司：仅本身
  status, created_at, updated_at
)
-- 白名单逻辑：用户扫码登录的 corpId 必须命中当前子域名对应租户的 whitelist_corp_ids

-- ===== 全平台产品库 =====
product(
  id, owner_company_id, owner_dept_id,
  handle UNIQUE,                                -- 全平台唯一匹配键
  title, body_html, vendor, product_category, type, tags, published,
  seo_title, seo_description, status,
  created_by, created_at, updated_at, deleted_at,
  INDEX(owner_company_id, owner_dept_id),
  INDEX(handle)
)
product_option(id, product_id, name, position, linked_to)
product_variant(
  id, product_id, sku, position,
  option1, option2, option3,
  grams, weight_unit, inventory_tracker, inventory_qty,
  inventory_policy, fulfillment_service,
  price, compare_at_price, requires_shipping, taxable, barcode,
  unit_price_total_measure, unit_price_total_measure_unit,
  unit_price_base_measure, unit_price_base_measure_unit,
  variant_image, tax_code, cost_per_item,
  created_at, updated_at, deleted_at,
  INDEX(product_id), INDEX(sku)
)
product_image(id, product_id, src, position, alt_text, gift_card)
product_metafield(id, product_id, namespace, key, value, type)
product_google_shopping(...)
product_external_link(
  id, product_id, kind ENUM('AD','BENCHMARK','MATERIAL'),
  url, note, created_by, created_at
)

-- 产品快照（status 变更或内容字段变更触发，5 分钟去抖）
product_snapshot(
  id, product_id, version, trigger_type ENUM('STATUS','CONTENT','MANUAL'),
  csv_r2_key,                                   -- R2 中的 CSV 备份路径
  json_r2_key,                                  -- R2 中的完整 JSON 备份路径
  changed_fields JSON, snapshot_at, created_by
)
-- 价格历史（量大走单独表）
product_price_history(
  id, variant_id, price, compare_at_price, changed_at, source ENUM('LOCAL','SHOPIFY')
)
-- 库存历史
product_inventory_history(
  id, variant_id, store_id, qty, changed_at, source ENUM('LOCAL','SHOPIFY')
)

-- ===== 采购管理 =====
purchase_info(
  id, product_id, variant_id,
  purchase_url, cost, currency, gross_weight, weight_unit,
  logistics_tags JSON,
  updated_by, updated_at, deleted_at
)
sku_change_log(                                 -- 改 SKU 必落
  id, variant_id, old_sku, new_sku,
  changed_by, confirmed_at, dingtalk_verify_id, sync_status, sync_result JSON
)

-- ===== 产品媒体 / 需求文档 =====
product_doc(
  id, product_id, type ENUM('RICH_TEXT','FILE'),
  rich_text_json,                               -- TipTap JSON
  file_r2_key, file_name, file_size, file_mime,
  created_by, created_at, deleted_at
)

-- ===== 审批中心 =====
approval_flow(
  id, biz_type ENUM('PRODUCT_ACCESS','CROSS_COMPANY_AUTH'),
  biz_id,
  applicant_id, applicant_org_id,
  approver_role_codes JSON,                     -- 任一角色单签
  status ENUM('PENDING','APPROVED','REJECTED','CANCELLED'),
  payload JSON,                                 -- 申请理由、范围、时效（必填）
  decided_by, decided_at, decision_note,
  created_at, updated_at
)
approval_log(id, approval_id, action, operator_id, note, created_at)

-- ===== 钉钉通知 / 邮件 =====
notification_event_def(
  code, category, default_enabled, description
)
notification_subscription(
  id, user_id, event_code, channel ENUM('DINGTALK','EMAIL','INAPP'),
  enabled, created_at, updated_at
)
notification_log(
  id, event_code, target_user_id, channel,
  payload JSON, status ENUM('PENDING','SENT','FAILED'),
  retry_count, sent_at, error_msg
)
email_template(id, code, subject, body_html, body_text)

-- ===== 模板库（平台级，仅平台超管管理） =====
base_template(
  id, code, name, description,
  status ENUM('ACTIVE','DEPRECATED'),
  created_by, created_at
)
base_template_version(                          -- 版本管理
  id, template_id, version, theme_zip_r2_key,
  default_replace_rules JSON,
  changelog, is_default BOOLEAN,
  created_by, created_at,
  UNIQUE(template_id, version)
)

-- ===== 平台指导文档（一键开店时弹出） =====
guide_doc(
  id, code, title, content_md, content_rich_json,
  related_template_id,                          -- 可关联模板
  status, created_by, created_at, updated_at
)

-- ===== 文件签名 token（折中签名方案） =====
file_sign_token(
  id, user_id, token, scope_keys JSON,         -- 该 token 可签的 R2 key 列表
  expires_at, used_count, max_uses,
  created_at
)

-- ===== 审计 =====
sys_audit_log(
  id, user_id, username, employee_no, org_id, tenant_id,
  module, action, resource_type, resource_id,
  request_method, request_uri, request_payload JSON,
  response_status, ip, user_agent,
  sensitive BOOLEAN,                            -- 高危操作标记
  created_at,
  INDEX(user_id, created_at), INDEX(module, created_at), INDEX(created_at)
)

-- ===== 软删除统一规则 =====
-- 所有业务表加 deleted_at TIMESTAMP NULL；MyBatis 全局拦截。90 天后由 ops 任务彻底物理删（含 R2 文件）
```

### 6.3 租户库（每个分公司一份）核心表

```sql
-- ===== 店铺 =====
store(
  id, myshopify_domain UNIQUE, custom_domain, brand_name, dept_id,
  token_type ENUM('cli','custom_app','oauth'),
  encrypted_access_token, encrypted_refresh_token,
  expires_at, scopes JSON, last_refresh_at,
  is_dev_store BOOLEAN,                         -- 标记是否合作者店铺池中的 dev store
  is_partner_collab BOOLEAN,                    -- 是否 Partner Collaborator 加入
  status, created_at, updated_at, deleted_at
)
store_webhook(id, store_id, topic, callback_url, shopify_id, status)

-- ===== 资产快照清单 =====
asset_snapshot(
  id, store_id,
  kind ENUM('THEME','POLICY','MENU','COLLECTION','FULL'),
  version, theme_id, theme_name, role,
  pulled_by, pulled_at,
  r2_prefix,
  manifest_r2_key,
  json_count, media_count, total_bytes,
  status ENUM('PULLING','SUCCESS','FAILED'),
  log_excerpt
)
asset_file(
  id, snapshot_id, path, type ENUM('LIQUID','JSON','ASSET','LOCALE','VIDEO','IMAGE','OTHER'),
  r2_key, size, sha256, mime
)

-- ===== 店铺-产品映射 =====
store_product(
  id, store_id, product_id,                     -- product_id 指向 platform.product.id
  shopify_product_id, shopify_handle,
  status_in_shopify ENUM('active','draft','archived'),
  last_pushed_at, last_synced_at,
  created_at, updated_at, deleted_at,
  UNIQUE(store_id, product_id)
)
store_product_variant(
  id, store_product_id, variant_id,
  shopify_variant_id, shopify_inventory_item_id
)

-- ===== 推送/拉取任务 =====
task(
  id, type ENUM('ASSET_PULL','PRODUCT_PUSH','THEME_PUSH','NEW_STORE','PREVIEW'),
  store_id, product_ids JSON, target_store_id,
  status ENUM('PENDING','RUNNING','SUCCESS','FAILED','CANCELLED'),
  progress INT, current_step, total_steps,
  started_by, started_at, finished_at,
  payload JSON, result JSON, error_msg,
  INDEX(status, created_at)
)
task_event(id, task_id, ts, level, message)

-- ===== 预览主题管理 =====
preview_theme(
  id, dev_store_id, user_id, product_id,
  shopify_theme_id, preview_url,
  last_accessed_at, expires_at,                 -- 24h 未访问到期
  status, created_at,
  INDEX(user_id, dev_store_id, last_accessed_at)
)

-- ===== 推送冲突暂存 =====
push_conflict(
  id, task_id, product_id, store_id,
  reason ENUM('HANDLE_EXISTS'),
  resolution ENUM('PENDING','OVERWRITE','SKIP'),
  decided_by, decided_at
)

-- ===== OAuth 临时 session（10 分钟过期） =====
oauth_session(
  id, session_id UNIQUE, store_domain, scope, state_hmac,
  status ENUM('PENDING','COMPLETED','EXPIRED','FAILED'),
  initiated_by, initiated_at, completed_at,
  access_token_temp                              -- 完成后立即转入 store 表，本字段清零
)
```

### 6.4 索引与性能策略
- 跨库 join 一律禁止，应用层组装
- `product_snapshot`、`sys_audit_log`、`product_price_history` 按月分区
- 归档脚本按月 drop partition
- `task` 表按 status 部分索引；finished 状态超 30 天移历史表
- 所有时间字段统一 UTC（`TIMESTAMP`），前端按用户时区展示
- RDS 启用 binlog（行级），便于灾备回放

---

## 7. R2 对象存储布局

桶名：`shopify-assets`（占位，由 .env 注入）

```
shopify-assets/
├── tenants/{tenantId}/stores/{storeId}/snapshots/{kind}/{version}/
│   ├── manifest.json
│   ├── liquid/...
│   ├── assets/...
│   ├── locales/...
│   ├── templates/*.json
│   ├── sections/*.json
│   ├── config/settings_data.json
│   ├── videos/*.mp4
│   └── images/*.{jpg,png,webp,gif,svg}
├── products/{productId}/
│   ├── csv/{version}.csv
│   ├── json/{version}.json
│   ├── images/{position}-{hash}.{ext}
│   └── docs/{docId}-{filename}
├── templates/base/{templateId}/{version}/theme.zip
├── public/                                  # 公开桶或公开前缀（头像、Logo、品牌素材）
│   └── ...
├── backup/mysql/{yyyyMMdd}/all.sql.gz.enc   # AES-256-GCM
└── archive/audit/{yyyy-MM}.jsonl.gz.enc     # 审计日志月归档
```

**Lifecycle 规则**：
- `backup/mysql/`：保留最新 1 份（覆盖式上传或显式删旧）
- `archive/audit/`：永久保留
- `tenants/.../snapshots/`：永久保留
- `products/.../images/`：永久保留
- 软删除 90 天后由 ops 任务物理删

**访问策略**：
- `public/` 前缀：公开访问（Logo 等无敏感）
- 其他前缀：私有；通过应用层签名 URL 访问（折中签名方案，见 8.x）

---

## 8. 关键模块详细设计

### 8.1 认证与登录

#### 8.1.1 登录方式
1. **钉钉扫码登录**（首选，多 corpId）
2. **用户名 + 备用密码**（兜底，钉钉宕机时用）

#### 8.1.2 钉钉扫码登录流程

子域识别 → 找到对应钉钉企业 → 拉二维码：

```
用户访问 hy.biounetwork.com/login
    ↓ 前端识别 host=hy
GET /api/auth/dingtalk/qrcode?tenant=hy
    ↓ 后端：
        - 查 sys_org WHERE code='hy' AND type='COMPANY'
        - 拿到 dingtalk_corp_id
        - 用 dingtalk_corp.app_key/agent_id 生成 OAuth URL（state=随机+HMAC）
返回二维码 URL
    ↓ 用户扫码
钉钉跳转 https://api.biounetwork.com/auth/dingtalk/callback?code=...&state=...
    ↓ 后端：
        1. 校验 state HMAC
        2. 用 code 调钉钉 getuserinfo_bycode → 拿 unionId、userId、corpId
        3. 校验 corpId ∈ 当前子域 whitelist_corp_ids（A-1.6 多 corpId 白名单）
           - hy.biounetwork.com 仅允许 hy 的 corpId
           - admin.biounetwork.com 允许所有 corpId
        4. 在 sys_user 按 (unionId, corpId) 查找：
           - 命中 + status=ACTIVE → 直接发 JWT
           - 命中 + status=FROZEN → 拒绝（"账号已冻结，请联系管理员"）
           - 未命中 → 进入"自助注册"流程
```

#### 8.1.3 自助注册流程（O-4 + A-1）

未命中时：
1. 调钉钉 `topapi/v2/user/get` 拉用户详情，包含：jobNumber、name、deptIds、mobile、email
2. 创建 `sys_user`：
   - `employee_no` = `jobNumber` 优先；为空则按数据库自增生成 `EMP000001`、`EMP000002` ...
   - `username` = `employee_no`（默认）
   - `password_hash` = 随机 12 位密码 BCrypt（明文以 sealed 形式发送给用户）
   - `password_must_change` = `true`
   - `dingtalk_unionid / userid / corp_id` 写入
   - 关联组织：按钉钉部门映射到 `sys_org`
3. 默认绑定 `EMPLOYEE` 角色（仅可登录、看自己部门列表名）
4. 发送钉钉工作通知：
   ```
   欢迎加入 [系统名]
   工号：EMP000123
   登录名：EMP000123
   备用密码：abc123XYZ#
   首次登录请到"个人中心 → 修改密码"
   ```
5. 同时签发 JWT 直接登录系统，落到"修改密码"页

#### 8.1.4 钉钉组织架构与员工自动同步

**实时事件订阅**（节点 B dingtalk-gateway 接收）：
- 监听事件：`user_add_org`、`user_modify_org`、`user_leave_org`、`org_dept_create`、`org_dept_modify`、`org_dept_remove`
- 收到事件 → 校验签名 → 投 RabbitMQ `dingtalk.sync` 队列
- backend-api 消费者按事件类型增删改 `sys_user / sys_org`

**离职处理**（A-1.5）：
- `user_leave_org` 触发 → `sys_user.status = FROZEN`，`frozen_at = NOW()`
- 不立即删，保留所有审计、审批关联
- 30 天后 ops 定时任务批量软删除（`status = DELETED`，`deleted_at` 标记）
- 90 天后物理删
- 如 30 天内员工返聘（`user_add_org` 同 unionId 命中已冻结账号），自动恢复

**定时全量兜底**（A-1.4）：
- 每天 02:00 拉钉钉部门树 + 全员
- 与本地 diff，补漏（修复事件丢失）
- 写 `sys_org_dingtalk_sync_log`

**多 corpId 白名单逻辑**（A-1.6）：
- `dingtalk_corp.is_main=true` 主公司：`whitelist_corp_ids` = 所有分公司 corpId（自动维护）
- `dingtalk_corp.is_main=false` 分公司：`whitelist_corp_ids` = 仅自身 corpId
- 子域路由时按 host → tenant → 取该租户的 dingtalk_corp 配置

#### 8.1.5 危险操作二次确认
触发场景：批量改 SKU、删除快照、删除产品、跨公司授权批准、模板删除等。
流程：
1. 前端调用接口时带 `X-Sensitive-Op: 1`
2. 后端发起钉钉工作通知给操作者本人，含 6 位随机码（5 分钟有效）
3. 前端弹窗输入码 → 二次提交 → 后端校验通过执行
4. 不用备用密码做敏感二次验证（A-1.2）

#### 8.1.6 临时员工邮件邀请

与 8.1.2/8.1.3 钉钉自助注册并行的**第二入口**，专门给外部合作方、短期外包、项目制人员使用。受邀人不进钉钉组织树，纯邮件账号体系。

##### 设计原则（强约束）

1. **无默认值**：邀请表单必填三件套——角色组合、数据范围、账号过期时间，**不允许任何默认兜底**（前后端双校验）
2. **过期时间上限 90 天**：从邀请发起时刻起算，超出报错；到期可"续期"= 重发邀请走同一流程
3. **邀请链接 48h 有效**：未点击则 `LINK_EXPIRED`，可重发新邀请（旧邀请自动 `REVOKED`）
4. **临时密码邮件下发**：受邀人首次登录强制改密，与正式员工自助注册保持一致
5. **绑定单一分公司**：临时员工只能绑定到邀请人所在的一个分公司，跨公司访问需走 `sys_data_scope` 显式审批
6. **到期即收回**：账号 `expires_at` 到期由 ops 定时任务自动切 `EXPIRED` + 关联 `sys_data_scope` 全部 `REVOKED`，无续期审批流程，过期就走

##### 邀请发起权限

| 角色 | 可邀请 | 可指定的最大账号有效期 |
|---|---|---|
| `PLATFORM_SUPER` | ✅ 所有分公司 | 90 天 |
| `COMPANY_ADMIN` | ✅ 仅本分公司 | 90 天 |
| `DEPT_LEAD` | ✅ 仅本部门 | 90 天 |
| 其他角色 | ❌ | — |

新增权限点：`TEMP_USER:INVITE`（发起邀请）、`TEMP_USER:REVOKE`（撤销未接受邀请或提前注销已接受账号）、`TEMP_USER:LIST`（查看本范围内的临时员工列表）。

##### 邀请流程

```
邀请人在控制台点"邀请临时员工"
   ↓ 表单：邮箱 + 角色组合（多选）+ 数据范围（store/product/company 多选）
         + 账号到期时间（必填，calendar picker 上限 today+90d）
         + 备注（可选，写明合作目的）
   ↓
POST /api/invitation/create
   ↓ backend：
       1. 校验当前用户有 TEMP_USER:INVITE 权限
       2. 校验 expires_at ∈ (now, now+90d]，否则 400
       3. 校验 email 未在 PENDING 邀请中（同邮箱多次发起需先 REVOKE 旧的）
       4. 生成 invitation_token = HMAC(secret, email + ts + nonce)
       5. 生成临时密码：12 位强随机（大小写+数字+符号），BCrypt 入库
       6. 写 user_invitation：status=PENDING, link_expires_at=now+48h
       7. 通过 mail.biounetwork.com SMTP 发邮件（模板 INVITATION）：
          - 邀请人信息、绑定公司/部门
          - 角色和权限范围说明
          - 账号有效期至 YYYY-MM-DD
          - 接受链接：https://admin.biounetwork.com/invitation/accept?token=...
          - 临时密码（明文，仅此邮件可见）
          - 提示：链接 48 小时内有效；首次登录后请修改密码
       8. 钉钉通知邀请人：邀请已发出
   ↓
受邀人收到邮件 → 点链接
   ↓
GET /api/invitation/preview?token=...
   ↓ backend 校验 token + link_expires_at + status=PENDING
   ↓ 返回：邀请方公司名、绑定部门、角色名、数据范围摘要、账号有效期
   ↓
受邀人确认 → 输入邮件中的临时密码 → 提交
   ↓
POST /api/invitation/accept { token, temp_password }
   ↓ backend：
       1. 再次校验 token + 状态 + 链接未过期
       2. 比对 temp_password 与 initial_password_hash
       3. 创建 sys_user：
          - employee_no = 自增 TMP000001
          - username = email
          - password_hash = 临时密码 hash（即邀请时生成的）
          - password_must_change = true
          - user_type = TEMP
          - expires_at = invitation.account_expires_at
          - default_tenant_id = invitation.target_company_id
          - invited_by / invitation_id 写入
       4. 按 invitation.role_codes 写 sys_user_role
       5. 按 invitation.data_scopes 写 sys_data_scope（source=INVITATION，expires_at 与账号同步）
       6. invitation.status=ACCEPTED, accepted_at=now, created_user_id=新user.id
       7. 直接签发 JWT 登录 → 落到"修改密码"页（与钉钉自助注册一致）
       8. 钉钉通知邀请人：受邀人已接受
```

##### 撤销与续期

- **撤销 PENDING 邀请**：邀请人或上级直接 REVOKE，邀请链接立即失效
- **撤销已接受账号**：调 `POST /api/invitation/revoke-user` → sys_user.status=EXPIRED + 数据范围全部 REVOKED + 钉钉通知账号持有者（如可达邮箱也发一封）
- **续期 = 重新邀请**：账号到期前可由邀请人发起"续期邀请"，相当于发一封新邀请邮件（新 token + 新临时密码），受邀人接受后 sys_user.expires_at 更新；旧账号在新邀请未接受期间继续有效

##### 到期巡检（ops 定时任务）

```
每小时跑一次（cron 0 * * * *）：
  1. SELECT * FROM sys_user
     WHERE user_type='TEMP' AND status='ACTIVE' AND expires_at <= NOW()
  2. 批量 UPDATE sys_user SET status='EXPIRED', expired_at=NOW()
  3. 批量 UPDATE sys_data_scope SET status='REVOKED' WHERE user_id IN (...)
  4. 失效该用户的 JWT（加入 Redis 黑名单）
  5. 发 TEMP_USER_EXPIRED 钉钉通知给邀请人 + 该账号上级

每天 09:00 跑：
  扫描 24h 内即将到期的 TEMP 账号 → 发 TEMP_USER_EXPIRING 给邀请人，提示是否续期
```

##### 审计

- 邀请发起、接受、撤销、到期收回全部落 `sys_audit_log`，`sensitive=true`
- 邀请邮件发送结果落 `notification_log`
- 临时密码明文仅出现在 SMTP 日志中（DKIM 加密传输），不入审计 payload

### 8.2 RBAC 与数据范围

#### 8.2.1 角色定义（初始化数据）

| 角色 code | 名称 | 范围 | 权限要点 |
|---|---|---|---|
| `PLATFORM_SUPER` | 平台超管 | 跨租户 | 全部，含模板库管理、租户增删、钉钉企业配置、可邀请临时员工到任意公司 |
| `COMPANY_ADMIN` | 分公司管理员 | 本租户 | 本公司全部业务，可邀请临时员工到本分公司 |
| `DEPT_LEAD` | 部门主管 | 本部门 | 本部门用户、店铺、产品、审批，可邀请临时员工到本部门 |
| `STORE_ADMIN` | 店铺管理员 | 授权店铺 | Token、店铺设置、合作者店铺池接入；不动产品 |
| `OPERATION` | 运营 | 授权范围 | 产品 CRUD、推送店铺、上架状态管理、店铺资产查看 |
| `PURCHASING` | 采购 | 授权范围 | 仅采购页：SKU/成本/克重/物流 tag；改 SKU 走二次确认 + 审计 |
| `DESIGNER` | 美工 | 授权范围 | 仅产品媒体、需求文档；不动 SKU/价格/标题 |
| `READONLY` | 只读 | 授权范围 | 全只读 |
| `EMPLOYEE` | 默认员工 | 本部门 | 仅可登录、看自己部门基本信息列表（无详情） |
| `TEMP_STAFF` | 临时员工（邮件邀请） | 邀请时显式指定 | 自身无任何权限点，所有权限来自邀请时附加的角色组合（如 `TEMP_STAFF + DESIGNER`）；账号 `expires_at` 强制到期；不能再邀请其他临时员工 |

权限点示例：`PRODUCT:READ`、`PRODUCT:WRITE`、`PRODUCT:PUSH`、`STORE:TOKEN_MANAGE`、`PURCHASE:SKU_EDIT`、`MEDIA:UPLOAD`、`THEME:DEPLOY`、`APPROVAL:DECIDE`、`RECYCLE_BIN:VIEW`、`RECYCLE_BIN:RESTORE`、`TEMP_USER:INVITE`、`TEMP_USER:REVOKE`、`TEMP_USER:LIST` 等。

#### 8.2.2 数据范围拼接逻辑
- 默认按用户所在 `org.path` 前缀过滤
- `sys_data_scope` 中 `ACTIVE` 且未过期的额外授权追加为 OR 条件
- MyBatis 拦截器自动注入 WHERE，业务代码不感知
- 前端只控制 UI 隐藏，**禁止依赖前端做权限**

#### 8.2.3 回收站权限（O-1）
- `RECYCLE_BIN:VIEW`：可见自己删除的项 + 本组织子树内被删的项
- `RECYCLE_BIN:RESTORE`：仅 `COMPANY_ADMIN` / `PLATFORM_SUPER` 可执行恢复
- `PLATFORM_SUPER` 看到全平台软删除项

### 8.3 多数据源动态路由

- 启动时从 `platform.sys_tenant_datasource` 加载所有数据源，注册进 `dynamic-datasource-spring-boot-starter`
- 请求进入：
  1. 解析 host → tenant code → tenant_id
  2. JWT 校验 → 用户的 `default_tenant_id` 与 host tenant_id 比对（不一致检查 `sys_data_scope` 是否授权）
  3. `DynamicDataSourceContextHolder.push("tenant_xx")`
- 平台库访问：方法上加 `@DS("platform")`
- **跨库事务禁止**；如需一致性走 RabbitMQ 事件 + 幂等 outbox

### 8.4 Shopify Token 与 OAuth（公网回调，简化版）

#### 8.4.1 Token 类型
- `custom_app`：Shopify Admin 后台手动建 Custom App 的永久 token，用户粘贴接入
- `oauth`：标准 Shopify OAuth 流程拿到的 access token（推荐）
- `cli`：通过 `shopify store auth` 拿的临时 token，配合 refresh 静默刷新（保留兼容）

#### 8.4.2 OAuth 接入流程（替代原内网 cloudflared 方案）

**原 v1.0 方案需要 cloudflared tunnel 是因为系统在内网。本版系统部署在公网，不需要 tunnel。**

```
用户 → 控制台"接入店铺"按钮 → 填店铺域名（xxx.myshopify.com）
   ↓
backend-api POST /api/store/oauth/init
   ↓
   1. 生成 sessionId + state HMAC
   2. 写 oauth_session 表（10 分钟过期）
   3. 拼 OAuth URL：
      https://{shop}/admin/oauth/authorize
        ?client_id=<APP_KEY>
        &scope=read_themes,write_themes,read_files,write_files,
               read_products,write_products,read_collections,write_collections
        &redirect_uri=https://oauth.biounetwork.com/oauth/callback
        &state=<HMAC>
返回 URL 给前端 → 前端打开新窗口跳转
   ↓
用户在 Shopify 同意授权
   ↓
Shopify 跳回 https://oauth.biounetwork.com/oauth/callback?code=...&state=...&shop=...
   ↓
backend-api：
   1. 验 HMAC、查 oauth_session
   2. 用 code 调 https://{shop}/admin/oauth/access_token 拿 token
   3. AES-GCM 加密 → 写 store 表
   4. 注册 webhook（products/update、products/delete、app/uninstalled）
   5. 关闭 oauth_session
   6. SSE 推前端 setup 完成
   7. 返回静态页面"授权完成，请回到系统继续"
```

**Cloudflare 配置**：
- DNS：`oauth.biounetwork.com` CNAME 到节点 A
- Cloudflare Access：oauth callback **不**启用（因为是机器对机器跳转，加 Access 会拦截）
- Rate Limit：`/oauth/callback` 每 IP 10r/m
- WAF 自定义规则：仅放行 `Referer` 含 `myshopify.com` 的请求 → 进一步降低被刷概率

#### 8.4.3 Token 刷新（沿用 skill 思路）
- `custom_app` / `oauth`：永不过期，无需刷新
- `cli`：每次使用前判活；过期由 worker 调 `shopify store execute` 触发 CLI 静默刷新；refresh 也死则发钉钉通知
- 后端定时任务每天检查所有 token，提前 7 天过期发 `TOKEN_EXPIRING` 事件

### 8.5 资产拉取 Worker（FastAPI）

接口：

| Method | Path | 说明 |
|---|---|---|
| POST | `/pull/theme` | 拉取整个 active theme |
| POST | `/pull/product` | 拉单个产品完整 JSON + 关联媒体 |
| POST | `/pull/policy` | 拉 policy |
| POST | `/pull/menu` | 拉 menu |
| POST | `/pull/collection` | 拉 collection |
| POST | `/push/product` | 推送产品到指定店铺 |
| POST | `/push/theme` | 推送主题（走 themes.json src=签名URL） |
| POST | `/push/media` | 上传媒体到 Shopify Files |
| POST | `/preview/build` | 在 dev 店建/复用 preview theme |

所有接口异步：返回 `taskId`，事件通过 RabbitMQ 投回 backend，再由 SSE 转给前端。

worker 内部沿用 skill 核心逻辑：
- Shopify CLI 走 `subprocess`（仅 cli token 类型店铺需要，其他类型走 API 直连）
- GraphQL 直连用 Python `urllib`
- 整 theme 拉取：API 直接调 `themes/{id}/assets.json` 列出全部 assets，逐个下载（每店铺通常 200~500 个文件）

### 8.6 产品快照（自动 + 手动）

#### 8.6.1 触发
- **status 变更**：必建（webhook `products/update` + diff `status`）
- **内容字段变更**：标题/描述/选项/图片/SEO/标签变化时建（diff 字段）
- **5 分钟去抖**：同一 productId 在 Redis `SET snapshot_pending:{id} 1 EX 300 NX`，到期由 RabbitMQ 延时消息触发实际生成
- **价格/库存/变体增减**：不建快照，落 `product_price_history` / `product_inventory_history`
- **手动**：用户点"立即快照"按钮，无视去抖

#### 8.6.2 内容
- CSV 形式（按 Shopify 42 列模板）→ 上传 R2 `products/{id}/csv/{version}.csv`
- JSON 形式（更全，含 metafields）→ 上传 R2 `products/{id}/json/{version}.json`
- DB 写 `product_snapshot` 索引行

### 8.7 产品推送

#### 8.7.1 单个/批量推送
请求体：
```json
{
  "productIds": [1, 2, 3],
  "targetStoreIds": [10, 11],
  "options": {
    "matchKey": "handle",
    "useProductPrice": true,
    "defaultInventory": 1000,
    "continueSellingWhenOOS": true
  }
}
```

#### 8.7.2 推送规则（A-2 已确认）
- **价格**：使用产品库 `product_variant.price`（不再统一为 1000）
- **库存数量**：默认 1000
- **Continue selling when out of stock**：默认开

#### 8.7.3 冲突处理
1. Worker 用 GraphQL 查目标店铺 `productByHandle`
2. 已存在 → 跳过当前产品，继续后续
3. 全部跑完，所有冲突落 `push_conflict`（`PENDING`）
4. 前端展示冲突列表 → 用户对每条选 `OVERWRITE` / `SKIP` → 二次任务执行

#### 8.7.4 推送内容
- 产品基础信息 + 选项 + 变体 + 价格（按上面规则）+ inventory_policy="continue"
- 媒体：先签名访问 R2 → 调 Shopify Files API 上传到目标店铺 → 拿到新 URL → 替换关联引用
- 推送成功后写 `tenant_xx.store_product` 映射

### 8.8 多店铺上架状态同步

#### 8.8.1 实时（webhook）
- 接入店铺时自动注册 webhook：`products/update`、`products/delete`、`app/uninstalled`
- backend 收到 `https://webhook.biounetwork.com/shopify/{storeId}/{topic}` → 校验 HMAC → 投 RabbitMQ → 消费者更新 `store_product.status_in_shopify`

#### 8.8.2 兜底（定时）
- 每天凌晨 04:00 全店全产品扫一遍，diff 后修正
- 每店铺 GraphQL 分页 `products(first: 250, query: "updated_at:>...")`

#### 8.8.3 双向同步规则
- **产品信息页**：状态可改（active/draft/archived）→ 推到所有已映射店铺
- **采购页 / 媒体页**：只读小标识，多店铺状态聚合显示

### 8.9 详情页预览

#### 8.9.1 合作者店铺池接入流程（O-8）

每个分公司维护自己的 dev store 池：

1. **指南文档**：系统内置一份"如何在 Shopify Partner 注册 development store + 邀请系统 App 作为合作者"的指导（`guide_doc` 表，code=`PARTNER_DEV_SETUP`）
2. **一键 OAuth 接入**：
   - `STORE_ADMIN` 在控制台"合作者店铺池 → 添加"
   - 填写 dev store 域名 → 点击"开始授权"
   - 走 8.4.2 OAuth 流程，scopes 同正式店铺
   - 完成后 `store.is_dev_store = true`、`is_partner_collab = true`
3. **池管理**：
   - 同一分公司可加多个 dev store
   - 控制台显示每个 dev store 的当前 preview theme 数量、剩余配额（100 - 已用）

#### 8.9.2 预览生成流程

1. 用户选产品 + 系统自动按 **最少负载** 策略选 dev store（A-3.2）：
   ```sql
   SELECT id FROM store
   WHERE is_dev_store=true AND tenant_id={当前分公司}
   ORDER BY (SELECT COUNT(*) FROM preview_theme WHERE dev_store_id=store.id AND status='ACTIVE') ASC
   LIMIT 1
   ```
2. backend 调 worker `/preview/build`：
   a. 把产品先推到 dev 店（覆盖更新 by handle，使用产品库价格 + 库存 1000）
   b. 在 dev 店调 `themeCreate` 建 unpublished theme（`preview-{userId}-{productId}-{ts}`）
   c. 把对应主题配置 + JSON 推到新 theme
   d. 媒体引用替换为 dev 店上传后的新 URL
3. 返回 preview URL：`https://{dev}.myshopify.com/products/{handle}?preview_theme_id={id}`
4. 写 `preview_theme` 表
5. 前端"眼睛 icon" 点击 = 打开链接 + `last_accessed_at` 更新

#### 8.9.3 清理任务（每 30 分钟跑）
- 同 (userId, devStoreId) 下保留最近 3 条 `ACTIVE`，超出按 `last_accessed_at` 升序删除（调 Shopify themeDelete）
- `last_accessed_at` 距今 > 24h 自动删

### 8.10 一键开店编排（saga 模式）

#### 8.10.1 状态机

```
PENDING
  ↓ [新店 OAuth 完成 + App 安装]
AUTH_DONE
  ↓
MEDIA_UPLOADING        # R2 → Shopify Files（先签名 URL，再调 Files API）
  ↓
COLLECTIONS_CREATING
  ↓
PRODUCTS_PUSHING       # 批量调 push 模块；遇冲突暂停等用户决策
  ↓
GUIDE_DOC_PUSHED       # menu/policy/markets 不接 API，跳指导文档页
  ↓
THEME_DEPLOYING        # 调 themes.json src=R2签名URL（A-4）
  ↓
THEME_PUBLISHING       # themeUpdate role=main
  ↓
SUCCESS / FAILED(可重试自上一步)
```

#### 8.10.2 主题安装（A-4）

```http
POST https://{store}/admin/api/2024-10/themes.json
Authorization: <token>
Content-Type: application/json

{
  "theme": {
    "name": "Wave 1 (auto-deployed)",
    "src": "https://r2.example.com/templates/base/tpl-001/v3/theme.zip?X-Amz-Signature=...",
    "role": "unpublished"
  }
}
```

R2 签名 URL 有效期 15 分钟（足够 Shopify 拉取）。Shopify 自己解压安装，不占我们带宽。
完成后再调 `themeUpdate role=main` 切换为主题。

#### 8.10.3 域名/品牌替换
- 主题 zip 解压前不改；上传到 Shopify 后调 `themes/{id}/assets.json` 列出文件
- 对所有 JSON 文件做正则替换：旧域名 → 新域名、旧品牌 → 新品牌
- `base_template_version.default_replace_rules` 可预设规则
- 用户可在开店向导临时改

#### 8.10.4 进度推送
每步完成发 RabbitMQ 事件，task 表更新 progress；钉钉发"成功/失败"通知（用户已订阅情况下）。

### 8.11 统一审批中心

- 单签：`approver_role_codes` 任一角色用户审即可
- 驳回：申请人可在原单基础上修改 `payload` 重新提交（生成新版本号）
- 申请类型：
  - `PRODUCT_ACCESS`：新店管理员申请使用全平台某产品 → 由 `产品开发` 或 `平台超管` 审批
  - `CROSS_COMPANY_AUTH`：申请跨公司读写权限 → **必填** 时效（无默认）→ 默认 7 天上限可调 → 到期前 24h 钉钉提醒续期 → 续期需重新审批 → 不续期自动 `EXPIRED`
- 审批通知：申请提交、批准、驳回、即将到期、已到期 各发钉钉

### 8.12 钉钉通知（可订阅）

#### 8.12.1 事件清单（FAQ3 + FAQ A6 确认）

| 事件 code | 默认开 | 说明 |
|---|---|---|
| `NEW_STORE_SUCCESS` | ✅ | 一键开店成功 |
| `NEW_STORE_FAIL` | ✅ | 一键开店失败 |
| `PRODUCT_PUSH_FAIL` | ✅ | 产品推送失败（成功不通知，避免噪音） |
| `THEME_PUSH_SUCCESS` | ✅ | 主题推送成功 |
| `THEME_PUSH_FAIL` | ✅ | 主题推送失败 |
| `TOKEN_EXPIRING` | ✅ | Token 7 天内过期 |
| `BACKUP_FAIL` | ✅ | 备份失败 |
| `APPROVAL_PENDING` | ✅ | 审批待办 |
| `APPROVAL_DECIDED` | ✅ | 审批结果 |
| `CROSS_AUTH_EXPIRING` | ✅ | 跨公司授权 24h 内到期 |
| `HIGH_RISK_OP` | ✅ | 批量改 SKU、删除快照等 |
| `STAFF_FROZEN` | ✅ | 员工离职冷冻通知（管理员） |
| `INVITATION_SENT` | ✅ | 临时员工邀请已发出（通知邀请人） |
| `INVITATION_ACCEPTED` | ✅ | 临时员工已接受邀请（通知邀请人） |
| `TEMP_USER_EXPIRING` | ✅ | 临时账号 24h 内到期（通知邀请人，提示续期） |
| `TEMP_USER_EXPIRED` | ✅ | 临时账号已到期，权限已自动收回（通知邀请人 + 上级） |

#### 8.12.2 用户订阅
- 前端"个人中心 → 通知设置"列出全部事件 + 复选框（钉钉/邮件/站内信三通道）
- 数据落 `notification_subscription`
- 发送时双重判断：用户订阅 + 用户在"目标接收人列表"内

#### 8.12.3 多 corpId 发送
- 通知接收人按其 `dingtalk_corp_id` 取对应 `dingtalk_corp.access_token` 发送
- access_token 自动 7000s 刷新

#### 8.12.4 失败重试
- 钉钉工作通知：`topapi/message/corpconversation/asyncsend_v2`
- 失败重试：指数退避 3 次，最终失败落 `notification_log` 状态 `FAILED`，纳入监控告警

### 8.13 邮件通道

#### 8.13.1 用途
- 密码重置（钉钉绑定丢失时）
- 系统告警（机器对机器，告警邮件给运维组）
- 重大事故双通道兜底（钉钉 + 邮件）
- **临时员工邀请**（8.1.6 邮件下发邀请链接 + 临时密码；这是邮件通道的主要业务用途之一）

#### 8.13.2 SMTP 配置（直连域名邮箱服务器）

`.env` 注入：
```ini
SMTP_HOST=mail.biounetwork.com
SMTP_PORT=465
SMTP_USERNAME=noreply@biounetwork.com
SMTP_PASSWORD=
SMTP_TLS=true
SMTP_FROM=Biou x Shopify Control Center <noreply@biounetwork.com>
```

链路：
```
backend-api (节点 A, US) ──SMTPS:465──▶ mail.biounetwork.com (节点 D, SG)
```

- backend 通过 Spring `JavaMailSender` 直连 SMTPS（465 端口，TLS 强制）
- 不再走 v1.2 的"节点 D smtp-gateway 转发"（节点 D 已撤销，邮箱本身就在 SG 不需要中转）
- 限流、模板渲染、退信处理统一在 backend 的 `notification` 模块内做
- 发件域 SPF / DKIM / DMARC 在邮箱方案侧配置，邀请邮件被打入垃圾箱概率极低

#### 8.13.3 模板管理
- `email_template` 表存 subject + body_html + body_text
- 关键模板：
  - `INVITATION`：临时员工邀请（含邀请人、绑定公司/部门、角色权限范围、账号有效期、接受链接、临时密码）
  - `INVITATION_REVOKED`：邀请被撤销（PENDING 状态被发起人主动撤销时通知受邀人）
  - `PASSWORD_RESET`：密码重置
  - `REGISTRATION`：钉钉自助注册（含工号 + 备用密码）
  - `ALERT`：告警邮件
- TipTap / 模板内嵌变量用 `{{var}}` 占位，渲染时按邮件类型注入

### 8.14 文件签名 token（折中方案，B 选项）

#### 8.14.1 设计目标
- 减少批量签名计算开销
- 解决 CDN 缓存命中
- 降低 URL 带 token 泄露面

#### 8.14.2 设计

```
用户访问需求文档详情页
   ↓
前端先调 POST /api/files/sign-batch
  body: { keys: ["products/123/docs/abc.pdf", "products/123/images/...", ...] }
   ↓
后端：
  1. 校验 user 对每个 key 的访问权
  2. 生成 file_sign_token：随机 token，scope_keys=请求列表，expires_at=now+4h
  3. 返回 token + 4h 有效期 + 每个 key 的 R2 签名 URL（有效期与 token 同步）
   ↓
前端用拿到的 URL 渲染图片
   ↓
有效期 5 分钟前，前端静默调 /api/files/refresh-token 续签
```

**好处**：
- 一次 HTTP 拿一组签名，N 张图 1 次签
- 同一文档多人打开 → 各自有自己的 token，不可互相泄露
- token 用完即弃，scope_keys 限制其能访问的 key

**对比纯单签**：CDN 仍不能命中（query 不同），但减少了网络往返。

### 8.15 审计日志

#### 8.15.1 落库
- AOP 切所有 Controller，自动写 `sys_audit_log`
- 敏感操作（改 SKU、删除快照、跨公司授权批准）额外落 `payload` 详情，`sensitive=true`
- 钉钉二次确认结果同样审计

#### 8.15.2 归档
- 定时任务每月 1 日 03:30：把上上月数据导出为 jsonl.gz、AES-256-GCM 加密、上传 R2 `archive/audit/{yyyy-MM}.jsonl.gz.enc`
- 校验上传 sha256 → 删除原数据 partition

### 8.16 备份

#### 8.16.1 RDS 自动备份
- RDS 自带每日备份（保留 7 天）
- 启用 PITR（Point-In-Time Recovery）

#### 8.16.2 应用层加密备份
- 每日 03:00：通过 RDS 快照 export 到 S3（同区域），节点 A 拉取 → AES-256-GCM 加密 → 上传 R2 `backup/mysql/{yyyyMMdd}/`
- 仅保留 **最新 1 份**（覆盖式或显式删旧）
- 加密密钥：[待确认 - KMS 选型] 默认放 .env，未来切 AWS KMS / HashiCorp Vault

#### 8.16.3 R2 自身
- R2 已是异地存储
- 桶启用 versioning + lifecycle 防误删

### 8.17 软删除

- 全业务表 `deleted_at`
- 列表查询默认过滤 `deleted_at IS NULL`
- 回收站页面给有权限角色，可恢复 90 天内删除项
- 90 天后由 ops 任务物理删（含 R2 文件）

### 8.18 CI/CD（GitHub Actions）

#### 8.18.1 仓库与分支策略

- **代码托管**：GitHub（私有仓库）
- **仓库结构**（推荐 monorepo）：
  ```
  shopify-hub/
  ├── frontend-admin/       # Next.js
  ├── backend-api/          # Spring Boot
  ├── asset-worker/         # FastAPI
  ├── ops/                  # 部署脚本、compose、k8s（未来）
  └── .github/workflows/    # CI/CD pipeline
  ```
- **分支**：
  - `main`：生产，受保护，仅经 PR + review + 全绿 CI 才能合
  - `develop`：测试集成
  - `feature/*` / `hotfix/*`

#### 8.18.2 Runner 策略

| 阶段 | Runner | 说明 |
|---|---|---|
| Lint / Test / Build | GitHub-hosted（公网 free runner） | 公开通用，可并行多 job |
| 镜像构建 + 推 GHCR | GitHub-hosted | 推到 GitHub Container Registry |
| 测试环境部署 | GitHub-hosted（带 SSH 私钥 secret） | 直连测试服 SSH 拉镜像 |
| 生产部署 | **节点 C self-hosted runner**（label: `prod-runner`） | 节点 A/C 经 WireGuard 私网直达；节点 B 经其公网 sshd（jump 用户）；私钥永不出节点 C |

#### 8.18.3 关键 workflow

**`.github/workflows/ci.yml`**（PR + push 到 develop/main 触发）：

```yaml
name: CI
on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: pnpm }
      - run: pnpm -C frontend-admin install --frozen-lockfile
      - run: pnpm -C frontend-admin lint
      - run: pnpm -C frontend-admin typecheck
      - run: pnpm -C frontend-admin test
      - run: pnpm -C frontend-admin build

  backend:
    runs-on: ubuntu-latest
    services:
      mysql:  { image: mysql:8.0, env: { MYSQL_ROOT_PASSWORD: root }, ports: ['3306:3306'] }
      redis:  { image: redis:8-alpine,  ports: ['6379:6379'] }
      rabbit: { image: rabbitmq:4-management, ports: ['5672:5672'] }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: 21, cache: maven }
      - run: mvn -B -f backend-api/pom.xml verify

  worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: 3.12, cache: pip }
      - run: pip install -r asset-worker/requirements-dev.txt
      - run: ruff check asset-worker
      - run: pytest asset-worker

  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aquasecurity/trivy-action@master
        with: { scan-type: fs, severity: HIGH,CRITICAL }
```

**`.github/workflows/deploy-staging.yml`**（push develop 后自动）：

```yaml
name: Deploy Staging
on:
  push:
    branches: [develop]
jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: ${{ github.actor }}, password: ${{ secrets.GITHUB_TOKEN }} }
      - run: docker buildx bake --push   # bake.hcl 定义 frontend / backend / worker 三镜像
        env: { TAG: staging-${{ github.sha }} }

  deploy:
    needs: build-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: webfactory/ssh-agent@v0.9.0
        with: { ssh-private-key: ${{ secrets.STAGING_SSH_KEY }} }
      - run: |
          ssh -o StrictHostKeyChecking=no deploy@${{ secrets.STAGING_HOST }} \
            "cd /opt/shopify-hub && \
             TAG=staging-${{ github.sha }} docker compose pull && \
             TAG=staging-${{ github.sha }} docker compose up -d && \
             docker image prune -f"
```

**`.github/workflows/deploy-prod.yml`**（打 tag `v*` 触发，手动审批）：

```yaml
name: Deploy Production
on:
  push:
    tags: ['v*.*.*']
jobs:
  approval:
    runs-on: ubuntu-latest
    environment: production       # 在 GitHub UI 配置 reviewer 强制审批
    steps:
      - run: echo "Awaiting reviewer approval..."

  build-push:
    needs: approval
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with: { registry: ghcr.io, username: ${{ github.actor }}, password: ${{ secrets.GITHUB_TOKEN }} }
      - run: docker buildx bake --push
        env: { TAG: ${{ github.ref_name }} }

  deploy-node-a:
    needs: build-push
    runs-on: [self-hosted, prod-runner]    # 节点 C self-hosted runner
    steps:
      - uses: actions/checkout@v4
      - run: |
          ssh -J jump@${{ secrets.JUMP_HOST }} deploy@${{ secrets.NODE_A_HOST }} \
            "cd /opt/shopify-hub && \
             TAG=${{ github.ref_name }} docker compose -f node-a-app/docker-compose.yml pull && \
             TAG=${{ github.ref_name }} docker compose -f node-a-app/docker-compose.yml up -d"

  deploy-node-c-bk:
    needs: deploy-node-a
    runs-on: [self-hosted, prod-runner]    # self-hosted runner 本机就在节点 C，不需要跳板
    steps:
      - uses: actions/checkout@v4
      - run: |
          cd /opt/shopify-hub && \
          TAG=${{ github.ref_name }} docker compose -f node-c-monitor/docker-compose.yml pull && \
          TAG=${{ github.ref_name }} docker compose -f node-c-monitor/docker-compose.yml up -d backend-api-bk

  deploy-node-b:
    needs: deploy-node-a
    runs-on: [self-hosted, prod-runner]
    steps:
      - uses: actions/checkout@v4
      - run: |
          ssh -p ${{ secrets.JUMP_SSH_PORT }} jump@${{ secrets.JUMP_HOST }} \
            "cd /opt/shopify-hub && \
             TAG=${{ github.ref_name }} docker compose -f node-b-dingtalk/docker-compose.yml pull && \
             TAG=${{ github.ref_name }} docker compose -f node-b-dingtalk/docker-compose.yml up -d dingtalk-gateway"
```

#### 8.18.4 镜像仓库

- 用 GitHub Container Registry（`ghcr.io/<org>/<image>:tag`）
- 镜像可见性：私有
- pull 凭证：节点机器各自 `docker login ghcr.io` 用 deploy token

#### 8.18.5 Secrets 管理

GitHub repo Settings → Secrets：

| Secret 名 | 用途 |
|---|---|
| `STAGING_HOST` / `STAGING_SSH_KEY` | 测试服 SSH |
| `JUMP_HOST` / `JUMP_SSH_PORT` / `JUMP_SSH_KEY` | 节点 B（HK）跳板机 |
| `NODE_A_HOST` / `NODE_B_HOST` | 生产节点 |
| `GHCR_TOKEN` | 镜像仓库 |
| `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` | R2 凭证（仅必要 job 用） |
| `DINGTALK_*` | 钉钉测试 / 生产应用 |
| `SHOPIFY_PARTNER_*` | Partner App 凭证 |

> 生产敏感 secret 不进 GitHub-hosted runner，仅在节点 C self-hosted runner 上由本地 .env 注入。

---

## 9. 关键流程时序

### 9.1 一键开店时序

```
User → Web → backend.newstore.create
                  ↓
              创建 task
                  ↓
              MQ: NEW_STORE.AUTH（用户浏览器走 OAuth）
                  ↓ 完成
              MQ: NEW_STORE.MEDIA
                  ↓
            循环：R2 签名 → Shopify Files 上传 → 收集新 URL
                  ↓
              MQ: NEW_STORE.COLLECTIONS
                  ↓
              MQ: NEW_STORE.PRODUCTS（含冲突处理子流程）
                  ↓
              MQ: NEW_STORE.GUIDE（推送指导文档链接）
                  ↓
              MQ: NEW_STORE.THEME (themes.json src=R2签名URL)
                  ↓
              MQ: NEW_STORE.PUBLISH (themeUpdate role=main)
                  ↓
              SSE → 前端进度 100%
              钉钉 → 操作人（按订阅）
```

### 9.2 钉钉扫码登录时序

```
访问 hy.biounetwork.com/login
  → backend 识别 host=hy → 查 dingtalk_corp(分公司)
  → 返回二维码 URL（含 state=HMAC(sessionId)）
用户扫码 + 同意
  → 钉钉跳转 https://api.biounetwork.com/auth/dingtalk/callback?code=...&state=...
  → backend：
       验签 → getuserinfo_bycode → 校验 corpId ∈ whitelist
       → 命中 sys_user：发 JWT
       → 未命中：自助注册 → 拉用户详情 → 创账号 → 发 JWT + 钉钉通知
  → 前端跳工作台
```

### 9.3 产品快照触发时序

```
Shopify webhook products/update
  → webhook.biounetwork.com 接收 + HMAC 校验
  → 计算 diff
    - status 变了：直接落延时队列（5 分钟 NX 标记）
    - 内容字段变了：同上
    - 仅价格/库存变了：写 product_price_history / product_inventory_history，结束
  → 5 分钟到期消费
  → worker 拉最新产品 JSON
  → 生成 CSV + JSON → 上传 R2
  → DB 写 product_snapshot
```

---

## 10. 安全设计（公网部署加固）

| 维度 | 措施 |
|---|---|
| **传输加密** | 全站 HTTPS（Cloudflare 边缘 + Origin Cert），TLS 1.2+，HSTS preload |
| **CDN/WAF** | Cloudflare Free WAF + Bot Fight Mode + Rate Limit + Managed Rules |
| **登录页防护** | `admin.biounetwork.com` 启用 Cloudflare Access（钉钉 SSO 或 IP 白名单），普通分公司域名开 Bot Fight |
| **凭证加密** | Shopify token / 钉钉 secret / R2 secret AES-GCM 入库；密钥 [待确认 - KMS] |
| **密码** | BCrypt cost=12，禁止明文/MD5 |
| **JWT** | RSA 签名，access 30min，refresh 7d，黑名单 |
| **失败锁定** | 5 次/15 分钟（用户名密码） |
| **二次确认** | 钉钉验证码（高危操作） |
| **SQL 注入** | MyBatis 全部 `#{}`；动态 SQL 白名单 |
| **XSS** | 后端响应固定 JSON；TipTap 内容存储/渲染前 sanitize |
| **CSRF** | API 用 Bearer Token，无 cookie 状态；前端禁止 cookie 自动发 |
| **越权** | RBAC + 数据范围 + 后端二次校验（绝不信前端） |
| **审计** | 全 API + 高危操作详情 |
| **webhook** | HMAC 校验 + 重放窗口 5 分钟 |
| **文件上传** | MIME 白名单 + magic number 校验 + 单文件 ≤ 20MB + 病毒扫描可选 |
| **防爆破** | Redis 限流（IP + user）滑动窗口；Cloudflare 边缘限流兜底 |
| **依赖扫描** | GitHub Actions 接 trivy + npm audit + maven dependency-check + Dependabot |
| **服务器** | SSH 改非默认端口 + 仅 key 登录 + fail2ban + ufw 默认 deny |
| **数据库** | RDS 仅安全组放行节点 A/B 私网 IP；RDS 加密存储；强制 SSL 连接 |
| **私网** | 节点 A/B/C WireGuard mesh；运维 SSH 仅经节点 B（HK）跳板机；节点 D 邮箱独立部署不入 mesh |
| **备份密钥** | 与 R2 凭证、AES 数据密钥分离保管；建议 KMS |
| **DDoS** | Cloudflare Free 抵御 L7；L4 大流量需升级套餐或迁移源站 IP |

---

## 11. 监控与告警

### 11.1 内部监控（节点 C）
- **Prometheus**：JVM、HTTP QPS/延迟、MQ 堆积、MySQL 慢查（RDS exporter）、Redis 内存、R2 上传失败率
- **Grafana 看板**：业务（任务成功率、推送失败率、Token 过期数、备份状态）+ 基础（CPU/内存/磁盘）
- **Loki**：所有节点 docker stdout/stderr，按 traceId 聚合
- **Alertmanager**：告警分发到钉钉（独立群机器人，不走业务用户订阅）+ 邮件兜底
- **关键告警阈值**：
  - 任意服务 5 分钟连续 5xx > 1%
  - MQ 队列堆积 > 1000
  - RDS CPU > 80% 持续 5 分钟
  - 磁盘 < 20%
  - R2 上传失败率 > 5%
  - 备份失败
  - SSL 证书 < 30 天过期

### 11.2 外部探活
- UptimeRobot（免费 50 个监控）每 1 分钟探活：
  - `https://admin.biounetwork.com`
  - `https://api.biounetwork.com/health`
  - `https://oauth.biounetwork.com/health`
  - `https://webhook.biounetwork.com/health`
- 失败立即钉钉机器人 + 邮件双通道告警

### 11.3 服务依赖健康检查
- backend-api `/health` 检查：DB、Redis、MQ、R2、钉钉 token
- worker `/health` 检查：DB、MQ、R2、Shopify 连通性

---

## 12. 灾备与恢复演练

### 12.1 场景与方案

| 场景 | 处理 |
|---|---|
| 某店铺被封 | 从 R2 拉最新 snapshot → 一键开店推到新店 |
| 节点 A 整体宕 | DNS 切到节点 C 备用副本（人工切流；蓝绿留 v2） |
| 节点 B 整体宕 | 钉钉网关 + 跳板双失：钉钉事件订阅短期断流，运维改走云厂商控制台 console；尽快重建 HK 节点（dingtalk-gateway 是 stateless，重新部署即可） |
| RDS 实例故障 | RDS 自动 failover（多可用区）；不能 failover 时从最新 PITR 恢复 |
| RDS 数据被误删 | PITR 回滚到误删前 5 分钟 |
| R2 全桶丢失（极小概率） | 用 mysql backup（每日加密 dump）+ 应用日志，重建数据库；图片不可恢复（R2 异地已是最高等级） |
| 服务器被入侵 | 立即切 DNS + 销毁实例 + 撤销所有 token + 备份恢复到新实例 |

### 12.2 演练频率
- 每季度一次：随机选一个分公司库，在测试环境完整跑恢复 + 一键开店

---

## 13. 上线节奏

### Wave 0（脚手架 + 本地能跑通，约 1 周）
**目标**：拉起本地一键 docker-compose，所有服务能起、能互通、健康检查全绿，CI 跑通。

- 仓库初始化：monorepo 结构 + `frontend-admin` / `backend-api` / `asset-worker` / `ops` 子目录
- `frontend-admin` Next.js 15 + shadcn/ui + TipTap 初始化，加登录页占位
- `backend-api` Spring Boot 3.3 + Java 21，集成 dynamic-datasource、Spring Security、MyBatis-Plus、Redisson、Flyway、SpringDoc
- `asset-worker` FastAPI + Shopify GraphQL/REST 客户端 + R2 SDK
- 本地 `docker-compose.dev.yml`：MySQL 8 + Redis 8 + RabbitMQ 4 + MinIO（R2 兼容本地）
- Flyway 迁移：platform 库基础表（`sys_user / sys_org / sys_role / sys_permission / sys_tenant_datasource / dingtalk_corp` 等）
- 健康检查 `/health` 接口（DB / Redis / MQ / R2 探活）
- GitHub Actions `ci.yml` 跑通（lint + test + build + trivy）
- `.env.example` 完整覆盖（见附录 B）
- `ops/bootstrap/` 脚手架脚本骨架（每个脚本先打印目标 + `--dry-run` 占位）
- README 写明本地启动步骤

### Wave 1（MVP，约 4~5 周）
- 节点 A/B/C 服务器开荒、网络 / WireGuard mesh / AWS RDS / ElastiCache / R2 接入（节点 D 邮箱由邮箱方案商独立交付，本期仅做 SMTP 连通性验证）
- HK 跳板机配置 + ProxyJump 链路打通
- 多数据源 + 平台库 + 5 个租户库 Flyway 迁移
- 用户名密码登录 + JWT + RBAC + 组织树
- 钉钉企业内部应用：扫码登录 + 自助注册 + 多 corpId 白名单 + 自动同步（事件 + 兜底）+ 离职冷冻
- **临时员工邮件邀请**（发起 / 接受 / 撤销 / 到期巡检 + 邮件模板 + 邀请落地页）
- 店铺管理 + Custom App Token / OAuth Token 接入（公网回调）
- 产品库（CRUD + 42 列 CSV 导入导出）
- R2 集成（媒体上传/下载 + 折中签名 token）
- 软删除框架 + 审计 AOP
- Cloudflare WAF + Access 配置 + UptimeRobot
- GitHub Actions 测试环境部署 + 生产部署 workflow 跑通

### Wave 2（约 3 周）
- Shopify CLI 接入（cli 类型 token + 静默刷新）
- 资产拉取 worker（theme / policy / menu / collection 全包）
- 产品快照（webhook + 去抖 + 价格/库存历史）
- 产品推送（单/多店铺 + 冲突暂存 + 价格用产品库 + 库存 1000）
- 多店铺上架状态同步

### Wave 3（约 4 周）
- 一键开店编排（saga 状态机 + themes.json src 安装）
- 合作者店铺池接入流程 + 详情页预览
- 采购管理 + 改 SKU 二次确认
- 产品媒体 + TipTap 需求文档 + 文档上传
- 平台模板库 + 版本管理

### Wave 4（约 2 周）
- 统一审批中心（产品访问 + 跨公司授权）
- 钉钉通知（事件订阅 + 多 corpId）
- SMTP 邮件通道（其余告警类邮件、密码重置；临时员工邀请邮件已在 Wave 1 落地）
- 审计日志归档 + MySQL 加密备份
- Prometheus + Grafana + Loki + Alertmanager 上线
- 灾备演练 1 次

---

## 14. 命名规范与编码约束

- **包名**：`com.{org}.shopifyhub.{module}`
- **REST 路径**：`/api/{module}/{resource}`，复数；版本前缀 `/api/v1/`
- **DB 命名**：snake_case，业务表前缀按模块（`store_*`、`product_*`、`sys_*`、`dingtalk_*`、`approval_*` 等）
- **前端**：组件 `PascalCase`，hooks `useXxx`，目录 kebab-case
- **MQ 主题**：`{module}.{event}`（如 `newstore.theme.deploying`）
- **R2 key**：全部小写 + kebab/segment 分隔
- **commit**：约定式提交 `feat/fix/refactor/docs/chore/test(scope): subject`
- **分支**：`main`（prod）/ `develop`（staging）/ `feature/*` / `hotfix/*`
- **代码评审**：所有 MR 至少 1 人通过 + CI 全绿才能合
- **域名/子域**：见 5.5 节，`companyCode` 全小写字母 2~6 位，禁用保留前缀
- **强约束**：
  - 跨模块禁止直调 Service，必须走事件
  - 禁止跨数据源事务
  - 禁止前端做权限拦截（仅做 UI 隐藏）
  - 所有外部凭证必须加密入库
  - 所有写接口必须可审计
  - 所有公网入口必须经 Cloudflare（包括 webhook 与 oauth callback）

---

## 15. 待确认遗留项（v1.2）

> v1.0 的 O-1~O-10、v1.1 的 R-1~R-4 全部已确认并落入正文。剩余项均为低风险，开发推进过程中再对齐。

### 15.1 已确认（FAQ A8 终稿）

| 编号 | 决策 |
|---|---|
| **R-1** | ✅ 生产 = AWS RDS（MySQL 8）+ AWS ElastiCache（Redis 8），区域 `us-west-2`；测试 = 自建 docker MySQL/Redis |
| **R-2** | ✅ 主域 `biounetwork.com` |
| **R-3** | ✅ 企业邮箱 `mail.biounetwork.com`，**专属域名邮箱服务器（节点 D，SG，已部署）**，业务系统直连 SMTPS:465，不再走转发 |
| **R-4** | ✅ 测试用 Shopify Partner App 已就绪；上线前需另建生产 App，并把 redirect_uri 添加 `https://oauth.biounetwork.com/oauth/callback` |

### 15.2 仍开放（低风险，开发途中确认）

| 编号 | 内容 | 当前默认 | 风险 |
|---|---|---|---|
| **R-5** | Cloudflare 账号与域名归属 | `biounetwork.com` 需托管至 Cloudflare（开发前 1 周内办） | 低 |
| **R-6** | VPN 不可用时的国内访问降级 | 默认不专门处理（v1.3 已无国内节点）；如必须，按需加 HK 节点反代或国内 OSS 镜像 | 低 |
| **R-7** | 备份加密密钥管理 | .env 起步；上线后 90 天内切 AWS KMS | 中 |
| **R-8** | 数据出境合规 | 服务器境外，员工通讯录出境严格按个保法需评估；上线前补合规材料 | 中（法务介入） |
| **R-9** | TipTap 截图粘贴图片 | 默认异步上传 R2 + 走折中签名；分公司间共享多时再优化 | 低 |
| **R-10** | Shopify CLI 在 docker 容器内兼容性 | 仅 cli 类型 token 店铺需要；优先 OAuth / Custom App | 低 |
| **R-11** | 节点 B 备用副本蓝绿 | v1.2 默认健康检查 + 人工切流；蓝绿留 v2 优化 | 低 |
| **R-12** | 2FA（除钉钉外） | 不加；密码登录 + 钉钉二次确认已足；未来可加 TOTP | 低 |
| **R-13** | 离职冷冻期可配置性 | 默认 30 天全局；按公司差异化配置留 v2 | 低 |
| **R-14** | Logo / 品牌色 | 占位 "Biou x Shopify Control Center"，shadcn zinc；等设计稿换 | 低 |
| **R-15** | 企业微信 / 飞书 等额外通知通道 | 默认仅钉钉 + 邮件；如需可接 | 低 |
| **R-16** | 测试环境主机位置 | 默认开发本地 docker-compose；如需共享测试服，建议在节点 B（HK）划一组容器 | 低 |
| **R-17** | 生产 Shopify App 与测试 App 是否分离 | 强烈建议分离，测试 App 仅用 Partner dev store；上线前建生产 App | 中 |
| **R-18** | GitHub 仓库可见性与组织 | 默认私有；建议放在 `biounetwork` GitHub 组织下 | 低 |

---

## 附录 A：Shopify 产品 CSV 字段映射

参考 `/Users/zhangxueqian/development/product-manngment/products_export.csv`，共 42 列。每列对应数据库字段如下（节选，完整映射在 `backend-api/product/csv/CsvFieldMapping.java` 维护）：

| CSV 列 | 表 | 字段 |
|---|---|---|
| Handle | product | handle |
| Title | product | title |
| Body (HTML) | product | body_html |
| Vendor | product | vendor |
| Product Category | product | product_category |
| Type | product | type |
| Tags | product | tags |
| Published | product | published |
| Option1 Name / Value / Linked To | product_option / product_variant | name / option1 / linked_to |
| Variant SKU | product_variant | sku |
| Variant Grams | product_variant | grams |
| Variant Inventory Tracker | product_variant | inventory_tracker |
| Variant Inventory Qty | product_variant | inventory_qty |
| Variant Inventory Policy | product_variant | inventory_policy |
| Variant Fulfillment Service | product_variant | fulfillment_service |
| Variant Price | product_variant | price |
| Variant Compare At Price | product_variant | compare_at_price |
| Variant Requires Shipping | product_variant | requires_shipping |
| Variant Taxable | product_variant | taxable |
| Variant Barcode | product_variant | barcode |
| Image Src / Position / Alt Text | product_image | src / position / alt_text |
| Gift Card | product_image | gift_card |
| SEO Title | product | seo_title |
| SEO Description | product | seo_description |
| Variant Image | product_variant | variant_image |
| Variant Weight Unit | product_variant | weight_unit |
| Variant Tax Code | product_variant | tax_code |
| Cost per item | product_variant | cost_per_item |
| Status | product | status |

---

## 附录 B：环境变量占位（.env.example）

```ini
# ===== 应用 =====
APP_ENV=production
APP_PORT=8080
APP_BRAND_NAME="Biou x Shopify Control Center"
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
ENCRYPT_KEY_AES_GCM=                  # 32 字节 base64
BACKUP_ENCRYPT_KEY=                   # 32 字节 base64

# ===== 域名 =====
ROOT_DOMAIN=biounetwork.com
ADMIN_HOST=admin.biounetwork.com
API_HOST=api.biounetwork.com
OAUTH_HOST=oauth.biounetwork.com
WEBHOOK_HOST=webhook.biounetwork.com
DINGTALK_HOST=dingtalk.biounetwork.com

# ===== MySQL（托管 RDS） =====
MYSQL_HOST=                           # RDS endpoint
MYSQL_PORT=3306
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_PLATFORM_DB=platform
MYSQL_TENANT_PREFIX=tenant_
MYSQL_TLS_REQUIRED=true

# ===== Redis（托管） =====
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TLS=true

# ===== RabbitMQ（自建，节点 A） =====
MQ_HOST=rabbitmq
MQ_USER=
MQ_PASS=

# ===== Cloudflare R2 =====
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=shopify-assets
R2_ENDPOINT=https://<account>.r2.cloudflarestorage.com
R2_PUBLIC_BASE=https://cdn.biounetwork.com   # 如启用 R2 自定义域名

# ===== 钉钉企业内部应用（多 corpId） =====
# 主公司
DINGTALK_MAIN_CORP_ID=
DINGTALK_MAIN_AGENT_ID=
DINGTALK_MAIN_APP_KEY=
DINGTALK_MAIN_APP_SECRET=
# 其他分公司在 DB dingtalk_corp 表配置，启动时加载

DINGTALK_EVENT_TOKEN=                 # 事件订阅签名 token
DINGTALK_EVENT_AES_KEY=

# ===== Shopify =====
SHOPIFY_API_VERSION=2024-10
SHOPIFY_APP_KEY=
SHOPIFY_APP_SECRET=
SHOPIFY_WEBHOOK_SECRET=
SHOPIFY_OAUTH_REDIRECT_URI=https://oauth.biounetwork.com/oauth/callback

# ===== Worker =====
ASSET_WORKER_BASE_URL=http://asset-worker:9000
ASSET_WORKER_TOKEN=

# ===== SMTP（backend 直连 SG 域名邮箱服务器） =====
SMTP_HOST=mail.biounetwork.com
SMTP_PORT=465
SMTP_USERNAME=noreply@biounetwork.com
SMTP_PASSWORD=
SMTP_TLS=true
SMTP_FROM=Biou x Shopify Control Center <noreply@biounetwork.com>

# ===== 临时员工邀请 =====
INVITATION_LINK_BASE=https://admin.biounetwork.com/invitation/accept
INVITATION_LINK_TTL_HOURS=48          # 邀请链接有效期
TEMP_USER_MAX_DAYS=90                 # 临时账号最长有效期
TEMP_USER_EXPIRY_CHECK_CRON=0 * * * * # 每小时跑到期巡检
TEMP_USER_REMINDER_CRON=0 9 * * *     # 每天 09:00 发 24h 内到期提醒

# ===== 监控（节点 C） =====
PROMETHEUS_ENDPOINT=https://monitor.biounetwork.com/prom
LOKI_ENDPOINT=https://monitor.biounetwork.com/loki
ALERTMANAGER_DINGTALK_WEBHOOK=
ALERTMANAGER_EMAIL_TO=ops@biounetwork.com

# ===== Cloudflare =====
CF_API_TOKEN=                         # 用于自动管理 DNS / WAF rule
CF_ZONE_ID=
CF_ACCESS_AUD=                        # admin 域名 Cloudflare Access AUD

# ===== 外部探活 =====
UPTIMEROBOT_API_KEY=

# ===== GitHub Actions 部署相关（仅 self-hosted runner 节点 C 持有） =====
GHCR_USER=
GHCR_TOKEN=                           # ghcr.io 镜像仓库
NODE_A_HOST=                          # 美国主应用节点（WireGuard IP）
NODE_C_HOST=                          # 新加坡监控/备用节点（WireGuard IP，部署 backend-api-bk 时用）
JUMP_HOST=                            # 香港跳板机（节点 B 公网 IP，运维 SSH 入口）
JUMP_SSH_USER=jump
JUMP_SSH_PORT=                        # 跳板 sshd 自定义端口
DEPLOY_SSH_PRIVATE_KEY=               # 本地仅用于 self-hosted runner，不进 GitHub Secrets
```

> 说明：上述 GitHub 部署变量在 GitHub Repo Settings → Secrets 中配置，运行时由 Actions 注入；`DEPLOY_SSH_PRIVATE_KEY` 仅在 self-hosted runner 上以本地文件形式存在，不上传 GitHub。

---

## 附录 C：仓库与部署目录结构

```
shopify-hub/                              # GitHub monorepo
├── frontend-admin/                       # Next.js 15 + shadcn/ui + TipTap
│   ├── app/
│   ├── components/
│   ├── lib/
│   └── package.json
├── backend-api/                          # Spring Boot 3.3 + Java 21
│   ├── src/main/java/com/biou/shopifyhub/
│   │   ├── core/
│   │   ├── auth/
│   │   ├── rbac/
│   │   ├── org/
│   │   ├── tenant/
│   │   ├── store/
│   │   ├── asset/
│   │   ├── product/
│   │   ├── purchase/
│   │   ├── push/
│   │   ├── preview/
│   │   ├── newstore/
│   │   ├── approval/
│   │   ├── notification/
│   │   ├── audit/
│   │   ├── ops/
│   │   └── oauth/
│   ├── src/main/resources/
│   │   ├── application.yml
│   │   └── db/migration/                 # Flyway
│   └── pom.xml
├── asset-worker/                         # FastAPI + Shopify clients
│   ├── app/
│   │   ├── main.py
│   │   ├── shopify/
│   │   ├── r2/
│   │   └── tasks/
│   ├── requirements.txt
│   └── Dockerfile
├── ops/
│   ├── bootstrap/                        # 服务器开荒脚本
│   │   ├── 01-install-docker.sh
│   │   ├── 02-init-system.sh
│   │   ├── 03-setup-wireguard.sh
│   │   ├── 04-deploy-stack.sh
│   │   ├── 05-init-db.sh
│   │   ├── 06-create-r2-lifecycle.sh
│   │   ├── 07-setup-cloudflare-access.sh
│   │   ├── 08-register-gh-runner.sh
│   │   └── 99-smoke-test.sh
│   └── deploy/
│       ├── docker-compose.dev.yml        # 本地开发：mysql + redis + mq + minio
│       ├── node-a-app/docker-compose.yml # nginx, frontend, backend(主), worker, mq, redis-local, promtail, exporter
│       ├── node-b-dingtalk/docker-compose.yml  # dingtalk-gateway, jump-host(sshd), promtail, exporter
│       └── node-c-monitor/docker-compose.yml   # prometheus, grafana, loki, alertmanager, gh-runner, backend-api-bk
│
│       # 节点 D（mail.biounetwork.com）由邮箱方案商独立部署，不在本仓库 compose 范围
├── .github/
│   └── workflows/
│       ├── ci.yml
│       ├── deploy-staging.yml
│       └── deploy-prod.yml
├── docker-bake.hcl                       # 多镜像并行构建
├── .env.example
└── README.md
```

每个 compose 共享根目录 `.env`（通过 `--env-file` 注入），仅启用本节点服务。

---

## 附录 D：参考与依赖

- Shopify Admin API: https://shopify.dev/docs/api/admin
- Shopify CLI: https://shopify.dev/docs/themes/tools/cli
- Shopify Theme Asset API（themes.json src=URL 安装）: https://shopify.dev/docs/api/admin-rest/2024-10/resources/theme
- 钉钉企业内部应用文档: https://open.dingtalk.com/document/orgapp
- 钉钉事件订阅: https://open.dingtalk.com/document/orgapp/configure-event-subcription
- shadcn/ui: https://ui.shadcn.com
- TipTap: https://tiptap.dev
- dynamic-datasource-spring-boot-starter: https://github.com/baomidou/dynamic-datasource-spring-boot-starter
- Cloudflare Access: https://developers.cloudflare.com/cloudflare-one/applications/configure-apps/
- Cloudflare R2: https://developers.cloudflare.com/r2/
- AWS RDS for MySQL: https://aws.amazon.com/rds/mysql/
- AWS ElastiCache for Redis: https://aws.amazon.com/elasticache/redis/
- GitHub Actions Self-hosted Runners: https://docs.github.com/en/actions/hosting-your-own-runners
- WireGuard: https://www.wireguard.com/

---

> 文档结束。后续修订请在版本号 +1 并在文档头变更日志记录变更日期与摘要。
