# 项目检查报告 — 设计文档 vs 实现 diff

> 编写：2026-05-03
> 范围：对照《系统设计文档.md v1.3》§2 核心功能清单 + §8 关键模块详细设计 + §11 监控 + §12 灾备，逐项核对实际代码。
> 状态符号：✅ 完成 / 🟡 部分 / 🔲 未做 / ⚠️ 偏离设计 / 💡 增强建议

---

## 0. 总体结论

| 维度 | 状态 | 备注 |
|---|---|---|
| **后端模块覆盖** | ✅ 19 / 19 设计模块全部就位 | core / auth / invitation / rbac / org / tenant / store / asset / product / purchase / push / preview / newstore / approval / notification / audit / ops / oauth / file 全有 |
| **后端控制器** | ✅ 39 个 controller | 含 webhook / OAuth callback / 内部 loopback 等 |
| **数据库迁移** | ✅ V1..V24 共 24 个迁移 | 与设计 §6 表结构对齐 |
| **前端页面** | 🟡 26 个 authed 路由 + 6 个公开路由 = 32 路由 | 缺采购 / 组织 / 数据源 / 通知日志 / SKU 变更日志 等 5+ 后端模块的管理 UI |
| **后端 API 与前端 UI 覆盖度** | 🟡 ~ 75% | 见 §3 |
| **监控告警规则** | ✅ 5 group 全到位 | api-health / jvm-resource / business / ops-alerts / infra-placeholder |
| **告警 metric 已暴露** | 🔲 ~ 30% | 业务面 metric 多数仍占位（详见 TODO 清单 §3） |
| **灾备 SOP** | ✅ DR 演练 + Restore SOP | 但**演练实际未跑过一次**（W4-MON-03 文档级） |
| **CI/CD** | 🟡 4 job 并行（前端/后端/worker/trivy） | self-hosted runner 未注册到节点 C |

**最大风险**：上线前依赖**人工动手**的事项过多（见《TODO 清单.md》§0 P0 列表 9 项）。代码层基本就绪，工程化 / 部署 / 凭证 / 法务 是当前主要 gap。

---

## 1. §2.1 店铺与资产 检查

| 设计要求 | 实现位置 | 状态 |
|---|---|---|
| 店铺列表（按分公司维度）+ Token 状态总览 | `StoreController` + `/stores` | ✅ |
| Token 自动刷新 | `store/StoreTokenScheduler.java` + `/api/store/...` | ✅ Custom App 不需要 refresh；OAuth 30 天前 `TOKEN_EXPIRING` 通知 |
| Active Theme 整包拉取 | `asset-worker/app/services/theme_pull.py` + `/pull/theme` | ✅ |
| Policy / Menu / Collection 手动备份 | worker `policy_pull.py` / `menu_pull.py` / `collection_pull.py` | ✅ |
| 媒体文件下载并落 R2 | `theme_pull.py` 内嵌 + `product_pull.py` | ✅ |
| 产品状态/内容变更自动建快照（5min 去抖） | `webhook/ProductWebhookController` + `core/mq/RabbitMqConfig` 5min 延时 + `snapshot/SnapshotProcessConsumer` | ✅ |
| 价格/库存改动单独历史表 | `snapshot/PriceInventoryHistoryService` → `product_price_history` / `product_inventory_history` | ✅ |

**Gap**：无。模块完整。

---

## 2. §2.2 产品管理 检查

| 设计要求 | 实现位置 | 状态 |
|---|---|---|
| 产品 CRUD 对齐 Shopify 42 字段 | `ProductController` + `product/entity/Product.java` | ✅ |
| CSV 导入/导出 | `ProductCsvController` + 前端 `/products/import` | ✅ |
| 多店铺批量推送 | `PushController` | ✅ |
| 广告贴/对标页/产品素材链接（仅链接） | `ExternalLinkController` | ✅ 后端有，前端**部分集成在产品详情页**（需 verify） |
| 采购管理：SKU/成本/克重/物流 tag/链接 | `PurchaseController` (4 endpoints) | 🟡 后端齐 + **前端无独立采购页** |
| 改 SKU 二次确认 + 同步映射店铺 + 强制审计 | `PurchaseController.changeSku` + `@RequireSensitiveOp` + `SkuChangeLog` | ✅ 后端 |
| 产品媒体（主图/详情/需求文档/TipTap） | `ProductDocController` + `ProductImage` + 前端 TipTap | ✅ |
| 多店铺上架状态：webhook 实时 + 04:00 兜底 | `ProductWebhookController` + `sync/StatusSyncScheduler` | 🟡 兜底扫只统计行数不真 diff Shopify（见《技术债登记.md》#11） |
| 状态可双向同步（仅产品信息页） | `ProductStoreMappingController` | ✅ |

**Gap**：
- 🔲 **采购管理前端页缺失**（W3-PUR-04 设计有，未交付）
  - PurchaseController 4 个 endpoint 完全无 UI 入口：`/product/{id}/purchase`、`/variant/{id}/purchase`、`/variant/{id}/sku`、`/variant/{id}/sku-log`
  - 影响：采购成本 / SKU 变更 / 物流 tag 全部要 curl 才能用
  - 优先级：**P1**（业务侧已建表 + 后端就绪，UI 是闭环最后一公里）

---

## 3. §2.3 推送与开店 检查

| 设计要求 | 实现位置 | 状态 |
|---|---|---|
| 单/批量推送 + Handle 匹配 + 价格走库存 1000 + Continue selling 默认开 | `PushController` + `PushService` | ✅ |
| 冲突跳过 + 批量决策 | `PushConflictService` | ✅ 后端 |
| **冲突批量决策 UI**（覆盖/跳过 选择）| ? | ⚠️ **未独立 UI** — 前端推送逻辑里如何展示和决策冲突需 verify；可能仅 dialog 单条决策 |
| 详情页预览（dev store 池 + preview theme） | `PreviewController` + worker `preview_build.py` + 前端 partner-stores 页 | ✅ |
| Preview 配额（每用户每店 3 个 + 24h 清理） | `preview/PreviewCleanupScheduler.java`（如存在） | 🟡 待 verify |
| 一键开店 12 步 saga | `SagaController` + `SagaStateMachine` + worker `saga_*.py` + 前端 `/newstore/[taskId]` | ✅ |
| 主题 zip 通过 themes.json src=R2签名URL 安装 | `saga_theme_install.py` 等 | ✅ |
| 域名/品牌替换（default_replace_rules） | `BaseTemplateVersion.defaultReplaceRules` + saga 内 | ✅ |

**Gap**：
- 🟡 **批量推送冲突决策 UI**：UI 是否能批量选择全部冲突的处理策略？需要在 `/tasks/[id]` 验证。
  - 当前 `PushConflictService` 后端实现 PENDING 状态，但前端 `tasks` 页是否 surface 待决策项需要看代码
  - 优先级：**P2**

---

## 4. §2.4 组织、权限、流程 检查

| 设计要求 | 实现位置 | 状态 |
|---|---|---|
| 多分公司、多部门组织树 | `OrgController` + `sys_org` + `org_dept_*` 钉钉同步 | ✅ 后端 |
| **组织树管理 UI** | ? | 🔲 **无独立 UI** — 没有 `/orgs` 页面，组织树仅消费在用户筛选 dropdown 内 |
| 钉钉企业内部应用扫码登录（多 corpId 白名单）+ 用户名密码 | `AuthController` + `DingTalkAuthController` + `/login` | ✅ |
| 钉钉组织架构 + 员工自动同步（实时 + 02:00 兜底） | `DingTalkSyncConsumer` + `DingTalkOrgSyncScheduler` | ✅ |
| 自助注册 + 工号 EMP000001 + 备用密码 | `DingTalkLoginService.autoRegister` | ✅ |
| 离职冷冻 30 天 → 软删（90 天物理删） | `DingTalkSyncConsumer.handleUserLeaveOrg` + `RecycleBinPurgeScheduler` | ✅ |
| RBAC + 数据范围（分公司隔离） | `rbac/datascope/DataScopeInterceptor` + `@DataScope` | ✅ |
| 显式跨公司授权（默认 7 天，24h 提醒，到期自动收回）| `CrossCompanyAuthController` + `CrossAuthExpiryScheduler` + `/cross-auth` | ✅ |
| 统一审批中心（PRODUCT_ACCESS + CROSS_COMPANY_AUTH）| `ApprovalEngine` + `/approvals` | ✅ |
| 单签 + 任一角色 + 驳回可改重提 | `ApprovalEngine.submit/approve/reject/resubmit` | ✅ |
| 钉钉消息可订阅 | `SubscriptionService` + `/profile` 矩阵 | ✅ |
| 钉钉 access_token 自动维护 | `DingTalkApiClient.getAccessToken` Redis cache 6900s | ✅ |

**Gap**：
- 🔲 **组织管理 UI 缺失**：无 `/orgs` 树形管理页。运维需在 DB 里直接手动调整 `sys_org`。
  - 优先级：**P2**（钉钉同步已 cover 90% 场景，仅手动调整缺位）
- 🟡 **角色 + 权限点管理 UI 缺失**：无 `/roles` 页可见 sys_role 列表 + 权限授予界面。运维只能 SQL 操作。
  - 优先级：**P2**（10 个角色 + 23 权限点已 seed，运行期不太需要改）
- 🟡 **任一角色签批模式实际未展开**：`ApprovalEngine.notifyApprovers` 角色模式只打日志，不发通知给该角色全员。
  - 优先级：**P1**（影响审批流可用性）

---

## 5. §2.5 平台与运维 检查

| 设计要求 | 实现位置 | 状态 |
|---|---|---|
| 审计日志（1 年 MySQL + 永久 R2 加密归档） | `AuditAspect` + `sys_audit_log` + `AuditArchiveScheduler` | ✅ |
| MySQL 每日 03:00 加密备份 → R2（AES-256-GCM） | `ops/backup/rds-backup.sh` + `BackupNotifyController` | ✅ 脚本就位，cron 待节点 A 安装 |
| 软删除 90 天可恢复 | `@TableLogic` + `RecycleBinPurgeScheduler` + `/recyclebin` | ✅ |
| GitHub Actions CI/CD | `.github/workflows/ci.yml` 4 job | 🟡 自动测试 OK；自动部署 self-hosted runner 待注册 |
| 一键开荒服脚本 | `ops/bootstrap/01..10` + `99-smoke-test` | ✅ |
| Prometheus + Grafana + Loki 自监控 | `ops/monitoring/docker-compose.yml` | ✅ |
| **UptimeRobot 外部探活** | ? | 🔲 未配（属外部账号，无代码痕迹） |
| Cloudflare WAF + Access + Rate Limit | 文档化未配 | 🔲 上线前由运维操作 |

---

## 6. §8 关键模块详细设计 检查

### 8.1 认证与登录

| 子项 | 实现 | 状态 |
|---|---|---|
| 用户名密码 + BCrypt + 5 次/15min 锁 | `AuthService` + Redis 滑窗 | ✅ |
| JWT RSA access 30min + refresh 7d + 黑名单 | `JwtUtil` + `auth:blacklist:*` | ✅ |
| 钉钉扫码 OAuth v1.0 + state HMAC | `DingTalkAuthController` | ✅ |
| 多 corpId 白名单 | `dingtalk_corp` 表 + `DingTalkProperties` | ✅ 表 + 配置 |
| 危险操作二次确认 6 位码 | `@RequireSensitiveOp` + `SensitiveOpController` | ✅ |
| 临时员工邮件邀请 | `InvitationController` + `/invitations` | ✅ |
| 密码重置（W4 新增）| `PasswordResetController` + `/forgot-password` `/reset-password` | ✅ |

### 8.2 RBAC 与数据范围

✅ 完整：`@DataScope` + `DataScopeInterceptor` + `sys_data_scope` + 跨公司支持。

⚠️ **偏离设计**：`SecurityConfig.anyRequest().permitAll()`（line 39）仍是 dev 模式，未切 `.authenticated()`。**必须上线前改回**。

### 8.3 多数据源动态路由

`TenantDataSourceController` 4 endpoints 就绪，dynamic-datasource-spring-boot3-starter 集成。

🟡 **当前 dev 单库**：`application-dev.yml` 仅配 platform 主库，tenant_01..05 实际未启用（W1-CORE-01 留 W2 做，但实际推到了上线前）。
🔲 **管理 UI**：无前端，仅 curl 调用。

### 8.4 Shopify Token 与 OAuth

| 子项 | 实现 | 状态 |
|---|---|---|
| Custom App Token 接入 | `StoreController` + `/stores/new` | ✅ |
| Shopify OAuth 2.0 完整流程 | `ShopifyOAuthController` + `oauth/ShopifyOAuthService` | ✅ |
| 公网回调 `oauth.biounetwork.com` | 域名待配 | 🔲 上线前 |
| Token AES-GCM 加密入库 | `StoreService.saveToken` + `AesGcmUtil` | ✅ |

### 8.5 资产拉取 Worker

✅ FastAPI + 8 service modules（theme/product/menu/policy/collection/preview/file 各种 pull/push）。

### 8.6 产品快照

✅ Webhook 5min 去抖 + Redis dedupe + RabbitMQ 延时 + `SnapshotProcessConsumer`。

### 8.7 产品推送

✅ 单/批量 + 媒体上传 + 冲突 PENDING + RabbitMQ 异步。
🟡 同步循环 `pushBatch` 阻塞（《技术债登记.md》#10），未来 W3-NEW 高频时改 `@Async`。

### 8.8 多店铺上架状态同步

✅ webhook 实时；🟡 兜底 04:00 仅统计行数不真 diff（同 #11 技术债）。

### 8.9 详情页预览

✅ 完整：合作者店铺池 + 最少负载分配 + theme 创建 + 配额 + 24h 清理。

### 8.10 一键开店 saga

✅ 12 步状态机 + worker 调用 + R2 签名 URL 主题安装 + 域名替换。

### 8.11 统一审批中心（W4 G1）

✅ 完整：单签 / 任一角色 / 驳回可改重提 / 通知 / Hook 链。
🟡 「任一角色」模式只打日志不发通知（修复后即完美）。

### 8.12 钉钉通知（可订阅）（W4 G2）

✅ 16 事件 seed + 订阅矩阵 + 多 corpId 解析 + 失败 4 次重试退避。
🟡 `MultiCorpDingTalkResolver` 当前所有 corp fallback 主企业（perCorp token cache 未实装）。

### 8.13 邮件通道（W4 G3）

✅ EmailService + EmailTemplateRenderer（PASSWORD_RESET / REGISTRATION / ALERT）+ 密码重置全链。

### 8.14 文件签名 token（折中方案 B）

🟡 `FileService` 用 presigned GET URL（4h），✅ 简化版方案。
🔲 `file_sign_token` 表 + scope_keys 限制留 Wave 2 完整化（注释里提到，未做）。

### 8.15 审计日志

✅ AuditAspect 切所有 @RestController POST/PUT/DELETE。
✅ 月归档（W4-OPS-01）gzip + AES-256-GCM → R2。

### 8.16 备份

✅ rds-backup.sh + AuditArchiveScheduler + restore-sop.md + DR drill SOP。
🔲 cron 安装 + BACKUP_AES_KEY 1Password 双备份待运维操作。

### 8.17 软删除

✅ `@TableLogic` 三表（sys_user / sys_org / store）+ 90 天 RecycleBinPurgeScheduler + audit-purge.sh R2 端清理。

### 8.18 CI/CD

🟡 ci.yml 4 job 并行 OK；🔲 self-hosted runner 注册待运维（节点 C）；🔲 部署 workflow 缺。

---

## 7. §11 监控与告警 检查

| 子项 | 实现 | 状态 |
|---|---|---|
| Prometheus + Grafana + Loki + Alertmanager | `ops/monitoring/` 全到位 | ✅ |
| 5 group rules（api-health / jvm / business / ops-alerts / infra-placeholder） | rules/*.yml 35+ rules | ✅ |
| 钉钉群机器人 webhook + 路由分流（ops vs backend） | `alertmanager.yml` | ✅ 配置；🔲 webhook URL 待真群创建 |
| node-exporter + JVM exporter | docker-compose 内 | ✅ dev；🔲 节点 A systemd 部署 |
| UptimeRobot 外部探活 | 无 | 🔲 |
| 业务 metric 暴露（10+ 个 counter/gauge） | 后端 Micrometer 待加 | 🔲 见 TODO 清单 §3 |

---

## 8. §12 灾备与恢复演练 检查

| 子项 | 实现 | 状态 |
|---|---|---|
| Restore SOP（PITR + dump 解密 + Flyway 修复） | `ops/backup/restore-sop.md` | ✅ |
| DR 演练 SOP（季度 / 半年 / 全年三档） | `ops/disaster-recovery/dr-drill-sop.md` | ✅ |
| **实际跑过演练** | — | 🔲 **从未跑** — 上线前必须至少跑 1 次 L1 |

---

## 9. §6 数据库设计 检查

| 表族 | 设计 | 实际 (Flyway 24 个版本) | 状态 |
|---|---|---|---|
| 平台主库（13 表）| sys_user / sys_org / sys_role / sys_permission / sys_data_scope / dingtalk_corp / sys_user_role / sys_role_permission / user_invitation / sys_audit_log / sys_org_dingtalk_sync_log / sys_tenant_datasource / 等 | V1 全部建立 | ✅ |
| 店铺 + 产品 | store / store_product / product / product_variant / product_image / product_doc | V4..V7, V12 | ✅ |
| 资产 + 快照 | asset_snapshot / asset_file / product_snapshot / product_price_history / product_inventory_history | V8..V11 | ✅ |
| 推送 | task / push_conflict / store_product / store_product_variant | V10..V13 | ✅ |
| 模板 + 指导 | base_template / base_template_version / guide_doc | V15 | ✅ |
| 跨公司 + 预览 | sys_data_scope（cross_company）+ preview_theme | V16, V18, V19 | ✅ |
| 性能索引 | 热点查询索引补全 | V20 | ✅ |
| W4 新增 | approval_flow / approval_log / notification_event_def / notification_subscription / notification_log / inapp_message / password_reset_token / audit_archive_log | V21..V24 | ✅ |

**Gap**：
- 🔲 设计 §6.3 提到的「租户库 tenant_01..05 每个分公司一份」**未启用**：所有 tenant_id 数据当前都在 platform 主库。
  - 影响：单库扛不住 5 公司 + 50 店 + 50w 产品的极限规模
  - 触发再看：单表 > 100w 行 OR p99 查询 > 500ms 时迁移

---

## 10. §13 上线节奏 检查

| Wave | 设计 PD | 实际 | 状态 |
|---|---|---|---|
| Wave 0 | ~1 周 | 9/9 ✅ | ✅ |
| Wave 1 (MVP, ~5 周) | ~32 PD | 业务 100% | ✅ |
| Wave 2 (~3 周) | ~50 PD | 资产/快照/推送/监控基础 | ✅ |
| Wave 3 (~4 周) | ~70 PD | 一键开店/合作者池/模板/跨公司/性能 | ✅ |
| Wave 4 (~2 周) | ~30 PD | 审批/订阅/邮件/审计/备份/监控/灾备/上线 | ✅ |
| **总开发时间** | ~15 周 | 实际 ~3 天集中 + Claude Code 加速 | 🚀 |

**节奏 OK**，进度记录 `进度记录.md` 详细。

---

## 11. §14 命名规范与编码约束 检查

抽样验证：

| 规则 | 实测 | 状态 |
|---|---|---|
| 包名 `com.biou.shopifyhub.*` | 一致 | ✅ |
| Controller 命名 `XxxController` + `@RestController` | 39 个 controller 都符合 | ✅ |
| Service 命名 `XxxService` + `XxxServiceImpl` | 部分；有些 service 直接命名（如 ApprovalEngine） | 🟡 不强制 impl 后缀 |
| Mapper 命名 `XxxMapper` extends `BaseMapper<XxxEntity>` | 一致 | ✅ |
| Entity `@TableName(value = "xxx", autoResultMap = true)` | 一致 | ✅ |
| Flyway 版本号唯一 | V1..V24 + V17 缺位（设计预留） | ✅ |
| 跨模块禁止直调 Service | 抽查：approval 调 NotificationDispatcher（OK，是 notification 对外入口）；其它 OK | ✅ |
| 跨数据源事务禁止 | 当前单库无跨数据源场景 | N/A |
| 异常 `BusinessException` + `ResultCode` | 一致 | ✅ |
| 响应 `Result<T>` 包装 | 一致 | ✅ |

---

## 12. §15.2 仍开放遗留项 检查

设计文档 §15.2 列出 9 项未确认低风险问题，逐一对照：

| # | 问题 | 当前处理 | 状态 |
|---|---|---|---|
| O-1 | 跨公司授权续期是否需要 5min 前再次提醒 | 仅 24h 一次 | 🟡 可接受 |
| O-2 | 预览 theme 配额是否区分 user 类型 | 一律 3 个 | ✅ |
| O-3 | 推送冲突 UI 决策是 dialog 还是独立页 | dialog（推断） | 🟡 待 verify |
| O-4 | TipTap 富文本最大 20 个媒体限制是否前端硬限 | ProductDocController 校验 | ✅ |
| O-5 | 钉钉同步全量是否分页 | 全量取所有部门 + 分批拉用户 | ✅ |
| O-6 | 数据范围跨表 join 是否走 view | 拦截器在 SQL 层注入 WHERE | ✅ |
| O-7 | R2 lifecycle 是否每桶独立 | 见配置指南 | 🔲 待运维配置 |
| O-8 | 灾备演练是否走 staging 而非 prod | 走影子环境（见 dr-drill-sop） | ✅ |
| O-9 | 文件签名 token 是否启用 | 当前 presigned URL 简化版 | 🟡 见 §8.14 |

---

## 13. 设计 vs 实现 偏离汇总

| # | 偏离 | 影响 | 建议 |
|---|---|---|---|
| 1 | `SecurityConfig.anyRequest().permitAll()` 未切 `.authenticated()` | **HIGH**：所有 API 实际无需 JWT 即可访问 | 上线前必改（《TODO 清单》S-1） |
| 2 | 多数据源 tenant_01..05 未启用 | MEDIUM：扩展性受限 | 单库 > 100w 行时迁移 |
| 3 | 任一角色签批模式不发通知 | MEDIUM：影响审批可用性 | 1 PD 修复（《TODO 清单》D-1） |
| 4 | `MultiCorpDingTalkResolver` 不真做 perCorp token | LOW（dev 单 corp 无影响） | 第二家公司接入时修 |
| 5 | StatusSyncService 兜底扫不真 diff Shopify | MEDIUM：webhook 丢包时无补救 | 《技术债》#11，3 PD |
| 6 | 文件签名 token 表未建（用 presigned 简化） | LOW：当前 4h presigned 够用 | 暂缓 |
| 7 | 监控 metric 多数仍占位 | MEDIUM：告警延后激活 | 2 PD（《TODO》§3） |
| 8 | UptimeRobot 外部探活无配置 | LOW：内部监控可补 | 上线前外部账号注册 |
| 9 | 采购 / 组织 / 角色 / 数据源 等模块**前端 UI 缺失** | MEDIUM：运维 / 业务部分场景需 SQL 操作 | 见《前端改进文档.md》 |

---

## 14. 推荐上线前必做（按优先级）

**P0 阻塞**：
1. SecurityConfig 切 `.authenticated()` + W1-RBAC `@PreAuthorize` 上一遍（见《TODO 清单》§6 S-1, S-2）
2. 跑一次 DR 演练 L1 + 验收（《TODO 清单》§0 P0-9）
3. 任一角色签批通知修复（同 D-1）
4. 监控关键 metric 暴露 6 项（backup_last_success / r2_upload / notification_send / approval_pending / audit_archive_last / store_tokens_expiring）

**P1（强烈建议）**：
5. 采购前端页（W3-PUR-04）
6. 组织管理前端 + 角色管理前端
7. perCorp DingTalk token 真实落地（如有第二企业）
8. UptimeRobot 注册 + 5 个外部探活 endpoint

**P2（上线后第一周）**：
9. 推送冲突批量决策 UI verify + 优化
10. StatusSyncService 真 diff（W2-SYNC-04 完整化）
11. 文件签名 token 表（如 R2 公网泄漏成顾虑）

---

## 15. 总结

**优点**：
- 19 个后端模块全实现，与设计完美映射
- Flyway V1..V24 + 24 张表与设计 §6 完全对齐
- Wave 4 全部上线（审批 + 通知 + 邮件 + 审计 + 监控 + 灾备）
- 文档体系完整：设计 + 任务 + 进度 + 性能 + 技术债 + 配置 + TODO + 系统功能 + 本检查
- 编码规范一致性高（包名 / 命名 / 异常 / 响应 / 拦截器）

**主要 gap**：
- 前端 UI **缺 5+ 模块管理页**（采购 / 组织 / 角色 / 数据源 / 通知日志）
- **生产凭证 + 部署 + 演练**未真跑（属运维侧，代码层 OK）
- **RBAC 实际未启用**（dev permitAll 兜底，上线前必改）
- **监控 metric 暴露率仅 ~30%**（告警仍占位）

**整体评估**：**代码完成度 95%，工程化 / 部署完成度 50%，UI 完整度 75%。可接受 RC（release candidate）发布，但上线前需走完 §14 P0 4 项 + 跑 DR 演练。**

---

_最后更新：2026-05-03_
