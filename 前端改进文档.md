# 前端改进文档 — 缺失界面 + 交互友好性建议

> 编写：2026-05-03
> 范围：前端 (Next.js 15 + shadcn/ui) 32 路由的功能完整度审计 + UX 改进建议
> 配套：《项目检查报告.md》§5 §11，列出后端已实现但前端无 UI 的模块

---

## 0. 优先级一览

| 类别 | P0（上线阻塞） | P1（强烈建议） | P2（迭代优化） | P3（长尾）|
|---|---|---|---|---|
| **缺失界面** | 0 | 5 | 4 | 3 |
| **交互改进** | 1 | 8 | 12 | 10 |
| **设计系统** | 0 | 4 | 6 | 5 |

P0 = 影响上线 / 数据安全；P1 = 影响日常运营效率；P2 = 体验提升；P3 = 锦上添花。

---

## A. 缺失的功能界面（按 backend 模块倒序）

### A.1 采购管理（W3-PUR-04）— **P1**

**后端已就绪**：`PurchaseController` 4 endpoints
```
GET    /product/{productId}/purchase             获取采购信息
PUT    /variant/{variantId}/purchase             更新采购成本/克重/物流 tag/链接
POST   /variant/{variantId}/sku                  改 SKU（敏感操作 + 全店同步）
GET    /variant/{variantId}/sku-log              SKU 变更历史
```

**缺失 UI**：完全无入口。

**建议设计**：
- 在 `/products/[id]` 页加 tab「采购信息」
- 字段：variant 列表 → 每行【SKU / 采购成本 / 克重 / 物流 tag / 采购链接】可编辑
- SKU 列点击 → dialog 弹出钉钉 6 位码二次确认 → 提交后右侧显示「同步进度」（多店 progress 条）
- 底部抽屉：「SKU 变更历史」时间线（actor / old / new / 同步成功的店数）

**工时估算**：1.5 PD（含 dialog + 二次确认 + 历史时间线）

---

### A.2 组织树管理（W1-ORG）— **P2**

**后端已就绪**：`OrgController` 5 endpoints (`GET /tree`, CRUD)

**缺失 UI**：完全无入口；钉钉同步覆盖 90% 场景，但**离线场景** / **手动调整**（如新建虚拟部门 / 跨部门调岗）无法操作。

**建议设计**：
- 新增 `/orgs` 路由
- 左侧：树形组件（shadcn 没有现成 Tree，可用 `react-arborist` 或 `@radix-ui/react-tree-view`）
- 右侧：选中节点的详情 + 子部门列表 + 该部门员工数
- 顶部：「新建子部门」「重命名」「删除」按钮（删除二次确认）
- 标识：从钉钉同步来的部门带 🔗 icon 不可删除（仅可改本地别名）

**工时估算**：2 PD

---

### A.3 角色 + 权限管理（W1-RBAC）— **P2**

**后端已就绪**：`SysRole` + `SysPermission` + `SysRolePermission` + `SysUserRole` 表 + Mapper（无 controller，需补）

**缺失 UI**：完全无入口；10 个内置角色 + 23 个权限点目前只能 SQL 操作。

**建议设计**：
- 新增 `/roles` 路由
- 左侧：角色列表（10 个内置 + 自定义创建）
- 右侧：选中角色的权限点矩阵（行=模块、列=操作 read/write/delete）
- 底部：该角色当前的用户列表 + 「批量授予」按钮
- 内置角色（SUPER_ADMIN 等）灰色不可删

**前置**：补一个 `SysRoleController` 后端 controller（CRUD + 权限授予），约 1 PD。
**前端工时**：2 PD（矩阵 UI 较繁琐）

---

### A.4 多数据源管理（W1-CORE）— **P2**

**后端已就绪**：`TenantDataSourceController` 4 endpoints (`GET /list`, `POST /reload`, `POST /{tenant}/register`, `DELETE /{key}`)

**缺失 UI**：完全无入口；`tenant_01..05` 多租户数据源**当前未启用**，但 controller 在线。

**建议设计**：
- 新增 `/admin/datasources` 路由（仅 SUPER_ADMIN 可见）
- 列表：tenant_code / url / username / pool_max / status / 健康（在线 ping）
- 操作：注册 / 删除 / 一键 reload（重建连接池）
- 顶部：当前路由命中的 tenant_code 实时显示

**工时估算**：1 PD

---

### A.5 通知发送日志（W4-NTF-04）— **P1**

**后端已就绪**：`notification_log` 表 + `NotificationLogMapper`（无对外 controller，需补）

**缺失 UI**：完全无入口；运维查看「为什么钉钉没发出」时只能 SQL。

**建议设计**：
- 新增 `/admin/notification-log` 路由
- 顶部过滤：event_code 下拉 / channel 下拉（DINGTALK/EMAIL/INAPP）/ status / 时间范围
- 列表：时间 / 用户 / 事件 / 通道 / status badge / attempt_count / next_retry_at
- 行点击：展开 body_text + error_msg
- 操作：「立即重试」按钮（status=FAILED 时可点）
- 顶部统计卡：今日 PENDING / SENT / FAILED 数

**前置**：补 `NotificationLogController` GET 列表 + POST `/{id}/retry`，约 0.5 PD。
**前端工时**：1 PD

---

### A.6 审批模板配置 — **P2**

**后端**：审批 type hardcoded 在 `ApprovalTypeHook` 子类（PRODUCT_ACCESS / CROSS_COMPANY_AUTH）

**缺失 UI**：管理员无法在线增删审批类型（每加一种新审批要发版）。

**建议设计（v2 阶段）**：
- 新增 `/admin/approval-types` 路由
- 列表：type code / 显示名 / 默认审批人角色 / payload schema (JSON)
- 新增：表单驱动的 payload 字段定义
- **难度**：高（需要后端做动态 type 注册 + 前端动态表单）；当前阶段可放 v1.1 之后

**优先级**：P3（不影响上线）

---

### A.7 审计日志查询 — **P2**

**后端已就绪**：`sys_audit_log` 表 + `SysAuditLogMapper`（无对外 controller）

**缺失 UI**：完全无入口；合规请求要查某用户某段时间所有操作时只能 SQL。

**建议设计**：
- 新增 `/admin/audit-log` 路由
- 顶部过滤：用户 / 模块 / action / 时间 / sensitive=true
- 列表：时间 / 用户 / 模块.action / URI / IP / 状态码 / sensitive badge
- 行展开：request_payload (JSON pretty) + user_agent
- 顶部「导出 CSV」按钮（按当前过滤）
- 「跳归档」入口：归档月（YYYY-MM）→ 拉 R2 解密 → 在线查询（高难度，可放 v1.1）

**前置**：补 `SysAuditLogController` GET 列表（带分页），0.5 PD。
**前端工时**：1.5 PD

---

### A.8 备份 / 归档监控面板 — **P2**

**后端已就绪**：`audit_archive_log` 表（仅元数据）+ `BackupNotifyController`（loopback）

**缺失 UI**：完全无入口；运维查看备份是否成功只能 SQL 或看告警。

**建议设计**：
- 新增 `/admin/ops` 路由
- 4 个卡片：上次 RDS 备份 / 上次审计归档 / 最近备份失败 / 最近归档失败
- 「audit_archive_log 列表」：每月一行 / status / r2_key / sha256 / row_count / 解密下载（管理员二次确认）
- 「立即触发归档」按钮 → 调 `/api/ops/backup/audit-archive/run`（loopback 限制需在 backend 同机调用）

**前置**：把 `BackupNotifyController` 改成支持非 loopback 但 SUPER_ADMIN 角色访问的接口；约 0.5 PD。
**前端工时**：1.5 PD

---

### A.9 监控 Grafana 跳转 — **P3**

简单加菜单项「监控面板」→ 外链到 `https://grafana.biounetwork.com`。

**工时估算**：5 分钟。

---

### A.10 推送冲突批量决策 UI — **P1**

**后端已就绪**：`PushConflictService` PENDING 状态

**当前 UI**：未确认；可能仅 dialog 单条决策。

**建议设计**：
- 在 `/tasks/[id]` 详情页增加「冲突待决策」section
- 列表：每条 product / store / conflict_type（HANDLE_EXISTS / SKU_CONFLICT 等） + 「覆盖」「跳过」「跳过余下批次」三按钮
- 顶部「批量决策」：全选 + 一键决策当前过滤
- 决策后实时刷新，PENDING 数减 1

**工时估算**：1 PD

---

## B. 现有页面的 UX 改进建议

### B.1 全局导航与布局（高频痛点）— **P0~P1**

**当前**：`AppShell.tsx` NAV 是一维列表 16+ 项，已经太长，新人迷路。

**建议**：

**B.1.1 二级菜单分组** — **P0**（影响所有页面）
```
首页
─ 业务
   产品库
   店铺管理
   合作者店铺池
   一键开店
─ 资产
   资产快照
   产品快照
   模板库
─ 流程
   审批中心
   临时员工邀请
   跨公司授权
─ 我的
   通知中心
   个人中心
─ 系统（仅 SUPER_ADMIN 可见）
   组织管理
   角色管理
   数据源
   审计日志
   备份归档
   通知日志
─ 工具
   任务监控
   回收站
   指导文档
```

**实现**：把 NAV 重构为 `{ section, items[] }` 结构，渲染 `<details>` 折叠。约 0.5 PD。

**B.1.2 全局未读 inapp 红点** — **P1**
- 顶部 header 加 🔔 icon + 红点 badge
- 30s 轮询 `/notification/inapp/unread-count`（或 SSE / WebSocket 进阶）
- 点击跳 `/inbox`

**B.1.3 顶部 Breadcrumb** — **P2**
当前面包屑用「← 返回 X」按钮（如 `/approvals/[id]` 顶部），不规范且易丢。
建议引入 shadcn Breadcrumb 组件，自动按路由生成。

**B.1.4 顶部用户菜单 dropdown** — **P2**
当前 header 右上 user info + 退出按钮直接展示。
改为 avatar + 下拉菜单（个人中心 / 通知设置 / 切换主题 / 退出）。

**B.1.5 移动端适配** — **P3**
当前 `w-56 shrink-0` 侧边栏在 < 768px 屏幕上会挤压主内容。
建议 < 768px 时侧边栏改 hamburger drawer（用 shadcn Sheet）。

---

### B.2 列表页通用问题 — **P1**

**当前**：所有列表页（products / stores / approvals / tasks 等）都自己手写 `<table>`，没有统一组件。

**问题**：
- 排序、分页、过滤逻辑每页重写
- 列宽 / 对齐 / hover 风格不一致
- 大数据量没有虚拟滚动

**建议**：
1. **抽取 `<DataTable>` 组件**（可基于 `@tanstack/react-table`）
   - props: columns / data / pagination / sortable / filterable
   - 内置 loading skeleton + empty state
2. **抽取 `<FilterBar>` 组件**：tab + dropdown + date-range + search 统一布局
3. **大数据量虚拟滚动**：W3-PERF-03 已识别，留产品库 > 1000 行时启用 `@tanstack/react-virtual`

**工时估算**：3 PD（一次重构，所有列表页受益）

---

### B.3 表单组件统一化 — **P1**

**当前**：所有表单（邀请新建 / 推送 dialog / 审批重提 / 跨公司授权 / 等）手写 `<input>` + `<label>`，错误提示散乱。

**建议**：
- 引入 `react-hook-form` + `zod` 校验
- 抽 `<Form>` `<FormField>` `<FormError>` `<FormSubmit>` 组件
- 统一字段：必填红星 / 错误红框 / 提交时禁用 + spinner / 成功后 toast

**工时估算**：2 PD（一次重构 + 5+ 个表单页改造）

---

### B.4 单页改进点（按现有页面）

#### B.4.1 `/dashboard` — **P1**
**当前**：内容偏简单，无实时数据卡片。
**建议**：
- 顶部 4 卡片：产品总数 / 在线店铺数 / 今日推送数 / 待办审批数
- 中部：今日通知发送量折线图（按 channel 分色）
- 右上「最近 5 个失败任务」快捷入口

#### B.4.2 `/products` — **P1**
- ✅ 已有：列表 + 搜索 + 分页
- 🟡 缺：批量选择 + 批量推送 / 批量删除 / 批量加 tag
- 🟡 缺：列宽用户可调整 + 列可见性下拉
- 💡 建议：表格行 hover 显示「快速预览」缩略图（产品主图）

#### B.4.3 `/products/[id]` — **P1**
- ✅ 已有：基本信息 / 媒体 / 富文本 / 店铺映射 panel
- 🔲 缺：采购 tab（见 §A.1）
- 🟡 缺：「快照历史」tab（看该产品所有 snapshot）
- 🟡 缺：「推送历史」tab（该产品被推过的所有任务）
- 💡 顶部加 status badge（active / draft / archived）+ 一键改 status（含同步多店）

#### B.4.4 `/stores` — **P1**
- ✅ 已有：列表 / 添加 / token 状态
- 🟡 缺：「店铺产品数」统计列
- 🟡 缺：「最近推送时间」列
- 🟡 缺：批量操作（多店一键拉资产 / 多店一键禁用）
- 💡 加「健康检查」按钮：调 store/{id}/test → 验证 token 有效性

#### B.4.5 `/snapshots` — **P2**
- ✅ 已有：列表 / 详情 / 立即快照 dialog
- 🟡 缺：snapshot 详情页里「diff 上一版」对比视图（CSV diff / JSON diff）
- 🟡 缺：「回滚到此快照」按钮（需后端支持，慎重）
- 💡 加「对比两个 snapshot」入口（多选 2 个 → 对比）

#### B.4.6 `/tasks` — **P1**
- ✅ 已有：列表
- 🟡 缺：失败任务一键重试
- 🟡 缺：父子任务展开（saga 主任务 → 12 子步骤树形显示）
- 💡 加 SSE 实时刷新 status（不需要手动 F5）
- 💡 加 task duration 列（创建到完成耗时）

#### B.4.7 `/newstore` 系列 — **P1**
- ✅ 已有：列表 / 新建 / saga 详情
- 🟡 缺：saga 12 步骤可视化（进度条或步骤指示器）当前可能只是 task 列表
- 🟡 缺：「重试上一失败步」按钮显式 surface
- 💡 完成时弹「庆祝」动画 + 一键跳到新店控制台

#### B.4.8 `/approvals` 列表 — **P1**
- ✅ 已有：tab + 状态过滤
- 🟡 缺：「申请人 / 审批人」展示**用户名**而非数字 ID（join sys_user）
- 🟡 缺：type 列加 icon + 中文（已有 TYPE_LABEL 但视觉提升）
- 🟡 缺：「申请时间距今」相对时间（"3 小时前"）
- 💡 顶部加「我有 N 个待办」提醒卡片

#### B.4.9 `/approvals/[id]` 详情 — **P1**
- ✅ 已有：payload / 决策 / 重提 / 时间线
- 🟡 **payload 编辑器是 textarea + JSON.parse**（普通用户不友好）
- 建议：按 type 渲染表单
  - PRODUCT_ACCESS → 产品 picker + 申请理由 textarea
  - CROSS_COMPANY_AUTH → 用户 picker + scope_type select + scope_id picker + expires_at date-time
- 💡 时间线带 actor 头像 + 着色（APPROVE 绿 / REJECT 红 / RESUBMIT 蓝）

#### B.4.10 `/inbox` — **P2**
- ✅ 已有：tab + markRead + linkUrl
- 🟡 缺：按事件 category 过滤
- 🟡 缺：按时间分组（今天 / 昨天 / 本周 / 更早）
- 💡 删除单条 / 批量删除（archived = soft delete）
- 💡 高亮未读（已有但可加细微动画）

#### B.4.11 `/profile` 通知订阅 — **P2**
- ✅ 已有：矩阵 + 保存
- 🟡 缺：「全选 DINGTALK / 全弃 EMAIL」横向 / 纵向快捷操作
- 🟡 缺：「重置为默认」按钮
- 💡 加事件描述 tooltip（移上去显示 description 字段）
- 💡 加 preview「示例钉钉消息」按钮（不真发，仅渲染模板）

#### B.4.12 `/forgot-password` `/reset-password` — **P2**
- ✅ 已有
- 🟡 `/reset-password` 缺密码强度可视化（progress bar 红黄绿）
- 🟡 提交成功后无明确「立即去登录」按钮
- 💡 token 失效时给明确友好提示 + 「重新发请求」入口

#### B.4.13 `/recyclebin` — **P3**
- ✅ 已有：3 tab + 恢复
- 💡 加「距离物理删 N 天」醒目倒计时

#### B.4.14 `/cross-auth` — **P2**
- ✅ 已有：列表 + 新建 dialog + 撤销
- 🟡 缺：「24h 内即将到期」section 顶部置顶
- 🟡 缺：批量授权（同 scope 给多个用户）
- 💡 加「续期」按钮（直接重提 7 天，免重新走表单）

---

### B.5 设计系统补全 — **P1~P2**

**当前缺的 UI 组件**（shadcn 都有现成可加）：

| 组件 | 用途 | 优先级 |
|---|---|---|
| `Skeleton` | 替换 LoadingBlock 在列表 / 卡片场景 | P1 |
| `Breadcrumb` | 替换各页手写「← 返回」 | P2 |
| `Tabs` | 替换各页手写 button 切换（如 inbox / approvals / recyclebin）| P1 |
| `Pagination` | 列表页统一分页（当前都自己写）| P1 |
| `Select` (radix) | 替换原生 `<select>` | P2 |
| `Combobox` | 用户 picker / 产品 picker（表单内）| P1 |
| `DateRangePicker` | 审计 / 通知日志按时间过滤 | P1 |
| `Sheet` | 移动端侧边栏 + 桌面端右侧抽屉详情 | P2 |
| `DropdownMenu` | 行操作菜单（替换并排按钮）| P2 |
| `Tooltip` | 字段说明 / 状态解释 | P2 |
| `Toast` | ✅ 已有 | — |
| `Dialog` | ✅ 已有 | — |
| `Card` | 替换手写 `<div className="rounded-lg border ...">` | P3 |
| `Form` (react-hook-form 集成) | 见 §B.3 | P1 |
| `DataTable` (tanstack) | 见 §B.2 | P1 |
| `Badge` | 状态 badge 当前各页手写 STATUS_BADGE 常量 | P2 |

**建议**：一次性 setup `pnpm dlx shadcn-ui@latest add` 把以上加齐（约 0.5 PD），后续重构受益。

---

### B.6 加载 / 错误 / 空状态规范 — **P1**

**当前**：`StatusBlocks.tsx` 提供了 `LoadingBlock` / `EmptyState` / `ErrorBanner`，但使用不一致：
- 有些页用，有些直接 `加载中...` text
- EmptyState `title` vs `message` API 不一致（之前修过一次）
- ErrorBanner 在 toast 之外重复显示，体验冗余

**建议**：
- 制定统一规则：列表页 loading=Skeleton，错误=toast (silent: false)，空=EmptyState
- 全局 ErrorBoundary 已有 `ErrorBoundary.tsx`，但没在 layout 包裹
- 网络重试机制：fetch 自动重试 1 次（带 jitter）

---

### B.7 Accessibility（无障碍）— **P2**

**当前**：基本 semantics OK（`<table>` `<button>` `<form>`），但：
- 缺 `aria-label` 在 icon-only 按钮（撤回 / 删除 等）
- 模态 dialog focus trap 待 verify（用 shadcn Dialog 应自动有）
- 表单错误未与字段 `aria-describedby` 关联
- 颜色对比度（amber / rose 类 badge 在 light 主题对比度需测试）

**建议**：跑一遍 axe DevTools，按 issue 数排修复。

---

### B.8 国际化（i18n）— **P3**

**当前**：硬编码中文。

**建议（v1.1+）**：
- 引入 `next-intl`
- 提取硬编码 → `messages/zh-CN.json` / `messages/en-US.json`
- 工时：3 PD（小项目可分两次做）

---

### B.9 Dark mode — **P3**

shadcn 默认支持，需在 layout 加 toggle + Theme provider。约 0.5 PD。

---

### B.10 性能优化 — **P1**

**已识别**（W3-PERF）：
- 产品库 > 1000 行虚拟滚动（待启用）
- API 客户端无请求去重 / 缓存（建议引入 TanStack Query 替换裸 fetch）

**建议**：
- 引入 `@tanstack/react-query`：自动去重 + 缓存 + revalidate
- 用 `Suspense` + RSC 在合适页面启用流式渲染
- Bundle analyze + tree shake 检查（next build --analyze）

**工时**：3 PD（重构 fetch 链）

---

## C. 推荐落地顺序

### Sprint 1（1 周）— P0 + P1 阻塞项
1. ⭐ B.1.1 NAV 二级分组（0.5 PD）
2. ⭐ A.1 采购前端页（1.5 PD）
3. ⭐ A.5 通知日志 UI（1.5 PD）
4. ⭐ B.4.9 审批 payload 表单化（1 PD）
5. ⭐ B.4.4 stores 健康检查 + 批量（1 PD）
6. ⭐ B.5 加 Tabs / Pagination / Skeleton / Form / DataTable 组件（0.5 PD）

**总**：~6 PD

### Sprint 2（1 周）— P1 完善
1. A.2 组织管理（2 PD）
2. A.3 角色管理（2 PD + 后端 1 PD）
3. B.1.2 全局未读红点 + B.1.3 Breadcrumb（1 PD）
4. B.4.1 dashboard 4 卡片 + 折线图（1 PD）

**总**：~7 PD

### Sprint 3（1 周）— P2 体验
1. A.7 审计日志 UI + 后端 controller（2 PD）
2. A.8 备份监控面板（2 PD）
3. B.2 DataTable 重构所有列表页（3 PD）

### Sprint 4+ — P3 长尾
- A.4 数据源 / A.6 审批模板配置 / A.9 Grafana 跳转
- B.4.5 移动端 / B.7 a11y / B.8 i18n / B.9 dark mode

---

## D. 估算与建议

**总工时**：~25 PD（5 周）按上述 sprint 分批迭代。

**前端 1 人 + 后端 0.3 人配合**即可（绝大多数前端工作；A.3 / A.5 / A.7 / A.8 需后端补 controller）。

**建议交付节奏**：
- 上线前必做：B.1.1（NAV 分组）+ A.1（采购前端）+ A.10（推送冲突 UI verify）+ B.6（loading/error 规范）
- 上线后第 1 月：Sprint 1+2 完成
- 上线后第 2-3 月：Sprint 3+4

---

_最后更新：2026-05-03_
