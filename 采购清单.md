# Shopify Hub 采购清单

> 基于《系统设计文档.md v1.3》采购
> 起草日期：2026-05-01
> 责任人：[填运维负责人]
> 状态：待采购

## 0. 总览

| 类别 | 项数 | 一次性投入 (USD) | 月度成本 (USD) | 年度成本 (USD) |
|---|---|---|---|---|
| 服务器（3 台 VPS） | 3 | 0 | ~95~140 | ~1,140~1,680 |
| AWS RDS / ElastiCache | 2 | 0 | ~165 | ~1,980 |
| 对象存储 R2 | 1 | 0 | ~5~15（按用量） | ~60~180 |
| 域名 + Cloudflare | 1 | ~10~20 | 0（Free 套餐） | ~10~20 |
| 第三方账号 | 5 | 0 | 0 | 0 |
| **合计** | — | **~10~20** | **~265~320** | **~3,190~3,860** |

**约 ¥1,900~¥2,300 / 月，¥23,000~¥28,000 / 年**（按 7.2 汇率折算）。

> 节点 D 域名邮箱已部署，本清单不再列其费用。
> 邮箱方案侧的费用由邮箱供应商单独计费，不在 IT 系统预算内。

---

## 1. 服务器（VPS）

### 1.1 节点 A — 美国主应用

| 项 | 值 |
|---|---|
| 角色 | nginx + frontend + backend(主) + asset-worker + RabbitMQ + redis-local |
| 区域 | **美国西海岸**（建议 Los Angeles / San Jose / Seattle，与 AWS us-west-2 同区域） |
| 规格 | **4 vCPU / 6 GB RAM / 100 GB SSD** |
| 带宽 | ≥ 5 TB/月 出站；≥ 100 Mbps 峰值 |
| 公网 IPv4 | 1 个固定 IP |
| 操作系统 | Ubuntu 22.04 LTS（aarch64 或 x86_64 都可，统一即可） |
| 推荐厂商 + 月费 | **Vultr High Frequency** 4c8g $48 / **DigitalOcean** Premium 4c8g $48 / **Hetzner US** CPX31 4c8g $14（性价比之王但库存紧） |
| 预算 | $30~$50 / 月 |

**为什么这个区域**：
- 与 AWS RDS（us-west-2）同区域 → 私网延迟 < 2ms
- 紧邻 Shopify API（< 80ms）

**为什么这个规格**：
- backend-api JVM 启动 ~1.5G，运行稳态 ~2.5G
- frontend-admin Next.js ~512M
- asset-worker FastAPI ~256M
- RabbitMQ ~512M
- redis-local 热点缓存 ~256M
- 4G 跑稳，6G 留 buffer 应对推送高峰；4 核保证 worker 拉取主题时不阻塞主应用

### 1.2 节点 B — 香港钉钉网关 + 跳板

| 项 | 值 |
|---|---|
| 角色 | dingtalk-gateway + jump-host（运维 SSH 入口） |
| 区域 | **香港**（不能是国内，否则需 ICP 备案） |
| 规格 | **2 vCPU / 2 GB RAM / 40 GB SSD** |
| 带宽 | ≥ 1 TB/月；≥ 50 Mbps 峰值 |
| 公网 IPv4 | 1 个固定 IP |
| 操作系统 | Ubuntu 22.04 LTS |
| 推荐厂商 + 月费 | **Vultr HK** 2c2g $12 / **Linode HK** 2c4g $24 / **腾讯云轻量 HK** 2c2g ¥48 |
| 预算 | $15~$25 / 月 |

**为什么 HK 不是国内**：
- 国内服务器需 ICP 备案，钉钉 webhook 公网回调要求"无墙"
- HK 节点公网 IPv4 直接可用，钉钉 API 延迟 30~50ms

**注意事项**：
- 必须支持**改 SSH 端口** + key only 登录
- 部分国内云厂商的"轻量 HK"无独立 IP，避坑
- Vultr / Linode / DigitalOcean SG 都有 HK 节点

### 1.3 节点 C — 新加坡监控 + CI + 备用副本

| 项 | 值 |
|---|---|
| 角色 | Prometheus + Grafana + Loki + Alertmanager + GitHub Actions self-hosted runner + backend-api 备用副本 |
| 区域 | **新加坡** |
| 规格 | **4 vCPU / 8 GB RAM / 100 GB SSD** |
| 带宽 | ≥ 2 TB/月；≥ 100 Mbps 峰值 |
| 公网 IPv4 | 1 个固定 IP |
| 操作系统 | Ubuntu 22.04 LTS |
| 推荐厂商 + 月费 | **Vultr SG High Frequency** 4c8g $48 / **DigitalOcean SG** 4c8g $48 / **Linode SG** 4c8g $48 |
| 预算 | $40~$65 / 月 |

**为什么 4c8g**：
- Loki + Prometheus 内存敏感，加上 backend-api 备用副本（1~1.5G），2c4g 会比较紧
- GitHub Actions runner 跑 SSH 部署命令负载小，但 CI 触发时偶发突发，4 核 buffer 充足

### 1.4 节点 D — 域名邮箱（已部署）

| 项 | 值 |
|---|---|
| 角色 | mail.biounetwork.com（专属邮箱服务器） |
| 状态 | ✅ 已部署，本期不采购 |
| 业务侧操作 | backend-api 直连 SMTPS:465 发件，无需 SSH 接入 |
| 注意 | 须确认 SPF / DKIM / DMARC 已配置；测试发件不进垃圾箱 |

### 1.5 服务器采购通用要求

- **支持 IPv6**（可选但推荐）
- **支持快照备份**（Vultr / DigitalOcean / Linode 都自带）
- **能在 5 分钟内重建**（云厂商控制台一键重装）
- **VNC / Console 救援能力**（HK 跳板挂掉时唯一进入手段）
- **允许自定义 SSH 端口**（多数厂商默认允许）
- 不要选"突发性能"实例（CPU 信用机制会导致 GitHub Actions runner 偶发限流）

### 1.6 服务器月费汇总

| 节点 | 推荐厂商 | 月费 (USD) |
|---|---|---|
| 节点 A（US 4c6g） | Hetzner CPX31 / Vultr HF | $14~$48 |
| 节点 B（HK 2c2g） | Vultr / 腾讯云 HK | $12~$25 |
| 节点 C（SG 4c8g） | Vultr / DigitalOcean SG | $40~$48 |
| **合计** | — | **$66~$121** |

---

## 2. AWS 云数据库

### 2.1 RDS MySQL 8.0

| 项 | 值 |
|---|---|
| 区域 | **us-west-2**（Oregon），与节点 A 同区域 |
| 引擎 | MySQL 8.0.x |
| 实例 | **db.t4g.medium**（2 vCPU / 4 GB RAM） |
| 存储 | gp3 100 GB（可弹性扩容） |
| 多可用区 | ✅ Multi-AZ（生产强制） |
| 备份 | 自动备份保留 7 天 + PITR |
| 加密 | at-rest + in-transit 全开 |
| 强制 SSL | `require_secure_transport=ON` |
| 月费（按需）| ~$140 / 月（含 Multi-AZ + 100GB gp3） |

> 起步规格够用 6 个分公司（platform + tenant_01~05）；分公司 ≥ 6 时纵向升级到 m6g.large 或加新实例横向拆。

### 2.2 ElastiCache Redis 8

| 项 | 值 |
|---|---|
| 区域 | **us-west-2** |
| 引擎 | Redis 8（OSS）或 Valkey 8 |
| 节点类型 | **cache.t4g.small**（2 vCPU / 1.5 GB RAM） |
| 副本 | 0（单节点起步；流量上来后启用 Multi-AZ + replica） |
| 加密 | at-rest + in-transit 全开 |
| 月费（按需）| ~$25 / 月 |

### 2.3 安全组配置

仅放行节点 A / 节点 C 的 WireGuard 私网 IP 段：
- 入站 3306（MySQL）/ 6379（Redis）→ source = `10.x.x.0/24`（WG 网段）
- **绝对禁止** 0.0.0.0/0 或公网 IP

### 2.4 AWS 月费汇总

| 服务 | 月费 (USD) |
|---|---|
| RDS db.t4g.medium Multi-AZ + 100GB gp3 | ~$140 |
| ElastiCache cache.t4g.small | ~$25 |
| 数据传输（出站到节点 A） | ~$5（WG 内私网走 VPC peering 或 Public IP） |
| **合计** | **~$170 / 月** |

> 节点 A 在 AWS 外（Vultr/Hetzner），跨 AZ 流量按公网计费。如成本敏感，可改 Lightsail 或把节点 A 也搬到 AWS EC2（年成本 +$200~300，但流量内网零费）。

---

## 3. 对象存储 R2（Cloudflare）

| 项 | 值 |
|---|---|
| 服务 | Cloudflare R2（S3 兼容） |
| 桶 | `shopify-assets`（生产） + `shopify-assets-dev`（测试） |
| 月免费额度 | 10 GB 存储 + 1M Class A 操作 + 10M Class B 操作（上行流量免费） |
| 超出价格 | 存储 $0.015/GB/月；Class A $4.5/M；Class B $0.36/M |
| 自定义域名 | `cdn.biounetwork.com` 绑定（免费） |
| 月费（预估） | $5~$15（看媒体上传量；首半年大概率在免费额度内） |

**关键优势**：出站流量**完全免费**，比 S3 / OSS 便宜一个数量级。

---

## 4. 域名 + DNS

| 项 | 值 |
|---|---|
| 主域名 | **biounetwork.com**（须确认是否已注册；未注册的话先去 Namecheap / Cloudflare Registrar 注册） |
| 注册商 | 推荐 **Cloudflare Registrar**（成本价续费，无套路），或 Namecheap |
| 续费 | ~$10~$20 / 年（.com） |
| DNS 托管 | **Cloudflare Free**（免费） |
| 子域规划 | 见《系统设计文档》5.5 节 |

**子域 DNS 记录**（待添加）：
- `admin.biounetwork.com` → A 记录指向节点 A，**橙云开**（Cloudflare 代理）
- `{companyCode}.biounetwork.com` → 同上（通配 `*.biounetwork.com` 或按需添加）
- `api.biounetwork.com` → 同节点 A，橙云开
- `oauth.biounetwork.com` → 同节点 A，橙云开
- `webhook.biounetwork.com` → 同节点 A，橙云开
- `dingtalk.biounetwork.com` → A 记录指向节点 B，橙云开
- `monitor.biounetwork.com` → A 记录指向节点 C，橙云开 + Cloudflare Access
- `mail.biounetwork.com` → MX + A 记录指向邮箱机，**灰云**（不走代理，邮件协议要直连）
- `cdn.biounetwork.com` → CNAME 到 R2 公开桶，橙云开

---

## 5. Cloudflare 服务（Free 套餐够用）

| 服务 | Free 套餐 | 是否启用 |
|---|---|---|
| DNS | ✅ 免费 | 启用 |
| CDN | ✅ 免费 | 启用 |
| Universal SSL | ✅ 免费 | 启用 |
| WAF（Managed Rules） | ✅ 5 条免费规则 | 启用 |
| Bot Fight Mode | ✅ 免费 | 启用 |
| Rate Limit | ✅ 1 条免费规则 | 启用（用于 `/oauth/callback`） |
| Cloudflare Access（Zero Trust） | ✅ 50 用户内免费 | 启用（保护 `admin` + `monitor`） |
| R2 | 已计入 §3 | — |
| **合计月费** | — | **$0** |

如未来用户量超过 50 / 需要更多 WAF 规则，升级到 Pro $20/月即可。

---

## 6. 第三方账号

| 服务 | 用途 | 套餐 | 月费 |
|---|---|---|---|
| **GitHub Organization** | 代码托管 + Actions + GHCR | Free（私有仓库无限）/ Team $4/seat | $0~$12 |
| **Shopify Partner** | 测试 dev store + 生产 App | Free | $0 |
| **钉钉开放平台** | 企业内部应用（扫码 + 通知 + 通讯录） | Free | $0 |
| **UptimeRobot** | 外部探活（4 个监控） | Free（50 个监控） | $0 |
| **AWS 账户** | RDS + ElastiCache（已计入 §2） | — | — |
| **Cloudflare 账户** | DNS + WAF + R2 + Access（已计入 §3/§5） | — | — |

### 6.1 GitHub Organization

- 组织名建议：`biounetwork`
- 仓库名：`shopify-hub`（私有 monorepo）
- 必装：CODEOWNERS / Branch Protection（main 受保护） / Dependabot

### 6.2 Shopify Partner App

需要创建 **2 个 App**：
- **测试 App**（已就绪，对接 Partner dev store）
- **生产 App**（上线前 1 周创建）：redirect_uri 添加 `https://oauth.biounetwork.com/oauth/callback`

### 6.3 钉钉企业内部应用

每个分公司一个 corpId（多 corpId 白名单），**主公司 corpId** 是必备：
- 应用类型：企业内部应用
- 必备权限：扫码登录、通讯录读取、工作通知发送、事件订阅
- 事件订阅 URL：`https://dingtalk.biounetwork.com/dingtalk/event`（指向节点 B）

---

## 7. 采购优先级与时间线

| 顺序 | 项 | 截止日 | 阻塞 |
|---|---|---|---|
| **第 1 周** | | | |
| 1 | 域名 biounetwork.com 确认/注册 | Day 1 | 阻塞所有子域配置 |
| 2 | Cloudflare 账户 + 域名托管 | Day 1 | 阻塞 DNS / WAF / R2 |
| 3 | GitHub Organization 创建 + 仓库 | Day 1 | 阻塞 CI/CD（Wave 0 W0-08） |
| 4 | 节点 A（US 4c6g）采购 | Day 2 | 阻塞 Wave 1 部署 |
| 5 | 节点 B（HK 2c2g）采购 | Day 2 | 阻塞钉钉网关 / 跳板 |
| 6 | 节点 C（SG 4c8g）采购 | Day 2 | 阻塞监控 / CI runner |
| 7 | AWS 账户开通 + 信用卡 | Day 3 | 阻塞 RDS / ElastiCache |
| 8 | R2 桶创建 + 凭证签发 | Day 3 | 阻塞媒体上传 |
| **第 2 周** | | | |
| 9 | RDS MySQL Multi-AZ 创建 | Day 8 | 阻塞 Wave 1 数据库 |
| 10 | ElastiCache Redis 创建 | Day 8 | 阻塞 Wave 1 缓存 |
| 11 | 钉钉企业内部应用申请（主公司） | Day 8~10 | 阻塞 Wave 1 钉钉登录 |
| 12 | 节点 D 邮箱 SPF / DKIM / DMARC 验证 | Day 10 | 阻塞 Wave 1 邮件邀请 |
| 13 | UptimeRobot 4 个监控配置 | Day 12 | — |
| **上线前** | | | |
| 14 | Shopify 生产 App 创建 + redirect_uri | 上线前 1 周 | 阻塞生产 OAuth |

---

## 8. 验收清单

### 服务器
- [ ] 节点 A：SSH 可登（已改端口 + key only），`docker --version` 正常，公网带宽 > 100Mbps（speedtest）
- [ ] 节点 B：同上 + 香港 IP 验证（钉钉 API ping < 80ms）
- [ ] 节点 C：同上 + GitHub Actions runner 注册成功（label `prod-runner`）
- [ ] WireGuard mesh：A↔C 互 ping < 200ms，A/C ↔ B 公网走

### AWS
- [ ] RDS：连接测试（节点 A 私网可达，公网不可达）
- [ ] RDS：自动备份开启 + 强制 SSL 验证
- [ ] ElastiCache：连接测试 + in-transit 加密验证
- [ ] CloudWatch 基础告警（CPU/连接数/磁盘）

### Cloudflare
- [ ] biounetwork.com 域名解析全部正常（`dig admin.biounetwork.com` 命中 CF）
- [ ] WAF Bot Fight Mode 已开
- [ ] Rate Limit 规则在 `/oauth/callback` 生效（10r/min）
- [ ] Access 在 `admin.biounetwork.com` 生效（未登录跳 SSO）

### R2
- [ ] 测试上传 / 下载 / 签名 URL 生成都正常
- [ ] `cdn.biounetwork.com` 自定义域名解析正常
- [ ] Lifecycle 规则验证（删除测试对象后 90 天物理删）

### 邮箱
- [ ] backend 测试机器 telnet `mail.biounetwork.com:465` 通
- [ ] 测试发邮件到外部邮箱（Gmail/QQ）不进垃圾箱
- [ ] SPF / DKIM / DMARC `dig TXT biounetwork.com` 配置完整

### 第三方
- [ ] GitHub repo `biounetwork/shopify-hub` 创建，main 受保护
- [ ] 钉钉主公司 corpId / app_key / app_secret 已记录在密码管理器
- [ ] Shopify Partner 测试 App 已能创建 dev store
- [ ] UptimeRobot 4 个监控 5 分钟内全绿

---

## 9. 风险与备注

- **国内访问**：HK + SG + US 节点对国内访问需 VPN，未在本期专门处理国内裸网降级（v1.3 R-6）
- **AWS 信用卡**：境内信用卡走 AWS 国际版有时被拒，建议 Visa 双币
- **钉钉企业内部应用审核**：通常 1~2 个工作日，提前发起申请
- **节点 D 邮箱 SPF/DKIM/DMARC**：邮箱方案侧已部署不代表已配好这三个 DNS 记录，**须 backend 第一次发件前自行验证**
- **Cloudflare 国内**：CF 边缘对国内访问偶发慢，如出现敏感场景考虑国内 CDN 兜底
- **节点 A 的 us-west-2 出口流量**：节点 A 与 RDS 跨 VPC 走公网时按 AWS 出站计费，单月超 100GB 时建议改 EC2 同 VPC

---

## 10. 一次性预付清单（建议年付折扣）

| 项 | 月费 | 年付（多数厂商 -10%） |
|---|---|---|
| 节点 A | $48 | $518（年付节省 $58） |
| 节点 B | $20 | $216 |
| 节点 C | $48 | $518 |
| AWS RDS + ElastiCache | $165 | 按月计费（无年付） |
| R2（按用量） | $10 | 按月 |
| 域名 | $1 | $12 |
| **总计** | **$292 / 月** | **~$3,100 / 年（节省约 $360）** |

---

## 11. 待确认项（采购前需对齐）

| 编号 | 项 | 当前默认 | 决策人 |
|---|---|---|---|
| P-1 | 域名 biounetwork.com 是否已注册 | 假设已注册（v1.3 R-2） | 法务/IT |
| P-2 | AWS 账户主体（公司 / 个人） | 推荐公司主体，便于发票 + 计费 | 财务 |
| P-3 | 是否启用 RDS 预留实例（1 年付）省 30% | 默认按需，运行 3 个月稳定后切预留 | 财务 |
| P-4 | 节点 B 用云厂商还是租赁国内供应商 HK 机 | 默认 Vultr | 运维 |
| P-5 | GitHub 计费（Free vs Team $4/人） | 默认 Free（私有仓库无限） | 视团队规模 |
| P-6 | 是否启用 Cloudflare Argo（智能路由）$5/月 | 默认不启用，先看 Free 套餐效果 | 性能再评估 |
