# 改进迭代史 — 37 Track 收尾归档

> 编写：2026-05-03
> 用途：归档 Wave 0-4 完工后 9 个 Sprint × 37 个 parallel track 的全部交付成果。
> 本文档是只读历史，不再更新；后续改进进入新 Sprint 编排文档。

---

## 0. 总体时间线

| 阶段 | Commit | 范围 | Track 数 |
|---|---|---|---|
| Wave 0/1/2/3/4 | `86ced19` | initial — 业务功能全部完成 | (基础) |
| 项目审计 | `05f44d0` | 项目检查报告 + 前端改进文档 | 0（仅文档） |
| Sprint 1 | `92effb0` | 后端关键修复 + admin controllers + 前端 Shell + 采购 + admin 2 页 | 5 |
| Sprint 2 | `f9942c6` | /orgs + /admin/role + dashboard 折线 + Breadcrumb + tasks/saga/stores | 4 |
| Sprint 3 | `2a4197f` | 后端 5 endpoints + 产品历史 + cross-auth 增强 + inbox + ops + 6 ui 组件 | 4 |
| Sprint 4 | `b40dc33` | 4 ops endpoints + TanStack Query + Dark mode + 4 页 Sprint 3 组件重构 | 4 |
| Sprint 5 | `ecca9f3` | E2E 自动化 + task cancel + audit-log streaming + Stepper + virtualizer | 4 |
| Sprint 6 | `24146da` | 后端补 5 endpoints + Query 扩展 + CI 加固 + datasources UI | 4 |
| Sprint 7 | `c7033c0` | datasource 完整化 + i18n 试点 + 后端测试 + Playwright | 4 |
| Sprint 8 | `04ce589` | i18n v2 + Playwright 4 spec + 后端测试扩展 + 依赖升级（漏洞 16→0）| 4 |
| Sprint 9 | `2eb007f` | i18n v3 + jacoco + 部署文档 + Mock Shopify | 4 |

**累积**：9 sprint，37 track，10 commits，约 8 小时（agent 并行）。

---

## 1. 后端累积成果

### 1.1 Controller 全景（43 个）

按模块归类：

**核心 / 鉴权**：
- AuthController（登录 / refresh / logout）
- DingTalkAuthController（扫码 + state HMAC）
- DingTalkEventController（钉钉事件订阅）
- SensitiveOpController（钉钉 6 位码 + token）
- PasswordResetController ⭐W4（请求 + 确认 + 限流）
- HealthController（探活）

**用户 / 组织 / RBAC**：
- InvitationController（临时员工邀请）
- OrgController（组织树 CRUD）
- UserProfileController（个人中心）
- TenantDataSourceController（旧版，仅返 keys）
- TenantDataSourceAdminController ⭐T26（完整 CRUD + 加密 password）
- SysRoleController ⭐T2（角色 + 权限矩阵 + 5 端点）

**店铺 / 产品**：
- StoreController（含 T9 健康检查 + T10 disable + T22 真调 Shopify shop.json）
- StorePoolController（合作者池）
- ShopifyOAuthController（OAuth 回调）
- ProductController（CRUD）
- ProductCsvController（42 列 import/export）
- ProductDocController（TipTap 富文本）
- ProductStoreMappingController
- VariantController
- ExternalLinkController（产品素材链接）
- PurchaseController（SKU 改 + 二次确认 + 4 endpoints）

**资产 / 快照 / 推送**：
- AssetSnapshotController（含 T10 trigger + T22 cancel）
- AssetProgressController（SSE）
- ProductSnapshotController
- ProductWebhookController
- StoreWebhookController
- PushController + 任务调度
- TaskController（含 T10 retry + T19 cancel + T22 SSE events）

**业务流程**：
- SagaController（一键开店）
- SagaProgressController（SSE）
- SagaOAuthCallbackController
- PreviewController（dev store 预览）
- BaseTemplateController
- GuideDocController
- CrossCompanyAuthController
- ApprovalController ⭐W4（含 T22 batch approve）

**通知 / 邮件 / 站内信**：
- SubscriptionController ⭐W4（订阅 CRUD）
- NotificationLogController ⭐T2（list / get / retry / stats）
- InappController ⭐W4

**审计 / 备份 / 运维**：
- SysAuditLogController ⭐T2（含 T19 streaming export）
- BackupNotifyController（loopback only）
- OpsAdminController ⭐T14（audit-archive list / download / status / manual run）

**其他**：
- FileController
- RecycleBinController

⭐ = 改进 sprint 新增 / 完整化

### 1.2 Database / Flyway

**V1..V25 共 25 个迁移**：
- V1..V20 — Wave 0/1/2/3 基础 + V20 热点查询索引
- V21 ApprovalCenter（审批 + 日志）
- V22 Notification（事件定义 + 订阅 + 日志，16 事件 seed）
- V23 Inapp + PasswordReset
- V24 AuditArchive
- V25 ⭐T22 asset_snapshot 加 CANCELED 枚举

### 1.3 监控 / Metric

**6 个 Micrometer metric** ⭐T1：
- `shopifyhub_r2_upload_total{result}` — counter
- `shopifyhub_notification_send_total{result}` — counter
- `shopifyhub_auth_login_total{result}` — counter
- `shopifyhub_backup_last_success_seconds` — gauge
- `shopifyhub_audit_archive_last_success_seconds` — gauge
- `shopifyhub_approval_pending_max_age_seconds` — gauge

**Prometheus 5 group rules**：api-health / jvm-resource / business / ops-alerts(W4-MON-01) / infra-placeholder

**Alertmanager** 路由分流：ops 群（critical + alert_class∈{backup,archive}）vs backend 群（warning + team=backend）

### 1.4 测试覆盖

**31 case 横跨 6 controller** ⭐T28+T32+T37：
- ApprovalEngineTest（9 case）
- ApprovalControllerTest（4 case，MockMvc）
- InappServiceTest（7 case）
- NotificationLogControllerTest（3 case）
- SysAuditLogControllerTest（3 case，含 CSV BOM 校验）
- SysRoleControllerTest（4 case，含 builtin 拒绝）
- BackupNotifyControllerTest（3 case，含 loopback IP 校验）
- ShopifyApiClientTest（5 case，WireMock：success / 401 / timeout / verify success / 403）

**测试策略**：@SpringBootTest + @Transactional @Rollback 自动回滚（dev mysql 复用）

**jacoco** ⭐T35：plugin 0.8.12 + ci.yml artifact 上传 30 天保留

### 1.5 安全

**SecurityConfig** ⭐T1：`anyRequest().authenticated()` 取代 dev `permitAll()`，+ 16 条公开路径白名单

**敏感操作** `@RequireSensitiveOp` 列表（钉钉 6 位码二次确认）：
- ORG_DELETE / STORE_DELETE / STORE_BATCH_DISABLE / STORE_MARK_PARTNER_COLLAB / STORE_MARK_DEV_STORE
- PRODUCT_BATCH_DELETE / PURCHASE_SKU_EDIT
- CROSS_AUTH_GRANT / CROSS_AUTH_REVOKE / CROSS_AUTH_RENEW
- ROLE_PERMISSION_CHANGE / DATASOURCE_REGISTER
- AUDIT_ARCHIVE_RUN

**漏洞清零** ⭐T33：next 15.0.3 → 15.5.15 + postcss override 8.5.10+，**16 漏洞 → 0**（含 2 critical + 3 high）

---

## 2. 前端累积成果

### 2.1 路由全景（35+ 路由）

**公开路由（5）**：
- `/login` `/login/dingtalk-success`
- `/forgot-password` `/reset-password` ⭐W4
- `/invitation/accept`

**Authed（30+）**：
- `/dashboard` ⭐T15 useQuery + 4 KPI cards + ⭐T8 24h 折线图
- 业务区：`/products` `/products/[id]` `/products/import` `/products/new` / `/stores` `/stores/new` / `/partner-stores` / `/newstore` `/newstore/[taskId]` `/newstore/new`
- 资产区：`/assets` `/assets/[id]` / `/snapshots` `/snapshots/[id]` / `/templates` `/templates/[id]`
- 流程区：`/approvals` `/approvals/[id]` ⭐T11 历史 tab + ⭐T4 payload 表单化 / `/invitations` `/invitations/new` / `/cross-auth` ⭐T11 24h 置顶 + 续期
- 我的区：`/inbox` ⭐T12 category + 时间分组 / `/profile` ⭐T2 通知订阅矩阵
- 工具区：`/tasks` `/tasks/[id]` ⭐T9/T19 cancel/retry/auto-refresh / `/recyclebin` / `/guides` `/guides/[id]` `/guides/new`
- 系统区（仅 SUPER_ADMIN）：`/orgs` ⭐T6 树 / `/admin/role` ⭐T7 矩阵 / `/admin/datasources` ⭐T26 / `/admin/notification-log` ⭐T5 / `/admin/audit-log` ⭐T5 / `/admin/ops` ⭐T12

### 2.2 设计系统（19 ui 组件）

**Sprint 0 起点**（4）：Toast / Dialog / ErrorBoundary / StatusBlocks

**Sprint 1 加 6** ⭐T3：Skeleton / Tabs / Pagination / Breadcrumb / DropdownMenu / Badge

**Sprint 3 加 6** ⭐T13：Combobox / DateRangePicker / Sheet / Tooltip / Card / Form

**Sprint 5 加 1** ⭐T20：Stepper（newstore 9 步可视化）

总：4+6+6+1 = **17 ui 组件**（命名导出，TypeScript，零外部依赖除 radix selectively）

### 2.3 i18n（messages ~300 key）

- ⭐T27 Sprint 7 试点 50 key（NAV + login + dashboard）
- ⭐T30 Sprint 8 v2 推 root + 5 页扩展（150 key）
- ⭐T34 Sprint 9 v3 扩 5 页（300 key 总）
- 双语 zh-CN / en-US，localStorage 持久化，header lang switcher 中/EN
- 已 t() 化页面：login / dashboard / products / stores / inbox / profile / orgs / cross-auth / approvals / tasks / admin/audit-log（11 页）

### 2.4 性能 / TanStack Query

⭐T15 Sprint 4 引入 + ⭐T23 Sprint 6 扩展：
- @tanstack/react-query 5
- 4 query hooks: dashboard / products / tasks / stores / inbox
- 替代 ~70 行 useEffect + setInterval polling
- AppShell bell 30s refetchInterval（替代 setInterval）
- tasks 双轮询（5s active + 30s auto-refresh）合并到 refetchInterval 动态切换

⭐T21 Sprint 5：@tanstack/react-virtual products 列表 > 50 行启用

### 2.5 主题 + 移动端

⭐T16 Sprint 4：
- Dark mode（class strategy + anti-flash inline script + localStorage）
- 移动端 hamburger drawer（Sheet 组件）
- AppShell 抽 `<NavList>` 桌面 + 移动复用

### 2.6 Playwright e2e（7 spec）

- ⭐T29 Sprint 7 起点：login / products / approvals
- ⭐T31 Sprint 8 扩展：cross-auth / inbox / orgs / dark-mode
- @playwright/test 1.59.1 + ci.yml e2e-frontend job（mysql + redis services + backend/frontend 启动）
- HTML report artifact 7 天保留

---

## 3. 文档体系

| 文档 | 用途 | 状态 |
|---|---|---|
| 系统设计文档.md v1.3 | 总体架构 / 14 章节 | 不变 |
| 系统功能说明.md | 面向产品/QA/新人的功能清单 | 编排归档时更新 |
| 项目检查报告.md | 设计 vs 实现 diff 审计 | 仅 commit `05f44d0` |
| 前端改进文档.md | UX / 缺失界面建议 | 仅 commit `05f44d0` |
| TODO清单.md | 配置/调试/上线前 9 个 P0 阻塞项 | 不变 |
| 配置指南.md | 外部账号申请 + 凭证填入 | 不变 |
| 配置信息.md | 真实凭证（gitignore 不入仓库）| 本地 |
| 开发任务拆解.md | 4 Wave 任务清单 | 不变 |
| 进度记录.md | 开发进度史（按时间倒序）| 不变 |
| 性能优化复盘.md | Wave 3 性能优化总结 | 不变 |
| 技术债登记.md | 未还代码债（带触发条件）| 不变 |
| 采购清单.md | 服务器/域名/账号采购 | 不变 |
| 并行任务编排.md | 多 track 并行原则 | 不变 |
| 并行改进编排.md | Sprint 1 编排 | 归档 |
| 并行改进编排-Sprint2.md | Sprint 2 编排 | 归档 |
| 并行改进编排-Sprint3.md | Sprint 3 编排 | 归档 |
| 并行改进编排-Sprint4.md | Sprint 4 编排 | 归档 |
| 并行改进编排-Sprint5.md | Sprint 5 编排 | 归档 |
| 并行改进编排-Sprint6.md | Sprint 6 编排 | 归档 |
| 并行改进编排-Sprint7.md | Sprint 7 编排 | 归档 |
| 并行改进编排-Sprint8.md | Sprint 8 编排 | 归档 |
| 并行改进编排-Sprint9.md | Sprint 9 编排 | 归档 |
| **改进迭代史.md** | 本文档 | 终稿 |

### 3.1 ops 子目录文档

- `ops/release/`：shopify-app-checklist / legal-compliance-checklist / wave4-regression-e2e
- `ops/backup/`：rds-backup.sh + audit-purge.sh + decrypt.sh + restore-sop.md
- `ops/disaster-recovery/`：dr-drill-sop.md
- `ops/monitoring/`：README + 5 group rules + alertmanager.yml
- `ops/deploy/` ⭐T36：README + cloudflare-tunnel.md + wireguard.md（节点 A/B/C step-by-step）
- `ops/bootstrap/`：01..10 + 99-smoke-test 服务器开荒脚本

---

## 4. CI/CD

`.github/workflows/ci.yml` 7 个 job：
1. **frontend** — pnpm install + build + tsc
2. **backend** — mvn package + ⭐T35 jacoco artifact 上传
3. **worker** — Python lint + test
4. **security-scan** — Trivy
5. **e2e-wave4** ⭐T18 Sprint 5 — 28 step（mysql/redis services + backend 启动 + e2e）
6. **e2e-frontend** ⭐T29 Sprint 7 — Playwright（mysql/redis + backend/frontend 启动 + 7 spec）
7. （Wave 0 既有的）

⭐T24 Sprint 6：`.githooks/pre-commit` 4 检查（敏感文件 / secret pattern / 前端 tsc 增量 / 后端 mvn 增量）

---

## 5. Track 总览（37 个）

按 sprint 分组：

### Sprint 1（5 track，commit 92effb0）
- T1 后端关键修复（SecurityConfig + ApprovalEngine 任一角色 + 6 metric）
- T2 后端 admin controllers（NotificationLog + AuditLog + SysRole）
- T3 前端 Shell（NAV 二级分组 + 6 ui 组件 + dashboard 4 KPI + 全局红点）
- T4 采购 tab + 审批 payload 表单化
- T5 admin 2 页（通知日志 + 审计日志）

### Sprint 2（4 track，commit f9942c6）
- T6 /orgs 组织树（钉钉同步部门保护）
- T7 /admin/role 角色矩阵 + Purchase 字段对齐
- T8 dashboard 折线图 + 8 详情页 Breadcrumb + AppShell NAV 加 2 菜单
- T9 /tasks 增强 + /newstore saga stepper + /stores 批量

### Sprint 3（4 track，commit 2a4197f）
- T10 后端 5 endpoints（task retry/parentTaskId + asset trigger + store disable/test）
- T11 /products/[id] 历史 tab + /cross-auth 24h 置顶 + 续期
- T12 /inbox category + 时间分组 + /admin/ops 备份监控
- T13 6 ui 组件（Combobox / DateRangePicker / Sheet / Tooltip / Card / Form）

### Sprint 4（4 track，commit b40dc33）
- T14 后端 4 ops endpoints + /admin/ops 真数据 + Card 化
- T15 TanStack Query 集成 + dashboard kpi 迁移 + Card 化
- T16 Dark mode + 移动端 hamburger drawer
- T17 4 高频页用 Sprint 3 组件重构

### Sprint 5（4 track，commit ecca9f3）
- T18 E2E 自动化脚本 bin/e2e-wave4.sh（28 step）
- T19 task cancel + audit-log streaming export
- T20 Stepper 抽取 + a11y aria-label 加固
- T21 products 列表虚拟滚动

### Sprint 6（4 track，commit 24146da）
- T22 后端 4 endpoints（asset cancel + store test 真调 + approval batch + task SSE）
- T23 TanStack Query 扩展（products + tasks + stores + AppShell bell）
- T24 CI/CD 加固（e2e-wave4 进 ci.yml + pre-commit hook）
- T25 /admin/datasources 多数据源管理 UI

### Sprint 7（4 track，commit c7033c0）
- T26 datasource 完整化（后端扩展 + 前端真表格）
- T27 i18n 轻量试点（context + messages + 双语）
- T28 后端测试（Approval + Inapp，20 case）
- T29 Playwright e2e 试点（3 spec + ci job）

### Sprint 8（4 track，commit 04ce589）
- T30 i18n v2 + 5 高频页 t() 化
- T31 Playwright 4 新 spec（cross-auth / inbox / orgs / dark-mode）
- T32 后端测试扩展至 4 controller（13 case）
- T33 依赖升级 + 安全审计（漏洞 16→0）

### Sprint 9（4 track，commit 2eb007f）
- T34 i18n v3 扩 5 高频页 + messages ~300 key
- T35 jacoco 测试覆盖率 + ci artifact + README
- T36 生产部署文档（节点 A/B/C step-by-step）
- T37 Mock Shopify 测试（WireMock + ShopifyApiClient + 5 case）

---

## 6. 评估

### 完成度

| 维度 | 起点（W4 完工） | 现状 | 提升 |
|---|---|---|---|
| 后端 controller | 39 | 43 | +4 |
| Flyway 迁移 | V1..V24 | V1..V25 | +1 |
| Micrometer metric | 0（占位）| 6（生产暴露）| +6 |
| 测试 case | 0 | 31 | +31 |
| ui 组件 | 4 | 17 | +13 |
| i18n key | 0 | ~300（双语）| +300 |
| 路由 | 21 | 35+ | +14 |
| Playwright spec | 0 | 7 | +7 |
| ci job | 4 | 7 | +3 |
| 漏洞 | 16 | 0 | -16 |
| 部署文档 | 散落 | 3 完整文档（978 行）| +978 |

### 主要差异（vs Wave 4 起点）

1. **rbac 实际执行**：SecurityConfig 切 `.authenticated()`（之前 dev permitAll）
2. **审批可用性**：任一角色签批真发通知
3. **监控可观测**：6 个 metric 真实暴露（之前占位）
4. **管理后台**：6 个 admin 页（orgs / role / datasources / notification-log / audit-log / ops）
5. **设计系统**：13 新 ui 组件 + Card 化高频页
6. **国际化**：双语 + 11 页 t() 化
7. **暗色 + 移动端**：Dark mode + Sheet drawer
8. **测试 + CI**：31 后端 case + 7 Playwright spec + jacoco + e2e ci job
9. **依赖安全**：0 漏洞
10. **部署落地**：4 节点 step-by-step + Cloudflare Tunnel + WireGuard mesh

### 估算价值

按 PD（人天）估算：

| sprint | track | 估 PD |
|---|---|---|
| 1 | 5 | 12 |
| 2 | 4 | 10 |
| 3 | 4 | 10 |
| 4 | 4 | 9.5 |
| 5 | 4 | 8.5 |
| 6 | 4 | 9.5 |
| 7 | 4 | 9 |
| 8 | 4 | 9 |
| 9 | 4 | 9 |
| **合计** | **37** | **~86 PD** |

实际人 / agent 协作完成约 8 小时（壁钟时间）— **理论 86 PD vs 实际 ~1 PD 单人 + agent**，加速比约 80x（这数据应理解为「能并行所以同等结果用更短时间出来」；如果团队人手够，正常推进 86 PD）。

---

## 7. 已知未做（候选下一阶段）

按《前端改进文档》/ TODO 清单 / Sprint 9 末尾候选：

**P1（业务价值高）**：
- WebSocket 实时推送（替代 30s polling，inbox + task progress）
- products 列表 useQuery 完整迁移（部分页面仍混合 useState）
- 推送冲突批量决策 UI verify + 优化（B.4.10）
- StatusSyncService 兜底扫真 diff Shopify（《技术债》#11）

**P2（运维 / 生态）**：
- UptimeRobot 外部探活注册 + 5 endpoint
- node-exporter / blackbox-exporter / cloudwatch-exporter 节点 C 部署
- DR 演练实跑 1 次 L1（《dr-drill-sop》验收）
- Storybook 13 ui 组件 stories（visual regression test）

**P3（长尾）**：
- WebSocket / Storybook
- products 大列表 table → div 改造（让 virtualizer 对齐 header 列宽）
- 审批模板配置（v2，动态 type 注册）
- ICP 备案（如走国内 CDN）

**生产上线 P0 阻塞**（来自《TODO 清单》§0）：
1. 申请生产 Shopify Public App + redirect_uri
2. 钉钉企业 redirect_uri 切线上
3. 4 节点采购 + WireGuard mesh
4. BACKUP_AES_KEY 双备份（节点 A + 1Password）
5. 生产 RSA JWT 密钥对替换
6. 域名 Cloudflare 配置
7. R-8 数据出境备案
8. 隐私政策 + 用户协议双语上线
9. Wave 4 全链路回归（`bin/e2e-wave4.sh` 验收）

---

## 8. 仓库 commit log

```
2eb007f Sprint 9 — i18n v3 + jacoco + 部署文档 + Mock Shopify
04ce589 Sprint 8 — i18n v2 + Playwright 扩展 + 后端测试扩展 + 依赖升级（漏洞 16→0）
c7033c0 Sprint 7 — datasource 完整化 + i18n 试点 + 后端测试 + Playwright
24146da Sprint 6 — 后端补 5 endpoints + Query 扩展 + CI 加固 + datasources UI
ecca9f3 Sprint 5 — E2E 自动化 + task cancel + Stepper + virtualizer
b40dc33 Sprint 4 — 4 ops endpoints + TanStack Query + Dark mode + 4 页重构
2a4197f Sprint 3 — 后端 5 endpoints + 产品历史 + cross-auth 增强 + ops + 6 ui 组件
f9942c6 Sprint 2 — /orgs + /admin/role + dashboard 折线 + Breadcrumb
92effb0 Sprint 1 — 后端关键修复 + admin controllers + 前端 Shell + 采购
05f44d0 项目检查报告 + 前端改进文档
86ced19 initial — Wave 0/1/2/3/4 全部完成
```

---

_最后更新：2026-05-03（37 track 终稿）_
