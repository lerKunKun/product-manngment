# 并行任务编排

> 用途：把《开发任务拆解.md》Wave 2 / Wave 3 / Wave 4 剩余任务按"文件冲突边界 + 依赖关系"切成可并行的 track，方便单人跨 session/agent 切片推进。
> 编排日期：2026-05-02
> 仓库结构：monorepo（`backend-api/` + `asset-worker/` + `frontend-admin/` + `ops/`）

---

## 0. 编排原则

判定两个任务"可并行无冲突"的标准：

1. **物理隔离**：不改同一个 `.java` / `.ts` / `.py` 文件
2. **DB 不撞 Flyway 版本号**（`V8__*` ≠ `V9__*`）
3. **没有依赖链上的强阻塞**（B 不需要 A 的 API/类型/表）
4. **共享文件以"加而不改"方式扩展**（同 enum 加新 case、同 config 加新 bean）

满足 1+2+3 = 完全并行；满足 1+2 但有 3 = 顺序约束的并行（A 不阻塞 B 起步，但 B 完工依赖 A）；只满足 1 = 提交时序冲突，需协调。

---

## 1. 全景 Track 划分

```
Track A  asset-worker (Python)         Track B  backend 快照+webhook
Track C  backend 推送                   Track D  backend Wave3 平台模块
Track E  frontend admin 页面            Track F  ops 监控+备份+部署
Track G  backend Wave4 审批+通知+邮件
```

各 track 内部**强串行**；track 之间**完全并行**或**只在合并点弱依赖**。

---

## 2. Wave 2 阶段（~50 PD）

### Track A · asset-worker 拉取链（~9 PD）

> 完全独立：`asset-worker/` 目录单独一个 Python 项目，与 backend 不共享代码。

| 顺序 | ID | 标题 | 文件影响 |
|---|---|---|---|
| 1 | W2-CLI-01 | Shopify CLI token + 静默刷新 | `asset-worker/app/clients/shopify_cli.py` |
| 2 | W2-AST-02 | `/pull/theme` 落 R2 | `asset-worker/app/routes/pull_theme.py` + `app/services/r2.py` |
| 3 | W2-AST-03 | `/pull/policy` `/pull/menu` `/pull/collection` | `asset-worker/app/routes/pull_misc.py` |
| 4 | W2-AST-04 | `/pull/product` 完整 JSON + 媒体 | `asset-worker/app/routes/pull_product.py` |

**与其他 track 的合并点**：
- Track B 的 W2-AST-01（`asset_snapshot` 表）必须先有，worker 才有地方写索引 → 建议 W2-AST-01 由 B 先做，A 再启动；但 A 的 W2-CLI-01 可以**完全独立**先做（不写库）。
- Track A 的 W2-AST-04 是 Track C（W2-PUSH-02）的强依赖（推送复用产品 JSON 规范）。

### Track B · 快照 + Webhook + 状态同步（~14 PD）

> 全部在 `backend-api/`，主要新增独立 controller / service / scheduler。

| 顺序 | ID | 标题 | 文件影响 |
|---|---|---|---|
| 1 | W2-AST-01 | `asset_snapshot` `asset_file` 表 + R2 prefix | Flyway **V8** |
| 2 | W2-SNAP-01 | `product_snapshot` `product_price_history` `product_inventory_history` 月分区 | Flyway **V9** |
| 3 | W2-SNAP-02 | `products/update` webhook + HMAC | `webhook/ProductWebhookController.java`（新建） |
| 4 | W2-SNAP-03 | 5min 去抖 + RabbitMQ 延时 | `core/mq/RabbitMqConfig.java`（追加 queue） |
| 5 | W2-SNAP-04 | 快照生成（CSV+JSON 上 R2） | `snapshot/SnapshotService.java`（新建） |
| 6 | W2-SNAP-05 | 价格/库存历史表 only | `snapshot/PriceHistoryService.java`（新建） |
| 7 | W2-SYNC-01 | `products/update/delete` `app/uninstalled` webhook 处理 | 同 ProductWebhookController（增加 handler 方法） |
| 8 | W2-SYNC-02 | 04:00 全店全产品兜底扫 | `sync/StatusSyncScheduler.java`（新建） |
| 9 | W2-SYNC-03 | 双向同步（active/draft/archived） | `sync/StatusSyncService.java` |
| 10 | W2-AST-05 | RabbitMQ 任务 + SSE 进度 | `asset/AssetTaskController.java`（新建 SSE） |

**与其他 track 的合并点**：
- W2-AST-01（V8）必须先于 Track A 的 worker 写库逻辑提交。
- W2-SNAP-04 依赖 Track A 的 W2-AST-04（产品 JSON 拉取已经能跑）。
- W2-SYNC-04 依赖 Track C 的 W2-PUSH-05（多店铺映射 API 存在）→ 放到 Wave 2 最后。

### Track C · 产品推送（~16 PD）

> 全部在 `backend-api/` + `asset-worker/` 的 `/push` 端点。

| 顺序 | ID | 标题 | 文件影响 |
|---|---|---|---|
| 1 | W2-PUSH-01 | `store_product` `store_product_variant` `task` `push_conflict` 表 | Flyway **V10** |
| 2 | W2-PUSH-02 | worker `/push/product` | `asset-worker/app/routes/push_product.py` |
| 3 | W2-PUSH-03 | 媒体上传 Shopify Files + URL 替换 | `asset-worker/app/services/shopify_files.py` |
| 4 | W2-PUSH-04 | 冲突 PENDING + 用户决策 | `push/PushConflictService.java`（新建） |
| 5 | W2-PUSH-05 | 单/批量推送 API + 任务调度 | `push/PushController.java` + `push/PushService.java`（新建） |
| 6 | W2-PUSH-07 | 失败钉钉通知 `PRODUCT_PUSH_FAIL` | `notification/NotificationEventCode.java`（追加 enum） |
| 7 | W2-PUSH-08 | 推送审计 + 任务日志 | 已有 AuditAspect 自动捕获，无需改 |

**强依赖**：
- W2-PUSH-02 依赖 Track A 的 W2-AST-04 完工（worker 共用产品 JSON 解析模块）。
- W2-PUSH-05 依赖 W2-PUSH-01..04。

**与其他 track 的合并点**：
- 加新 NotificationEventCode 与 Track G 的 W4-NTF 同一文件，**累加不覆盖**即无冲突。
- Track E 的 W2-PUSH-06（前端推送页）依赖 W2-PUSH-05 接口落定。

### Track F · 监控基础（~6 PD，完全独立）

> 仅 `ops/` 目录 + 节点 C 部署，完全不动应用代码。

| ID | 标题 | 文件影响 |
|---|---|---|
| W2-MON-01 | Prometheus + Grafana + Loki + Promtail + Alertmanager | `ops/monitoring/docker-compose.yml`（新建） |
| W2-MON-02 | node-exporter + JVM Exporter | `ops/monitoring/exporters.yml` |
| W2-MON-03 | 业务看板 | `ops/monitoring/dashboards/*.json` |
| W2-MON-04 | 关键告警 | `ops/monitoring/alertmanager.yml` |

**与所有应用 track 零冲突**，可任意时间点并行做。

---

## 3. Wave 2 三 Track 并行模式（推荐）

```
Day 1   A: W2-CLI-01            B: W2-AST-01 (V8)            F: W2-MON-01
Day 2   A: W2-AST-02            B: W2-SNAP-01 (V9)           F: W2-MON-02
Day 3   A: W2-AST-03            B: W2-SNAP-02..03            F: W2-MON-03
Day 4   A: W2-AST-04            B: W2-SNAP-04..05            F: W2-MON-04
─── 合并点：A.AST-04 完工 ───
Day 5   C: W2-PUSH-01 (V10)     B: W2-SYNC-01..02            E: W2-AST-06 前端
Day 6   C: W2-PUSH-02..03       B: W2-SYNC-03                E: W2-SNAP-06 前端
Day 7   C: W2-PUSH-04..05       B: W2-AST-05 (SSE)
Day 8   C: W2-PUSH-07..08       E: W2-PUSH-06 前端
Day 9   B: W2-SYNC-04（依赖 PUSH-05）
```

按 6h/PD 一人节奏，2 周左右收尾 Wave 2。

---

## 4. Wave 3 阶段（~70 PD）

> 关键路径：W3-NEW（一键开店）强依赖 Wave 2 全部 → 必须 Wave 2 完工后才能起步。
> **W3-PV / W3-TPL / W3-DS / W3-GUIDE 四块完全互不依赖**，可同时开工。

### Track D1 · 一键开店 saga（~22 PD，强串行）

W3-NEW-01..12 内部强串行（状态机 + saga），但与 D2/D3/D4 并行。

| 主文件 | 任务 |
|---|---|
| `newstore/SagaStateMachine.java` | NEW-01 |
| `newstore/NewStoreOAuthService.java` | NEW-02 |
| `asset-worker/app/routes/saga_*.py` | NEW-03..07 |
| `app/(authed)/newstore/page.tsx` | NEW-10 |

### Track D2 · 合作者店铺池 + 预览（~10 PD，独立）

| 主文件 | 任务 |
|---|---|
| Flyway **V11**：`is_dev_store` `is_partner_collab` `preview_theme` | PV-01, PV-03 |
| `preview/PreviewController.java` | PV-04 协调 worker |
| `asset-worker/app/routes/preview_build.py` | PV-04 |
| `app/(authed)/partner-stores/page.tsx` | PV-02 |

### Track D3 · 平台模板库 + 指导文档（~8 PD，独立）

| 主文件 | 任务 |
|---|---|
| Flyway **V12**：`base_template` `base_template_version` `guide_doc` | TPL-01, GUIDE-01 |
| `template/TemplateController.java` | TPL-02 |
| `app/(authed)/templates/page.tsx` | TPL-03 |
| `app/(authed)/guides/page.tsx` | GUIDE-02 |

### Track D4 · 跨公司授权基础（~3 PD，独立）

| 主文件 | 任务 |
|---|---|
| 复用 `sys_data_scope` 表（已有），仅扩 service | DS-01 |
| `datascope/CrossCompanyController.java` | DS-01..03 |
| `app/(authed)/cross-auth/page.tsx` | DS-02 |

### Track D5 · 性能优化（~9 PD，独立尾部任务）

可在 Wave 3 任意空档插入，与 D1..D4 都不冲突：
- W3-PERF-01 Redis 缓存
- W3-PERF-02 慢查询索引
- W3-PERF-03 前端虚拟滚动
- W3-PERF-04 回归 / bug fix

---

## 5. Wave 3 四 Track 并行模式

```
        D1 一键开店 saga    D2 预览/店铺池    D3 模板/指导    D4 跨公司授权
PD 总数      22                 10               8                3
```

按"主线 D1 + 任意 D2/D3/D4 替换"切片：
- 每天 6h 主推 D1
- D1 卡住或想换脑子时切 D2（首选）/ D3 / D4
- D5 性能优化插在收尾期

3.5 ~ 4 周完成 Wave 3。

---

## 6. Wave 4 阶段（~30 PD，全并行）

任务拆解文档已显式标注"可并行"。每个子 track 1 ~ 1.5 周内能独立做完。

### Track G1 · 审批中心（~9 PD）

`approval/` 包独立。Flyway **V13**：`approval_flow` + `approval_log`。

### Track G2 · 钉钉通知订阅（~5 PD）

`notification/subscription/` 子包独立。Flyway **V14**：`notification_event_def` + `notification_subscription` + `notification_log`。

### Track G3 · 邮件其他模板 + 站内信（~4 PD）

仅扩 `notification/EmailService.java` + 新加 `notification/inapp/` 子包。

### Track G4 · 审计归档 + 备份（~5 PD）

`ops/scheduler/AuditArchiveScheduler.java` + 节点 A 的 `ops/backup/rds-backup.sh`。

### Track G5 · 监控完善 + 灾备演练（~4 PD）

仅改 `ops/monitoring/` 配置 + SOP 文档。

### Track G6 · 上线 + 缓冲（~3 PD）

非编码，材料整理 + Shopify Partner App 申请。

---

## 7. 共享文件冲突清单 ⚠️

下列文件多 track 都会动，**累加不覆盖**就没冲突，但同一行编辑会撞：

| 文件 | 影响 track | 冲突避免 |
|---|---|---|
| `backend-api/src/main/resources/db/migration/V*.sql` | A/B/C/D | **每 track 预占编号**（B=V8/V9，C=V10，D2=V11，D3=V12，G1=V13，G2=V14） |
| `backend-api/.../core/mq/RabbitMqConfig.java` | B (SNAP-03), C (PUSH-05), G (NTF) | 每 track 加自己的 `@Bean` 而非改既有；或拆成 `core/mq/SnapshotMq.java` 等子配置 |
| `backend-api/.../notification/NotificationEventCode.java` | C, G1, G2, D1, D2 | 仅 append 新 enum 常量，禁止重命名既有 |
| `backend-api/.../core/SecurityConfig.java` | B (webhook), C, D | append `requestMatchers` 即可 |
| `backend-api/.../Application.java` `@MapperScan` | 所有新增 mapper 包 | append 即可 |
| `frontend-admin/components/layout/AppShell.tsx` 的 `NAV` 数组 | E, D2, D3, D4, G1, G2 | append 菜单项 |
| `frontend-admin/lib/api/*.ts` 新文件 | E 各页 | 每页一个 `xxx.ts`，互不干扰 |

---

## 8. 不要并行的几种情况 🚫

- **同一 Flyway 版本号**：永远只能一个 PR 占 V_N，否则 Flyway checksum 撞库
- **改同一 controller 同一行**：例如两个 webhook handler 都想塞 `ShopifyWebhookController`，先把它拆成 `ProductWebhookController` + `StoreWebhookController` 再并行
- **改同一 entity**：往 `Product` 加字段时全队停手 → 加完合并再继续
- **改同一拦截器/AOP**：`AuditAspect` `DataScopeInterceptor` `JwtAuthFilter` 一改全改

---

## 9. 推荐执行顺序（单人切片版）

如果只有一个人轮转开发，按"长任务 + 切换换脑"模式：

```
Phase 1（Wave 2 开局，1.5 周）
  主线: Track A (asset-worker pull 链)
  插针: Track F (监控搭建 docker-compose)，每天 1h

Phase 2（Wave 2 中段，1.5 周）
  主线: Track B (快照 + webhook)
  插针: Track E (前端资产/快照页)
  并行: Track A 完工后立即起 Track C 推送

Phase 3（Wave 2 收尾，0.5 周）
  Track B 的 W2-SYNC-04 + Track E 推送页 + 联调

Phase 4（Wave 3，3.5 周）
  主线: Track D1 一键开店
  插针顺序: D3 模板 → D2 预览 → D4 跨公司
  尾部: D5 性能优化

Phase 5（Wave 4，2 周）
  全并行 G1..G6 + 上线材料
```

---

## 10. 关键路径回顾

```
W2-CLI-01 ──> W2-AST-04 ──> W2-PUSH-02 ──> W2-PUSH-05 ──> W3-NEW-05 ──> W3-NEW-10
                  │              │
                  └──> W2-SNAP-04┘
```

**最长串行链**：CLI-01 → AST-04 → PUSH-02 → PUSH-05 → NEW-05 → NEW-10 ≈ 14 PD
**理论极限**：3 人完全并行 → 整个 Wave 2 + Wave 3 缩到 ~6 周
**单人极限**：长任务为主 + 短任务插针 → ~9 周（按本文档模式）
