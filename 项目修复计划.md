# 项目修复计划

来源：`项目检查修改建议.md`。

执行规则：

- 每个修复项尽量保持小范围、可独立验证、可独立提交。
- 每完成一项，将本文件对应条目标记为 `- [x] ~~...~~`，并补充验证结果与提交信息。
- 每完成一项执行一次 `git commit` 和 `git push`。
- 不触碰无关未跟踪文件。

## 当前优先级

### P0：安全与链路正确性

- [x] ~~P0-01 修复 worker 进度回调 URL 约定错位，统一 `BACKEND_PROGRESS_URL` 语义并补测试。~~
- [ ] P0-02 收窄 `/internal/**` 放行范围，内部 token 缺失时避免静默放行。
- [ ] P0-03 为 asset-worker 对外接口增加 token 鉴权，并让后端调用 worker 时统一携带 token。
- [ ] P0-04 建立最小 RBAC 门禁，先保护 admin、tenant、template、store 等高风险写接口。
- [ ] P0-05 核心产品、店铺查询改为服务端强制数据范围，避免依赖前端传入租户或公司参数。

### P1：运行时稳定性

- [ ] P1-01 修正 `push_conflict.conflict_type` 与代码写入值不一致问题。
- [ ] P1-02 修正审批授权写入 `sys_data_scope` 时必填字段不一致问题。
- [ ] P1-03 前端 multipart、download 请求统一复用 API client 的 Authorization 与 refresh 逻辑。
- [ ] P1-04 上传搬运任务增加条件抢占，避免多实例重复处理。
- [ ] P1-05 大文件上传到 R2 改为流式处理，避免 `readAllBytes` 带来的内存风险。

### P2：工程质量与开发体验

- [ ] P2-01 将 CI 中 lint、worker pytest、安全扫描逐步改为阻断。
- [ ] P2-02 统一本地 dev 端口默认值，避免绕过脚本时连错 MySQL/Redis。
- [ ] P2-03 明确 JWT 密钥缺失时的 dev/prod 行为。
- [ ] P2-04 对齐 Session 滑动续期注释与实现。
- [ ] P2-05 前端 Node、pnpm、Next 与 eslint-config-next 版本收敛。
- [ ] P2-06 将前端 CSP 放到 Next.js 或 nginx 层，避免误以为后端 API CSP 已保护页面。

## 完成记录

- P0-01：worker 进度回调 endpoint 改为集中生成，兼容 origin、`/api`、`/api/internal/asset` 和完整 progress URL；`docker-compose.dev.yml` 默认改为 origin 配置。验证：`python -m compileall app tests` 通过；`python -m pytest tests/test_progress.py -q` 未执行成功，原因是当前 Python 环境缺少 `pytest`。
