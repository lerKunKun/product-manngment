# 配置指南

> 用途：开发上线前必须配置的外部系统 + 凭证清单
> 完成情况记录：每项配好后在表格中打 ✅
> 优先级：🔴 阻塞本地开发 / 🟡 阻塞 Wave 1 测试 / 🟢 阻塞生产上线

## 0. 整体顺序

| # | 项 | 优先级 | 状态 | 阻塞什么 |
|---|---|---|---|---|
| 1 | JWT RSA 密钥对 | 🟡 | ✅ 2026-05-02 已生成于 `.secrets/` 并填 .env | — |
| 2 | 加密主密钥 (AES-GCM) | 🟡 | ✅ 2026-05-02 已生成（应用密钥/备份密钥/邀请 token secret 三件） | — |
| 3 | 钉钉企业内部应用 | 🟡 | ✅ 2026-05-02 复用旧项目 corpId+app（ding5a2a... / ding2o5e...） | redirect URI 上线前需改 |
| 4 | Cloudflare 域名托管 | 🟢 | 🔲 待办（W1-INF-06） | 生产 DNS / WAF / R2 域名 |
| 5 | Cloudflare R2 凭证 | 🟢 | ✅ 2026-05-02 复用旧项目（桶 shopify-themes + product-media） | — |
| 6 | Shopify Partner App | 🟡 | ✅ 2026-05-02 复用旧 App（fb439dc3... / shpss_8d435d22...） | webhook secret + redirect URI 仍需补 |
| 7 | 邮箱 SMTP（mail.biounetwork.com） | 🟡 | 🔲 待邮箱账号到位 | 邀请/密码重置邮件目前降级到日志 |
| 8 | AWS RDS / ElastiCache | 🟢 | 🔲 生产部署时再做 | 本地 docker 已替代 |
| 9 | GitHub Org + Token | 🟢 | 🔲 待办 | CI/CD + GHCR |
| 10 | 钉钉群机器人（告警） | 🟢 | 🔲 Wave 4 时再做 | Alertmanager 推送 |

> 🔴 项已在 `.env.example` 给了本地默认值（MySQL/Redis/RabbitMQ/MinIO 直连 docker），本地起服务无需任何账号申请。

---

## 1. JWT RSA 密钥对 🟡

### 生成

```bash
# 在仓库根执行；生成 dev 用的密钥对（生产用更强的 keylength + KMS）
mkdir -p .secrets
openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out .secrets/jwt-private.pem
openssl rsa -pubout -in .secrets/jwt-private.pem -out .secrets/jwt-public.pem
chmod 600 .secrets/jwt-*.pem
```

### 填入 .env

读取文件内容，**保留 PEM 头尾的 `-----BEGIN/END`**，整个内容（含换行）作为环境变量值；建议在 .env 中用单引号包裹整个值：

```ini
JWT_PRIVATE_KEY='-----BEGIN PRIVATE KEY-----
MIIEv...（多行）
-----END PRIVATE KEY-----'

JWT_PUBLIC_KEY='-----BEGIN PUBLIC KEY-----
MIIBI...
-----END PUBLIC KEY-----'
```

> **生产环境**：私钥不进 .env 明文文件；用 AWS Secrets Manager / Vault / 启动时 sealed secret 注入。

### 完成 □

---

## 2. 加密主密钥 (AES-GCM) 🟡

用于加密 Shopify Token、钉钉 secret、备份文件等。32 字节随机 base64。

### 生成

```bash
# 应用密钥（凭证加密）
openssl rand -base64 32
# 备份密钥（数据库 dump 加密）
openssl rand -base64 32
```

### 填入 .env

```ini
ENCRYPT_KEY_AES_GCM=<生成的 32 字节 base64>
BACKUP_ENCRYPT_KEY=<生成的 32 字节 base64>
INVITATION_TOKEN_SECRET=<生成的 32 字节 base64>     # 邀请 token 签名 secret
```

> **生产环境**：切到 KMS（系统设计文档 R-7）。

### 完成 □

---

## 3. 钉钉企业内部应用 🟡

最关键的外部系统。开通流程：

### 3.1 主公司 corpId 申请

1. 访问 https://open-dev.dingtalk.com/
2. 登录企业管理员钉钉账号
3. 左侧 → "应用开发" → "企业内部应用" → "创建应用"
4. 应用类型：**H5 微应用 / 小程序**（H5 为主）
5. 应用图标 / 名称：`Biou Shopify Hub`
6. 应用首页：`https://admin.biounetwork.com`（域名上线前先填占位 `https://example.com`）
7. 创建后记下：
   - `CorpId`（"应用首页地址" 上方）
   - `AgentId`（"应用信息"）
   - `AppKey` / `AppSecret`（"凭证与基础信息"）

### 3.2 必备权限申请

应用详情 → "权限管理" → 申请以下权限：

| 权限点 | 用途 |
|---|---|
| 通讯录个人信息读权限 | 自助注册时拉用户详情 |
| 通讯录部门信息读权限 | 组织树同步 |
| 成员信息读权限 | 同上 |
| 身份验证（含工作台扫码登录） | 扫码登录 |
| 企业事件订阅 | 实时同步组织变更 |
| 工作通知消息 | 系统通知 + 二次确认验证码 |

### 3.3 事件订阅回调（须先有公网域名）

- 应用详情 → "事件订阅"
- 回调 URL：`https://dingtalk.biounetwork.com/api/auth/dingtalk/event`（节点 B HK）
- 订阅事件：`user_add_org` / `user_modify_org` / `user_leave_org` / `org_dept_*`
- 记下 **Token** 和 **EncodingAESKey**

### 3.4 填入 .env

```ini
DINGTALK_MAIN_CORP_ID=ding_xxxxxxxxxxxxxxxx
DINGTALK_MAIN_AGENT_ID=1234567890
DINGTALK_MAIN_APP_KEY=dingxxxxxxxx
DINGTALK_MAIN_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DINGTALK_EVENT_TOKEN=xxxxxxxxxxxxxxxx              # 事件订阅 Token
DINGTALK_EVENT_AES_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx  # EncodingAESKey
```

### 3.5 测试

- 后端起来后访问 `http://localhost:8080/api/auth/dingtalk/qrcode?tenant=test`
- 未配置：返回 `DEPENDENCY_DOWN` + 提示
- 已配置：返回钉钉 OAuth URL（Wave 1 实现完整逻辑）

### 完成 □

> 分公司 corpId（每个分公司单独的钉钉企业）后续在 `dingtalk_corp` 表逐个录入，主公司 corpId 也最终落库到该表。本环境变量仅用于启动时引导。

---

## 4. Cloudflare 域名托管 🟢

### 4.1 注册 / 验证 biounetwork.com

- 如未注册：去 https://www.cloudflare.com/products/registrar/ 直接 Cloudflare Registrar 注册（成本价）
- 如已注册：在原注册商处把 nameserver 改为 Cloudflare 提供的两个 NS

### 4.2 DNS 记录

按《采购清单.md》第 4 节配置。关键记录：
- `admin / api / oauth / webhook / monitor / dingtalk` → 节点对应 IP，**橙云开**（CF 代理）
- `mail` → 邮箱机 IP，**灰云**（不代理）
- `cdn` → R2 CNAME

### 4.3 API Token（脚本/CI 用）

- "我的个人资料" → "API Tokens" → "创建 Token"
- 模板：`Edit zone DNS` + 添加权限 `Zone.WAF:Edit`、`Zone.Page Rules:Edit`
- Zone Resources：限定 `biounetwork.com`

### 4.4 填入 .env

```ini
ROOT_DOMAIN=biounetwork.com
CF_API_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
CF_ZONE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx        # 域名概要页右侧
CF_ACCESS_AUD=                                      # 4.5 配完后填
```

### 4.5 Cloudflare Access（保护 admin / monitor）

- "Zero Trust" → "Access" → "Applications" → "Add application"
- Type: Self-hosted
- Application domain: `admin.biounetwork.com`
- Identity provider: 暂用 One-time PIN（邮箱验证码）；后续可接钉钉 SSO（OIDC）
- 创建后页面顶部显示 **AUD tag** → 填 `CF_ACCESS_AUD`

### 完成 □

---

## 5. Cloudflare R2 🟢

### 5.1 创建桶

- Cloudflare Dashboard → R2 → "Create bucket"
- 名称：`shopify-assets`（生产）+ `shopify-assets-dev`（测试）

### 5.2 凭证

- R2 → "Manage R2 API Tokens" → "Create API token"
- 权限：Object Read & Write
- 限定到上述两个桶

### 5.3 自定义域名（可选但推荐）

- 桶详情 → "Settings" → "Public Access" → "Connect Domain"
- 域名：`cdn.biounetwork.com`
- DNS 自动添加 CNAME

### 5.4 填入 .env

```ini
R2_ACCOUNT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
R2_ACCESS_KEY_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
R2_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
R2_BUCKET=shopify-assets
R2_ENDPOINT=https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com
R2_PUBLIC_BASE=https://cdn.biounetwork.com
```

### 5.5 本地开发

本地用 MinIO 替代（已在 docker-compose.dev.yml 中），凭证用 `minioadmin/minioadmin`。`.env` 默认值即可，无需改动。

### 完成 □

---

## 6. Shopify Partner App 🟡

### 6.1 测试 App（已就绪）

参照 v1.3 R-4 即可。redirect_uri 之一须包含 `http://localhost:8080/api/oauth/callback`（本地）。

### 6.2 生产 App（上线前 1 周创建）

- https://partners.shopify.com → Apps → Create app
- App name: `Biou Shopify Hub`
- App URL: `https://admin.biounetwork.com`
- redirect URLs:
  - `https://oauth.biounetwork.com/api/oauth/callback`
  - `http://localhost:8080/api/oauth/callback`（联调用）

### 6.3 必要 scopes

```
read_themes,write_themes,
read_files,write_files,
read_products,write_products,
read_collections,write_collections,
read_content,write_content,
read_locales,write_locales
```

### 6.4 Webhook secret

App Settings → Webhooks → Webhook signature secret，记下后填入 .env：

### 6.5 填入 .env

```ini
SHOPIFY_API_VERSION=2024-10
SHOPIFY_APP_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SHOPIFY_APP_SECRET=shpss_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SHOPIFY_WEBHOOK_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SHOPIFY_OAUTH_REDIRECT_URI=http://localhost:8080/api/oauth/callback   # 本地
# 生产改为 https://oauth.biounetwork.com/api/oauth/callback
```

### 完成 □（测试） / □（生产）

---

## 7. 邮箱 SMTP（mail.biounetwork.com）🟡

### 7.1 业务发件账号

向邮箱供应商申请专用账号 `noreply@biounetwork.com`，用于系统发件。

### 7.2 SPF / DKIM / DMARC（关键，影响 deliverability）

在 Cloudflare DNS 配置以下 TXT 记录（具体值由邮箱供应商提供）：

```
biounetwork.com.            TXT  "v=spf1 include:<邮箱供应商-spf-host> ~all"
default._domainkey.biounetwork.com.   TXT  "v=DKIM1; k=rsa; p=<邮箱供应商提供的公钥>"
_dmarc.biounetwork.com.    TXT  "v=DMARC1; p=quarantine; rua=mailto:dmarc@biounetwork.com"
```

验证：
```bash
dig TXT biounetwork.com +short
dig TXT default._domainkey.biounetwork.com +short
dig TXT _dmarc.biounetwork.com +short
```

### 7.3 填入 .env

```ini
SMTP_HOST=mail.biounetwork.com
SMTP_PORT=465
SMTP_USERNAME=noreply@biounetwork.com
SMTP_PASSWORD=<邮箱密码或专用 app password>
SMTP_TLS=true
SMTP_FROM=Biou x Shopify Control Center <noreply@biounetwork.com>
```

### 7.4 本地开发

`SMTP_USERNAME` 留空时，`EmailService` 会跳过实际发送、把邮件正文打到日志，方便联调（不污染真实邮箱）。本地阶段可以不配。

### 7.5 测试发件

```bash
# 后端起来后调用 invitation 接口（邮箱填自己）
curl -X POST http://localhost:8080/api/invitation/create \
  -H "Content-Type: application/json" -H "X-Operator-Id: 1" \
  -d '{
    "email": "你的邮箱@example.com",
    "targetCompanyId": 1,
    "roleCodes": ["TEMP_STAFF", "DESIGNER"],
    "dataScopes": [{"scopeType":"PRODUCT","scopeId":1,"access":"READ"}],
    "accountExpiresAt": "2026-08-01T00:00:00"
  }'
```

未配 SMTP：日志里看到完整邀请邮件 HTML（含临时密码）；
已配 SMTP：邮箱里收到。

### 完成 □

---

## 8. AWS RDS / ElastiCache 🟢

**本地开发不需要**（docker-compose.dev.yml 已替代）。

生产部署时按《采购清单.md》§2 创建，填以下 .env：

```ini
MYSQL_HOST=<rds-endpoint>.us-west-2.rds.amazonaws.com
MYSQL_PORT=3306
MYSQL_USER=admin
MYSQL_PASSWORD=<强随机密码，AWS Secrets Manager 注入更安全>
MYSQL_TLS_REQUIRED=true

REDIS_HOST=<elasticache-endpoint>.cache.amazonaws.com
REDIS_PORT=6379
REDIS_PASSWORD=<auth-token>
REDIS_TLS=true
```

### 完成 □（本地无需） / □（生产）

---

## 9. GitHub Org + Token 🟢

### 9.1 创建组织

- https://github.com/organizations/new
- 组织名：`biounetwork`
- 计费：Free（私有仓库无限）

### 9.2 创建仓库

```bash
cd /Users/zhangxueqian/development/product-manngment
git init -b main
git add .
git commit -m "feat: wave 0 scaffold + wave 1 auth/invitation skeleton"

gh repo create biounetwork/shopify-hub --private --source=. --push
```

### 9.3 GHCR Token（CI 推镜像用）

- Settings → Developer settings → Personal access tokens → "Tokens (classic)"
- Scopes: `write:packages`, `read:packages`, `delete:packages`
- 在仓库 → Settings → Secrets → 添加 `GHCR_TOKEN`

### 9.4 填入 .env（本地几乎用不到）

```ini
GHCR_USER=<github 用户名>
GHCR_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 完成 □

---

## 10. 钉钉群机器人（告警）🟢

Alertmanager 推送 + 系统通知备用通道。

### 10.1 创建机器人

钉钉群（`Biou Shopify Hub Ops`）→ 群设置 → 智能群助手 → 添加机器人 → 自定义
- 安全设置：勾选 "加签"，记下 Secret
- Webhook URL 记下

### 10.2 填入 .env

```ini
ALERTMANAGER_DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=xxx
ALERTMANAGER_DINGTALK_SECRET=SEC<加签 secret>
ALERTMANAGER_EMAIL_TO=ops@biounetwork.com
```

### 完成 □

---

## 配置完成后

回到对话窗，告诉我"配置 X 已完成"或"全部配置完成"，我会接着推 Wave 1 后续任务（钉钉同步消费者 / 多数据源动态加载 / 数据范围拦截器 / 跨公司授权 / 邮件邀请前端落地页 等）。

如果某项配置卡住、不确定值或拿不到，**直接来问，不要硬试**。
