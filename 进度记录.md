# 开发进度记录

> 用途：每次推进按时间倒序记录，含完成的任务 ID、产出文件、卡点、下一步
> 与《开发任务拆解.md》配套使用

## 2026-05-02（深夜）· 🎉 Wave 3 完工 100% — 收尾 PERF-03/04 + 清债 #12

**最终战报**：D1 W3-PERF-03 虚拟滚动 + D2 W3-PERF-04 整体回归。**2/2 全 ✅，Wave 3 40/40 完成**。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **D1** | W3-PERF-03 虚拟滚动 | `pnpm add @tanstack/react-virtual@3.13.24`；重构 `app/(authed)/products/page.tsx`：弃用 table 改 grid div（共享 `GRID_COLS` 列宽常量）+ sticky header + 虚拟化 body（`h-[60vh] overflow-auto` + `useVirtualizer({estimateSize:44, overscan:5})`）；保留 checkbox 全选 + 批量工具栏 + 状态 badge + Link to detail + truncate；loading/empty 分支换 body 不换 header | typecheck ✓；27 routes build；`/products` 10.3KB（First Load 126KB）；dev /products 200 |
| **D2** | W3-PERF-04 + 清债 #12 | **#12**：`SagaService.start` 加 `mergeInitialPayload(ctx, p)` + `numberToLong` helper，把 initialPayload 中 templateId/replaceRules/productIds(→productIdsToPush) merge 到 SagaContext 写 saga_data_json；`bin/e2e-saga.sh` 删除 PRODUCTS 步 SQL workaround（保留为注释引用）；`技术债登记.md` 把 #12 移到 §3 已闭 | **4 套测试全绿**：asset-worker pytest **70/70** + backend mvn **clean** + smoke-test.sh **6/6** + e2e-saga.sh **18/18 PASS**（PRODUCTS 步无 SQL workaround 自然通过 → #12 验证）；frontend 双 pass build 一致 31 routes |

### 🎉 Wave 3 最终完成度

| 模块 | 完成度 |
|---|---|
| **3.1 一键开店 saga** | **12/12 ✅** |
| **3.2 合作者池 + 预览** | **7/7 ✅** |
| **3.3 采购管理** | **5/5 ✅**（W1 提前完）|
| **3.4 产品媒体 + 需求文档** | **6/6 ✅**（W1 提前完）|
| **3.5 平台模板库 + 指导文档** | **5/5 ✅** |
| **3.6 跨公司授权** | **3/3 ✅** |
| **3.7 性能优化** | **4/4 ✅** |
| **合计** | **40/40 ✅** |

### Wave 3 关键资产盘点

**端到端 saga 跑通**：INIT → AUTH_DONE → MEDIA → COLLECTIONS → PRODUCTS → GUIDE → THEME → PUBLISH → SUCCESS（9 状态机），每步可单独 trigger / 整体连跑（`bin/e2e-saga.sh` 18/18 PASS），任意点 FAIL → retry attempt+1 + 钉钉/邮件通知 + ctx 累积输出保留。

**预览池**：dev/合作者店铺 → preview_theme 分配（按当前活跃数挑最闲，单店上限 3）→ worker 创 unpublished theme + push 产品 + 返 preview URL → 每 30 分钟 cron 清理过期/超容量。

**模板库**：CRUD + R2 zip 多版本 + sha256 + 默认替换规则；前端 /templates 列表 + 详情 + 版本上传 dialog（含正则替换规则编辑）。

**跨公司授权**：sys_data_scope 加 cross_company / expires_at 时效（5min~180d）+ 24h 前提醒（idempotent via expiring_notified_at）+ 0:05 硬过期；前端 /cross-auth + 授权 dialog（含 sensitive op 真流程）+ revoke。

**性能基础设施**：Redis 缓存层（StoreService.list / ProductService.detail，hit/miss Micrometer counter，Redis 不可用 fallback）+ V20 6 个热点索引 + 前端虚拟滚动（10k+ 行不卡）+ 性能优化复盘.md。

### 累计技术债（Wave 3 末）

| 状态 | # | 描述 |
|---|---|---|
| ✅ 已闭 | 1, 2, 3, 4, 6, 8, 9, 12 | **8 个** |
| 🟡 延后 | 5 | Worker progress emit 同步 httpx — 单 pull ≥100 文件再优化 |
| 🟡 延后 | 7 | webhook 没 currency → 历史表硬编 USD — W3-PUR 多币种时改 |
| 🟡 延后 | 10 | 推送批量同步阻塞 HTTP — 单批 >20 产品时升 @Async 或 RabbitMQ |
| 🟡 延后 | 11 | StatusSyncService 是骨架 — 真 Shopify diff 等数据漂移变多再做 |

每条延后债都有可观测触发条件，详见 `技术债登记.md`。

### 总进度

| Wave | 状态 | PD 完成度 |
|---|---|---|
| **Wave 0 脚手架** | ✅ 9/9 | ~7 PD |
| **Wave 1 MVP** | ✅ 业务面 100%（W1-INF 等服务器到位）| ~74 PD |
| **Wave 2 资产+推送** | ✅ 29/29 | ~50 PD |
| **Wave 3 开店+采购+模板** | ✅ **40/40** | ~50 PD 净（PUR/MED 在 W1 提前） |
| Wave 4 审批+通知+运维 | 🔲 0/30 |  |

**已完成 ~200 PD（剩 Wave 4 + W1-INF 部署）= 总进度 ~85%**。

### 下一步：Wave 4

按《并行任务编排.md》Phase 5：4.1 审批中心（~9 PD）+ 4.2 钉钉通知订阅（~5 PD）+ 4.3 邮件其他模板 + 站内信（~4 PD）+ 4.4 审计归档 + 备份（~5 PD）+ 4.5 监控完善 + 灾备演练（~4 PD）+ 4.6 上线 + 缓冲（~3 PD）。

四 track 完全并行（任务拆解文档已显式标注），约 2 周收尾。

---

## 2026-05-02（深夜）· Wave 3 Day 5 — 🎉 saga 整段闭环 + e2e 通过

D1 主题 zip + 替换规则 + D2 指导跳转 + 完成通知 + D3 e2e 测试脚本 + D4 慢查询索引复盘。**4/4 全 ✅**。**3.1 一键开店 saga 12/12 闭环 + bin/e2e-saga.sh 18/18 checks PASS**。

> **📝 W3-PERF-04 整体回归（同日补）**：跑全套测试 4/4 绿 —— **asset-worker 70/70 pytest** ✓ / **backend mvn compile** ✓ BUILD SUCCESS / **bin/smoke-test.sh** 6/6 ✓ / **bin/e2e-saga.sh** 18/18 ✓（PRODUCTS 步在移除 SQL workaround 后仍 PASS，证明 #12 fix 生效）；**frontend pnpm build** 31 routes 双跑 baseline 一致（D1 的 react-virtual commit 暂未到本进程，2 次 pass /products 都是 10.3 kB）。**清技术债 #12 已闭环**（移到《技术债登记.md》§3）：`SagaService.start()` 现在调 `mergeInitialPayload(ctx, initialPayload)` 自动把 `templateId / replaceRules / productIdsToPush` 拷进 ctx；`bin/e2e-saga.sh` 第 315-319 行的 `JSON_SET productIdsToPush / storeId / templateId / shopDomain` 注释化（保留为排查用）。无回归。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **D1** | W3-NEW-06 + W3-NEW-07 主题 zip + 替换规则 | Worker 加 `theme_create_from_src` (REST POST /themes.json 让 Shopify 拉 src URL) + `theme_publish` (PUT role=main)；`ThemePushService`（R2 拉 zip + `_apply_rules`：unzip + 文本扩展白名单（liquid/json/css/js/txt/md/yaml/yml/html/svg）+ `re:` 前缀正则 vs 字面量 + 二进制保留 + repack + sha8 后缀新 R2 key + R2_PUBLIC_BASE 拼 src URL + 可选 publish）；6 新 pytest；Backend `SagaThemeStepService.run`（`executeStep(GUIDE,...)` + 合并 base_template_version.default_replace_rules + ctx.replaceRules + HTTP/1.1 worker call + 写 ctx.stepOutputs.theme） | **70/70 pytest**（64→70）；端到端 saga GUIDE→THEME 转换 + ctx.stepOutputs.theme = `{shopify_theme_id, role:"unpublished", rules_applied_count:2, modified_zip_r2_key:".../theme.zip.modified-dryrun.zip"}` ✓ |
| **D2** | W3-NEW-08 + W3-NEW-11 GUIDE 步 + 完成通知 | `SagaGuideStepService.run`：`executeStep(PRODUCTS,...)` + 收集 guide_doc 中 `show_in_saga_step='GUIDE' AND status='PUBLISHED'` 行 + `relatedTemplateId` 过滤（NULL 通用，匹配 ctx.templateId 时优先）+ `frontendUrl=/guides/{id}` 注入；`SagaController` 加 `POST /saga/{id}/step/run-guide`；`SagaService` 加 `@Lazy NotificationDispatcher` + `notifySagaSuccess` (在 advance(SUCCESS)) + `notifySagaFail` (在 fail()) 调 notifyUser；NotificationEventCode 早就有 NEW_STORE_SUCCESS/FAIL 静态常量 | smoke A：seed 2 guide → run-guide → step=GUIDE + ctx.stepOutputs.guides=2 entries with frontendUrl ✓；smoke B：force PUBLISH → /advance → SUCCESS + log "[saga] NEW_STORE_SUCCESS notify dispatched" + "[notify] event=NEW_STORE_SUCCESS user=admin email=... dtSent=false" ✓；smoke C：触发 run-media 失败 → FAIL notify dispatched ✓ |
| **D3** | W3-NEW-12 e2e 测试脚本 | `bin/e2e-saga.sh` 380 行：色彩输出 + PASS/FAIL/SKIP 三态 + 预检 4 docker 服务 + 启动 worker dry-run + 启 backend + seed (store + product + variant + base_template + version + guide_doc) + 走 saga 9 步（INIT→AUTH_DONE→MEDIA→COLLECTIONS→PRODUCTS→GUIDE→THEME→PUBLISH→SUCCESS）+ endpoint_exists 探测（404 → SKIP 不 FAIL）+ trap finish 幂等 cleanup（自然键删除 + pre-run 清旧）+ summary 输出 | **18/18 PASS**（连跑 2 次结果一致）；ctx.stepOutputs 各 step 累积；唯一 SQL workaround 在 PRODUCTS 步 `JSON_SET productIdsToPush`（saga.start 接受 initialPayload 但 SagaService 不自动合到 ctx）|
| **D4** | W3-PERF-02 慢查询索引 | grep 仓库 LambdaQueryWrapper 模式 + 各 mapper/scheduler 的 where 条件 → V20 加 6 个真有需求的索引：`product_price_history (store_id, product_external_id, changed_at)` / `product_inventory_history` 同 / `task (type, id)` / `product_snapshot (store_id, status, created_at)` / `store_product (tenant_id, status, updated_at)` / `user_invitation (status, invited_at)`；新建 `性能优化复盘.md` 含方法论 + 各表前后对比 + EXPLAIN 实例 | V20 ✓；EXPLAIN 实证：`user_invitation WHERE status='PENDING' ORDER BY invited_at DESC` → key=新索引 + **Backward index scan, no filesort**；`store_product WHERE tenant_id=? AND status=? ORDER BY updated_at DESC` 同；4 个原计划 SKIPPED（audit 表当前无查询路径 / triggered_by 未被查 / completed_at 无视图 / preview status+expires V18 已覆盖）|

**关键发现 / 决策**：
- D1：Shopify themeCreate 用 REST `src` URL 比 GraphQL 简单；URL 必须公开可达 —— dev MinIO + R2_PUBLIC_BASE 拼出来即可；生产用 R2 的 R2_PUBLIC_BASE 自定义域名（已在 .env）。
- D1：theme_publish 失败不致命（log warn + 留 unpublished 给用户审核）；theme_create 失败致命（502）。
- D1：默认 `SHOPIFY_THEME_PUBLISH_AFTER_INSTALL=false` 安全起见 —— saga 完成后主题留 unpublished，让用户在 Shopify 后台手动审核 + 发布。
- D2：NotificationEventCode 早就有 NEW_STORE_SUCCESS/FAIL 静态常量（不知哪一波 Wave 加的，可能是 W1-INV 时一起占位）。
- D2：admin 用户没 dingtalk_userid → fallback "降级日志"；dev SMTP 不可达 → 邮件投递失败但 saga 不卡 —— **失败完全容忍**。
- D2：FAIL 通知通过真实失败路径（worker /push/files 错误）触发 + 验证，不靠 SQL 强 update。
- D3：e2e 启动后实测 **D1 + D2 endpoints 都已 ready** —— 18/18 PASS（不是部分 SKIP）；guide + theme + publish + success 4 步全跑通。
- D3：唯一 SQL workaround 是 PRODUCTS 步 ctx 注入 productIdsToPush —— SagaService.start 接 initialPayload 但不自动 merge 进 ctx；**记小技术债 #12**（自动 merge initialPayload 到 ctx）。
- D4：审计表 `sys_audit_log` 当前**无查询路径**（仅 AOP 写入，没控制台读取页）—— 加索引为时过早；先记 follow-up 等 W4-AUD 加管理界面再加。

### Wave 3 完成度

| 模块 | 完成度 |
|---|---|
| **3.1 一键开店 saga | 12/12 ✅** |
| 3.2 合作者池 + 预览 | 7/7 ✅ |
| 3.3 采购管理 | 5/5 ✅（W1）|
| 3.4 产品媒体 + 需求文档 | 6/6 ✅（W1）|
| 3.5 平台模板库 + 指导文档 | 5/5 ✅ |
| 3.6 跨公司授权 | 3/3 ✅ |
| 3.7 性能优化 | 2/4（PERF-01, PERF-02 ✅；PERF-03 + PERF-04 🔲）|

**已完成 38/40 = 95%**。**3.1 + 3.2 + 3.3 + 3.4 + 3.5 + 3.6 六段全闭环**。剩 W3-PERF-03（前端虚拟滚动）+ W3-PERF-04（回归 + bug fix 缓冲）。

### 新技术债（小）

- #12 SagaService.start 不自动 merge initialPayload 到 SagaContext —— PRODUCTS 步要手动 SQL UPDATE 才能拿到 productIdsToPush。修：在 SagaService.start() 把 initialPayload 中 templateId/replaceRules/productIdsToPush/shopDomainHint 等字段放入初始 SagaContext 并写入 saga_data_json。0.5 PD。

### Day 6 候选 (Wave 3 收尾)

剩 2 个性能任务：
- **D1**：W3-PERF-03 前端虚拟滚动（产品列表 1w+ 行）—— Frontend 改 `/products/page.tsx` 用 react-virtuoso 或自实现简易 virtualizer
- **D2**：W3-PERF-04 Wave 3 整体回归 + bug fix 缓冲 —— 跑 full smoke + e2e + 修发现的小问题 + 顺手清技术债 #12

或者：直接进 Wave 4（审批中心 / 钉钉通知订阅 / 邮件 / 备份 / 监控完善），剩 PERF-03/04 留 Wave 5 之前清。

---

## 2026-05-02（深夜）· Wave 3 Day 4 — saga 主体落地 + 预览池整段闭环 + 缓存

D1 saga PRODUCTS + 进度 SSE + D2 预览池前端收尾 + D3 一键开店向导前端 + D4 Redis 缓存。**4/4 全 ✅**。**3.2 预览池整段完结 + saga 主体推进到 PRODUCTS step**。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **D1** | W3-NEW-05 + W3-NEW-09 | `SagaProductsStepService`（调 PushService.pushBatch；空列表 skip + advance；全失败 throw FAILED；部分失败仍 advance）+ POST /saga/{id}/step/run-products；`SagaProgressService`（per-saga emitter map + Redis last-event TTL 1h replay）+ `SagaProgressController` GET /saga/{id}/events；SagaService 在 start/advance/fail/retry 4 处用 `@Lazy` 注入的 progressService emit；SecurityConfig 加 `/saga/*/events` permit | 端到端 PRODUCTS：seed product+variant+store → run-products → ctx.stepOutputs.products = `{success:1, parentTaskId, subTaskIds:[...]}` → step=PRODUCTS ✓；SSE smoke：subscribe → replay last 事件 + 后续 retry 触发 live event ✓ |
| **D2** | W3-PV-02 + W3-PV-05 + W3-PV-07 | Backend `PreviewBuildOrchestrator`（allocate 后 BUILDING → load Store + decryptToken + buildProductPayload + worker 调 /preview/build HTTP/1.1 → 回填 shopifyThemeId+previewUrl+READY，全 try/catch 永不 throw 5xx）；`PreviewController.allocate` 改为同步编排；Frontend `lib/api/preview.ts` + `components/preview/PreviewButton.tsx`（products/[id] 集成）+ /partner-stores 池页（filter + 7 列 table 含"当前预览数 n/3"客户端聚合 + sensitive op 标记池/dev）；W3-PV-07 PARTNER_DEV_SETUP guide seed 方法已文档化（不走 migration） | 端到端：seed dev store + product → POST /preview → status=READY + previewUrl=`https://pv-d4.myshopify.com/products/pv-d4-prod?preview_theme_id=2475031` ✓；worker 死时 → status=FAILED + errorMessage（不 5xx）✓；frontend build 27 routes 全过 |
| **D3** | W3-NEW-10 一键开店向导前端 | `lib/api/saga.ts` (sagaApi 6 方法 + subscribeSagaEvents EventSource 包装 + onError 回调供降级)；3 新页：/newstore（saga 列表 + 5s active 轮询）+ /newstore/new（5 步向导 ~580 行：基本+替换规则+接入模式+选产品+确认 + start saga + mock auth 自动跳）+ /newstore/[taskId]（9-step stepper + SSE 优先+3s 轮询 fallback + 终态停轮询 + INIT mock-auth 表单 + FAILED retry 按钮 + 手动 run-{media,collections,products} + 已禁用 guide/theme/publish 标 W3-NEW-06+ 后续）；AppShell 加菜单 | typecheck + build **27 routes**（含 3 新 dynamic）；3 个 URL 200；SSE 双 payload 形态都支持（直接 SagaState 或 `{payload:SagaState}`）|
| **D4** | W3-PERF-01 Redis 缓存 | `core/cache/CacheKeys.java`（store_list/product_detail/user_permissions 三 key 模板）+ `core/cache/CacheService.java`（getOrLoad 通用 + 失败 fallback 不抛错 + Micrometer hit/miss counter）；StoreService.list 加缓存（仅无 filter 时）+ 4 处 mutation invalidate；ProductService.detail 加缓存 + 2 处 mutation invalidate；application-dev.yml 加 cache.enabled / default-ttl-seconds | hit/miss counter 在 `/actuator/prometheus` 实测：3 调 = 1 miss + 2 hit；invalidate 后再 1 miss；Redis stop → API 仍 200 + log warn ✓；RBAC 缓存跳过（无明显热点；CacheKeys 占位留 W3-RBAC-04） |

**关键决策 / 踩坑**：
- D1：`SagaService` 注入 `SagaProgressService` 用 `@Lazy` 字段注入（避免潜在循环依赖未来出现）+ emit 全 try/catch 吞错（SSE 永不阻塞状态机）。
- D1：saga 状态机语义「step name = 最后已完成步」 —— `executeStep(COLLECTIONS, fn)` 完成后 step 变 PRODUCTS（不是 GUIDE）。run-products 后 step=PRODUCTS 是符合预期的；GUIDE step 由后续 W3-NEW-06 的 run-guide 落地。
- D2：PreviewBuildOrchestrator 同步调用 worker（dry-run 快返回 < 1s，可接受）；生产真 OAuth 时如果 Shopify 慢，HTTP 请求会阻塞 —— 标 TODO 异步化（与 W2-PUSH-05 同问题）。
- D2：PreviewButton fallback 当 status != READY 时显示 toast.info 让用户去 /partner-stores 看 —— 因为 D2 没接 SSE。
- D2：`StoreItem` 加 `isPartnerCollab` + `tenantId` 可选字段；`AuthUser` 仍只 expose userId（D2 D3 D4 都遇到这个），用 `tenantId ?? 1` 兜底。
- D3：5 步向导用纯 useState（非 react-hook-form），约 580 行；逻辑简单清晰但代码较长，未来有需要再切组件。
- D3：SSE 兼容两种 payload 形态（`{event,step,status,...}` 直平铺 vs `{payload:{...}}`），实测 D1 emit 的是直平铺。
- D4：RBAC 缓存跳过 —— `JwtAuthFilter` 只注 `ROLE_USER` 静态，`DataScopeInterceptor` 是 SQL 子查询不是 service 调用，没有明显热点 lookup 可包。CacheKeys.java 留了 `userPermissions(id)` 占位让 W3-RBAC-04 接入。
- D4：缓存 fallback 验证：docker stop redis → API 仍 200 + log warn `[cache] read failed` —— Redis 不可用确实不阻塞。

### Wave 3 完成度

| 模块 | 完成度 |
|---|---|
| 3.1 一键开店 saga | **7/12**（NEW-01..05, 09, 10 ✅；NEW-06..08, 11, 12 🔲）|
| **3.2 合作者池 + 预览 | 7/7 ✅** |
| 3.3 采购管理 | 5/5 ✅（W1）|
| 3.4 产品媒体 + 需求文档 | 6/6 ✅（W1）|
| **3.5 平台模板库 + 指导文档 | 5/5 ✅** |
| **3.6 跨公司授权 | 3/3 ✅** |
| 3.7 性能优化 | 1/4（PERF-01 ✅；PERF-02/03/04 🔲）|

**已完成 34/40 = 85%**。**3.2/3.3/3.4/3.5/3.6 五段全闭环**。剩 6 任务：saga 中后段（NEW-06 主题 zip / NEW-07 域名替换 / NEW-08 指导文档跳转 / NEW-11 通知 / NEW-12 e2e）+ 性能（PERF-02 慢查询 / PERF-03 虚拟滚动 / PERF-04 回归 buffer）。

### Day 5 候选

按"saga 完工"的节奏：
- **D1**：W3-NEW-06 主题 zip 安装（worker `/push/theme` 解压上传 → themeUpdate role=main）+ W3-NEW-07 域名/品牌正则替换（在 worker 上传 zip 前对 liquid 文件做 sed）
- **D2**：W3-NEW-08（menu/policy/markets 指导文档跳转，复用 W3-GUIDE-01 + 弹窗组件）+ W3-NEW-11（NEW_STORE_SUCCESS/FAIL 钉钉通知，复用 W2-PUSH-07 模式）
- **D3**：W3-NEW-12 端到端 e2e 测试（脚本：start saga → mock auth → run-media→collections→products → 模拟剩余 step → SUCCESS）
- **D4**：W3-PERF-02 慢查询 + 索引复盘（审计日志、产品快照、task 表 EXPLAIN ANALYZE，加缺失索引）

或：4 路 saga 收尾后再做 PERF-03（虚拟滚动）+ PERF-04（缓冲回归）拿 100% Wave 3。

---

## 2026-05-02（深夜）· Wave 3 Day 3 — D3 + D4 整段闭环 + saga 推到 COLLECTIONS

D1 saga MEDIA + COLLECTIONS 步 + D2 worker /preview/build + D3 模板/指导前端 + D4 跨公司授权前端。**4/4 全 ✅**。**D3 + D4 整段完结**。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **D1** | W3-NEW-03 + W3-NEW-04 saga 步 | Worker：`POST /push/files`（base64→GraphQL fileCreate + R2FetchError + 空列表 noop + 5 pytest）+ `POST /push/collections`（per-item error capture + 5 pytest）；Backend：`SagaMediaStepService.run(taskId)` + `SagaCollectionsStepService.run(taskId)` 通过 `SagaService.executeStep` 链路；`SagaController` 加 POST /saga/{id}/step/run-{media,collections} dev 触发器 | **64/64 pytest**（51→64，+13）；端到端 saga：start→mock auth→AUTH_DONE→run-media→MEDIA→run-collections→COLLECTIONS（ctx.stepOutputs 累积）✓ |
| **D2** | W3-PV-04 worker /preview/build | `PreviewBuildService`（dry-run 合成 theme_id + URL；real 路径调 `theme_create_unpublished` GraphQL + `create_product` REST，product 失败时 fallback theme-only 不阻断）+ `POST /preview/build` 路由（502/503 错误映射）+ 6 pytest（dry-run / real / product 失败容忍 / theme 失败 / 显式 token / 无 handle 时 omit product_preview_url） | dry-run smoke ✓，preview_url + product_preview_url 都正确生成（`?preview_theme_id=`）；4 路 /pull/* 回归全过 |
| **D3** | W3-TPL-03 + W3-GUIDE-02 模板/指导前端 | Backend 加 `GuideDocController`（list/get/create/update/publish/delete）；Frontend：`lib/api/template.ts`（含多部分 uploadVersion 走 raw fetch）+ `lib/api/guide.ts`；5 个新页面 `/templates`、`/templates/[id]`（基本+版本 2 tab）、`/guides`、`/guides/new`、`/guides/[id]`（new/edit 共享 `components/guide/GuideForm.tsx` 复用 RichTextEditor）；AppShell 加 2 菜单 | mvn ✓；guide endpoint smoke ✓；frontend typecheck + build **28 routes** 全过；5 新路由 dev 200 |
| **D4** | W3-DS-02 跨公司前端 | `lib/api/crossAuth.ts`（含 sensitive helpers）+ `components/crossAuth/CrossAuthGrantDialog.tsx`（5min~180d 校验 + 默认 +7d 本地时间 + sensitive 真实流程：request → prompt 6 位码 → verify → grant）+ `/cross-auth` 列表页（status tab 默认 ACTIVE + userId/granterCompanyId 过滤 + revoke + "即将过期" badge < 24h） | typecheck + build **21 routes** 全过；/cross-auth dev 200；AppShell 加菜单 ✓ |

**关键发现 / 决策**：
- D1：saga 状态机的"step name = 最后已完成步"语义 —— `executeStep(AUTH_DONE, fn)` 走完后 step 变 MEDIA（不是 COLLECTIONS），与 SagaService.advance 行为一致。
- D1：默认 collections fallback `[{all},{new}]` 让无模板配置的 saga 也能跑通 demo。
- D2：Shopify Theme 创建失败是致命的（502 PreviewBuildError），Product 创建失败是非致命的（log warn + 继续 theme-only preview），因为 theme 是预览的核心，product 是锦上添花。
- D2：`product_payload.id` 是我们的内部 id，发给 Shopify 前要 strip，否则 Shopify 报错。
- D3：`api.client` 强制 JSON content-type，多部分上传必须用 raw `fetch` + 手动从 `useAuthStore` 拿 token —— 工程上写在 `templateApi.uploadVersion` 里，业务侧无感。
- D3：guide form new + edit 用同一个 `GuideForm.tsx` 组件，避免代码重复。
- D4：`AuthUser` 类型只 expose `userId` 不 expose `id` 也无 `tenantId`，`granterCompanyId` 留空让 backend 从 JWT claim 推断。
- D4：`new Date().toISOString().slice(0,16)` 在非 UTC 时区会显示错时间 —— 默认 +7d 用本地时间字符串构造。

### Wave 3 完成度

| 模块 | 完成度 |
|---|---|
| 3.1 一键开店 saga | 4/12（NEW-01..04 ✅；NEW-05..12 🔲）|
| 3.2 合作者池 + 预览 | 4/7（PV-01, PV-03, PV-04, PV-06 ✅；PV-02, PV-05, PV-07 🔲）|
| 3.3 采购管理 | 5/5 ✅（W1 已完）|
| 3.4 产品媒体 + 需求文档 | 6/6 ✅（W1 已完）|
| **3.5 平台模板库 + 指导文档 | 5/5 ✅** |
| **3.6 跨公司授权 | 3/3 ✅** |
| 3.7 性能优化 | 0/4 🔲 |

**已完成 27/40 ≈ 68%**。**3.5 + 3.6 整段完结**。剩主要是 saga 中后段（PRODUCTS / GUIDE / THEME / PUBLISH / 通知 / e2e / 前端向导）+ 预览前端（PV-02/05/07）+ 性能优化。

### Day 4 候选

- **D1**：W3-NEW-05（批量产品推送，复用 W2-PUSH）+ W3-NEW-09（失败重试 + 进度 SSE）—— saga PRODUCTS 步 + 错误处理
- **D2**：W3-PV-02（合作者店铺池前端页）+ W3-PV-05（产品详情页"眼睛"按钮 + 触发 preview/build）+ W3-PV-07（指导文档 PARTNER_DEV_SETUP）—— D2 收尾整段
- **D3**：W3-NEW-10（一键开店向导前端：多步表单 + 进度条 + 替换规则配置）—— saga 主前端
- **D4**：W3-PERF-01（Redis 缓存：店铺列表 / 产品基础信息 / 角色权限）—— 性能优化起手

或者：D1+D3 联手做 saga 完整链路（D1 后端 PRODUCTS+GUIDE+THEME+PUBLISH，D3 前端向导 + SSE 接 W2-AST-05）。

---

## 2026-05-02（深夜）· Wave 3 Day 2 — 4 track 持续推进

D1 saga OAuth + D2 preview 池 + D3 模板 CRUD + D4 跨公司过期通知。**4/4 全 ✅**。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **D1** | W3-NEW-02 saga AUTH_DONE | `SagaAuthService`（buildInstallUrl + signState HMAC-SHA256 + verifyAndExtractTaskId + handleCallback 真 OAuth + mockAuth dev 路径 + upsertStore AES-encrypt token + 自动 advance 到 AUTH_DONE）；`SagaOAuthCallbackController` GET /oauth/saga-callback 302 跳前端 wizard；`SagaController` 加 POST /saga/start（返 oauthUrl）+ POST /saga/{id}/dev-mock-auth；application-dev.yml 加 3 env（dev-mock-oauth / oauth-callback-base / saga-frontend-base） | mvn ✓；端到端 mock 流：start→taskId=14→mock→step=AUTH_DONE→ctx 填 storeId=23/shopDomain/token→DB store 行 ACTIVE+oauth+12 scopes JSON ✓；OAuth URL 真 build（含 client_id+state HMAC）✓ |
| **D2** | W3-PV-03 + W3-PV-06 | V18 preview_theme（13 列 + 3 索引 + ENUM PENDING/BUILDING/READY/EXPIRED/FAILED）+ entity/mapper；`PreviewThemeAllocator`（pickLeastLoadedDevStore + allocate + markAccessed）；`PreviewThemeCleanupScheduler` `@Scheduled(0 */30 * * * *)` 真实现 expireOverdue + enforceQuota；`PreviewController` 5 endpoint（POST 分配 / GET 列表 / GET detail / POST accessed） | V18 ✓；5 分配 → 2 dev stores 均匀（products 1/3/5 → S21，2/4 → S22），第 6 个填 S22，第 7 个超容量 500 IllegalStateException ✓；3 env 引入 |
| **D3** | W3-TPL-02 | `BaseTemplateService`（list 分页搜索 + detail 含 versions + create + update + publish/deprecate/delete + **uploadVersion zip→R2 + sha256 + 大小校验 100MB** + setDefaultVersion）；`BaseTemplateController` 8 endpoint，sensitive op 标 4 个（publish/deprecate/delete/version-upload）+ TODO RBAC PLATFORM_SUPER | mvn ✓；CRUD 基础 smoke：create id=2 → list/get/update/keyword search/category 过滤全过；version upload 401 由 sensitive op 拦截（按预期）；R2 上传链路依赖 FileService（已通） |
| **D4** | W3-DS-03 真实 | V19 加 `expiring_notified_at` + `idx_data_scope_notify_window`（dedupe 提醒）；`CrossAuthExpiryScheduler` 替换 Day 1 skeleton：`warnExpiring`（cron `0 0 * * * *`）扫 24h 内到期未通知 → `notifyUser(userId, CROSS_AUTH_EXPIRING, subject, text, html)` + 标 expiring_notified_at；`hardExpire`（cron `0 5 0 * * *`）状态过期→REVOKED；CROSS_AUTH_EXPIRING 已在 enum，无需加 | V19 ✓；查询 SQL 验证：seed 12h 到期行 → warn WHERE 命中 1 行；seed 已过期行 → hardExpire WHERE 命中 1 行；mvn ✓ |

**关键发现 / 决策**：
- D1：`store.token_type` 是 ENUM('cli','custom_app','oauth')，mock 走 `oauth` 类型；`store.scopes` 是 MySQL JSON 列，需要 `encodeScopesJson` 把 Shopify 逗号字符串转 JSON 数组。
- D1：state 签名格式 `base64url(taskId).base64url(HMAC-SHA256(taskId, key))`，key fallback 链：`SHOPIFY_APP_SECRET → ENCRYPT_KEY_AES_GCM`。
- D2：`PreviewThemeAllocator.pickLeastLoadedDevStore` 用 store_id GROUP BY count，O(N) 但 dev store 数量小（< 50），可接受。生产可加缓存。
- D3：`ResultCode` 没有 `SYSTEM_ERROR`，用 `INTERNAL_ERROR` 替换。
- D3：boot 期间被并发 agent 的 pkill 误杀两次 —— 用 `nohup ... < /dev/null & disown` 加固。
- D4：`NotificationDispatcher.dispatch(EventCode, Map)` 不是真实 API —— 实际是 `notifyUser(userId, eventCode, subject, bodyText, bodyHtml)`，每行授权构造文本+HTML body 后调用。

### Wave 3 完成度

| 模块 | 完成度 |
|---|---|
| 3.1 一键开店 saga | 2/12（NEW-01, NEW-02 ✅；NEW-03..12 🔲）|
| 3.2 合作者池 + 预览 | 3/7（PV-01, PV-03, PV-06 ✅；PV-02, PV-04, PV-05, PV-07 🔲）|
| 3.3 采购管理 | 5/5 ✅（W1 已完）|
| 3.4 产品媒体 + 需求文档 | 6/6 ✅（W1 已完）|
| 3.5 平台模板库 + 指导文档 | 3/5（TPL-01, TPL-02, GUIDE-01 ✅；TPL-03, GUIDE-02 🔲）|
| 3.6 跨公司授权 | 2/3（DS-01, DS-03 ✅；DS-02 🔲）|
| 3.7 性能优化 | 0/4 🔲 |

**已完成 21/40 个 Wave 3 子任务 ≈ 53%**（含 W1 提前完成的 PUR + MED）。

### Day 3 候选

- **D1**：W3-NEW-03 媒体批量上传 R2 → Shopify Files（saga MEDIA 步）+ W3-NEW-04 Collections 创建（saga COLLECTIONS 步）—— 两个最简单的步骤一起做
- **D2**：W3-PV-04 worker `/preview/build`（asset-worker 端：推产品 → 创 unpublished theme → 替换媒体 URL）
- **D3**：W3-TPL-03 模板库前端页（上传 zip / 版本切换 / 默认标记）+ W3-GUIDE-02 指导文档前端 + 一键开店时弹窗
- **D4**：W3-DS-02 跨公司授权前端表单（已闭一半，剩 UI）

或：先做 W3-PV-02 合作者店铺池前端（D2 主线 UI 收尾）+ W3-PV-05 详情页"眼睛"按钮联通 PV-04，凑齐预览闭环。

---

## 2026-05-02（深夜）· Wave 3 启动 — 4 track 并行 Day 1

按《并行任务编排.md》Phase 4 编排，4 sub-agent 并行：D1 一键开店 saga 框架 + D2 合作者池 + D3 模板/指导骨架 + D4 跨公司授权基础。**4/4 全 ✅**，零文件冲突。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **D1** | W3-NEW-01 saga 状态机 | Flyway **V13**（task 加 saga_step + saga_data_json + saga_attempt + idx）；4 个新 Java 类：`SagaStep` enum（INIT→AUTH_DONE→MEDIA→COLLECTIONS→PRODUCTS→GUIDE→THEME→PUBLISH→SUCCESS/FAILED）+ `SagaContext` record（taskId/storeId/shopDomain/shopAccessToken/templateId/replaceRules/productIdsToPush/stepOutputs）+ `SagaService`（start / advance / fail / retry / loadContext / executeStep 6 个 API）+ `SagaStepException` + `SagaController`（GET /saga/{id} + POST /retry + POST /advance dev-only）；Task 实体加 3 字段 | mvn ✓；INSERT saga 行 → GET 返 step=INIT → POST /advance → step=AUTH_DONE ✓；强标 FAILED → POST /retry → status=RUNNING + attempt=1 ✓ |
| **D2** | W3-PV-01 合作者池字段 + OAuth | V14 **仅加 2 索引**（is_dev_store + is_partner_collab 列在 V4 已就位，避免重复 ALTER）；StoreService 重载 list(tenantId, partnerCollab, devStore)；3 个 sensitive 接口（mark/unmark partner-collab + mark dev-store）+ 新 StorePoolController GET /store/pool/partner-collab | V14 ✓；过滤 partnerCollab=true 返已标记店铺 + partnerCollab=false 不返；sensitive 接口 401 拦截正确（无钉钉 token）|
| **D3** | W3-TPL-01 + W3-GUIDE-01 表骨架 | V15（**3 表合并**：base_template + base_template_version + guide_doc，全有 status ENUM + 索引 + 中文 COMMENT）；6 个 Java 文件（3 entity + 3 mapper）；Application.java @MapperScan 加 template/guide 两包 | mvn ✓；3 表 DESCRIBE OK + ENUM 取值正确；INSERT smoke 行通过约束 |
| **D4** | W3-DS-01 跨公司授权基础 | V16（V1 已有 granted_by + expires_at，所以 V16 **只加 3 真新列**：cross_company + revoked_at + granter_company_id；source ENUM 加 APPROVAL）；CrossCompanyAuthController（list / grant 5min~180d 校验 / revoke，全 sensitive op）；CrossAuthExpiryScheduler 骨架（W3-DS-03 实现 24h 提醒 + 0:05 硬过期）；application-dev.yml 加 rbac.cross-auth.* cron 配置 | V16 ✓；DESCRIBE 5 字段在；GET /cross-auth 空数组；POST /cross-auth 401（sensitive 拦截正确）|

**关键决策**：
- D1：SagaContext 用 record；SagaService 提供 `executeStep(taskId, expected, fn)` 高阶 API，让 W3-NEW-02..08 直接传 lambda 实现各步业务即可，自动处理 advance/fail。
- D1：SagaController POST /advance 是 **dev-only**（不接受 ctxPatch merge）—— 生产 step handlers 直接调 `SagaService.advance(taskId, ctx)` 传完整新 context。
- D2：发现 V4__init_store.sql 已经有 is_dev_store / is_partner_collab 列！进度记录里也提过它在 Day 4 时是"留 W3-PV"，但实际列已存在 —— 所以 V14 只加索引，避免 information_schema 冲突。
- D3：把 base_template + base_template_version + guide_doc 三个独立任务的表合并到一个 V15 文件（更原子化），不要分多个 V。
- D4：V1 已经把 expires_at 标 NOT NULL 了 —— 与原任务"时效字段必填"一致，V16 不需要再加 NOT NULL；只补 3 个真缺失列 + 扩 ENUM。
- D4：source ENUM 之前是 `('CROSS_AUTH', 'INVITATION')`（V1 留位），V16 加 'APPROVAL'。

### Wave 3 完成度

| 模块 | PD 估 | 完成度 |
|---|---|---|
| 3.1 一键开店 saga | 22 PD | 1/12（W3-NEW-01 ✅；02..12 🔲）|
| 3.2 合作者店铺池 + 预览 | 10 PD | 1/7（PV-01 ✅；02..07 🔲）|
| 3.3 采购管理 | 8 PD | **已在 Wave 1 完成基础版**（PUR-01..05 全 ✅）|
| 3.4 产品媒体 + 需求文档 | 10 PD | **已在 Wave 1 完成基础版**（MED-01..06 全 ✅）|
| 3.5 平台模板库 + 指导文档 | 8 PD | 2/5（TPL-01 + GUIDE-01 ✅；TPL-02/03 + GUIDE-02 🔲）|
| 3.6 跨公司授权 | 3 PD | 1/3（DS-01 ✅；DS-02/03 🔲）|
| 3.7 性能优化 | 9 PD | 0/4 🔲 |

**已完成 5/29 个 Wave 3 子任务 ≈ 17%（Day 1 一砖一瓦）**。

### Day 2 候选（同色继续）

- **D1**：W3-NEW-02 新店 OAuth 授权 + App 安装（在 saga AUTH_DONE 步实现）
- **D2**：W3-PV-03 preview_theme 表 + 最少负载选 dev store + W3-PV-06 30 分钟清理 cron 骨架
- **D3**：W3-TPL-02 模板 CRUD（仅 PLATFORM_SUPER）+ R2 zip 上传 + 版本管理
- **D4**：W3-DS-03 24h 提醒 + 硬过期 cron 真实实现（替换 Day 1 skeleton）+ NotificationEventCode.CROSS_AUTH_EXPIRING 加 enum

或换种节奏：D2/D3/D4 都是骨架，同步实现完整 CRUD 后启动 D1 主线推进。

---

## 2026-05-02（深夜）· Wave 2 完工 — 技术债 #9 + W2-SYNC-03/04 收尾

🎉 **Wave 2 100% 完成（29/29）**。

**第一波：技术债 #9 闭环**

直接 Edit（无须派 agent）：
- `PushService.java` 写 `payloadJson` 时把 `productId` 提到顶层（保留原 reqBody 字段不动）
- `TaskController.java` 改用顶层 productId 直查 `store_product`，去掉 `last_push_task_id` 反查 + handle 兜底分支（保留旧 task 行的兜底以兼容）
- `mvn clean compile` ✓

**第二波：Wave 2 收尾 2 并行 ✅**

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-SYNC-03 状态广播 | `product/service/ProductStatusBroadcaster`（扫 store_product 映射 → 跳过 NOT_PUSHED/FAILED 行 + UNINSTALLED 店铺 → 逐 store `pushService.push`，per-store try/catch）；`ProductService.update` 拆 `@Transactional applyUpdate`（捕 oldStatus）+ 外层 broadcast（事务提交后才 fan-out，失败 swallow） | 端到端：seed product(active) → 2 store_product 映射 → PUT `/product/{id} {status:"draft"}` → log `[broadcast] productId=12 status active -> draft pushed=2 skipped=0 failed=0` + DB 2 个新 PRODUCT_PUSH 任务（triggered_by=NULL 系统 fan-out）+ store_product.last_push_task_id 回填 |
| **B** | W2-SYNC-04 多店铺聚合页 | Backend `push/ProductStoreMappingController` GET `/product/{id}/store-products`（store_product join store 返 brand/domain/storeStatus + mapping status + shopify ids + last_pushed/pulled_at）；Frontend `lib/api/storeProduct.ts` + `components/product/StoreMappingPanel.tsx`（7 列只读表 + LoadingBlock + EmptyState + STORE_PRODUCT_STATUS 6 状态色票）；`products/[id]/page.tsx` 加第 8 个 tab "店铺映射"（位置：媒体/需求文档 ↔ SEO 之间） | 空数组 OK；seeded 1 mapping → 完整字段透出 ✓；frontend typecheck + build **21 routes** 全过；dev /products/1 200 |

### Wave 2 完成度（最终）

| 模块 | 完成度 |
|---|---|
| 2.1 Shopify CLI + 资产拉取 | **7/7 ✅** |
| 2.2 产品快照 | **6/6 ✅** |
| 2.3 产品推送 | **8/8 ✅** |
| 2.4 多店铺状态同步 | **4/4 ✅** |
| 2.5 监控 | **4/4 ✅** |
| **合计** | **29/29 ✅** |

### Wave 2 关键资产（一线开发可直接用）

**端到端能跑通**：
- 产品快照：webhook → Redis NX 去抖 → RabbitMQ TTL+DLX → consumer → worker `/pull/product` → R2 存 product.json + images + manifest → 生成 15 列 CSV → DB 行 SUCCESS（含 csv_r2_key/json_r2_key/started_at/total_bytes）
- 产品推送：`POST /push/product` 单 / `POST /push/batch` 批量（parent + children task） → worker（含 R2 → base64 attachment 媒体路径）→ 三分支 SUCCESS/PARTIAL/FAILED → 失败钉钉通知 + sys_audit_log
- 状态同步：本地 Product.status 改 → fan-out 到所有映射店；webhook app/uninstalled → store.status=UNINSTALLED；04:00 cron 兜底扫骨架（W3 接 GraphQL bulk）
- 资产拉取：5 个端点（theme/policy/menu/collection/product）全部 dry-run 兜底；SSE 进度推送（worker → backend → frontend，Redis 缓存 last-event 1h replay）

**前端**：21 路由全 build pass。新增页面：`/assets`、`/snapshots`、`/tasks`（轮询 active）+ `/products/[id]` 8 tab（含店铺映射）+ 触发 dialog（snapshot/push）。

**监控**：Prometheus + Grafana + Loki + Promtail + Alertmanager 全栈 + 4 scrape job + 12 告警规则（含 5 个 placeholder 等真 metrics 落地）+ 12 panel 看板。

### 累计技术债（移交《技术债登记.md》集中管理）

✅ 已闭环：#1, #2, #3, #4, #6, #8, #9（7 个）

🟡 延后（带触发条件）：
| # | 描述 | 触发再看 |
|---|---|---|
| #5 | Worker progress emit 同步 httpx 每 file 一 POST | 单 pull ≥100 文件 / backend 慢 / 用户报慢 |
| #7 | webhook 没 currency → 历史表硬编 USD | W3-PUR 多店多币种成本对比时 |
| #10 | 推送批量同步阻塞 HTTP，真异步队列待加 | W3-NEW 启动 / 单批 >20 产品时 |
| #11 | StatusSyncService 是骨架，无真 Shopify diff | W3-NEW 上线 + 合作者池店多了时 |

详细工作量 + 解法 / 风险见 `技术债登记.md`。

### 下一步候选

Wave 2 闭环 → 进入 Wave 3 选项：
- **W3-NEW 一键开店 saga**（关键路径，~22 PD，11 子任务）—— 关键路径上的最大模块
- **W3-PV 合作者店铺池 + 详情页预览**（~10 PD，独立）—— 解锁 saga 的 Partner dev store 验证
- **W3-TPL 平台模板库 + 指导文档**（~8 PD，独立）—— 一键开店要用的素材源
- **W3-DS 跨公司授权基础**（~3 PD，最小）

按《并行任务编排.md》Phase 4 推荐：D1 (一键开店) 主线 + D2/D3/D4 任意切片，4 路并行可三周完工 Wave 3。

---

## 2026-05-02（深夜）· 技术债 #4/#6/#8 + Wave 2 Day 7 三 track 收尾

**第一波：技术债清账（3 并行 ✅）**

| # | 修复 | 验证 |
|---|---|---|
| **#4** ProductSnapshot.started_at | Flyway V11 加列 + entity 字段 + SnapshotProcessConsumer / SnapshotGenerationService 在 RUNNING 翻转时埋时间戳 + 防御性 backfill（旧行 RUNNING 但 started_at NULL 也补） | 端到端 webhook → SUCCESS 行：`created_at < started_at < completed_at`（22:59:52 / 22:59:57 / 22:59:58）|
| **#6** ProductImage.r2_key | Flyway V12 加列（512） + idx 索引 + ProductImage entity 字段 + PushService 把 r2Key 非空的图片填入 `media_r2_keys`（与 `images[].src` 共存，worker `_attach_r2_media` 先 src 后 r2 attachment）| 端到端：seed 1 src + 1 r2_key 图片 → push → task.payload_json 同时含 `images:[{src:...}]` 和 `media_r2_keys:["tenants/1/products/4/img2.jpg"]` |
| **#8** 立即快照 modal | 新建 `components/ui/Dialog.tsx`（bespoke 模态：Esc + body lock + click-outside）+ `components/snapshot/SnapshotTriggerDialog.tsx`（store 下拉 + external_id 输入 + toast）；`products/[id]` 移除 prompt 改 setOpen | typecheck + build 19 routes 全过；dev /products/1 200；剩余 prompt 仅 SKU 改动 + 敏感码用，不动 |

**第二波：Wave 2 Day 7（3 并行 ✅）**

A=W2-PUSH-06 + Task UI、B=W2-SYNC-01+02、C=W2-PUSH-05+07+08。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-PUSH-06 推送前端 + Task 列表 | Backend：`push/TaskController` GET `/task` (lean list + 11 字段过滤 type/status/storeId) + GET `/task/{id}` (含 storeProduct 反查 + 最近 10 push_conflict) + GET `/task/{id}/payload` (懒加载)；Frontend：`lib/api/{task,push}.ts` + `components/push/PushTriggerDialog`（复用 Day 6 模式 + useAuthStore triggeredBy + 跳 /tasks/[id]）+ `/tasks` 列表（5s 轮询 active）+ `/tasks/[id]` 详情（4 tab + 3s 轮询 + payload 懒加载）+ `products/[id]` 加"推送到店铺"按钮 + AppShell 加菜单 | mvn ✓；endpoint 空表 OK + filter（type/status/storeId）全过 + detail flatten + payload 懒加载 OK；frontend typecheck + build **20 routes** 全过；/tasks dev 200；轮询 stop 条件正确（`status ∈ {PENDING,RUNNING}` 才走） |
| **B** | W2-SYNC-01+02 多店铺状态同步 | `webhook/StoreWebhookController`（POST `/webhook/shopify/app/uninstalled` → store.status=UNINSTALLED；POST `/webhook/shopify/shop/update` log+200）；`sync/StatusSyncScheduler` `@Scheduled(0 0 4 * * *)`（ACTIVE store 遍历 + per-store + top-level 双层 try/catch）；`sync/StatusSyncService` 骨架（统计 store_product 行数，真 Shopify diff 留 W3）；`application-dev.yml` 加 `shopify.sync.full-cron`；`Wave2-配置占位.md` §14 | 端到端 app/uninstalled 200 + DB 状态 ACTIVE→UNINSTALLED ✓；boot 不报 cron 解析错（fail-fast 即报）+ taskScheduler 线程池 active；既有 ProductWebhookController 已处理 products/delete 不动 |
| **C** | W2-PUSH-05+07+08 调度+通知+审计 | `PushService` 加 `pushBatch(productIds[], storeId, triggeredBy)` 同步循环 + parent task RUNNING + 子任务 parent_task_id 回填 + SUCCESS/PARTIAL 汇总；`pushBatch` 三 FAILED 分支（IOException / 422 / 非 2xx）调用 `notificationDispatcher.dispatch(PRODUCT_PUSH_FAIL, ctx)`；`PushController` 加 POST `/push/batch`；fallback userId 配置；W2-PUSH-08 验证既有 AuditAspect 自动覆盖（无代码改）；`Wave2-配置占位.md` §15 | 单 push 回归 OK；批量 productIds=[5,6] → `parentTaskId:5, subTaskIds:[6,7], success:2`；fail smoke：worker stop → push FAILED + log `[notify] dispatched PRODUCT_PUSH_FAIL recipient=1`；sys_audit_log 实测 `/api/push/product` + `/api/push/batch` 都有行（response_status=200）|

**关键决策**：
- Track A：TaskController 没法从 task 行直接拿 productId（payloadJson 是 JSON string）→ 通过 `store_product.last_push_task_id = task.id` 反查关联，标 W3 follow-up 在 PushService.pushBatch 把 productId 提到 payload 顶层。
- Track C：W2-PUSH-05 选了**同步批量循环**（不上 @Async / RabbitMQ）。原因：Spring `@Async` self-injection 循环依赖坑 + 加 RabbitMQ 队列又是一摊事。批量大时 HTTP 阻塞，标 TODO（小量推送是 OK 的）。
- Track C：`NotificationEventCode.PRODUCT_PUSH_FAIL` 早就在 enum 里了（不知道哪一波加的），无须改。
- Track C：Audit 完全自动 —— @within(@RestController) AOP 切面把所有 PushController + TaskController 方法都覆盖，0 代码改动。

### Wave 2 完成度（**收尾后**）

| 模块 | 完成度 |
|---|---|
| 2.1 Shopify CLI + 资产拉取 | **7/7 ✅** |
| 2.2 产品快照 | **6/6 ✅** |
| 2.3 产品推送 | **8/8 ✅** |
| 2.4 多店铺状态同步 | 2/4 — SYNC-01/02 ✅；SYNC-03/04 🔲 |
| 2.5 监控 | **4/4 ✅** |

**已完成 27/29 = 93%**。剩 SYNC-03（产品状态推所有映射店；本质是 W2-PUSH 重复调用）+ SYNC-04（前端只读聚合页）—— 都是组合既有能力。

### 累计技术债（Day 7 后状态）

| # | 描述 | 状态 |
|---|---|---|
| 1, 2, 3, 4, 6, 8 | | ✅ 全闭 |
| 5 | Worker progress emit 同步 httpx | 🟡 perf 优化，等真出问题 |
| 7 | webhook 没 currency → 硬编 USD | 🟡 W3-PUR 多币种时改 |
| **9 新** | PushService.payloadJson 不含 productId 顶层字段，TaskController 反查麻烦 | 🟡 1h 改 |
| **10 新** | 真异步批量推送：@Async self-injection 坑 + RabbitMQ 队列待加 | 🟡 量大才需要 |
| **11 新** | StatusSyncService 是骨架，真 Shopify diff 待 W3（worker GraphQL 分页 + bulk operations） | 🟡 W3 territory |

### 下一步候选

Wave 2 还剩 2 子任务（SYNC-03/04），都是组合既有能力，约 3 PD：
- **A**：W2-SYNC-03（产品状态变更触发推所有映射店；本质 ProductController.update 加 hook 调 PushService.pushBatch）
- **B**：W2-SYNC-04（前端聚合页：列出每个产品在所有店铺的状态 ACTIVE/DRAFT/ARCHIVED/NOT_PUSHED + 统一只读视图）
- 收尾后 → 可启动 **Wave 3**（一键开店 saga / 合作者店铺池 / 模板库 / 审批中心）

---

## 2026-05-02（深夜）· Wave 2 三 track 并行 Day 6 — 前端落地 + 价格/库存历史 + 推送闭环

A=W2-AST-06+SNAP-06、B=W2-SNAP-05、C=W2-PUSH-03+04。3/3 全 ✅。Wave 2 现在有完整的 backend → frontend → push 三段链路。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-AST-06 + W2-SNAP-06 | Backend：`asset/AssetSnapshotController`（GET 列表 + 详情 + manifest 代理）+ `snapshot/ProductSnapshotController`（GET 列表 + 详情含最近 10 条 price/inventory + manifest + CSV 代理 + POST `/manual` 触发）；Frontend：`lib/api/snapshot.ts` + `/assets/{,[id]}` + `/snapshots/{,[id]}`（4 tab：基本/价格历史/库存历史/Manifest）+ `products/[id]` 加"立即快照"按钮（prompt 模式选 storeId） | mvn ✓；空表 endpoint 返 `records:[],total:0` ✓；POST `/manual` 第一次 `queued:true,snapshotId:17`，第二次 `queued:false,leaderId:17`（debounce 生效）；frontend 19 路由全 build pass，4 个新 dynamic 路由 dev 200 |
| **B** | W2-SNAP-05 | `snapshot/PriceInventoryHistoryService`（diff-only：QueryWrapper LIMIT 1 找 latest → BigDecimal `compareTo` 对比 → 仅变化才 insert）+ webhook 路径加 try/catch 调用（topic ≠ delete 时执行）；硬编 currency=USD 留 TODO；`Wave2-配置占位.md` §11 | 第一次 webhook（2 variants）→ 2 price 行（price=9.99/19.99，old=NULL）+ 2 inventory 行（qty=100/50，old=NULL）；第二次（仅 V1 price 9.99→11.99，其余不变）→ 仅 1 新 price 行（id=3，old_price=9.99），inventory 不增 |
| **C** | W2-PUSH-03 + W2-PUSH-04 | Worker：`R2Client.get_object`（懒加载 + propagate botocore）；`ProductPushService` 加 `R2FetchError` + `_attach_r2_media`（拷贝 payload 防 mutate caller，shallow copy → 下载 → base64 → append `attachment` 到 images）；strip `media_r2_keys`；dry-run 合成 base64；`routes/push.py` 单例 R2Client + 3 新 pytest（**47/47**）；Backend：`push/PushService`（load product+variants+images+store → build payload【库存 1000 + continue】→ task PENDING→RUNNING → HTTP/1.1 worker 调用 → 200/422/error 三分支：SUCCESS upsert store_product / PARTIAL 写 push_conflict PENDING / FAILED 写 error_message）+ `push/PushController` POST `/push/product`；`Wave2-配置占位.md` §12 | 47/47 worker pytest；mvn ✓；端到端：seed store(id=6) + product(id=3) + variant → POST `/push/product {productId:3,storeId:6}` → `taskId:1`；DB：task#1 type=PRODUCT_PUSH status=SUCCESS；store_product 行 shopify_product_id=6707464753 status=ACTIVE last_push_task_id=1；push_conflict 0 行（dry-run 没冲突） |

**关键决策 / 踩坑**：
- Track A：products/[id] 没有 storeId 字段（产品归属公司不归属店铺）→ "立即快照"按钮用 `prompt()` 选 storeId，留 TODO 改 modal + storeApi.list 下拉。
- Track B：Shopify webhook payload 不带 `currency` —— 历史表 currency 列硬编 'USD'，TODO 在 W3-PUR 时改成从 store 配置读。
- Track B：分区表的 latest-row 查询用 `LIMIT 1` 是 OK 的（changed_at 索引可命中），不用全表扫所有 partition。
- Track C：`ProductImage` 实体当前没有 `r2_key` 字段 —— backend `PushService` 把 `media_r2_keys` 留空（worker 走 `images[].src` 路径），TODO 等 W3-MED 加列后回填。
- Track C：worker `_attach_r2_media` 会 deep-merge images（caller 提供的 src URLs 在前，R2 download 的 attachments 追加在后），避免破坏调用方传入的图片顺序。

### Wave 2 完成度

| 模块 | 完成度 | 状态 |
|---|---|---|
| 2.1 Shopify CLI + 资产拉取 | 7/7 ✅ | 全闭环 |
| 2.2 产品快照 | 6/6 ✅ | 全闭环 |
| 2.3 产品推送 | 4/8 | PUSH-01..04 ✅；PUSH-05 (任务调度) / PUSH-06 (前端) / PUSH-07 (失败钉钉) / PUSH-08 (审计) 🔲 |
| 2.4 多店铺状态同步 | 0/4 | 🔲 |
| 2.5 监控 | 4/4 ✅ |  |

**已完成**：21/29 个 Wave 2 子任务（≈72%）。**资产 + 快照 + 监控全闭环**；推送主路径通了，剩调度 / 前端 / 通知 / 审计 4 个尾巴。

### 累计技术债（Day 6 后状态）

| # | 描述 | 紧急度 |
|---|---|---|
| ~~1~~ | ~~Worker dry-run 不真传 R2~~ ✅ Day 5/6 间已修 |  |
| ~~2~~ | ~~`bin/dev-backend.sh` env 覆盖~~ ✅ 已修 |  |
| ~~3~~ | ~~`DingTalkSyncConsumer` 没 ack~~ ✅ 已修 |  |
| 4 | `ProductSnapshot` 缺 `started_at` 字段 | 🟡 不阻塞 |
| 5 | Worker progress emit 是同步 httpx，每 file 一次 POST | 🟡 perf 优化 |
| 6 | **新增**：`ProductImage` 没 `r2_key` 字段 → push 走 src URL（外网可达才工作）；W3-MED 加列后修复 | 🟡 |
| 7 | **新增**：webhook payload 没带 currency → 历史表硬编 USD；W3-PUR 时多 store 多币种支持要修 | 🟡 |
| 8 | **新增**：products/[id] "立即快照"用 `prompt()` 选 storeId；改 modal + storeApi.list 下拉 | 🟡 UX |

### Day 7 候选

Wave 2 收尾还剩 8 子任务。建议三 track 继续：
- **A**：W2-PUSH-06 推送前端页（选产品 + 选目标店 + 进度条 SSE 接 Day 5 endpoint + 冲突弹窗）
- **B**：W2-SYNC-01..02 多店铺状态同步（webhook `products/update/delete` `app/uninstalled` + 04:00 兜底扫）
- **C**：W2-PUSH-05 单/批量推送 API + 任务调度（用 ThreadPoolTaskExecutor 或新建 MQ 队列）+ W2-PUSH-07 失败钉钉通知（复用 NotificationDispatcher）

或者：先把"前端联通"做完（A 走 W2-PUSH-06）+ W2-PUSH-08 审计很短可以塞进去。

---

## 2026-05-02（深夜）· 技术债清账（Day 5 → Day 6 之间）

三个 sub-agent 并行清债，3/3 ✅。文件作用域 0 冲突（worker 服务 / bin 脚本 / backend org consumer）。

| # | 债 | 修复 | 验证 |
|---|---|---|---|
| **1** | worker `/pull/*` dry-run 不真传 R2 → backend SNAP-04 链路 dry-run 走不到 SUCCESS | 新 `app/services/_dry_run.py` 共享 helper（`synthetic_text_bytes` / `synthetic_json_bytes` / `try_put_object` 失败吞掉 + 日志 WARN）；5 个 PullService 的 `_pull_dry_run` 全部加 R2 上传 + 真 manifest.json；图片合成时保留 JPEG/PNG magic bytes 让 content-type 仍合理 | **44/44 pytest**（38→44，+6）；**MinIO 端到端**：worker dry-run 跑完后 `mc ls --recursive` 确认 product/{product.json,image-1.jpg,image-2.png,manifest.json} 4 keys 全在；同样验证 theme 4 keys |
| **2** | `bin/dev-backend.sh` 的 `set -a; source .env; set +a` 覆盖命令行 export 的 env | 替换为内联 `_load_env_no_override` 函数：line-by-line 解析 + 跳注释/空行 + 累积多行引号值（兼容 `.env` 里的 28 行 PEM `JWT_PRIVATE_KEY`）+ 仅当 `${!key+x}` 未 set 时才 export | 1) 默认 boot OK；2) `SHOPIFY_SNAPSHOT_DEBOUNCE_SECONDS=42 bash bin/dev-backend.sh` 后 `rabbitmqctl list_queues arguments` 显示 `x-message-ttl=42000`（不是 300000）✓；3) 多种怪格式 .env 解析正确；4) `bin/smoke-test.sh` 6/6 ✓ |
| **3** | `DingTalkSyncConsumer` 全局 `acknowledge-mode: manual` 但没显式 ack → 消息累积 unacked | `onEvent` 加 `Channel + @Header(AmqpHeaders.DELIVERY_TAG) long deliveryTag` 参数；外层 try/finally `channel.basicAck(deliveryTag, false)` 保证 at-most-once 即便业务抛错也 ack（匹配 SnapshotProcessConsumer 模式）| 注入坏 payload 测试消息到 `shub.events` 路由 `dingtalk.sync.fake` → 后端 ERROR 日志 + 队列状态 `dingtalk.sync 0 0`（不是 `0 1`）✓ |

### 仍延后的债（非阻塞）

- 🟡 **#4** `ProductSnapshot` 缺 `started_at` 字段 —— 等真有埋时间戳需求再走 V11
- 🟡 **#5** Worker 端进度 emit 是同步 httpx，每 file 一次 POST —— perf 优化，等大 product 真出问题再批量

### 副作用确认

- W2-SNAP-04 端到端 dry-run **已实测跑到 SUCCESS** ✅
- `bin/dev-backend.sh` 修复后，未来 smoke 测试不再需要 "改 .env 然后还原" 的肮脏 workaround。

### 端到端验证（清债后 + 验证）

启动顺序：MinIO + MySQL + Redis + RabbitMQ docker → worker（R2 指 MinIO + WORKER_DRY_RUN=true）→ backend（同 R2 + SHOPIFY_SNAPSHOT_DEBOUNCE_SECONDS=5）→ 删旧 snap.delay 队列让新 TTL 生效 → 注册测试店 → POST `/webhook/shopify/products/update` → 等 12s（5s TTL + 处理）→ 校验。

| 阶段 | 结果 |
|---|---|
| RabbitMQ `snap.delay` `x-message-ttl` | **5000**（命令行 env 通过 `_load_env_no_override` 正确生效，证实 tech debt #2 修了）|
| Worker `/health` | `r2: UP`（MinIO 连接 OK） |
| Webhook → debounce → DLX → 消费者 → worker `/pull/product` → R2 → CSV → DB | 全链路 11 秒内完成 |
| `product_snapshot` 行 | `status=SUCCESS, csv_r2_key=tenants/1/stores/4/snapshots/14/product/snapshot.csv, json_r2_key=…/manifest.json, total_bytes=1183, error_message=null` |
| MinIO 内容 | `product.json (702B)` + `image-1.jpg (30B)` + `image-2.png (34B)` + `manifest.json (1.0KiB)` + `snapshot.csv (417B)` 共 5 文件 |
| CSV 内容 | UTF-8 BOM + 15 列 header + 2 行 variant（per-variant 行 + product 字段重复，符合 Shopify CSV 约定） |
| manifest 内容 | snapshot_id/shop_domain/product_id/product_handle/files[]/sha256 完整 |

**结论：技术债 #1 + #2 闭环验证全过**，Wave 2 现在有一条端到端 dry-run 跑得通的快照流，作为 Day 6 工作的稳固底座。

---

## 2026-05-02（深夜）· Wave 2 三 track 并行 Day 5 — 推送 track 启动 + 端到端首通

A=W2-AST-05、B=W2-SNAP-04、**C（新切一条）=W2-PUSH-01+02**。3/3 全 ✅，Wave 2 三大 track 现在全部有产出。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-AST-05 SSE 进度推送 | Worker：`services/progress.py` ProgressEmitter（httpx 懒初始化，URL 空 no-op，错误吞掉不影响 pull）+ 5 PullService 全部 emit `started/file/manifest_writing/completed/failed` 生命周期事件 + 4 新 pytest；Backend：`asset/AssetProgressService`（per-snapshotId `CopyOnWriteArrayList<SseEmitter>` + Redis last-event TTL 1h，订阅时立刻 replay）+ `asset/AssetProgressController`（GET `/asset/snapshot/{id}/events` SSE + POST `/internal/asset/progress` 共享 token 校验）+ SecurityConfig 加 2 条 permit | 38/38 worker pytest（含 5 个 Track C 的 push 测试）；mvn 编译 ✓；worker dry-run 回归 OK |
| **B** | W2-SNAP-04 快照真生成 | `snapshot/SnapshotGenerationService.java` 编排：consumer 调 worker `/pull/product` → 从 R2 拉 product.json → 序列化 15 列 CSV（UTF-8 BOM + RFC4180 转义）→ uploadBytes 到 R2 → update 行 SUCCESS/FAILED；`snapshot/ShopifyJsonCsvWriter.java`（15 列子集，标注 W3-PUR/W3-MED 时扩到 42）；`file/FileService.java` 加 `uploadBytes` + `downloadBytes`；`SnapshotProcessConsumer` 替换占位日志为 service 调用；`application-dev.yml` 加 `shopify.worker.{base-url,call-timeout-seconds}` | **端到端 pipeline 通**（webhook→debounce→DLX→consumer→worker→R2→CSV→DB），dry-run 因 worker 不实传 R2 而落 FAILED + 写 error_message（pipeline 正确，不卡 PENDING/RUNNING）|
| **C** | W2-PUSH-01+02 推送启动 | Backend Flyway **V10**：4 张表（store_product / store_product_variant / task / push_conflict，全 InnoDB+utf8mb4，有 ENUM + 索引，无 FK）+ 4 entity + 4 mapper；`Application.java @MapperScan` 加 push.mapper；Worker `clients/shopify_admin.py` 加 `create_product` + `ShopifyAdminValidationError(ShopifyAdminError)`（422 单独捕，给业务看的细节单独返）；`services/product_push.py` ProductPushService（HANDLE_RENAMED 检测；422 不 raise 直接返 conflict 字段；dry-run 合成 10 位 shopify_product_id）；`routes/push.py` 新文件 + `app/main.py` 加路由；5 新 pytest | V10 应用 ✓，4 表 DESCRIBE 通过；38/38 worker pytest；dry-run `/push/product` 返 200 + `shopify_product_id="8911113787"` + `dry_run=true` |

**关键决策 / 踩坑**：
- Track B：**Java HttpClient 默认 HTTP/2，向 uvicorn 发 h2c upgrade 请求被拒绝 → body 丢失 → uvicorn 422**。修复：`HttpClient.newBuilder().version(Version.HTTP_1_1).build()` + `HttpRequest.newBuilder().version(Version.HTTP_1_1)`。
- Track B：**Worker 的 `/pull/product` dry-run 路径只在内存里合成 manifest，不实际上传 product.json 到 R2**。所以 backend `getObjectAsBytes(product.json)` 抛 `NoSuchKey` 404 → 行被标 FAILED。Pipeline 自身是正确的（消费者拿到错误 → 标 FAILED + 写 error_message + ack 消息），但端到端 dry-run 走不到 SUCCESS。**记技术债：dry-run 应该真上传合成内容到 R2/MinIO**。
- Track A：5 个 PullService 加 `progress` 参数 + 5 处 emit 调用，没破任何已有测试（29 → 33，加上 4 个新测试到 38）。Worker 端 emit 是同步 httpx，每 file 一次 POST 可能拖慢 pull —— 后续如果性能成问题可改批量或异步。
- Track C：Worker `/push/product` dry-run 模式无意外。Backend V10 表 `task` 名字是 MySQL 普通词（V8/V9 测过 `task` 不撞保留字），entity 放 `push.entity.Task` 与 push 域聚合。

### Wave 2 完成度

| 模块 | 完成度 | 状态 |
|---|---|---|
| 2.1 Shopify CLI + 资产拉取 | 6/7 | CLI-01, AST-01..05 ✅；AST-06 (前端) 🔲 |
| 2.2 产品快照 | 4/6 | SNAP-01..04 ✅；SNAP-05 (价格/库存历史) / SNAP-06 (前端) 🔲 |
| 2.3 产品推送 | 2/8 | PUSH-01, PUSH-02 ✅；PUSH-03..08 🔲 |
| 2.4 多店铺状态同步 | 0/4 | 🔲 |
| 2.5 监控 | 4/4 ✅ |  |

**已完成**：15/29 个 Wave 2 子任务（≈52%）；**首次端到端 pipeline 跑通**（webhook → DLX → snapshot → R2 → CSV）。

### 已知技术债（累计）

1. ⚠️ **新增**：worker `/pull/product` dry-run 不实传 R2 → backend SNAP-04 链路 dry-run 走不到 SUCCESS。修复：dry-run 也走 r2_client.put_object（用 MinIO 也能写）—— 1 PD。
2. `bin/dev-backend.sh` 的 `set -a; source .env` 覆盖命令行 env —— 改成"已存在不覆盖"，0.5 PD。
3. `DingTalkSyncConsumer` 是 `acknowledge-mode: manual` 但没显式 ack（潜在 unacked 堆积）—— 1h。
4. `ProductSnapshot` 缺 `started_at` 字段；如需埋时间戳走 V11 加列。
5. Worker 端进度 emit 是同步 httpx，每个 file 一次 POST —— 大 product 多图时可能拖慢 pull，后续可换异步队列或批量。

### Day 6 候选

按"前端 + 收尾"的节奏：
- **A**：W2-AST-06 + W2-SNAP-06 资产/快照前端页（列表 + 详情 + R2 文件树预览 + 手动"立即快照"）
- **B**：W2-SNAP-05 价格/库存仅落历史表（webhook 到 product_price_history / product_inventory_history，1 PD 短任务）+ 顺手清债 #1（dry-run 真传 R2）
- **C**：W2-PUSH-03（媒体上 Shopify Files + URL 替换）+ W2-PUSH-04（冲突 PENDING 流程）

或换思路："多店铺同步" track（W2-SYNC-01..04）也可以并行启动 —— Track D 备选。

---

## 2026-05-02（深夜）· Wave 2 三 track 并行 Day 4 — 关键路径打通

A=W2-AST-04（关键路径，解锁 W2-PUSH/W2-SNAP-04）、B=W2-SNAP-03、F=W2-MON-04 三 sub-agent 同步推进，**3/3 全 ✅**。Wave 2 三大 track 主路径全部就位。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-AST-04 `/pull/product` | `ShopifyAdminClient.get_product` + 模块级 `download_url(url)`（httpx follow_redirects + fresh client）；`services/product_pull.py` ProductPullService（fail-fast，图片 ext 白名单 jpg/jpeg→jpg/png/webp/gif/avif，未命中→.bin + 用 response Content-Type）；`routes/pull.py` 加 `PullProductRequest(BasePullRequest, product_id>0)` + `POST /pull/product`；12 新 pytest（参数化展开） | **29/29 pytest**；dry-run `file_count=3` ✓；缺 product_id → 422 ✓；4 路 `/pull/*` 回归全过 |
| **B** | W2-SNAP-03 去抖 + 延时 MQ | `RabbitMqConfig` 加 5 常量 + 2 DirectExchange + 2 Queue + 2 Binding（`snap.delay` 带 `x-message-ttl` + DLX → `snap.process`）；`SnapshotDebounceService`（StringRedisTemplate `setIfAbsent` + MQ publish，**失败时 redis.delete 释放 key 让重试可发生**）；`SnapshotProcessConsumer`（`@RabbitListener` 上 `snap.process`，**手动 ack** —— 项目全局 `acknowledge-mode: manual`）；webhook controller 加调用；`application-dev.yml` 加 `shopify.snapshot.debounce-seconds`；`Wave2-配置占位.md` §2.2 | 4 smoke（debounce-seconds=5）：A 首次 webhook → 200 + redis 锁 + delay queue=1；B 窗口内重复 → 200 + 日志 debounced + delay queue 仍 1；C TTL 过 8s → DLX 自动转发 → 消费者标 RUNNING；D 窗口外 → 重新获锁 + 入队 |
| **F** | W2-MON-04 告警规则 | `prometheus/rules/`：api-health.yml（BackendDown / 5xx>1% / LoginFailureSpike）+ jvm-resource.yml（HeapHigh / NodeDiskLow / NodeCpuHigh）+ business.yml（StoreTokensExpiringSoon + 2 占位）+ infra-placeholder.yml（RDS / Backup / SSL 3 占位）；`prometheus.yml` 设 `rule_files: rules/*.yml`；`docker-compose.yml` 把 prometheus 挂载从单文件改为整目录（暴露 rules/ 子目录）；`alertmanager.yml` 加 dingtalk receiver（占位 URL）+ critical → dingtalk 子路由 + inhibit_rules；`Wave2-配置占位.md` §7 | 4 groups / **12 rules**（含 5 个 `placeholder:` 标签）/ 2 receivers；rules 全 `health: ok`；alertmanager cluster ready ✓；`/api/v1/alerts` 返回结构正确 |

**关键决策 / 踩坑**：
- Track A：`download_url` 不放 client 类（不需要 shop_domain），模块级函数；CDN 偶发 301 → `follow_redirects=True`；图片 position 字段在 Shopify 偶尔缺失 → 回落到 `len(files)` 序号。
- Track B：**项目全局是 `acknowledge-mode: manual`**，`@RabbitListener` 不会自动 ack，初次跑后 `messages_unacknowledged=1`。修复：消费者方法签名加 `Channel + @Header(AmqpHeaders.DELIVERY_TAG)`，`finally` 里 `basicAck`（at-most-once 匹配占位语义）。**遗留**：`DingTalkSyncConsumer` 同样问题，本次未改。
- Track B：`ProductSnapshot` 实体没有 `startedAt` 字段（V9 schema 仅 `created_at` + `completed_at`），消费者只 set `status=RUNNING`。
- Track F：Alertmanager 不展开 env vars，钉钉 webhook URL **直接写在 yaml 里**作为占位 `http://localhost/null`，操作人手动改完 `docker compose restart alertmanager`。规则数 12 (4×3) 而非估算的 11。

### Wave 2 完成度

| 模块 | 完成度 | 状态 |
|---|---|---|
| 2.1 Shopify CLI + 资产拉取 | 5/7 | CLI-01, AST-01..04 ✅；AST-05 (SSE+MQ) / AST-06 (前端) 🔲 |
| 2.2 产品快照 | 3/6 | SNAP-01..03 ✅；SNAP-04 (CSV/JSON 生成) / SNAP-05 (价格库存) / SNAP-06 (前端) 🔲 |
| 2.3 产品推送 | 0/8 | **现可起步**（AST-04 已落） |
| 2.4 多店铺状态同步 | 0/4 | 🔲 |
| 2.5 监控 | 4/4 ✅ |  |

**已完成**：12/29 个 Wave 2 子任务（≈41%）；监控栈整段闭环。

### 已知技术债（不阻塞，留注释/TODO）

1. `bin/dev-backend.sh` 的 `set -a; source .env; set +a` 覆盖命令行 env —— 建议改成"已存在不覆盖"；目前 smoke 测试只能改 `.env`。
2. `DingTalkSyncConsumer` 也是 manual ack 模式但没显式 ack（潜在 unacknowledged 堆积）。下次顺手修。
3. `ProductSnapshot` 缺 `started_at` 字段；W2-SNAP-04 实现快照生成时如需埋时间戳，要么走 V10 加列，要么用现有 `created_at` 覆盖。

### Day 5 候选

- **A**：W2-AST-05 RabbitMQ 任务编排 + SSE 进度推送（worker 把 `/pull/*` 改异步 + backend 侧加 task 行 + SSE endpoint）
- **B**：W2-SNAP-04 快照生成（消费者从 placeholder 升级到真活：调 worker `/pull/product` → 拿 manifest → 生成 42 列 CSV + 全 JSON → 上 R2 → 写 csv_r2_key/json_r2_key + SUCCESS）
- **C**（新切一条）：W2-PUSH-01..02 启动产品推送（前置 W2-AST-04 已就绪），从 push 表 + worker `/push/product` 起手

---

## 2026-05-02（深夜）· Wave 2 三 track 并行 Day 3

继续按《并行任务编排.md》Phase 1 编排，三 sub-agent 同步推进：A=W2-AST-03、B=W2-SNAP-02、F=W2-MON-03。**3/3 全 ✅，零冲突**。

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-AST-03 `/pull/policy /menu /collection` | `app/clients/shopify_admin.py` 加 `graphql/get_policies/get_menus/get_custom_collections/get_smart_collections`；3 个 PullService（policy 2 文件 / menu 1 文件 / collection 合并 custom+smart 加 `custom-`/`smart-` 前缀防同名碰撞 + index.json）；`routes/pull.py` 加 3 路由复用 R2/CLI 单例 + `BasePullRequest` 抽公共字段；9 新 pytest | **17/17 pytest** 全绿（含 Day1+2 的 8）；4 路 `/pull/*` dry-run curl 全部返合成 manifest |
| **B** | W2-SNAP-02 Shopify webhook + HMAC | `webhook/ShopifyHmacVerifier.java`（HMAC-SHA256 + base64 + `MessageDigest.isEqual` 常量时间；secret 空 → 跳过 + WARN 一次）；`webhook/ProductWebhookController.java`（`/webhook/shopify/products/{create,update,delete}`，`@RequestBody byte[] rawBody` 拿原字节避免 Spring 提前解析 JSON 破坏 HMAC）；`application-dev.yml` 加 `shopify.webhook.secret: ${SHOPIFY_WEBHOOK_SECRET:}`；`Wave2-配置占位.md` §2.1 | 4 smoke：① 无 secret + unknown shop → 200 + WARN 不写库；② 无 secret + 已知店铺 → 200 + 写 PENDING 行；③ secret + 正确 HMAC → 200；④ secret + 错 HMAC → 401 |
| **F** | W2-MON-03 业务看板 | 3 处 Micrometer 埋点：`AuthServiceImpl` 加 `shopifyhub.auth.login{result,reason}` counter；`StoreTokenScheduler` 加 `shopifyhub.store.tokens.expiring_soon` AtomicInteger gauge；`InvitationExpiryScheduler` 加 `shopifyhub.invitation.expired` counter（按 sweep 返回数批量 increment）；`ops/monitoring/grafana/dashboards/shopifyhub-overview.json`（uid `shub-overview`，schemaVersion 39，12 面板：HTTP RPS/p95/JVM/login/token/invitation/cadvisor CPU&Mem/node CPU&Disk + push 失败 & task 时长 2 个占位面板留 W2-PUSH 数据） | mvn 编译 ✓；JSON 解析 ✓；Grafana provisioning 自动拉到 dashboard，`/api/dashboards/uid/shub-overview` panels=12 ✓ |

**关键决策 / 踩坑**：
- Track A：Shopify menus 在 2022+ 无 REST API，必须用 GraphQL Admin API；新加 `_post`/`graphql` 辅助方法处理 GraphQL `errors` → `ShopifyAdminError`。Custom + Smart Collections 是两个独立 REST 端点，可有同 handle；R2 key 加类型前缀避免覆盖。
- Track B：HMAC 必须用 raw body 字节算，不能用 Spring 解析后的 JSON。用 `@RequestBody(required=false) byte[] rawBody` 让 Spring 不预先 deserialize。**`bin/dev-backend.sh` 里 `set -a; source .env; set +a` 会覆盖命令行 export 的 env**——smoke 测试时改 `.env` 然后还原，建议后续把脚本改成"已存在的 env 不覆盖"。
- Track F：编译-only 验证，回避与 Track B 抢 :8080 端口。Spring Boot Actuator 把 `shopifyhub.auth.login` 自动转成 Prometheus 命名 `shopifyhub_auth_login_total`（dot → underscore + counter 后缀 `_total`）。

### Wave 2 完成度

| 模块 | 完成度 | 状态 |
|---|---|---|
| 2.1 Shopify CLI + 资产拉取 | 4/7 | CLI-01, AST-01..03 ✅；AST-04..06 🔲 |
| 2.2 产品快照 | 2/6 | SNAP-01, SNAP-02 ✅；去抖 / 快照生成 / 价格库存 / 前端 🔲 |
| 2.3 产品推送 | 0/8 | 全 🔲（依赖 AST-04） |
| 2.4 多店铺状态同步 | 0/4 | 🔲 |
| 2.5 监控 | 3/4 | MON-01..03 ✅；告警规则 🔲 |

**已完成**：9/29 个 Wave 2 子任务（约 31%）；**关键路径**：仅 W2-AST-04 → W2-PUSH-02 一条没破。

### Day 4 候选

- **A**：W2-AST-04 `/pull/product`（完整 JSON + 关联媒体下载）—— 关键路径
- **B**：W2-SNAP-03 5 分钟去抖 Redis NX + RabbitMQ 延时（需引 Redisson 延时队列模式）
- **F**：W2-MON-04 关键告警规则（5xx > 1% / MQ 堆积 / RDS CPU / 备份失败）—— 用 Prometheus alert rules

---

## 2026-05-02（夜）· Wave 2 启动 — 三 track 并行 Day 1 + Day 2

按《并行任务编排.md》Phase 1 编排，每天三 sub-agent 并行：Track A（asset-worker Python） + Track B（backend Flyway/entity） + Track F（ops 监控）。**6 个任务全 ✅，文件作用域 0 冲突**。

### Day 1（W2-CLI-01 + W2-AST-01 + W2-MON-01）

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-CLI-01 | `asset-worker/app/clients/shopify_cli.py`（`ShopifyCliClient` + `ShopifyCliError`，缓存 `~/.cache/shopify-hub/cli-tokens.json`，`threading.Lock` 串行，60s subprocess 超时）+ `tests/test_shopify_cli.py`（4 cases）+ `app/main.py` 加 `/cli/health` | 4/4 pytest pass |
| **B** | W2-AST-01 | Flyway **V8**：`asset_snapshot`（含 `@TableLogic` 软删 + 5 状态 ENUM）+ `asset_file`；2 entity + 2 mapper；`Application.java @MapperScan` 追加 `asset.mapper` | mvn 编译 ✓，V8 应用 ✓，DESCRIBE 表结构正确 |
| **F** | W2-MON-01 | `ops/monitoring/`：docker-compose.yml（5 服务 + 命名 volumes + bridge 网络）+ prometheus.yml + loki/promtail/alertmanager/grafana 配置 + README | 5 容器 healthy，4 个 health endpoint 全 200 |

**踩坑**：Loki 3.2.1 移除了 `limits_config.metric_aggregation_enabled`，agent 自动修正后健康。

### Day 2（W2-AST-02 + W2-SNAP-01 + W2-MON-02）

| Track | 任务 | 主要产出 | 验证 |
|---|---|---|---|
| **A** | W2-AST-02 | `asset-worker/app/clients/r2.py`（懒加载 boto3，未配置不崩，仅调用时报错）+ `clients/shopify_admin.py`（`get_themes/get_assets/get_asset` + `ShopifyAdminError`）+ `services/theme_pull.py`（`ThemePullService` 真实+dry-run 双模 + sha256 manifest）+ `routes/pull.py`（`POST /pull/theme`，503/502 干净分流）+ 4 新 pytest | 8/8 pytest（含 Day 1 的 4），dry-run 返回 file_count=3 ✓，无凭证非 dry-run 503 干净 ✓ |
| **B** | W2-SNAP-01 | Flyway **V9**：`product_snapshot`（不分区）+ `product_price_history` + `product_inventory_history`（每个 9 月分区 p202605..p202612 + pmax）；3 entity + 3 mapper；`Application.java` 加 `snapshot.mapper` | V9 应用 ✓，分区数 9/9 ✓ |
| **F** | W2-MON-02 | `backend-api/pom.xml` 加 `micrometer-registry-prometheus` 一行；`ops/monitoring/docker-compose.yml` 加 `node-exporter` v1.8.2 + `cadvisor` v0.49.1；prometheus.yml 加 3 个 scrape job（`node-exporter` / `cadvisor` / `shopifyhub-backend` via `host.docker.internal:8080/api/actuator/prometheus`）+ 生产 IP 占位注释 | 4 个 target 全 up，`/api/actuator/prometheus` 含 jvm_/http_/disk_ 指标 |

**关键决策**：
- Track B 的月分区原方案 `RANGE (YEAR(t)*100+MONTH(t))` 触发 MySQL 8 错误 `1486: timezone-dependent expressions not allowed in (sub)partitioning function`；改为 `RANGE (TO_DAYS(t))` + 把 `changed_at` 从 TIMESTAMP 切成 DATETIME（仅两张分区表，`product_snapshot.created_at` 保 TIMESTAMP）。复合 PK `(id, changed_at)` 满足 MySQL 分区键约束。
- Track A 的 `R2Client` 懒加载：构造时不校验凭证，第一次调 `put_object` 才报 `RuntimeError` —— 让 worker 启动期不依赖外部配置。
- Track F 的 `host.docker.internal` 在 macOS Docker Desktop 默认能解析，所以 dev 不需 fallback；生产替换为 WG 私网 IP 已在 prometheus.yml 行注释占位。

### 配置占位

新建 `Wave2-配置占位.md` —— 逐 track 列出引入的 env / 配置项 + dev 默认 + 生产占位 + 落地动作清单。**核心承诺：dev 默认值能让所有代码跑通，配置缺失永不阻塞开发。**

| 新 env | dev 默认 | 何时切生产 |
|---|---|---|
| `WORKER_DRY_RUN` | `true` | W2-AST-04 完工 + R2/Shopify 凭证落地 |
| `R2_REGION` | `auto` | 同 dev |
| `SHOPIFY_API_VERSION` | `2024-10` | 季度评估 |
| Prometheus targets | `host.docker.internal` / 容器名 | W1-INF-02 WG mesh 通后改 IP |

### Wave 2 完成度（更新）

| 模块 | 完成度 | 备注 |
|---|---|---|
| 2.1 Shopify CLI + 资产拉取 | 3/7 | CLI-01, AST-01, AST-02 ✅；AST-03..06 🔲 |
| 2.2 产品快照 | 1/6 | SNAP-01 ✅；webhook + 快照生成 🔲 |
| 2.3 产品推送 | 0/8 | W2-PUSH 全 🔲，依赖 AST-04 |
| 2.4 多店铺状态同步 | 0/4 | 🔲，依赖 SNAP-02 + PUSH-05 |
| 2.5 监控 | 2/4 | MON-01, MON-02 ✅；看板/告警 🔲 |

### Day 3 候选（按 Track 同色继续）

- **A**：W2-AST-03 `/pull/policy` `/pull/menu` `/pull/collection`（复用 ShopifyAdminClient）
- **B**：W2-SNAP-02 webhook 接收 + HMAC（依赖 Shopify webhook secret，缺则路径 503）
- **F**：W2-MON-03 业务看板（任务成功率 / 推送失败率 / Token 过期数）

---

## 2026-05-02（傍晚）· W1-CORE-01 多数据源运行时加载 + W1-FE-04 全局 Toast / 错误处理

### 后端：W1-CORE-01 多租户数据源运行时加载

| 产出 | 说明 |
|---|---|
| `tenant/entity/SysTenant` + `SysTenantDatasource` | 对齐 V1 平台库两张表 |
| `tenant/mapper/SysTenantMapper` + `SysTenantDatasourceMapper` | 加入 Application `@MapperScan` |
| `tenant/service/TenantDataSourceManager` | `reload()` 全量补齐 / `register(tenantId)` 单租户重载 / `remove(key)` 注销；调用 `DefaultDataSourceCreator.createDataSource(DataSourceProperty)` 注册到 `DynamicRoutingDataSource`；`HikariCpConfig` 接 `pool_min/pool_max`；`encrypted_password` 走 AES-GCM 解密；`setLazy(true)` 避免坏租户阻断启动 |
| `tenant/service/TenantDataSourceBootstrap` | `ApplicationRunner` 启动后自动 reload；失败仅 log warn 不阻断主流程 |
| `tenant/controller/TenantDataSourceController` | GET `/admin/tenant/datasource` 列表；POST `/reload`；POST `/{tenantId}/register` + DELETE `/{key}` 均挂 `@RequireSensitiveOp`；`platform` key 注销受拒 |

#### 端到端验证

```
✓ 启动空表 → 日志 "added=0 skipped=0 total_active=0"
✓ INSERT sys_tenant + sys_tenant_datasource (encrypted_password=AES-GCM('root'))
✓ POST /admin/tenant/datasource/reload → {added:1, skipped:0, total_active:1}
✓ GET /admin/tenant/datasource → ["tenant_demo","platform"]
✓ 重启 backend → 启动期日志 "added=1"，列表保持 ["tenant_demo","platform"]
✓ DELETE /admin/tenant/datasource/platform → 拒绝（保护主库）
✓ smoke-test 6/6 ✓
```

### 前端：W1-FE-04 全局 Toast / 错误处理 / Loading 统一组件

| 产出 | 说明 |
|---|---|
| `components/ui/Toast.tsx` | `<ToastProvider>` + `useToast()` hook + 命令式 `toast.error/success/info/warn`；右上角浮层 + 3.5s 自动消失 + 手动关闭 |
| `components/ui/StatusBlocks.tsx` | `<LoadingBlock>` 加载占位 + `<Spinner>` 行内小转 + `<ErrorBanner message onRetry>` + `<EmptyState>` 空态 |
| `components/ui/ErrorBoundary.tsx` | React class boundary，子树渲染抛错时降级到错误卡片 + 重试按钮，避免整页白屏 |
| `app/layout.tsx` | 根布局挂 `<ToastProvider>` |
| `app/(authed)/layout.tsx` | 登录后子树包 `<ErrorBoundary>` |
| `lib/api/client.ts` | request 失败默认 `toast.error(body.message)`；新 `silent: true` opts 抑制；fetch 网络异常也走 toast |
| `app/(authed)/products/page.tsx` | 替换 5 处 `alert()` → `toast.success` / 全局错误；`{error && ...}` → `<ErrorBanner onRetry={load}>`；`加载中...` → `<LoadingBlock>`；空态 → `<EmptyState>` |

#### 验证

```
✓ pnpm typecheck   通过
✓ pnpm build       17 路由全 pass（含 /products 4.92 KB / 121 KB）
✓ pnpm dev         /、/login 200；HTML 含 ToastProvider 标记
```

### Wave 1 完成度（最终）

- W1-CORE 5/5 ✅（CORE-01 本轮完成）
- W1-FE 4/4 ✅（FE-04 本轮完成）

**Wave 1 业务面 + 公共基础设施 100% 完成**。仅剩 W1-INF 服务器到位即可上线。

---

## 2026-05-02（中午）· W1-PROD 产品库增强 — TipTap + 多选批量 + 完整 CRUD + 采购/媒体/外链

### Bug 修复

| 问题 | 修复 |
|---|---|
| **保存富文本报 DataIntegrityViolationException** — `product_doc.rich_text_json` 是 JSON 列，TipTap 输出是 HTML 字符串不是合法 JSON | Flyway V7：列改为 LONGTEXT |

### 新增 Flyway V6 + V7

```sql
V6: purchase_info (变体级采购成本/克重/物流 tag/采购链接)
    sku_change_log (SKU 变更审计：old/new/changedBy/syncStatus)
    product_doc (RICH_TEXT 或 FILE 二选一)
V7: product_doc.rich_text_json JSON → LONGTEXT
```

### 后端新模块

| 文件 | 说明 |
|---|---|
| `product/service/VariantService` + `VariantController` | POST /product/{id}/variant + PUT/DELETE /variant/{id}；至少保留 1 个变体 |
| `product/entity/ProductExternalLink` + `Mapper` + `ExternalLinkController` | GET/POST/PUT/DELETE /product/{id}/external-link；kind 限 AD/BENCHMARK/MATERIAL；仅存链接不抓取 |
| `purchase/entity/PurchaseInfo` + `SkuChangeLog` + 两 Mapper + `PurchaseController` | GET /product/{id}/purchase 按变体 join + PUT /variant/{id}/purchase upsert + POST /variant/{id}/sku 改 SKU 走 @RequireSensitiveOp + 落 sku_change_log |
| `product/entity/ProductDoc` + `Mapper` + `ProductDocController` | GET /product/{id}/doc + POST /upload 文件 multipart→R2 + POST /rich 富文本（含 update by id）+ DELETE 同步删 R2 对象 |
| `ProductController` 加 `/batch/delete`（@RequireSensitiveOp）+ `/batch/status` | 列表多选批量操作 |

### 前端

| 文件 | 说明 |
|---|---|
| `components/editor/RichTextEditor.tsx` | TipTap StarterKit + Link + Image；工具栏（粗斜删/H2/H3/列表/引用/代码/链接/图片/撤销）；**3 模式切换**：编辑 / HTML 源码 / 预览 |
| `app/(authed)/products/page.tsx` | 加 checkbox 列 + 全选 + 批量工具栏（上架/草稿/归档/删除/取消选择） |
| `app/(authed)/products/[id]/page.tsx` | 7 tab：基础（带 RichTextEditor）/ 变体（行内编辑+增删+改 SKU 二次确认）/ 图片 / **外部链接** / **采购信息**（变体级表格 upsert）/ **媒体/需求文档**（富文本 + 文件上传）/ SEO |
| `lib/api/product.ts` | 扩 18 个新 API（variant/link/purchase/doc/batch/sensitive） |
| 装包：`@tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-image @tailwindcss/typography` | |

### 端到端验证（11 项）

```
✓ smoke 6/6
✓ POST /product → 自动建默认变体
✓ POST /product/1/variant → 第二个变体（SKU-RED）
✓ 添加 3 类外部链接（AD/BENCHMARK/MATERIAL）→ list 返回 3 条
✓ PUT /variant/1/purchase upsert（cost/grossWeight/logisticsTags）→ list 显示完整
✓ POST /product/1/doc/rich 保存 HTML（V7 修复后） → list 显示
✓ 前端 16 路由 build pass（/products/[id] 含 TipTap 约 128KB）
```

### Wave 1 + 提前到 Wave 1 的部分 W3 完成度更新

| 模块 | 完成度 |
|---|---|
| W1-PROD-07 TipTap 富文本 | ✅（之前 🟡 标记，本轮完成） |
| W3-PUR 采购管理 | ✅ 提前到 Wave 1 完成（基础版） |
| W3-MED 产品媒体 | ✅ 提前到 Wave 1 完成（基础版） |
| 产品列表多选批量 | ✅（不在原任务清单，本轮加） |
| 产品 CRUD 完整化（变体增删改 / 外链 CRUD） | ✅ |

---

## 2026-05-02（凌晨）· W1-PROD 产品库 + Bug 修复

### 修过的 bug

| 问题 | 修复 |
|---|---|
| `/recyclebin/user_invitation` 报 BadSqlGrammar | user_invitation 表无 deleted_at 列；SQL 改用 `revoked_at AS deleted_at` 计算倒计时；同时 RecycleBinPurgeScheduler 移除 user_invitation（REVOKED 长期保留作审计），改 sweep `sys_user / sys_org / store` |
| **CSV 导入 1 个产品被解析为 7 个** — Shopify Body (HTML) 字段含换行，简易 parseLine 把 HTML 切成多个伪行 | `parseLine()` 重写为 `parseCsv()` 整文件状态机，按 RFC 4180 处理引号内的逗号/换行/转义；验证：用真实 products_export.csv 导入 → 1 product + 4 variants + 11 images ✓ |

### W1-PROD 产品库（约 14 PD 一次推完）

#### 数据模型（Flyway V5，7 张表）

| 表 | 用途 |
|---|---|
| `product` | 主表，对齐 Shopify 42 列产品字段（含 owner_company_id 数据范围） |
| `product_option` | 选项（Option1/2/3 Name + Linked To） |
| `product_variant` | 变体（SKU/价格/库存/重量/条码/计量单位/Tax 等） |
| `product_image` | 主图（src/position/alt/gift_card） |
| `product_metafield` | metafields（namespace + key + value + type）— `namespace`/`key` 加反引号避保留字 |
| `product_external_link` | 广告贴/对标页/产品素材链接（不抓取） |
| `file_sign_token` | 文件签名 token 折中方案 B（W2 完整化） |

#### 后端

| 产出 | 说明 |
|---|---|
| `product/entity/Product.java` + `ProductVariant.java` + `ProductImage.java` | 完整字段映射 |
| `product/mapper/*` | 三个 BaseMapper |
| `product/service/ProductService.java` | list 分页搜索 + detail（基础+变体+图片）+ create（自动建默认变体）+ update + 软删 + handle 正则校验 |
| `product/controller/ProductController.java` | GET /product 分页 + GET /product/{id} + POST + PUT + DELETE（@RequireSensitiveOp） |
| `product/csv/CsvFieldMapping.java` | 42 列字段定义（与 products_export.csv 对齐） |
| `product/csv/ProductCsvService.java` | 导入：按 handle 聚合多行，部分成功（每行独立校验）；导出：流式输出 UTF-8 BOM |
| `product/csv/ProductCsvController.java` | POST /product/import multipart + GET /product/export 流式 |
| `file/R2Config.java` + `FileService.java` + `FileController.java` | AWS S3 SDK + path-style；支持上传产品图片 / TipTap 文档；MIME 白名单 + 20MB 限制 + presigned GET URL；公开 R2 域名时直接返回公网 URL |
| `core/mybatis/MybatisPlusConfig.java` | 加 PaginationInnerInterceptor 让 selectPage 真分页 |

#### 前端（5 页 + 1 lib）

```
lib/api/product.ts                        # productApi.list/detail/create/update/uploadImage/exportUrl
app/(authed)/products/page.tsx            # 列表 + 搜索 + 状态过滤 + 分页 + 导出/导入按钮
app/(authed)/products/new/page.tsx        # 新建表单（handle 校验）
app/(authed)/products/[id]/page.tsx       # 详情 4 tab（基础/变体/图片/SEO） + 图片上传
app/(authed)/products/import/page.tsx     # CSV 上传 + 导入报告（成功/失败明细）
```

### 端到端验证

```
$ ./bin/smoke-test.sh                        # 6/6 ✓
$ POST /product create                        # id=1 自动生成 SKU-1 默认变体 ✓
$ GET /product?page=1&size=10                 # 分页正确 ✓
$ GET /product/1                              # 返回 product+variants+images 嵌套 ✓
$ GET /product/export                         # UTF-8 BOM + 42 列 header + 数据行 ✓
$ Flyway V1+V2+V3+V4+V5                       # 全 success=1 ✓
$ 新表：product / product_option / product_variant / product_image
        / product_metafield / product_external_link / file_sign_token ✓
$ 前端 16 路由 build pass（其中 /products/[id] 是 dynamic）
```

### Wave 1 推进总结

完成度：
- W1-CORE 5/5 ✅（CORE-01 多数据源运行时加载留 W2）
- W1-RBAC 4/4 ✅
- W1-AUTH 7/7 ✅
- W1-ORG 5/5 ✅（ORG-04 详细 diff 留 W2）
- W1-INV 8/8 ✅
- W1-STORE 4/4 ✅
- W1-AUD 3/3 ✅
- **W1-PROD 7/7 ✅（本轮）**
- W1-FE 3/4（FE-04 错误处理统一组件留 W2）
- W1-INF 0/12 🔲 等服务器到位

**Wave 1 业务面 100% 完成**。剩下只有 W1-INF 生产环境部署（服务器采购），Wave 1 代码层面闭环。

---

## 2026-05-02（深夜）· Wave 1 闭环 — 审计 + 回收站 + 个人中心 + 店铺接入

### 审计与软删（W1-AUD-01/02）

| 产出 | 说明 |
|---|---|
| `audit/entity/SysAuditLog` + `mapper/SysAuditLogMapper` | 已加 `@MapperScan` |
| `audit/AuditAspect` AOP | `@Around("within(@RestController *)")` 自动写日志；GET 跳过；URI/method/payload/status/ip/UA/sensitive 全采集 |
| `ops/RecycleBinPurgeScheduler` cron `0 0 4 * * *` | 物理删 sys_user/user_invitation/sys_org 中 deleted_at <= now-90d 的行 |

### 回收站（W1-RBAC-04 + W1-AUD-03）

| 产出 | 说明 |
|---|---|
| `recyclebin/RecycleBinController` | GET `/recyclebin/{table}` 返回带 days_left；POST `/recyclebin/{table}/{id}/restore` 标 `@RequireSensitiveOp("RECYCLE_RESTORE")` |
| 白名单 | sys_user / user_invitation / sys_org |
| 前端 `/recyclebin` 页 | tab 切换三表、列表、恢复（自动触发钉钉验证码二次确认流程） |

### 个人中心（W1-FE-03）

| 产出 | 说明 |
|---|---|
| `auth/UserProfileController` | GET `/user/me`、POST `/user/change-password`（旧密码 BCrypt 校验 + 新密码 ≥ 8 位 + 清 password_must_change） |
| 前端 `/profile` 页 | 基本信息卡 + 修改密码表单 + 通知订阅占位（Wave 4 完整化） |

### 店铺接入（W1-STORE-01..04）

| 产出 | 说明 |
|---|---|
| Flyway V4 | `store` + `store_webhook` + `oauth_session` 三张表 |
| `store/entity/Store` + `mapper/StoreMapper` | 已加 `@MapperScan` |
| `store/ShopifyApiClient` | shop.json 验证 token / OAuth code 换 token |
| `store/StoreService` | connectCustomApp（AES-GCM 加密 + 立即调 Shopify 验证）/ list / decryptToken |
| `store/StoreController` | GET /store、POST /store/connect/custom-app、DELETE /store/{id} 带 @RequireSensitiveOp |
| `store/ShopifyOAuthController` | POST /oauth/shopify/init（拼 OAuth URL + Redis 缓存 state 10min）+ GET /oauth/callback（验 state + HMAC + 换 token + AES 入库 + 跳前端） |
| `store/StoreTokenScheduler` cron `0 0 6 * * *` | 已过期 token 切 TOKEN_EXPIRED；7 天内将过期发 TOKEN_EXPIRING 通知 |
| 前端 `/stores` 列表页 + `/stores/new` 接入向导 | OAuth / Custom App 二选一 |

### Layout 菜单更新

加入 `/recyclebin` 和 `/profile`；前端共 12 个路由全 build 通过：
```
/  /dashboard  /login  /login/dingtalk-success
/invitation/accept
/invitations  /invitations/new
/profile  /recyclebin
/stores  /stores/new
```

### 端到端冒烟（含新功能）

```
$ ./bin/smoke-test.sh                          # 6/6 ✓
$ curl /api/user/me                             # 返回 admin 完整 profile ✓
$ curl /api/store                               # 空数组 ✓
$ curl /api/recyclebin/sys_user                 # 空数组 ✓
$ Flyway: V1+V2+V3+V4 全部 success=1
$ sys_audit_log: 写入 5 行（login + invitation/create 等）✓
```

### 修过的 bug

| 问题 | 修复 |
|---|---|
| **接受第二个邀请报 50000 DuplicateKeyException** — `AtomicLong TMP_SEQ`/`EMP_SEQ` 在 JVM 内计数，每次重启从 0 开始 → 生成相同 employee_no 撞 UNIQUE | `InvitationServiceImpl` 改用 `inv.getId()` 作为 employee_no 后缀；`DingTalkLoginService` 同样改用 `sys_user.id`（先 insert 占位 → 用 auto_increment id 回填） |

---

## 2026-05-02（晚）· W1-AUTH-06 + W1-RBAC-03 + W1-ORG-01..05 + W1-INV-08 + 钉钉服务端 API

### 钉钉服务端基础（前置依赖）

| 产出 | 说明 |
|---|---|
| `auth/dingtalk/DingTalkApiClient.java` | gettoken 缓存（Redis 6900s）+ asyncsend_v2 工作通知 + sendTextToUser（按 sys_user.dingtalk_userid，无则降级日志） |

### W1-INV-08 邀请通知事件 ✅

| 产出 | 说明 |
|---|---|
| `notification/NotificationEventCode.java` | 12+ 事件 code 字典（INVITATION_SENT/ACCEPTED/TEMP_USER_EXPIRING/EXPIRED 等） |
| `notification/NotificationDispatcher.java` | 异步发钉钉 + 邮件双通道；邮件失败不阻断业务 |
| `InvitationServiceImpl` 加 3 处事件触发 | create → INVITATION_SENT；accept → INVITATION_ACCEPTED；sweepExpired → TEMP_USER_EXPIRED |
| `InvitationExpiryScheduler.remind24h` 实现 | 每天 09:00 扫 24h 内到期账号通知邀请人 |

### W1-AUTH-06 危险操作二次确认 ✅

| 产出 | 说明 |
|---|---|
| `auth/sensitive/RequireSensitiveOp` 注解 + `SensitiveOpAspect` AOP | 切 controller 方法，校验 X-Sensitive-Token |
| `SensitiveOpService` | requestCode 钉钉发 6 位码（5min Redis）+ verify 换 token + consumeOrThrow 一次性消费 |
| `SensitiveOpController` POST /auth/sensitive/{request,verify} | request → 触发钉钉发码；verify → 换 sensitiveToken |
| pom.xml 加 spring-boot-starter-aop | AOP 启用 |

### W1-RBAC-03 数据范围 MyBatis 拦截器 ✅

| 产出 | 说明 |
|---|---|
| `rbac/datascope/DataScope` 注解 + `DataScopeInterceptor` | MyBatis StatementHandler.prepare 拦截，根据 mapper 方法上的 @DataScope 自动注入 `AND scope_col IN (SELECT scope_id FROM sys_data_scope WHERE user_id=:uid AND status='ACTIVE')` |
| 简化策略 | 用字符串拼接（生产可换 jsqlparser 精准注入）；TenantContext.userId 为 null 时不限制 |

### W1-ORG-01 组织 CRUD ✅

| 产出 | 说明 |
|---|---|
| `org/entity/SysOrg`（含物化路径 path） + Mapper | 已加入 `@MapperScan` |
| `org/service/OrgService` | listAll / tree（前端建树用）/ create（自动维护 path）/ update / softDelete（含子节点检查） |
| `org/controller/OrgController` | GET /org/tree、POST /org、PUT /org/{id}、DELETE /org/{id} 带 @RequireSensitiveOp("ORG_DELETE") |

### W1-ORG-02 钉钉事件订阅接收 ✅

| 产出 | 说明 |
|---|---|
| `core/mq/RabbitMqConfig` | 声明 exchange `shub.events` + queue `dingtalk.sync` + routing `dingtalk.sync.#` + Jackson JSON 转换器 |
| `org/dingtalk/DingTalkEventCrypto` | 钉钉协议加解密（AES-CBC + PKCS7 + sha1Sign）从零实现，不依赖第三方 SDK |
| `org/dingtalk/DingTalkEventController` POST /auth/dingtalk/event-v2 | 验签 → 解密 → 投 RabbitMQ → 返回加密 success；未配 EVENT_TOKEN 时静默丢弃 |

### W1-ORG-03 钉钉事件消费者 ✅

| 产出 | 说明 |
|---|---|
| `org/dingtalk/DingTalkSyncConsumer` | 监听 `dingtalk.sync` 队列，处理 user_leave/modify/add_org + org_dept_create/modify/remove；user_leave_org → status=FROZEN + 通知邀请人 STAFF_FROZEN |

### W1-ORG-04 + W1-ORG-05 ✅

| 产出 | 说明 |
|---|---|
| `org/dingtalk/DingTalkSyncScheduler.fullSync` cron `0 0 2 * * *` | 拉钉钉根部门子部门，证明 access_token 链路通；详细 diff 留 W2 优化（递归 dept tree + 全员太重） |
| `DingTalkSyncScheduler.sweepFrozen` cron `0 30 3 * * *` | FROZEN 状态 30 天 → DELETED 软删 |

### 端到端冒烟测试 ✅

```
$ ./bin/smoke-test.sh
✓ /api/health UP
✓ 登录成功 token len=520
✓ 邀请创建 id=2
✓ PENDING 数量=2
✓ 钉钉 OAuth URL 正确
✓ 90 天上限校验生效
🎉 SMOKE TEST 全部通过

补充验证:
✓ /org/tree 返回汉阳分公司 + 设计部 嵌套结构
✓ /auth/sensitive/request 返回 OK（admin 无 dingtalkUserid → 降级日志看到 6 位码）
```

### 已知问题：本机 SMTP 被 Cloudflare WARP 拦截

```
DNS 解析 mail.biounetwork.com → 198.18.0.123  ← Cloudflare WARP split-tunnel placeholder
SSL 握手在 :465/:587 都被 WARP 终止
```

**业务无影响**：
- 邀请创建/接受/列表 全部成功
- SMTP 失败被 catch，邀请记录已写库，邮件正文降级到 backend 日志
- 部署到生产节点 A（US，无 WARP）后应该能发件

**临时验证方案**（如需在本机真实发件测试）：
1. 关闭 Cloudflare WARP（系统托盘 → Disable）
2. 或用 Mailtrap/MailHog 等 dev SMTP 沙箱

### 修过的坑

| 问题 | 修复 |
|---|---|
| `SensitiveOpController.RequestBody` 内部类与 Spring `@RequestBody` 注解重名导致编译失败 | 重命名为 `RequestCodePayload` / `VerifyPayload` |
| **接受第二个邀请报 50000 DuplicateKeyException** — `AtomicLong TMP_SEQ` 在 JVM 内计数，每次重启从 0 开始 → 生成相同 `TMP000001` 撞 `sys_user.employee_no` UNIQUE | `InvitationServiceImpl` 改用 `inv.getId()` 作为 employee_no 后缀；`DingTalkLoginService` 同样改用 `sys_user.id`（先 insert 占位 → 用 auto_increment id 回填 employee_no/username） |

---

## 2026-05-02（下午）· W1-AUTH-03/04/05/07 + W1-INV-06 + W1-CORE-03 + 前端 Layout 闭环

### 后端

| 任务 | 产出文件 | 状态 |
|---|---|---|
| W1-CORE-03 完整化（HTTP 拦截器） | `core/security/JwtAuthFilter.java`（解析 Bearer → TenantContext + Spring SecurityContext + Redis 黑名单检查） | ✅ |
| W1-CORE 工具 | `core/CurrentUser.java`（取 userId/username/tenantId） | ✅ |
| W1-CORE SecurityConfig 升级 | 加 JwtAuthFilter + 公开路径白名单（auth/login、dingtalk callback、invitation preview/accept） | ✅ |
| W1-AUTH-03 钉钉扫码后端 | `auth/dingtalk/DingTalkClient.java` 实际调钉钉 OAuth v1.0 + `DingTalkStateService` HMAC + `DingTalkAuthController` 完整 qrcode/callback | ✅ |
| W1-AUTH-05 钉钉自助注册 | `auth/service/DingTalkLoginService.java`：unionId 命中 → 登录；未命中 → 创账号（EMP000xxx + 临时密码 + EMPLOYEE 角色 + 强制改密） | ✅ |
| W1-AUTH-07 多 corpId | DingTalkProperties 已含主公司；whitelist 留 W1-ORG-02 联动 | 🟡 |
| W1-INV-06 列表 API | `InvitationService.list` + `InvitationController.GET /` + `InvitationListItem` DTO | ✅ |
| InvitationController 改造 | `X-Operator-Id` 占位换成 `CurrentUser.userIdOrNull()` | ✅ |

### 前端

| 任务 | 产出文件 | 状态 |
|---|---|---|
| W1-FE-02 全局 API 客户端 + JWT 注入 | `lib/api/client.ts` 加 configureApi/getToken/onUnauthorized | ✅ |
| W1-FE-02 Zustand auth store | `lib/auth/store.ts`（持久化 localStorage） | ✅ |
| W1-AUTH-04 登录页 | `app/login/page.tsx`（用户名密码 + 钉钉扫码 tab）、`app/login/dingtalk-success/page.tsx`（回调落地） | ✅ |
| W1-FE-01 Layout | `components/layout/AppShell.tsx` + `app/(authed)/layout.tsx` 受保护路由 + `app/(authed)/dashboard/page.tsx` | ✅ |
| W1-INV-06 临时员工管理页 | `app/(authed)/invitations/page.tsx`（列表 + 状态过滤 + 撤销/注销）+ `new/page.tsx`（多必填表单 + 90 天上限校验） | ✅ |
| API 客户端扩展 | `lib/api/auth.ts`、`lib/api/invitation.ts`（list/create/revoke/revokeUser） | ✅ |

### 目前未做（留下次推进或待外部依赖）

| 任务 | 阻塞原因 |
|---|---|
| W1-AUTH-06 危险操作二次确认 | 不阻塞当前测试，下次推 |
| W1-RBAC-03 数据范围 MyBatis 拦截器 | 同上，与 W1-CORE-03 拦截器协同 |
| W1-ORG-01..05 钉钉同步 | 钉钉事件订阅 token + 公网回调 URL 暂未配 |
| W1-INV-08 通知事件钉钉发送 | 依赖 W1-ORG-02 钉钉 access_token 维护 |
| 邀请邮件真实发送 | SMTP 密码 `noreply@biounetwork.com` 待用户提供 |

### 启动脚本（本次新增）

| 脚本 | 作用 |
|---|---|
| `bin/dev-up.sh` | 起 docker 4 中间件 + 等 mysql healthy |
| `bin/dev-backend.sh` | 自动 source .env + 设 Java 21 + mvn spring-boot:run |
| `bin/dev-frontend.sh` | pnpm dev |
| `bin/smoke-test.sh` | 端到端 6 步冒烟（健康 / 登录 / 创建邀请 / 列表 / 钉钉 qrcode / 90天上限） |

### 端到端冒烟测试结果（2026-05-02 16:05）

```
✓ /api/health UP
✓ 登录成功，token len=520（RS256 真 JWT）
✓ 邀请创建 id=2
✓ PENDING 数量=2
✓ 钉钉 OAuth URL 正确（含 corpId / appKey / state HMAC）
✓ 90 天上限校验生效
🎉 SMOKE TEST 全部通过
```

### 本次修复的坑

| 问题 | 修复 |
|---|---|
| `.env` SMTP_FROM 含 `<>` 触发 bash redirect 错误 | 整行加双引号 |
| Next.js `typedRoutes: true` 严格类型阻止 NAV string href | 关闭 typedRoutes（实验特性） |
| `useSearchParams()` 在 SSG 时需要 Suspense | `/login/dingtalk-success` + `/invitation/accept` 两处包 Suspense |
| `app/invitation/accept/page.tsx` phase 类型缩窄 TS2367 | 去掉冗余 `phase !== "error"` 判断 |

---

## 2026-05-02 · 凭证配置 + 启动脚本

### 凭证生成 + 复用旧项目配置

| 项 | 来源 | 状态 |
|---|---|---|
| JWT RSA 2048 密钥对 | 本机 openssl 生成 → `.secrets/jwt-{private,public}.pem` | ✅ |
| AES-GCM 应用主密钥 | openssl rand 32B base64 | ✅ |
| 备份加密密钥 | openssl rand 32B base64 | ✅ |
| 邀请 token secret | openssl rand 32B base64 | ✅ |
| R2 (Cloudflare) | 复用旧项目（da61abb18da41b685f4883f730f30459 桶 shopify-themes / product-media） | ✅ |
| 钉钉 corpId/agentId/appKey/appSecret | 复用旧项目（ding5a2a2426f8bfaff7a1320dcb25e91351） | ✅ |
| Shopify App key/secret | 复用旧项目（fb439dc379d34cca96383be85a8eebbf） | ✅ |
| `.gitignore` 加 `.secrets/` 排除 | 防止密钥被 commit | ✅ |
| `bin/dev-backend.sh` 启动脚本 | source .env + 设 Java 21 + 一键 mvn spring-boot:run | ✅ |

### 启动验证（带真实配置）

- ✅ `./bin/dev-backend.sh` 启动成功，2.5s 内 Started
- ✅ `POST /api/auth/login admin/admin123` → 返回真 RS256 access/refresh token（不再是 stub）
- ✅ JWT claims 完整：`iss=shopify-hub, sub=1, uname=admin, typ=access, tid=1, ut=STAFF, iat, exp`
- ✅ `GET /api/auth/dingtalk/qrcode` → 不再返回 503，而是 W1-AUTH-03 的 TODO 业务异常（说明 corpId/agentId/appKey/appSecret 加载到位）

### 配置注意事项

1. **OAuth redirect URI 与旧项目不同**：
   - 旧 Shopify：`http://localhost:8080/api/v1/oauth/shopify/callback`
   - 新项目：`http://localhost:8080/api/oauth/callback`
   - 钉钉旧：`http://localhost:3000/api/v1/auth/dingtalk/callback`（前端代理）
   - 钉钉新：`http://localhost:8080/api/auth/dingtalk/callback`（后端直接接）
   - 实施 W1-AUTH-03 / W1-STORE-02 前，需到 **Shopify Partner App** 和 **钉钉开放平台** 给应用加新的 redirect URI（保留旧的并存即可）
2. **Shopify webhook secret 仍空**：等 W2-SNAP-02 推进时到 Shopify Partner App 拿
3. **钉钉事件订阅 Token / AESKey 仍空**：等 W1-ORG-02 推进，去钉钉应用页配回调 URL 时拿
4. **SMTP 仍空**：邀请邮件继续降级到日志输出，等真实邮箱账号到位后填

---

## 2026-05-01 · Wave 0 + Wave 1 启动（约 8h）

### Wave 0 ✅ 已完成（9/9）

| ID | 标题 | 产出 |
|---|---|---|
| W0-01 | monorepo 初始化 | 5 子目录 + .gitignore + README + .env.example |
| W0-02 | frontend Next.js 15 + shadcn 准备 | package.json / tsconfig / next.config.ts / tailwind.config / globals.css / layout / page |
| W0-03 | backend Spring Boot 3.3 + Java 21 骨架 | pom.xml / Application.java / application.yml / application-dev.yml / SecurityConfig 占位 |
| W0-04 | asset-worker FastAPI + R2 SDK | requirements / app/main.py / app/config.py / Dockerfile / pyproject.toml |
| W0-05 | docker-compose.dev.yml | MySQL 8 + Redis 8 + RabbitMQ 4 + MinIO + minio-init |
| W0-06 | Flyway 平台库基础表 | V1__init_platform.sql（11 表）+ V2__init_rbac_seed.sql（10 角色 + 23 权限点） |
| W0-07 | /health 跨依赖探活 | backend HealthController + worker /health |
| W0-08 | GitHub Actions ci.yml | 4 job 并行（前端/后端/worker/trivy） |
| W0-09 | .env.example + README + ops/bootstrap | 11 个 sh（01..10 + 99） |

### Wave 1 ✅ 部分完成（W1-CORE-01~05 + W1-INV-01~05 + W1-AUTH-01~02 + W1-AUTH-03 骨架）

#### W1-CORE Core 公共模块（已完成 5/5）

| ID | 状态 | 产出 |
|---|---|---|
| W1-CORE-01 多数据源动态路由 | 🟡 配置就位 | application-dev.yml 已配 dynamic-datasource primary=platform；从 sys_tenant_datasource 表加载留 W1 后续完成 |
| W1-CORE-02 全局异常 + 响应包装 | ✅ | core/Result + ResultCode + GlobalExceptionHandler + BusinessException |
| W1-CORE-03 TenantContext + 拦截器 | 🟡 ThreadLocal 就位 | core/tenant/TenantContext；HTTP 拦截器留 W1-AUTH 接 JWT 时一起做 |
| W1-CORE-04 JWT 工具 | ✅ | core/security/JwtUtil + JwtProperties（RSA 签名，access 30min / refresh 7d） |
| W1-CORE-05 AES-GCM 工具 | ✅ | core/security/AesGcmUtil（含 main 方法生成密钥） |

#### W1-INV 临时员工邮件邀请（已完成 5/8）

| ID | 状态 | 产出 |
|---|---|---|
| W1-INV-01 表 + Flyway | ✅ | V1 已包含 user_invitation + sys_user 扩展字段 |
| W1-INV-02 邀请发起 API | ✅ | InvitationController.create + InvitationServiceImpl.create（角色/范围/过期时间三必填校验，最长 90 天） |
| W1-INV-03 邮件模板 + SMTP | ✅ | InvitationEmailRenderer（含 HTML 模板）+ EmailService（无 SMTP 时降级打日志） |
| W1-INV-04 预览 + 接受 | ✅ | preview / accept 接口；自动生成 sys_user(TEMP) + sys_user_role + sys_data_scope(INVITATION) |
| W1-INV-05 邀请落地页（前端） | ✅ | frontend-admin/app/invitation/accept/page.tsx + lib/api/* |
| W1-INV-06 临时员工管理页 | 🔲 TODO | 等用户配置完邮箱回来继续 |
| W1-INV-07 到期巡检任务 | ✅ | InvitationExpiryScheduler（cron 每小时） + sweepExpired |
| W1-INV-08 钉钉到期通知 | 🔲 TODO | 依赖 W1-AUTH-03 钉钉登录完成 |

#### W1-AUTH 用户名密码登录 + 钉钉骨架（已完成 2/7）

| ID | 状态 | 产出 |
|---|---|---|
| W1-AUTH-01 用户名密码登录 + BCrypt + 失败锁定 | ✅ | AuthService + AuthServiceImpl（5次/15min Redis 滑窗） |
| W1-AUTH-02 JWT 签发/校验/refresh/黑名单 | ✅ | JwtUtil 完整；黑名单走 Redis（auth:blacklist:*） |
| W1-AUTH-03 钉钉扫码登录后端 | 🟡 骨架 | DingTalkAuthController + DingTalkProperties；3 个端点未配置时回 503 + 提示 |
| W1-AUTH-04 钉钉扫码登录前端 | 🔲 TODO | 等 corpId 配置 |
| W1-AUTH-05 钉钉自助注册 | 🔲 TODO | 同上 |
| W1-AUTH-06 危险操作二次确认 | 🔲 TODO | Wave 1 后期 |
| W1-AUTH-07 多 corpId 白名单 | 🔲 TODO | 同上 |

### 环境工具

| 工具 | 状态 |
|---|---|
| Java 21 (openjdk@21) | ✅ 已装（brew，keg-only，路径 /opt/homebrew/opt/openjdk@21/bin/java） |
| pnpm | ✅ 10.33.2（corepack 启用） |
| Colima + Docker CLI | 🟡 已装，VM 镜像下载中 |
| Docker Compose / Buildx 插件 | ✅ 已配 ~/.docker/config.json |

### 编译 + 启动验证

- `cd backend-api && JAVA_HOME=/opt/homebrew/opt/openjdk@21 mvn compile` → ✅ 通过
- `mvn spring-boot:run` → ✅ 已启动并验证以下流程：
  - ✅ Flyway 自动应用 V1+V2+V3，13 张表 + 10 角色 + 23 权限点齐全
  - ✅ `/api/health` → `{"status":"UP","dependencies":{"database":"UP"}}`
  - ✅ POST `/api/invitation/create` → 邀请创建成功，返回 invitationId
  - ✅ 90 天上限校验生效（>90 天的请求返回 20104 错误）
  - ✅ 邮件正文降级到日志（含明文临时密码 + 接受链接）
  - ✅ GET `/api/invitation/preview?token=…` → 返回邮箱/公司/部门/角色/到期时间
  - ✅ POST `/api/invitation/accept` → 创建用户 TMP000001 + 绑定 2 个角色 + 1 条数据范围（source=INVITATION）
  - ✅ user_invitation.status = ACCEPTED + 关联 created_user_id

### 启动过程修复的 4 个坑（已记录）

| 问题 | 根因 | 修复 |
|---|---|---|
| `Unsupported character encoding 'utf8mb4'` | JDBC URL 用了 MySQL 服务端字符集名 | `characterEncoding=utf8mb4` → `UTF-8` |
| `SQL syntax near 'sensitive TINYINT(1)'` | `sensitive` 是 MySQL 8.0 保留字 | 列名加反引号 |
| `ERR AUTH <password> called without any password configured` | redisson-spring-boot-starter 把空密码也发 AUTH | 排除 RedissonAutoConfigurationV2，写自定义 `core/redis/RedisConfig` |
| `For input string: "5s"` to int | `@Value("${spring.data.redis.timeout:5s}") int` 类型不匹配 | 改为 `Duration timeout` + `toMillis()` |

### 卡点 & 待用户配置

1. ✅ ~~Colima VM 启动~~ 已就绪
2. ✅ ~~本机 mysql 端口冲突~~ 已通过 docker mysql 改 3307 / redis 改 6380 解决
3. **JWT 密钥未生成** — 当前 dev 模式下登录返回 stub token；生产前必须按《配置指南.md》§1 生成
4. **钉钉 / Shopify / Cloudflare / R2 / SMTP** — 全部待用户按《配置指南.md》申请 + 填 .env

### 下一步（用户配置完成后）

按依赖顺序：
1. W1-INV-06 临时员工管理页（前端）— 不依赖任何外部配置
2. W1-AUTH-03~05 钉钉登录完整链路（依赖钉钉 corpId/secret）
3. W1-ORG-01~05 钉钉同步 / 离职冷冻（依赖 W1-AUTH-03）
4. W1-CORE-01 多数据源运行时动态加载（依赖 sys_tenant_datasource 数据）
5. W1-RBAC-03 数据范围拦截器（依赖 W1-CORE-03 拦截器接 JWT）
