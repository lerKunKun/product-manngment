# 需要修改记录

> 状态标记：✅ 已完成 / 🟡 Wave 2 待确认后启动 / 🔵 Wave 3 backlog
> Wave 1 完成日期：2026-05-03

---

## 一、原始需求清单（按提交顺序）

### A. 前端通用
1. ✅ 前端状态弹框需要添加 code+中文描述
   - 实现：`frontend-admin/lib/i18n/error-codes.ts` 22 条字典 + `formatErrorToast()` 接到 `lib/api/client.ts`；toast 现在显示 `[10001] 未登录或登录已过期`

### B. 店铺管理
2. ✅ 接入店铺授权失败
   - 实现：`ShopifyOAuthController` HMAC 校验改用全部 query 参数（除 hmac/signature 外）；`scopes` 列写入前 `encodeScopesJson()` 转 JSON 数组（fix DataIntegrityViolation）

### C. 个人中心
7. ✅ 加头像（钉钉同步，空回退默认）
   - 实现：`SysUser.avatarUrl`（V26 migration）、`/user/me` 返回 avatarUrl + 默认图 `frontend-admin/public/assets/defaults/avatar.png`（来自项目根 default-avatar.png）
8. ✅ 基本信息新增 归属公司、职位
   - 实现：`SysUser.position`（V26）+ `companyName` 通过 `default_tenant_id` 反查 `sys_org.name`
9. ✅ 钉钉绑定按钮（未绑定时显示）
   - 实现：profile 页 dingtalk 行未绑定时附「绑定钉钉」按钮，跳 `/auth/dingtalk/qrcode`

### D. 产品库
19. ✅ 列表列改造（主图 + 名称 + 价格 + 上架状态）
    - 实现：后端 `ProductService.listWithCardInfo()` 4 SQL N+1 batch（产品列表 / 主图 / 最低 variant 价 / 已上架店铺数）；新增索引 `idx_storeprod_product_status`（V27）；前端列改造 + Badge

### E. 数据库治理
22. ✅ 全表 + 字段中文 comment
    - 实现：V28 migration（849 行），覆盖 47 张表 / 544 列，原 ENUM/NOT NULL/DEFAULT/ON UPDATE/AUTO_INCREMENT 全部保留

### F. 登录态 / XSS 加固（衍生需求）
- ✅ 登录态长寿（refresh token 7d）
- ✅ XSS 加固（refresh 改 httpOnly cookie、access 仅内存、CSP、DOMPurify 净化富文本）
- ✅ 在线设备 + 一键踢出其他设备（20s 心跳）

---

## 二、🟡 Wave 2 待确认项（启动前需要拍板）

### E1. 钉钉子系统（待修复 + 待新建）

**条目**
- 6. 🟡 钉钉验证码现在无法获取
- 7. 🟡 钉钉同步组织和员工功能缺失
- 8. 🟡 添加组织时填写钉钉应用配置（不再走 env），支持多组织多套配置，自动同步角色和员工

**待确认 ❓**
- E1-Q1. **多组织化的钉钉配置存哪？**
  - 推荐：新增 `sys_org_dingtalk_config` 表，per-COMPANY 一对一（org_id PK），存 corpId / appKey / appSecret(AES) / agentId / eventToken / eventAesKey / status。后端读配置改成按 tenantId 查这张表，env 仅做 fallback。
- E1-Q2. **同步触发时机？**
  - 推荐：①「加组织 → 立即拉一次部门 + 用户」②「组织详情页有「立即同步」按钮」③「定时同步留 Wave 3」（cron 每天凌晨）
- E1-Q3. **冲突策略？**
  - 推荐：unionId 已存在则**跳过 + 写 sync_log**，避免覆盖手工调整。`sync_log` 表记录每次同步的 added/skipped/failed 数。
- E1-Q4. **「钉钉验证码无法获取」具体什么场景**？是发短信验证码？还是钉钉扫码登录？还是组织内邀请用的工作通知？需要前端复现步骤 / 后端日志才能定位。

### E2. 用户（员工）管理（缺失模块）

**条目**
- 9. 🟡 用户（员工）管理缺失

**待确认 ❓**
- E2-Q1. UI 形态：先做 **table 版 CRUD**（搜索 / 编辑 / 角色分配 / 软删 / 重置密码 / 解冻冻结）还是直接合并到 G 的 canvas？建议先 table 版，G 的 canvas 单独做。
- E2-Q2. 是否需要批量操作（批量改部门 / 批量改角色 / 批量冻结）？
- E2-Q3. 是否暴露「以该员工身份登录（impersonate）」给超管？（高敏感操作，要审计 + 强二次确认）

### F. 产品详情 Shopify 化（最大头）

**条目**
- 16. 🟡 变体和图片合并到基础 table（不做单独 table），UI 模仿 Shopify
- 17. 🟡 外部链接放在基础 table 顶部、标题之上
- 19-1. 🟡 主图 CRUD + 预览大图 + 下载 + 拖拽改顺序 + 同步导出 CSV
- 19-2. 🟡 文件同步 R2
- 19-3. 🟡 导出 CSV：选中导出 / 全量导出
- 19-4. 🟡 变体和采购信息 Option1/2/3 列名按导入 CSV 的变体属性名动态加载
- 19-5. 🟡 采购信息编辑：填选框未对齐 → 改弹窗修改
- 19-6. 🟡 上传 UI 支持拖入
- 19-7. 🟡 媒体文件预览（图片/视频/PDF 直接预览，office 下载）+ 上传必填标签（已有 / 新加）+ 备注（选填）

**待确认 ❓**
- F-Q1. **拆 sub-track**？建议：
  - **F1 详情布局重构**（变体/图片合一、外链置顶、Option 动态列）
  - **F2 文件子系统**（拖拽上传、R2 同步、媒体预览、标签必填）
  - **F3 CSV 导出选中/全量**
- F-Q2. R2 同步是「上传时直传 R2」还是「先存本地再后台 worker 上传」？现有 asset-worker 是否复用？
- F-Q3. 「同步导出 CSV」（条目 19-1 末尾）是说图片改动要立即体现到下次导出 CSV 里？还是别的意思？
- F-Q4. 视频预览要不要做缩略图？office 下载是否要做病毒扫描占位？

### G. 角色管理可视化

**条目**
- 20. 🟡 角色管理权限矩阵：权限项加中英文描述
- 21. 🟡 角色管理 + 用户管理合并做可视化 canvas

**待确认 ❓**
- G-Q1. **canvas 长什么样**？两栏拖拽（左角色右权限）还是节点图（react-flow）？预算节点数？
- G-Q2. 中英文描述：每个 permission code 配 zh + en 短句，存哪？
  - 选项 A：写死在 `sys_permission` 表 description 列（DB 改）
  - 选项 B：前端 i18n 字典（运行时改不需要 DB migration）
  - 推荐 B，理由：权限新增频率不高，字典加 key 比 SQL 改字段快
- G-Q3. 角色 + 用户合并是「同一个页面双面板」还是「同一个 canvas 同时看到两类节点 + 它们的连线」？

---

## 三、🔵 Wave 3 backlog

- 钉钉用户/部门**定时全量同步**（cron 凌晨执行；E1 先做被动触发，等稳定再加）
- 角色权限**审计**：谁在什么时间改了哪个角色的权限矩阵
- 产品详情**版本对比**（基于 product_snapshot 已有数据）
- 媒体文件**病毒扫描**（office 下载前扫描）

---

## 四、推荐下一步

按依赖度先做 **E1 + E2**（钉钉子系统 + 用户管理），它们是别的功能的底座；然后投 F；最后做 G。

回我「按推荐做」我立即下发 Wave 2 agent。
