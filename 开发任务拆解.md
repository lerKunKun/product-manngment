# Shopify Hub 开发任务拆解

> 基于《系统设计文档.md v1.3》拆解
> 拆解日期：2026-05-01
> 单位：**PD = 1 人天（理想工时 6h）**
> 任务 ID 命名：`W{Wave}-{Module}-{Seq}`

## 0. 总览

| Wave | 周数 | PD 估算 | 主要交付 |
|---|---|---|---|
| Wave 0 脚手架 | 1 周 | ~7 PD | 本地 docker-compose 跑通，CI 全绿 |
| Wave 1 MVP | 4~5 周 | ~74 PD | 部署落地 + 登录 + 组织 + 临时员工邀请 + 店铺接入 + 产品库 |
| Wave 2 资产+推送 | 3 周 | ~50 PD | 资产拉取 + 产品快照 + 产品推送 + 状态同步 |
| Wave 3 开店+采购+模板 | 4 周 | ~70 PD | 一键开店 + 预览 + 采购 + 媒体 + 模板库 |
| Wave 4 审批+通知+运维 | 2 周 | ~30 PD | 审批中心 + 通知订阅 + 邮件 + 审计归档 + 监控 + 灾备 |
| **合计** | **14~15 周** | **~231 PD** | |

**人力建议**：2 后端 + 1 前端 + 0.3 运维兼职 ≈ 3.3 FTE × 14 周 ≈ 46 人周 ≈ 230 PD（±20% buffer）。

**关键路径**（任何延期都拖整体）：
`W0 脚手架 → W1-CORE 多数据源 → W1-AUTH 钉钉登录 → W1-INV 临时员工邀请 → W1-STORE OAuth → W2-PUSH 推送 → W3-NEWSTORE 一键开店`

**并行机会**：
- 前端 W1-FE-* 与 后端 W1-AUTH/ORG/INV 可并行（mock API 先行）
- W2 资产拉取 worker 与 W2 产品推送可并行
- W3 采购 / 媒体 / 模板 三块互不依赖

---

## Wave 0 — 脚手架（~7 PD，1 人 1 周）  ✅ **已完成**

**目标**：本地 docker-compose 一键起所有服务，前后端互通，CI 全绿。

| ID | 标题 | 模块 | PD | 依赖 | 备注 |
|---|---|---|---|---|---|
| W0-01 | monorepo 仓库初始化（含分支保护、CODEOWNERS） | infra | 0.5 | — | `frontend-admin/` `backend-api/` `asset-worker/` `ops/` `.github/` 五子目录 |
| W0-02 | frontend-admin: Next.js 15 + shadcn/ui + Tailwind + TipTap 初始化 + 登录页占位 | frontend | 1 | W0-01 | `pnpm dev` 跑通，shadcn zinc 主题 |
| W0-03 | backend-api: Spring Boot 3.3 + Java 21 骨架 + Spring Security + MyBatis-Plus + Redisson + Flyway + dynamic-datasource + SpringDoc | backend | 1.5 | W0-01 | `mvn spring-boot:run` 跑通，`/api/health` 返回 200 |
| W0-04 | asset-worker: FastAPI + R2 SDK (boto3) + Shopify GraphQL 客户端骨架 | worker | 0.5 | W0-01 | `uvicorn` 跑通，`/health` 返回 200 |
| W0-05 | `docker-compose.dev.yml`：MySQL 8 + Redis 8 + RabbitMQ 4 + MinIO（R2 兼容） | ops | 0.5 | W0-01 | 一键 `docker compose up -d` |
| W0-06 | Flyway 平台库初始迁移：`sys_user / sys_org / sys_role / sys_permission / sys_tenant_datasource / dingtalk_corp` | backend | 1 | W0-03, W0-05 | V1__init_platform.sql |
| W0-07 | `/health` 跨依赖探活（DB / Redis / MQ / R2/MinIO） | backend | 0.5 | W0-06 | Spring Actuator + 自定义 Indicator |
| W0-08 | GitHub Actions `ci.yml`：lint + test + build + trivy（前端 + 后端 + worker 并行 job） | ops | 1 | W0-02, W0-03, W0-04 | PR 触发，全绿才能合 |
| W0-09 | `.env.example` + 根 README + `ops/bootstrap/` 脚本骨架（每个 sh 先打印 + dry-run） | ops | 0.5 | W0-05 | 新人 30 分钟跑起来 |

---

## Wave 1 — MVP（~74 PD，4~5 周）

### 1.1 基础设施（infra/ops，~8.5 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W1-INF-01 | 节点 A(US 4c6g) / B(HK 2c2g) / C(SG 4c8g) 服务器采购 + 基础初始化（ufw/fail2ban/SSH 改端口） | 1 | — |
| W1-INF-02 | WireGuard mesh：A/B/C 三节点私网打通；节点 A/C 的 sshd 改 listen WG 网卡 | 1 | W1-INF-01 |
| W1-INF-03 | 节点 B（HK）jump-host 配置（jump 用户 + ProxyJump + 公网 sshd 加固） | 0.5 | W1-INF-02 |
| W1-INF-04 | AWS RDS MySQL 8（us-west-2，Multi-AZ，强制 SSL）+ ElastiCache Redis 8 创建 | 1 | W1-INF-01 |
| W1-INF-05 | RDS/ElastiCache 安全组：仅放行节点 A/C WG IP；公网零暴露验证 | 0.5 | W1-INF-02, W1-INF-04 |
| W1-INF-06 | Cloudflare：DNS（admin/api/oauth/webhook/dingtalk/monitor/mail/{companyCode}）+ WAF + Bot Fight + Rate Limit | 1 | W1-INF-01 |
| W1-INF-07 | Cloudflare Access：`admin.biounetwork.com` + `monitor.biounetwork.com` 启用 SSO/IP 白名单 | 0.5 | W1-INF-06 |
| W1-INF-08 | R2 桶 + lifecycle 规则 + 凭证签发 + `cdn.biounetwork.com` 自定义域名 | 0.5 | W1-INF-06 |
| W1-INF-09 | 节点 C 注册 GitHub Actions self-hosted runner（label `prod-runner`） | 0.5 | W1-INF-01 |
| W1-INF-10 | GitHub Actions `deploy-staging.yml`（push develop 自动部署） | 1 | W1-INF-09 |
| W1-INF-11 | GitHub Actions `deploy-prod.yml`（tag `v*` 触发 + manual approval + 三步部署 A/C-bk/B） | 1 | W1-INF-10 |
| W1-INF-12 | UptimeRobot 探活配置（admin/api/oauth/webhook 4 个） | 0.5 | W1-INF-06 |

### 1.2 Core / Tenant 框架（~6.5 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-CORE-01 | 多数据源动态路由：启动时从 `sys_tenant_datasource` 加载，`@DS("platform")` + `@DS("tenant_xx")` | 2 | W0-06 | ✅ TenantDataSourceManager + Bootstrap + /admin/tenant/datasource 管理 API |
| W1-CORE-02 | 全局异常处理 + 统一响应包装（`R<T>`） | 1 | — | ✅ |
| W1-CORE-03 | TenantContext（ThreadLocal）+ 拦截器：解析 host → tenant_code → tenant_id 入栈 | 1.5 | W1-CORE-01 | ✅ JwtAuthFilter 已实现（解析 JWT 入栈 + Redis 黑名单 + Spring Security） |
| W1-CORE-04 | JWT 工具：RSA 签名、access 30min、refresh 7d、Redis 黑名单 | 1.5 | — | ✅ |
| W1-CORE-05 | AES-GCM 加密/解密工具（凭证、Token 入库） | 0.5 | — | ✅ |

### 1.3 RBAC（~4.5 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-RBAC-01 | 内置角色初始化（PLATFORM_SUPER / COMPANY_ADMIN / DEPT_LEAD / STORE_ADMIN / OPERATION / PURCHASING / DESIGNER / READONLY / EMPLOYEE / **TEMP_STAFF**） | 1 | W0-06 | ✅ V2 SQL |
| W1-RBAC-02 | 权限点定义 + `sys_role_permission` 初始化（约 30 个权限点，含 `TEMP_USER:INVITE/REVOKE/LIST`） | 1 | W1-RBAC-01 | ✅ V2 SQL（23 个权限点） |
| W1-RBAC-03 | 数据范围 MyBatis 拦截器（自动注入 WHERE：`org.path` 前缀 + `sys_data_scope` ACTIVE OR） | 2 | W1-CORE-03 | ✅ @DataScope 注解 + DataScopeInterceptor（StatementHandler） |
| W1-RBAC-04 | 回收站权限点 + 列表/恢复 API | 0.5 | W1-RBAC-02 | ✅ /recyclebin/{table} GET/POST + @RequireSensitiveOp("RECYCLE_RESTORE") |

### 1.4 Auth（~9.5 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-AUTH-01 | 用户名密码登录 + BCrypt + 5次/15min 失败锁定（Redis 滑窗） | 1.5 | W1-CORE-04 | ✅ |
| W1-AUTH-02 | JWT 签发 / 校验 / refresh / 注销（黑名单） | 1 | W1-CORE-04 | ✅ |
| W1-AUTH-03 | 钉钉扫码登录后端：拉二维码 → state HMAC → callback → corpId 白名单校验 | 2 | W1-CORE-03 | ✅ DingTalkClient + StateService + AuthController 全链路 |
| W1-AUTH-04 | 钉钉扫码登录前端：登录页二维码 + 子域识别 host → tenant | 1.5 | W1-AUTH-03 | ✅ /login + /login/dingtalk-success |
| W1-AUTH-05 | 钉钉自助注册：未命中 → 拉详情 → 创账号 → 工号自增（EMP000001） + 备用密码邮件 + EMPLOYEE 角色 | 1.5 | W1-AUTH-03 | ✅ DingTalkLoginService.autoRegister（临时密码 12 位 + EMPLOYEE） |
| W1-AUTH-06 | 危险操作二次确认：`X-Sensitive-Op` 头 + 钉钉 6 位码 + Redis 5min 过期 | 1 | W1-AUTH-03 | ✅ @RequireSensitiveOp 注解 + AOP + 6 位码（OrgController DELETE 已挂） |
| W1-AUTH-07 | 多 corpId 白名单：`dingtalk_corp.whitelist_corp_ids` 动态校验逻辑 | 1 | W1-AUTH-03 | 🟡 主公司 corpId 已配；whitelist 待 W1-ORG 联动 |

### 1.5 Org / 钉钉同步（~7.5 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-ORG-01 | `sys_org` CRUD + 物化路径 `path` 维护 + 树查询 | 1.5 | W0-06 | ✅ entity/mapper/service/controller |
| W1-ORG-02 | 节点 B `dingtalk-gateway`：接收事件订阅 webhook + HMAC 验签 + 投 RabbitMQ | 2 | W1-INF-06 | ✅ DingTalkEventCrypto + DingTalkEventController + RabbitMqConfig |
| W1-ORG-03 | 钉钉同步消费者：`user_add/modify/leave_org` + `org_dept_*` 增删改 sys_user/sys_org | 2 | W1-ORG-02 | ✅ DingTalkSyncConsumer 6 个事件处理 |
| W1-ORG-04 | 每天 02:00 全量兜底：拉钉钉部门树 + 全员 → diff → 补漏 + 落 `sys_org_dingtalk_sync_log` | 1 | W1-ORG-03 | 🟡 cron + 拉根部门验证 token 链路；详细 diff 留 W2 |
| W1-ORG-05 | 离职冷冻：`user_leave_org` → FROZEN + `frozen_at`；30 天软删；返聘自动恢复 | 1 | W1-ORG-03 | ✅ FROZEN 切换在 consumer；30 天 sweepFrozen 任务；返聘留下次 |

### 1.6 Invitation 临时员工邮件邀请（~9 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-INV-01 | `user_invitation` 表 + `sys_user.user_type/expires_at/EXPIRED` 字段 Flyway | 0.5 | W0-06 | ✅ |
| W1-INV-02 | 邀请发起 API：角色/数据范围/过期时间**三必填**校验（前后端双校验，最长 90 天） | 1.5 | W1-RBAC-02 | ✅ |
| W1-INV-03 | 邮件模板 `INVITATION` + SMTP 直连 `mail.biounetwork.com:465` 发送 | 1 | W1-INF-06 | ✅（无 SMTP 时降级打日志） |
| W1-INV-04 | 邀请预览 + 接受 API：token 校验 + 临时密码比对 + 创建 sys_user + 写 sys_user_role + sys_data_scope（source=INVITATION） | 1.5 | W1-INV-02, W1-INV-03 | ✅ |
| W1-INV-05 | 邀请落地页（前端）：`/invitation/accept?token=`，预览 + 输入临时密码 + 强制改密 | 1.5 | W1-INV-04 | ✅ |
| W1-INV-06 | 临时员工管理页（前端）：列表 / 撤销邀请 / 提前注销 / 重发 | 1.5 | W1-INV-02 | ✅ /invitations + /invitations/new（多必填表单 + 90 天上限校验） |
| W1-INV-07 | 到期巡检任务：每小时切 EXPIRED + 数据范围 REVOKED + JWT 黑名单 | 1 | W1-INV-04 | ✅ |
| W1-INV-08 | 通知事件 `INVITATION_SENT/ACCEPTED/TEMP_USER_EXPIRING/EXPIRED` + 钉钉发送 | 0.5 | W1-INV-07 | ✅ NotificationDispatcher 双通道 + 4 处事件触发点 + 24h 提醒任务 |

### 1.7 Store / OAuth（~6.5 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-STORE-01 | `store` 表 + Custom App Token 接入（用户粘贴永久 token，AES-GCM 入库） | 1.5 | W1-CORE-05 | ✅ V4 + Store entity + StoreService.connectCustomApp + Shopify shop.json 验证 |
| W1-STORE-02 | Shopify OAuth 公网回调：init + callback + state HMAC + `oauth_session` 10min | 2 | W1-INF-06 | ✅ ShopifyOAuthController init/callback + Redis state + HMAC 校验 |
| W1-STORE-03 | Token 加密入库 + 每天检查过期 + 7 天前发 `TOKEN_EXPIRING` | 1 | W1-STORE-02 | ✅ StoreTokenScheduler cron 06:00 |
| W1-STORE-04 | 店铺管理前端：列表 + 接入向导（OAuth/Custom App 二选一） + Token 状态总览 | 2 | W1-STORE-02 | ✅ /stores + /stores/new |

### 1.8 Product 产品库（~14 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-PROD-01 | `product/product_variant/product_option/product_image/product_metafield/product_external_link` 表 + Flyway | 1 | — | ✅ V5 7 张表 |
| W1-PROD-02 | 产品 CRUD API（含数据范围拦截） | 2 | W1-RBAC-03 | ✅ ProductService + Controller，分页/搜索/handle 正则校验 |
| W1-PROD-03 | CSV 42 列导入解析 + 校验报错 + 部分成功 | 2 | W1-PROD-01 | ✅ 按 handle 聚合 + 错误行报告 |
| W1-PROD-04 | CSV 42 列导出（按 owner_company 数据范围过滤） | 1 | W1-PROD-01 | ✅ /product/export 流式 UTF-8 BOM |
| W1-PROD-05 | 产品图片上传 R2 + `file_sign_token` 折中签名机制 | 2 | W1-INF-08 | ✅ FileService + R2Config（AWS S3 SDK pathstyle） + presigned GET 4h；token 表完整化留 W2 |
| W1-PROD-06 | 产品列表/详情/编辑前端（shadcn DataTable + 详情 tab） | 4 | W1-PROD-02 | ✅ /products + /products/new + /products/[id] 4 tab + /products/import |
| W1-PROD-07 | TipTap 富文本编辑器集成（描述 + 需求文档） + 图片粘贴异步上传 | 2 | W1-PROD-05 | ✅ RichTextEditor 组件含工具栏 + 3 模式（编辑/HTML/预览）+ 图片上传 R2 |
| W1-PROD-08 | 产品 CRUD 完整化（变体增删改 + 改 SKU 二次确认 + 列表多选批量 + 外部链接 CRUD） | 2 | W1-PROD-02 | ✅（不在原拆解，本轮新增） |
| W1-PROD-09 | 采购管理（变体级 cost/克重/物流 tag/采购链接 + sku_change_log）— 提前自 W3-PUR | 1.5 | W1-PROD-08 | ✅ |
| W1-PROD-10 | 产品媒体/需求文档（RICH_TEXT + FILE，FileService 同步 R2，删除时同步删 R2 对象）— 提前自 W3-MED | 1.5 | W1-PROD-05 | ✅ |

### 1.9 审计 / 软删除（~3 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-AUD-01 | 审计 AOP：所有 Controller 自动写 `sys_audit_log`，敏感操作 `sensitive=true` | 1 | W1-CORE-03 | ✅ AuditAspect @Around within RestController；GET 跳过；含 ip/UA/payload/status |
| W1-AUD-02 | 软删除框架：MyBatis 全局拦截 `deleted_at IS NULL` | 1 | — | ✅ @TableLogic + RecycleBinPurgeScheduler 90 天物理删 |
| W1-AUD-03 | 回收站前端页：列表 + 恢复 + 90 天倒计时 | 1 | W1-RBAC-04 | ✅ /recyclebin 三 tab + 恢复带钉钉二次确认 |

### 1.10 前端公共（~5 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W1-FE-01 | Layout：侧边菜单 + 顶部用户菜单 + 子域识别（host → tenant） | 2 | W0-02 | ✅ AppShell + 受保护 layout (authed) |
| W1-FE-02 | TanStack Query + Zustand 全局状态 + Axios 拦截器（JWT 自动注入 + 401 刷新） | 1 | W0-02 | 🟡 zustand + fetch 客户端已就位（JWT 自动注入 + 401 重定向 /login）；TanStack Query 暂未引入 |
| W1-FE-03 | 个人中心：修改密码 / 修改邮箱 / 通知订阅占位 | 1 | W1-FE-01 | ✅ /profile + /user/me + /user/change-password |
| W1-FE-04 | 错误处理 / Loading / Toast 统一组件 | 1 | W0-02 | ✅ ToastProvider + LoadingBlock/ErrorBanner/EmptyState + ErrorBoundary；client.ts 默认全局上报 |

---

## Wave 2 — 资产拉取 + 产品快照 + 推送（~50 PD，3 周）

### 2.1 Shopify CLI Token + 资产拉取 worker（~14 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W2-CLI-01 | Shopify CLI token 类型 + 静默刷新（subprocess `shopify store auth`） | 2 | W1-STORE-01 | ✅ ShopifyCliClient + 4 pytest |
| W2-AST-01 | `asset_snapshot` + `asset_file` 表 + R2 prefix 规范 | 0.5 | — | ✅ Flyway V8 + entity/mapper |
| W2-AST-02 | worker `/pull/theme`：列出 assets → 逐个下载 → 落 R2 → 写 manifest | 3 | W2-CLI-01 | ✅ R2Client + ShopifyAdminClient + ThemePullService（dry-run 兜底）+ 4 pytest |
| W2-AST-03 | worker `/pull/policy` `/pull/menu` `/pull/collection` | 2 | W2-AST-02 | ✅ 3 service + 3 route + 9 pytest（17/17 全绿）；menus 走 GraphQL，collections 合并 custom+smart 加前缀防撞 |
| W2-AST-04 | worker `/pull/product`：完整 JSON + 关联媒体下载 | 2 | W2-AST-02 | ✅ ProductPullService（fail-fast + 图片 ext 白名单 jpg/png/webp/gif/avif）+ download_url helper（httpx follow_redirects）+ 12 新 pytest（29/29）|
| W2-AST-05 | RabbitMQ 任务 + SSE 进度推送给前端 | 2 | W2-AST-02 | ✅ Worker ProgressEmitter（httpx 回调，URL 空 no-op）+ 5 PullService 全部埋点（started/file×N/manifest_writing/completed/failed）；Backend AssetProgressService（per-snapshot SseEmitter map + Redis last-event TTL 1h，subscribe 时立即 replay）+ AssetProgressController（GET SSE + POST internal/asset/progress 共享 token 守卫）|
| W2-AST-06 | 资产快照前端页：列表 + 详情 + R2 文件树预览 | 2.5 | W2-AST-03 | ✅ Backend GET /asset-snapshot 列表 + 详情 + manifest 代理 + Frontend /assets + /assets/[id] |

### 2.2 产品快照（~8 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W2-SNAP-01 | `product_snapshot` `product_price_history` `product_inventory_history` 表 + 月分区 | 1 | — | ✅ Flyway V9 + 3 entity + 3 mapper（TO_DAYS 分区，每史表 9 个 partition） |
| W2-SNAP-02 | Webhook `products/update` 接收 + HMAC 校验 + diff 字段 | 2 | W1-STORE-02 | ✅ ProductWebhookController（create/update/delete 三 endpoint）+ ShopifyHmacVerifier（常量时间比较，secret 空跳过+WARN）+ ProductSnapshot PENDING 行；4 smoke 全过 |
| W2-SNAP-03 | 5 分钟去抖 Redis NX + RabbitMQ 延时消息 | 1.5 | W2-SNAP-02 | ✅ SnapshotDebounceService（Redis SET NX + MQ publish + 失败释放 key）+ TTL+DLX 拓扑（snap.delay → snap.process）+ SnapshotProcessConsumer（PENDING→RUNNING）+ 4 smoke 验证去抖窗口 |
| W2-SNAP-04 | 快照生成：CSV + JSON 上传 R2 + DB 写索引 | 2 | W2-SNAP-03, W2-AST-04 | ✅ SnapshotGenerationService（Java HttpClient 强制 HTTP/1.1 适配 uvicorn）+ ShopifyJsonCsvWriter（15 列子集 / UTF-8 BOM / RFC4180 转义）+ FileService 加 `uploadBytes/downloadBytes`；端到端 webhook→debounce→DLX→consumer→worker→R2→CSV→DB 链路通；worker dry-run 不真传 R2 → 标 FAILED 落 error_message（pipeline 验证 OK，dry-run 完整路径需 worker 修） |
| W2-SNAP-05 | 价格/库存仅落历史表（不建快照） | 1 | W2-SNAP-02 | ✅ PriceInventoryHistoryService（diff-only：值变化才插入）+ webhook 路径集成；2 次 webhook 测试 NULL→新值 + 仅变化插入逻辑均正确 |
| W2-SNAP-06 | 快照前端：列表 + diff 视图 + 手动"立即快照"按钮 | 0.5 | W2-SNAP-04 | ✅ Backend GET /product-snapshot + POST /manual + CSV proxy；Frontend /snapshots + /snapshots/[id] 4 tab（基本/价格历史/库存历史/Manifest）+ 立即快照按钮（products/[id]）|

### 2.3 产品推送（~16 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W2-PUSH-01 | `tenant_xx.store_product` `store_product_variant` `task` `push_conflict` 表 | 1 | — | ✅ Flyway V10 + 4 entity + 4 mapper |
| W2-PUSH-02 | worker `/push/product`：handle 匹配 + 价格用产品库价 + 库存 1000 + continue selling | 3 | W2-AST-04 | ✅ ProductPushService + create_product + ShopifyAdminValidationError；HANDLE_RENAMED 检测；dry-run 合成 10 位 shopify_product_id；422 不抛 raise（直接返 dict） |
| W2-PUSH-03 | 媒体上传到目标店 Shopify Files + URL 替换 | 2 | W2-PUSH-02 | ✅ Worker `/push/product` 接受 `media_r2_keys` → R2 get_object → base64 → Shopify `images[].attachment`；dry-run 合成 base64；R2 失败 → R2FetchError → 502 |
| W2-PUSH-04 | 冲突处理：先跳过 → `push_conflict` PENDING → 用户决策 → 二次任务 | 2 | W2-PUSH-02 | ✅ Backend PushService（编排：load product+variants → build payload(库存=1000+continue) → call worker HTTP/1.1 → 写 task SUCCESS / push_conflict PENDING + task PARTIAL / FAILED）+ PushController POST /push/product；端到端测试 dry-run task SUCCESS + store_product 行 |
| W2-PUSH-05 | 单/批量推送 API + 任务调度 | 2 | W2-PUSH-02 | ✅ POST /push/batch（同步循环 + parent_task_id 串联子任务 + 自动 SUCCESS/PARTIAL 汇总）；真异步队列 TODO |
| W2-PUSH-06 | 推送前端：选产品 + 选目标店 + 进度条 SSE + 冲突弹窗 | 4 | W2-PUSH-05 | ✅ PushTriggerDialog 复用 Day 6 模式 + /tasks 列表（5s 轮询 active） + /tasks/[id] 详情（4 tab + 3s 轮询 + 懒加载 payload）+ AppShell 加菜单 |
| W2-PUSH-07 | 推送失败钉钉通知 `PRODUCT_PUSH_FAIL` | 1 | W1-INV-08 | ✅ NotificationDispatcher 集成 + 三 FAILED 分支（IOException / 422 / 非 2xx）全埋点 + fallback userId 配置 |
| W2-PUSH-08 | 推送审计 + 任务日志 | 1 | W1-AUD-01 | ✅ 验证既有 AuditAspect 自动覆盖 PushController + TaskController（sys_audit_log 实测有 /push/product /push/batch 行）|

### 2.4 多店铺上架状态同步（~6 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W2-SYNC-01 | webhook `products/update/delete` + `app/uninstalled` 注册 + 处理 | 1.5 | W2-SNAP-02 | ✅ ProductWebhookController（products/* Day 3-5 已通）+ 新 StoreWebhookController（app/uninstalled → status=UNINSTALLED；shop/update log+200）|
| W2-SYNC-02 | 04:00 全店全产品兜底扫描 | 1.5 | W2-SYNC-01 | ✅ StatusSyncScheduler `@Scheduled(0 0 4 * * *)` + StatusSyncService 骨架（ACTIVE store 遍历 + store_product 计数 + 异常隔离），真 Shopify diff 留 W3 |
| W2-SYNC-03 | 产品信息页双向同步（active/draft/archived 推所有映射店） | 1 | W2-PUSH-05 | ✅ ProductStatusBroadcaster（status 变化时 fan-out 到所有 store_product 映射店）+ ProductService.update 拆 @Transactional applyUpdate / 包裹层 broadcast；跳过 NOT_PUSHED/FAILED + UNINSTALLED 店铺 |
| W2-SYNC-04 | 采购页 / 媒体页只读聚合多店铺状态 | 2 | W2-SYNC-01 | ✅ Backend GET /product/{id}/store-products + Frontend 第 8 个 tab "店铺映射"（StoreMappingPanel 7 列只读表 + LoadingBlock + EmptyState） |

### 2.5 监控（~6 PD，与 Wave 4 部分重叠，先打基础）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W2-MON-01 | 节点 C：Prometheus + Grafana + Loki + Promtail + Alertmanager 部署 | 2 | W1-INF-01 | ✅ ops/monitoring docker compose + 9 文件（dev 跑 host，生产替换 IP 留 W1-INF） |
| W2-MON-02 | 节点 A/B node-exporter + JVM Exporter | 1 | W2-MON-01 | ✅ micrometer-registry-prometheus + node-exporter/cadvisor 容器 + 4 scrape job 全 up |
| W2-MON-03 | 业务看板：任务成功率、推送失败率、Token 过期数 | 2 | W2-MON-01 | ✅ 3 处 Micrometer 埋点（auth_login / tokens_expiring_soon / invitation_expired）+ Grafana 12 面板 dashboard（uid shub-overview，含 push/task 占位面板待 W2-PUSH 数据） |
| W2-MON-04 | 关键告警：5xx > 1%、MQ 堆积 > 1000、RDS CPU > 80%、备份失败 | 1 | W2-MON-01 | ✅ 4 rule files / 4 groups / 12 rules（含 5 个 placeholder 占位）+ Alertmanager 钉钉 receiver + critical 路由 + inhibit_rules |

---

## Wave 3 — 一键开店 + 预览 + 采购 + 媒体 + 模板（~70 PD，4 周）

### 3.1 一键开店 saga（~22 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W3-NEW-01 | `task` 状态机扩展（PENDING → AUTH_DONE → MEDIA → COLLECTIONS → PRODUCTS → GUIDE → THEME → PUBLISH → SUCCESS/FAILED） | 1 | W2-PUSH-01 | ✅ Flyway V13（saga_step + saga_data_json + saga_attempt + index）+ SagaStep enum + SagaContext record + SagaService（start/advance/fail/retry/executeStep）+ SagaStepException + SagaController（GET / POST /retry / POST /advance）；端到端验证 INIT→AUTH_DONE 转换 + retry attempt 计数 |
| W3-NEW-02 | 新店 OAuth 授权 + App 安装 | 2 | W1-STORE-02 | ✅ SagaAuthService（HMAC-签名 state + 真 OAuth + dev mock 双路径）+ SagaOAuthCallbackController（GET /oauth/saga-callback）+ POST /saga/start 返 oauthUrl + POST /saga/{id}/dev-mock-auth 跳过真 Shopify；端到端 mock 流验证 INIT→AUTH_DONE + store 自动建 + ctx 填充 |
| W3-NEW-03 | 媒体批量上传 R2 → Shopify Files | 2 | W2-PUSH-03 | ✅ Worker `/push/files` (R2→base64→GraphQL fileCreate)+ Backend `SagaMediaStepService.run(taskId)` 通过 SagaService.executeStep 自动 advance；端到端 saga AUTH_DONE→MEDIA 转换 ✓ |
| W3-NEW-04 | Collections 创建 | 1.5 | — | ✅ Worker `/push/collections` (REST custom_collections per-item error capture) + Backend `SagaCollectionsStepService.run(taskId)` + 默认空时 fallback `[{all},{new}]`；端到端 MEDIA→COLLECTIONS 转换 ✓ |
| W3-NEW-05 | 批量产品推送（复用 W2-PUSH） + 冲突暂停等用户 | 1 | W2-PUSH-04 | ✅ SagaProductsStepService（调 PushService.pushBatch；空列表 skip+advance；全失败 throw FAILED；部分失败仍 advance）+ POST /saga/{id}/step/run-products；端到端 1 product → parent task + child PRODUCT_PUSH ✓ |
| W3-NEW-06 | 主题 zip 通过 `themes.json src=R2签名URL` 安装 → `themeUpdate role=main` | 3 | W3-NEW-04 | ✅ Worker `/push/theme`（R2 拉 zip + apply replace rules + repack + Shopify themeCreate(src=URL, role=unpublished) + 可选 publish→main）+ Backend SagaThemeStepService 编排（合 default + ctx replaceRules）+ 6 新 pytest |
| W3-NEW-07 | 域名/品牌正则替换（按 `default_replace_rules`） | 2 | W3-NEW-06 | ✅ ThemePushService._apply_rules：unzip + 仅文本扩展（liquid/json/css/js/txt/md/yaml/yml/html/svg）+ `re:` 前缀走正则 vs 字面量替换 + 二进制保留 + repack；rules_applied_count 统计 |
| W3-NEW-08 | menu/policy/markets 指导文档跳转 | 1 | W3-GUIDE-01 | ✅ SagaGuideStepService（show_in_saga_step='GUIDE' 收集 + relatedTemplateId 过滤 + frontendUrl 注入 ctx.stepOutputs.guides）+ POST /saga/{id}/step/run-guide |
| W3-NEW-09 | 失败重试（自上一步） + 进度 SSE | 2 | W3-NEW-01 | ✅ SagaProgressService（per-saga SseEmitter map + Redis last-event TTL 1h replay）+ SagaProgressController GET /saga/{id}/events；SagaService 在 start/advance/fail/retry 4 处 emit；SSE 端到端验证 replay + live event |
| W3-NEW-10 | 一键开店向导前端（多步表单 + 进度条 + 替换规则配置） | 5 | W3-NEW-01..09 | ✅ /newstore 列表（按 type 过滤 + active 5s 轮询）+ /newstore/new 5 步向导（基本/替换规则/接入模式/选产品/确认）+ /newstore/[taskId] 进度页（9-step stepper + SSE 优先 + 3s 轮询 fallback + INIT mock-auth + FAILED retry + 手动推进按钮）|
| W3-NEW-11 | 通知 `NEW_STORE_SUCCESS/FAIL` | 0.5 | W1-INV-08 | ✅ NotificationEventCode 已含 NEW_STORE_SUCCESS/FAIL；SagaService advance(SUCCESS) + fail() 调 notifyUser；@Lazy 注入 NotificationDispatcher 避循环；fallback 容忍（admin 无 dingtalk_userid 时降级日志）|
| W3-NEW-12 | 端到端 e2e 测试（Partner dev store） | 1 | W3-NEW-10 | ✅ `bin/e2e-saga.sh`（380 行）—— 18 checks 全 PASS，覆盖 INIT→AUTH_DONE→MEDIA→COLLECTIONS→PRODUCTS→GUIDE→THEME→PUBLISH→SUCCESS 9 步；endpoint_exists 探测优雅 skip；幂等 cleanup（自然键删除 + pre-run 清旧）|

### 3.2 合作者店铺池 + 详情页预览（~10 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W3-PV-01 | `is_dev_store` `is_partner_collab` 字段 + 池接入 OAuth 流程 | 2 | W1-STORE-02 | ✅ V14（仅加 2 索引，列在 V4 已就位）+ StoreService/StoreController 加 partnerCollab/devStore 过滤 + 3 个 sensitive 标记接口 + 新 StorePoolController GET /store/pool/partner-collab |
| W3-PV-02 | 合作者店铺池前端页（添加 / 配额展示） | 1.5 | W3-PV-01 | ✅ /partner-stores 页（filter 池/dev/全部 + table 7 列含"当前预览数 n/3"客户端聚合 + sensitive op 标记池/dev）+ AppShell 加菜单 |
| W3-PV-03 | `preview_theme` 表 + 最少负载选 dev store | 1 | W3-PV-01 | ✅ V18（preview_theme 表 + 3 索引）+ entity/mapper + PreviewThemeAllocator（pickLeastLoadedDevStore 按当前活跃数挑最闲）+ PreviewController CRUD；5 分配测试均匀分布到 2 dev stores，7 次超容量正确返 500 |
| W3-PV-04 | worker `/preview/build`：推产品 → 创建 unpublished theme → 替换媒体 URL | 3 | W3-NEW-03 | ✅ PreviewBuildService（dry-run + real path：theme_create_unpublished GraphQL + create_product 失败容忍 + product_preview_url with handle）+ POST /preview/build；6 新 pytest |
| W3-PV-05 | 详情页"眼睛"按钮 + 预览链接打开 + last_accessed 更新 | 1 | W3-PV-04 | ✅ Backend PreviewBuildOrchestrator（allocate→worker→回填 url+status，never throw）+ Frontend PreviewButton 组件（products/[id] 集成，READY 时 window.open + markAccessed）|
| W3-PV-06 | 30 分钟清理任务（保留 3 个 + 24h 未访问） | 1 | W3-PV-03 | ✅ PreviewThemeCleanupScheduler `@Scheduled(0 */30 * * * *)`（expireOverdue 过期软删 + enforceQuota 单店超容量删最老）|
| W3-PV-07 | 指导文档 `PARTNER_DEV_SETUP` | 0.5 | W3-GUIDE-01 | 🟡 Seed 方法已落地（curl POST /guide-doc + publish），等用户首次进入合作者池场景时通过 /guides 编辑器写正式内容（不入数据库 migration）|

### 3.3 采购管理（~8 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W3-PUR-01 | `purchase_info` `sku_change_log` 表 | 0.5 | — |
| W3-PUR-02 | 采购信息 CRUD（仅 PURCHASING 角色可写） | 1.5 | W1-RBAC-02 |
| W3-PUR-03 | 改 SKU 二次确认（钉钉验证码）+ 自动同步所有映射店铺 | 2 | W1-AUTH-06, W2-PUSH-05 |
| W3-PUR-04 | 采购前端页（仅采购列：SKU / 成本 / 克重 / 物流 tag / 采购链接） | 3 | W3-PUR-02 |
| W3-PUR-05 | 改 SKU 强制审计 | 1 | W1-AUD-01 |

### 3.4 产品媒体 + 需求文档（~10 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W3-MED-01 | `product_doc` 表（RICH_TEXT / FILE 二选一） | 0.5 | — |
| W3-MED-02 | 文件上传：MIME 白名单 + magic number + 单文件 ≤ 20MB + 最多 20 个 | 2 | W1-PROD-05 |
| W3-MED-03 | 主图 / 详情页媒体上传 + 排序 + 删除 | 2 | W3-MED-02 |
| W3-MED-04 | TipTap 需求文档：截图粘贴异步上传 R2 + 折中签名 | 2 | W1-PROD-07 |
| W3-MED-05 | 媒体前端 tab（拖拽排序 + 预览 + 删除） | 2.5 | W3-MED-03 |
| W3-MED-06 | 美工角色（DESIGNER）权限范围限制（仅媒体页 + 需求文档） | 1 | W1-RBAC-01 |

### 3.5 平台模板库 + 指导文档（~8 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W3-TPL-01 | `base_template` `base_template_version` 表 + R2 zip 上传 | 1 | — | ✅ V15（含 base_template + base_template_version + guide_doc 三表）+ entity + mapper + Application.java @MapperScan；R2 zip 上传留 W3-TPL-02 |
| W3-TPL-02 | 模板 CRUD（仅 PLATFORM_SUPER）+ 版本管理 + 默认替换规则 | 2 | W1-RBAC-01 | ✅ BaseTemplateService（list/detail/create/update/publish/deprecate/delete + uploadVersion sha256+R2 + setDefaultVersion）+ BaseTemplateController（@RequireSensitiveOp 标 publish/deprecate/delete/version-upload）；CRUD 端到端 ✓；R2 上传走 FileService；PLATFORM_SUPER RBAC 留 TODO |
| W3-TPL-03 | 模板库前端页（上传 zip / 版本切换 / 默认标记） | 2 | W3-TPL-02 | ✅ /templates 列表 + /templates/[id] 详情（基本+版本 2 tab）+ 内联创建 dialog + 多部分版本上传 dialog（semver/zip/changelog/JSON 替换规则）+ sensitive-op 全流程 |
| W3-GUIDE-01 | `guide_doc` 表 + CRUD + 关联模板 | 1 | — | ✅ 表 + entity + mapper（V15 与 W3-TPL-01 合并）；CRUD 留 W3-GUIDE-02 |
| W3-GUIDE-02 | 指导文档前端：编辑器（TipTap）+ 一键开店时弹窗展示 | 2 | W3-GUIDE-01 | ✅ Backend GuideDocController（list/get/create/update/publish/delete）+ Frontend /guides 列表 + /guides/new + /guides/[id]（共享 GuideForm 复用 RichTextEditor）；一键开店时弹窗留 W3-NEW-08 集成 |

### 3.6 跨公司授权（基础部分，审批中心 Wave 4 完整化）（~3 PD）

| ID | 标题 | PD | 依赖 | 状态 |
|---|---|---|---|---|
| W3-DS-01 | `sys_data_scope` 跨公司读写：CRUD + 时效字段必填 | 1 | W1-RBAC-03 | ✅ V16（cross_company + revoked_at + granter_company_id 三新列 + source ENUM 加 APPROVAL + 索引）+ CrossCompanyAuthController（list/grant/revoke 带 5min~180d 校验 + sensitive op）+ CrossAuthExpiryScheduler 骨架（W3-DS-03 真实实现）|
| W3-DS-02 | 跨公司授权前端表单（手动绑定，未走审批） | 1 | W3-DS-01 | ✅ /cross-auth 列表（status/userId/granterCompanyId 过滤 + revoke 操作 + "即将过期" badge）+ CrossAuthGrantDialog（5min~180d 校验 + sensitive-op 真流程）+ AppShell 加菜单 |
| W3-DS-03 | 24h 内到期自动 EXPIRING + 钉钉提醒 | 1 | W1-INV-08 | ✅ V19 加 expiring_notified_at（dedupe 列）+ CrossAuthExpiryScheduler 替换 Day 1 skeleton：warnExpiring 每小时扫 24h 内到期未通知 → notifyUser dispatch + 标 expiring_notified_at；hardExpire 每天 00:05 状态过期 → REVOKED |

### 3.7 性能优化 + 缓冲（~9 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W3-PERF-01 | Redis 缓存：店铺列表 / 产品基础信息 / 角色权限 | 2 | — | ✅ CacheService（getOrLoad 通用 + Micrometer hit/miss counter + Redis 不可用 fallback）+ StoreService.list / ProductService.detail 加缓存；3 处 mutation 主动 invalidate；RBAC 热点未明显 → 暂跳过（CacheKeys 占位）|
| W3-PERF-02 | 慢查询优化：审计日志、产品快照、task 表索引复盘 | 2 | — | ✅ V20 加 6 个热点索引（pricehist/invhist 复合 + task type+id + snapshot store+status+created + storeprod tenant+status + invitation status+invited）+ 性能优化复盘.md 含方法论 + 各表 query→现有 idx→新加 idx + EXPLAIN 实例（backward index scan 无 filesort）|
| W3-PERF-03 | 前端虚拟滚动 / 分页优化（产品列表 1w+ 行） | 2 | W1-PROD-06 | ✅ `@tanstack/react-virtual@3.13.24` + products/page.tsx 重构（grid 模板列宽 + sticky header div + 虚拟化 body div + 60vh overflow-auto，不用 table）；checkbox + 批量 + 状态 badge + Link to detail 全保留；27 routes build pass，/products = 10.3KB |
| W3-PERF-04 | Wave 3 整体回归 + bug fix 缓冲 | 3 | — | ✅ 4 测试全绿（asset-worker 70/70 + backend mvn ✓ + bin/smoke-test 6/6 + bin/e2e-saga 18/18）；顺手清技术债 #12（SagaService.start 加 mergeInitialPayload merge templateId/replaceRules/productIdsToPush → ctx，去掉 e2e-saga.sh 里的 SQL workaround）；frontend 双 build 一致 31 routes |

---

## Wave 4 — 审批 + 通知 + 邮件 + 审计归档 + 灾备（~30 PD，2 周）

### 4.1 审批中心（~9 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W4-APP-01 | `approval_flow` `approval_log` 表 | 0.5 | — |
| W4-APP-02 | 审批引擎：单签 + 任一角色 + 驳回可改重提 | 2 | W1-RBAC-02 |
| W4-APP-03 | `PRODUCT_ACCESS` 申请流程（产品库使用权） | 1.5 | W1-PROD-02 |
| W4-APP-04 | `CROSS_COMPANY_AUTH` 申请流程（时效必填，默认 7 天） | 1.5 | W3-DS-01 |
| W4-APP-05 | 审批前端：待办列表 + 详情页 + 通过/驳回 + 历史 | 3 | W4-APP-02 |
| W4-APP-06 | 通知 `APPROVAL_PENDING/DECIDED/CROSS_AUTH_EXPIRING` | 0.5 | W1-INV-08 |

### 4.2 钉钉通知订阅（~5 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W4-NTF-01 | `notification_event_def` `notification_subscription` `notification_log` 表 + 12+ 事件初始化 | 1 | — |
| W4-NTF-02 | 订阅设置前端（个人中心 + 复选框矩阵） | 1.5 | W1-FE-03 |
| W4-NTF-03 | 多 corpId 发送：按 `dingtalk_corp_id` 取 access_token 发送 | 1 | W1-AUTH-07 |
| W4-NTF-04 | 失败重试：指数退避 3 次 + `notification_log` FAILED 告警 | 1.5 | W4-NTF-01 |

### 4.3 邮件其他模板 + 站内信（~4 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W4-MAIL-01 | 邮件模板：`PASSWORD_RESET` `REGISTRATION` `ALERT` | 1 | W1-INV-03 |
| W4-MAIL-02 | 密码重置流程（钉钉绑定丢失场景） | 1.5 | W4-MAIL-01 |
| W4-MAIL-03 | 站内信通道（INAPP）+ 通知中心前端 | 1.5 | W4-NTF-02 |

### 4.4 审计归档 + 备份（~5 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W4-OPS-01 | 审计日志月归档：jsonl.gz + AES-256-GCM + R2 + 删 partition | 1.5 | W1-AUD-01 |
| W4-OPS-02 | RDS 每日 03:00 加密备份（dump → AES-GCM → R2，保留最新 1 份） | 1.5 | W1-INF-04 |
| W4-OPS-03 | 软删除 90 天物理删（含 R2 文件） | 1 | W1-AUD-02 |
| W4-OPS-04 | 备份失败钉钉告警 `BACKUP_FAIL` | 0.5 | W4-OPS-02 |
| W4-OPS-05 | 备份恢复 SOP 文档（PITR + 加密 dump 解密重建） | 0.5 | W4-OPS-02 |

### 4.5 监控完善 + 灾备演练（~4 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W4-MON-01 | 完整告警阈值：磁盘 < 20%、R2 上传失败 > 5%、SSL < 30 天 | 1 | W2-MON-04 |
| W4-MON-02 | Alertmanager → 钉钉机器人独立群（不走业务订阅） | 1 | W4-MON-01 |
| W4-MON-03 | 灾备演练 1 次：测试环境完整恢复 + 一键开店 | 2 | W4-OPS-02 |

### 4.6 上线 + 缓冲（~3 PD）

| ID | 标题 | PD | 依赖 |
|---|---|---|---|
| W4-RLS-01 | 生产 Shopify App 申请 + redirect_uri 配置 | 1 | W1-INF-06 |
| W4-RLS-02 | 法务/合规材料整理（数据出境，R-8） | 1 | — |
| W4-RLS-03 | Wave 4 回归 + 上线前夜 e2e | 1 | — |

---

## 跨 Wave 持续任务

| ID | 标题 | 频率 |
|---|---|---|
| X-TEST | 单元测试 + 集成测试 + 关键路径 e2e（每个 PR） | 持续 |
| X-DOC | 接口文档（SpringDoc）+ 业务文档（每模块完成时更新） | 持续 |
| X-REVIEW | Code Review（所有 MR ≥ 1 人 approve + CI 全绿） | 持续 |
| X-SEC | 依赖扫描（trivy / npm audit / Dependabot）告警跟进 | 周度 |

---

## 关键依赖图（核心路径）

```
W0 脚手架
   ↓
W1-CORE 多数据源 + JWT + AES
   ↓
W1-RBAC 内置角色 + 数据范围拦截
   ↓
W1-AUTH 钉钉登录 ──┐
   ↓              │
W1-ORG 钉钉同步    │
   ↓              ↓
W1-INV 临时员工邀请（可与 ORG 并行）
   ↓
W1-STORE OAuth 接入
   ↓
W1-PROD 产品库
   ↓
W2-AST 资产拉取 ── W2-PUSH 推送（可并行）
   ↓                ↓
W2-SYNC 状态同步   W2-SNAP 快照
   ↓
W3-NEW 一键开店（强依赖前面所有）
   ↓
W3-PV 预览 / W3-PUR 采购 / W3-MED 媒体 / W3-TPL 模板（4 路并行）
   ↓
W4-APP 审批 + W4-NTF 通知 + W4-OPS 备份 + W4-MON 监控（并行）
   ↓
上线
```

---

## 人力分工建议（3.3 FTE）

| 角色 | Wave 0 | Wave 1 | Wave 2 | Wave 3 | Wave 4 |
|---|---|---|---|---|---|
| **后端 A**（多数据源 + Auth + Org + Invitation） | W0-03/06/07 | W1-CORE/AUTH/ORG/INV | W2-SNAP/SYNC | W3-NEW/PUR | W4-APP/NTF |
| **后端 B**（Store + Product + Push + Worker） | W0-04 | W1-STORE/PROD/RBAC/AUD | W2-CLI/AST/PUSH | W3-PV/MED/TPL | W4-MAIL/OPS |
| **前端**（全栈兼任） | W0-02/08 | W1-FE/AUTH-04/INV-05/INV-06/STORE-04/PROD-06 | W2-AST-06/SNAP-06/PUSH-06 | W3-NEW-10/PV-02/PUR-04/MED-05/TPL-03/GUIDE-02 | W4-APP-05/NTF-02/MAIL-03 |
| **运维兼职**（0.3 FTE） | W0-05/09 | W1-INF-* | W2-MON | — | W4-MON/OPS-05 |

---

## 优先级建议

- **P0（不能砍）**：W0 全部、W1 全部（含 INV）、W2-PUSH、W3-NEW、W4-OPS-02 备份
- **P1（可推迟到 v1.1）**：W3-TPL 模板库（先用预置 zip）、W4-APP 审批中心（先手动绑定）、W3-PV 预览（先用浏览器原生跳转）
- **P2（可砍到 v2）**：W4-MAIL-03 站内信、W3-PERF-03 虚拟滚动、W4-MON-03 灾备演练（先文档化 SOP）

紧急情况下，砍 P1 + P2 后总量降到 ~180 PD，可压到 10 周交付。

---

## 下一步

1. **立即开始 Wave 0**：1 人 1 周拉起骨架，建议本周完成
2. **Wave 1 开工前**：服务器采购到位（W1-INF-01）+ AWS RDS 创建（W1-INF-04）+ Cloudflare 域名托管（R-5 落地）
3. **进 Linear/Jira**：本文档每行 = 一张卡，ID 直接复用，依赖关系按"依赖"列建 blocker 关系
