# 店铺资产同步 / 去重 / 跨店 Diff / 跨店推送 — 设计评估

> **版本**：v0.1（评估稿，待评审后拆 Track 入 `并行任务编排.md`）
> **日期**：2026-05-05
> **范围**：店铺接入后的全量静默同步、CAS 去重、跨店 diff、产品推送时的版本/占位符替换/template_suffix 匹配
> **不在范围**：现有产品 CRUD / 单产品推送 / 批量推送 / RBAC / 钉钉敏感操作 — 这些**不动**，本设计只做叠加

---

## 0. 一句话目标

> 把系统从"产品库 + 推送"升级为**"多店铺资产中台"**：店铺一接入就把它在 Shopify 上的所有可迁移资产（主题、产品、媒体、政策、菜单、集合、店铺设置）静默拉回来落到 R2，按版本/sha256 去重；任意两个店铺之间可以 diff；产品推送时按所选模板版本做域名/品牌/template_suffix 的占位符替换，把图片/详情页 JSON 一并推过去。

---

## 1. 现状评估

### 1.1 已经具备的能力（不要重做）

| 模块 | 现状 | 来源 |
|---|---|---|
| `asset_snapshot` 表 | snapshot_type ∈ {THEME, POLICY, MENU, COLLECTION, PRODUCT, FULL}；status ∈ {PENDING,RUNNING,SUCCESS,FAILED,CANCELED} | V8 / V25 |
| `asset_file` 表 | 已有 `r2_key + size_bytes + sha256` 字段 — sha256 是去重的现成支点 | V8 |
| `base_template_version` | **关键**：已有 `version` + `default_replace_rules_json` 占位符规则框架 | V15 |
| `store_product` / `store_product_variant` | 内部 product → Shopify product 映射 + push 状态 | V10 |
| Worker PULL endpoints (5 个) | `/pull/theme` `/pull/product` `/pull/menu` `/pull/policy` `/pull/collection`，已落 R2 | `asset-worker/app/routes/pull.py` |
| Worker PUSH endpoints (4 个) | `/push/product`（支持 `media_r2_keys`）`/push/theme`（**已实现 replace_rules**）`/push/files` `/push/collections` | `asset-worker/app/routes/push.py` |
| R2 命名规约 | `tenants/{tid}/stores/{sid}/snapshots/{snap_id}/{type}/...` | worker 各 `*_pull.py` |

### 1.2 关键缺口

| # | 缺口 | 影响 |
|---|---|---|
| **G1** | **店铺接入后无任何后续动作**：`StoreService.connectCustomApp` / OAuth 回调只插 `store` 行；无事件、无 @Async、无定时 | 静默同步无入口 |
| **G2** | **后端 → worker 拉取链路是断的**：`POST /asset-snapshot/trigger` 只插 PENDING 行，从不调 worker `/pull/*`（控制器自己注释了"worker 不会自动消费"） | 即使手动触发也没用 |
| **G3** | 资产覆盖不全：缺 shop settings（locations / shipping zones / markets）、Files 库（产品图以外）、metafields、blog/article | 迁移时这部分要手工补 |
| **G4** | **没有 CAS / 去重**：相同主题文件 / 相同图片在 N 个店铺间会被 N 次写 R2 | 存储成本随店铺数线性放大 |
| **G5** | **没有跨店 diff 引擎**：snapshot 拉回来只能"看自己"，不能和另一店铺对比 | 迁移决策完全靠人脑 |
| **G6** | `PushService.buildProductPayload`（PushService.java:122–286）原样塞数据：**没用 replace_rules、没传 template_suffix、没替换 brand/domain** | 推过去都是源店的硬编码 |
| **G7** | 没有 `store_template_binding`：店铺当前用的是哪个 `base_template_version` 不可知 | 推送时不知道要套哪份 rules |

---

## 2. 设计要点

### 2.1 架构骨架

```
Store 接入
   │
   │ Spring Event (StoreConnectedEvent, AFTER_COMMIT)
   ▼
asset.sync 模块
   │
   │ RabbitMQ "asset.sync.fullStore" (snapshotId, storeId, tenantId, opts)
   ▼
SyncConsumer (后端)
   │
   │ HTTP → asset-worker /pull/{theme,product,menu,policy,collection,shop_settings,files}
   ▼
asset-worker
   │
   │ 1. 调 Shopify Admin API 拉数据
   │ 2. 算 sha256 → HEAD R2 CAS → 命中则跳过上传 / 未命中则 PUT
   │ 3. 把 (relative_path, sha256, size, content_type) 写进 manifest.json
   │ 4. POST 回写 backend：sub-snapshot 完成
   ▼
backend
   │
   │ 累加 fileCount / totalBytes，全部 done → asset_snapshot.status=SUCCESS
   ▼
asset_snapshot SUCCESS / PARTIAL / FAILED
```

### 2.2 R2 布局重构（CAS + Manifest）

**当前**（每 snapshot 一份完整副本）：
```
tenants/{tid}/stores/{sid}/snapshots/{snap_id}/theme/sections/header.liquid
tenants/{tid}/stores/{sid}/snapshots/{snap_id}/theme/sections/header.liquid  ← 同样的字节，又写一遍
```

**目标**（content-addressable）：
```
tenants/{tid}/cas/{sha256[:2]}/{sha256}                              ← 真正的字节，全租户一份
tenants/{tid}/stores/{sid}/snapshots/{snap_id}/manifest.json         ← 列出 [(relative_path, sha256, size, content_type)]
```

**为什么 tenant 级 CAS、不是全局**：合规 / 数据隔离；不同租户的同一 PNG 也要分仓。

**写入算法**（worker 内）：
```
for each_file:
    bytes = fetch_from_shopify(...)
    sha = sha256(bytes)
    cas_key = f"tenants/{tid}/cas/{sha[:2]}/{sha}"
    if not r2.head(cas_key):
        r2.put(cas_key, bytes, content_type=...)
    manifest.append({path, sha256: sha, size: len(bytes), content_type})
r2.put(manifest_key, json.dumps(manifest))
```

**预期收益**：同一主题多店复用、同一产品图多店复用 → R2 占用从 `O(stores × assets)` 降到 `O(unique_assets)`。

### 2.3 Diff 引擎设计

#### 输入
两个 manifest（可来自同店不同时间、或不同店）：
- `manifest_A.json`：`[{path, sha256, size}]`
- `manifest_B.json`：`[{path, sha256, size}]`

#### 第一层 — Manifest Diff（廉价、毫秒级）
按 `path` 左外连接：
- A 有 B 无 → **REMOVED**
- A 无 B 有 → **ADDED**
- A 有 B 有，sha256 相同 → **UNCHANGED**（直接过滤掉）
- A 有 B 有，sha256 不同 → **MODIFIED**（进入第二层）

#### 第二层 — 内容 Diff（按需、贵）
按文件类型分流：
- `*.liquid / *.css / *.js / *.json`（文本）→ 行 diff（`difflib.unified_diff` 或 `diff-match-patch`）
- `*.png / *.jpg / *.webp / *.mp4`（二进制）→ 不算 diff，只标 size + sha256 变化（可选 perceptual hash 提示"图片视觉上接近"）
- `policies/*.html`、`product.body_html` → 文本 diff
- `shop_settings.json`、`menus.json`、`collections.json` → 结构化 diff（按 key 递归，jsondiffpatch 风格）

#### 第三层 — 资源级 Diff（产品 / 集合）
不在 manifest，按业务实体比：
- `products`：以 `external_id` 为 key 做集合 diff，单产品再做字段级 diff（title/handle/variants/images）
- `collections`：同上，按 `handle`
- `policies`：4 个固定 key（refund/privacy/tos/shipping）逐一比

#### 输出格式
```json
{
  "snapshotA": 123, "snapshotB": 456,
  "summary": { "added": 12, "removed": 3, "modified": 8, "unchanged": 401 },
  "changes": [
    {
      "category": "theme",
      "path": "sections/product-template.liquid",
      "kind": "MODIFIED",
      "shaA": "abc...", "shaB": "def...",
      "diffPreview": "@@ -34,7 +34,9 @@\n..."
    },
    { "category": "product", "external_id": "gid://...", "kind": "ADDED" },
    ...
  ]
}
```

#### 实现位置
- **第一层**（manifest diff）：放后端 Java，零依赖
- **第二、三层**（内容 diff）：放 worker（Python 生态有更好的 diff 库；worker 已经握着 R2 字节）
- 后端 → worker：`POST /diff/snapshot` body `{ snapshotIdA, snapshotIdB, scope: ["theme","products",...] }`，返回上面 JSON
- 结果缓存进 `snapshot_diff_cache(snap_a, snap_b, scope, result_json, computed_at)`，避免重复算

### 2.4 推送时的占位符替换 + template_suffix

#### 替换流程
```
PushService.buildProductPayload(productId, targetStoreId)
  ├─ 读 store_template_binding(targetStoreId) → base_template_version_id + custom_overrides
  ├─ rules = merge(base_template_version.default_replace_rules_json, custom_overrides)
  ├─ for each text field in [body_html, seo_description, metafield.value]:
  │     applyReplaceRules(text, rules)   // 例：BIOU → 目标 brandName，biou.com → 目标 customDomain
  ├─ template_suffix = resolveTemplateSuffix(productId, rules)
  │     // 来源优先级：product.template_suffix（如有列）> 规则表（按 type/tag/vendor 匹配）> 空
  └─ payload.media_r2_keys = product.images.map(i => i.r2Key)   // worker 拉 R2 字节再喂 Shopify
```

#### Worker 侧 `/push/product` 增量
- 透传 `template_suffix` 给 Shopify REST `/products.json`
- 推完后：用 GraphQL `metafieldsSet` 回种 metafields（包括 SEO description、自定义 metafield）
- 推完后：可选触发对该店的 PRODUCT 类型 incremental snapshot（验证回落）

#### UI 改动
- `PushTriggerDialog` / `BatchPushTriggerDialog` 加：
  - **模板版本选择器**（默认取 `store_template_binding.base_template_version_id`，可手动覆盖单次推送）
  - **预览替换 diff**（可选 v1.1）：把 body_html 替换前/后并排显示，确认无误再推

---

## 3. Track 拆分（建议入 `并行任务编排.md`）

### Track AS1 · 同步链路最小闭环（~6 PD，强串行起点）
- AS1-01 后端 `StoreConnectedEvent` + `@TransactionalEventListener(AFTER_COMMIT)`
- AS1-02 后端 `asset.sync` 模块：监听器 → 插 `asset_snapshot(FULL, PENDING)` → MQ publish
- AS1-03 后端 `SyncConsumer`：串行调 worker 5 个 pull endpoint，累加 fileCount/totalBytes
- AS1-04 worker 5 个 pull endpoint **接入 CAS 写入**（见 2.2，可与 AS2 并行）
- AS1-05 前端店铺列表/详情页加"同步状态"徽章 + "重新同步"按钮
- AS1-06 V35 迁移：`asset_snapshot.status` 加 `PARTIAL` 枚举

### Track AS2 · CAS 去重（~4 PD，独立）
- AS2-01 V36 迁移：新增 `asset_blob(sha256 PK, size, content_type, first_seen_at, ref_count)`
- AS2-02 worker 写入路径改成 CAS（HEAD → PUT），manifest 落到 `snapshots/{snap_id}/manifest.json`
- AS2-03 backend `asset_file` 表加 `blob_sha256` 列，原 `r2_key` 列保留以兼容老数据；新写入只填 `blob_sha256`
- AS2-04 老数据迁移脚本（Python 一次性）：扫旧 `r2_key` → 算 sha256 → 上传 CAS → 回填 `blob_sha256`（可后置）

### Track AS3 · 资产覆盖补齐（~5 PD，AS1 之后）
- AS3-01 worker `/pull/shop_settings`：拉 shop.json + locations + shipping_zones + markets 拼一份 manifest
- AS3-02 worker `/pull/files`：Shopify Files 库（GraphQL `files`）
- AS3-03 worker `/pull/metafields`：shop-level + product-level
- AS3-04 V37 迁移：`asset_snapshot.snapshot_type` 加 `SHOP_SETTINGS / FILE_LIBRARY / METAFIELD` 枚举
- AS3-05 SyncConsumer 把新 endpoint 编入 FULL 流程

### Track AS4 · 跨店 / 历史 Diff（~6 PD，AS1+AS2 之后）
- AS4-01 V38 迁移：`snapshot_diff_cache(id, snapshot_a_id, snapshot_b_id, scope, result_json, computed_at, idx_unique(a,b,scope))`
- AS4-02 后端 `DiffController`：`POST /diff/snapshot` + `GET /diff/snapshot/{id}` + 列表
- AS4-03 后端 manifest-level diff（第一层，Java 实现）
- AS4-04 worker `POST /diff/content`：接两个 manifest + scope，按文件类型走第二、三层 diff
- AS4-05 前端"对比店铺"页：选 storeA / storeB → 各取最新 SUCCESS 快照 → 调 diff → 树形 + 行 diff 视图
- AS4-06 前端"店铺历史"页：同店两个时间点 diff（自动检测变更）

### Track AS5 · 推送替换 + 模板版本绑定（~7 PD，AS1 之后；与 AS3/AS4 可并行）
- AS5-01 V39 迁移：新增 `store_template_binding(id, store_id PK, base_template_version_id, custom_replace_rules_json, bound_at, bound_by)`
- AS5-02 V40 迁移：`product` 表加 `template_suffix` 列 + 索引
- AS5-03 后端 `ReplaceEngine`：`applyReplaceRules(text, rules) → text`（纯函数 + 单测）
- AS5-04 改造 `PushService.buildProductPayload`：合并 rules → 替换 body_html/seo/metafields → 塞 template_suffix
- AS5-05 worker `/push/product`：透传 template_suffix → Shopify；推完跑 `metafieldsSet`
- AS5-06 前端 `PushTriggerDialog` / `BatchPushTriggerDialog` 加模板版本选择器
- AS5-07 前端"模板绑定"管理页（轻量，只 list + 改 binding）

### Track AS6（可选）· 模板版本管理 UI（~3 PD，独立）
- 上传 / 标记 default / 维护 default_replace_rules_json 的 CRUD 页面 — 当前 V15 已有表，UI 缺。

---

## 4. Flyway 版本占用

按当前最新 V34 顺延，本设计预留：

| 版本 | 文件名 | 作用 | 所属 Track |
|---|---|---|---|
| V35 | `asset_snapshot_status_partial.sql` | 加 PARTIAL 枚举 | AS1 |
| V36 | `init_asset_blob_cas.sql` | `asset_blob` 表 + `asset_file.blob_sha256` 列 | AS2 |
| V37 | `asset_snapshot_type_extras.sql` | 加 SHOP_SETTINGS/FILE_LIBRARY/METAFIELD 枚举 | AS3 |
| V38 | `init_snapshot_diff_cache.sql` | diff 缓存表 | AS4 |
| V39 | `init_store_template_binding.sql` | 店铺 ↔ 模板版本绑定 | AS5 |
| V40 | `product_template_suffix.sql` | `product.template_suffix` 列 | AS5 |

> ⚠️ 提交前要按 CLAUDE.md 规则查 `并行任务编排.md` 是否有人占用了这些版本号。

---

## 5. 不会被砍的现有功能（确认清单）

- 产品 CRUD / 详情页 / 变体 / SKU / 富文本描述 / 媒体 / 文档
- 单产品推送 / 批量推送（最近一次刚加）
- 产品快照 / 价格历史 / 库存历史
- 店铺接入流程（custom-app token + OAuth saga + dev-mock-oauth）
- RBAC / 钉钉敏感操作 / 审批 / 通知 / 站内信
- F3 导出（全量 / 选中）

整个改动**叠加在以上路径之外**，不替换任何现有 endpoint / 表。

---

## 6. 推荐第一刀（MVP，1.5 PD 可跑通）

只做 **AS1-01 ~ AS1-03 + AS1-05 的 product+theme 子集**：

1. 店铺接入 → 自动触发 FULL snapshot（PENDING）
2. SyncConsumer 只调 worker `/pull/product` + `/pull/theme`（先不上 menu/policy/collection/shop_settings/files）
3. 落 R2 用**现有非 CAS 路径**（CAS 留到 AS2 再切，避免一刀切风险）
4. 店铺列表显示徽章："同步中 / 已同步 / 失败"

**这一刀验证三件事**：
- Spring 事件 → MQ → consumer 链路通
- Worker 异步 pull 不阻塞用户接入店铺
- Manifest 形态可用（为后续 diff 做准备）

验证完再展开 AS2（CAS 切换）+ AS3（覆盖补齐）+ AS4（diff）+ AS5（推送替换）。

---

## 7. 风险 / 待决问题

| # | 问题 | 当前倾向 |
|---|---|---|
| R1 | 大店全量拉可能跑数小时（10w+ products / 主题数百文件）。同步页面要不要支持中断、断点续传？ | v1：只支持取消（写 CANCELED）；断点续传留 v1.1 |
| R2 | Shopify Admin API 限流（REST 2 req/s, GraphQL 50 cost/s）。多店并发同步要全局令牌桶 | worker 内置 per-store rate limiter；后端调度时按 store 串行、跨 store 可并行 ≤3 |
| R3 | CAS 切换时老数据怎么办？ | 双写期 + 后台一次性回填脚本；查询时优先 `blob_sha256`，回退 `r2_key` |
| R4 | 跨店 diff 在巨量产品下结果太大（几 MB JSON）渲染会卡 | 第一层只返 summary + 分页 changes；点开某条才调第二层 |
| R5 | 替换规则误伤（比如 body_html 里碰巧有个字符串等于 brandName）| 替换前 dry-run 给用户看 diff（v1.1）；v1 加日志记录每次替换次数 |
| R6 | template_suffix 的来源没定：产品级字段？规则表（type/tag/vendor → suffix）？模板版本里的映射？ | v1：产品级 `product.template_suffix` 列（最简）；v2 再上规则表 |
| R7 | OAuth 接入回调和 custom-app 接入是两条路径，事件要在两处都发 | 事件统一在 `StoreService.afterStoreReady()` 一个内部方法发 |
| R8 | metafields "双向同步"是误解——pull / push 是两条独立路径 | pull（AS3）只拉 shop-level 落 R2 快照（不写本地表）；push（AS5）只读本地 `product_metafield`（用户在 admin 手编）→ Shopify。首次推空 metafields 是预期行为，**不需要回写本地**。注释已落到 PushService.java:164 |

---

## 7.1 已完成的收尾工作（AS6.5 之后批次）

| Track | 内容 | 状态 |
|---|---|---|
| #4 | diff cache 7 天 cron 清理 (`SnapshotDiffCacheCleanupScheduler`) | ✅ |
| #6 | metafields 双向架构澄清（落 PushService 注释 + R8） | ✅ |
| #7 | `product.template_suffix` UI 入口（产品详情页右栏） | ✅ |
| #8 | newstore saga `upsertStore` 新店路径补 `publishStoreConnected` | ✅ |
| #3 | PRODUCT-level metafields 分页拉取（大店性能） | ⏭️ defer 到性能 sprint |
| #5 | task.ruleHits 专属列 | ⏭️ 等聚合查询需求 |
| CAS | 老数据回填脚本 | ⏭️ defer，dev 还没积累数据 |
| 断点续传 | R1 同步取消 / 续传 | ⏭️ v1.1 |
| 替换 dry-run | R5 推送前 diff 预览 | ⏭️ v1.1 |

---

## 8. 验收标准（拍脑袋版，待评审收紧）

**MVP（AS1）**：
- [ ] 用 dev-mock-oauth 接入一家 Shopify 测试店，5 分钟内 `asset_snapshot WHERE store_id=? AND snapshot_type='FULL'` 状态变 SUCCESS
- [ ] R2 上能看到 `tenants/{tid}/stores/{sid}/snapshots/{snap_id}/{theme,product}/...` 文件
- [ ] 前端店铺列表显示徽章
- [ ] 同步过程中前端可以正常做其他操作（不阻塞）

**AS2 完成**：
- [ ] 同样接入第二家店，主题相同 → 主题文件不再重复写入 R2，asset_blob.ref_count++
- [ ] CAS 切换不破坏 MVP 路径

**AS4 完成**：
- [ ] "对比店铺"页：选两家店 → 30s 内出 diff 树
- [ ] 点开某文件 → 行 diff 渲染正确
- [ ] 第二次访问同一对组合 → 走缓存（< 1s）

**AS5 完成**：
- [ ] 同一产品推到 storeA（绑模板版本 1.0.0）和 storeB（绑 1.1.0），body_html 中的 `BIOU` 分别替换成各自 brandName
- [ ] template_suffix 在 Shopify Admin 后台可见
- [ ] 推送任务详情页显示"应用了 N 条替换规则"

---

## 9. 参考文件位置（评估时定位用）

- `backend-api/src/main/java/com/biou/shopifyhub/asset/AssetSnapshotController.java` — 当前手动触发入口
- `backend-api/src/main/java/com/biou/shopifyhub/asset/entity/AssetSnapshot.java` — 现有字段
- `backend-api/src/main/java/com/biou/shopifyhub/store/StoreService.java` — 店铺接入入口（`connectCustomApp` + OAuth 完成回调），事件要在这两处加
- `backend-api/src/main/java/com/biou/shopifyhub/push/PushService.java:122` — `buildProductPayload`，AS5 的主战场
- `backend-api/src/main/resources/db/migration/V15__init_template_and_guide.sql` — `base_template_version` + `default_replace_rules_json`
- `asset-worker/app/routes/pull.py` — 现有 5 个 pull endpoint
- `asset-worker/app/routes/push.py` — 现有 4 个 push endpoint，theme 已实现 replace_rules（参考它做 product 的）
- `asset-worker/app/services/theme_push.py:154-160` — 现有 replace_rules 实现，AS5-04 可借鉴

---

> **下一步**：评审通过后，把 §3 Track 表合并进 `并行任务编排.md`（建议起一个 Wave5 段落），版本号同步登记到 `技术债登记.md`。
